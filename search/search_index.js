var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#presentation","title":"Presentation","text":"<p>Secretary icons by UltimateArm</p> <p>The Virtual Secretary is a Python framework allowing to write custom filters and actions to automate your digital office workflows and  improve information processing and retrieval. It provides connectors to synchronize information across:</p> <ul> <li>your emails,</li> <li>your contacts (address books),</li> <li>your Instagram posts &amp; comments,</li> <li>and more in the future:<ul> <li>your agenda events,</li> <li>your database entries.</li> </ul> </li> </ul> <p>Secretary: a person who works in an office, working for another person, dealing with mail and phone calls, keeping records, arranging meetings with people, etc. (Oxford dictionnary)</p> <p>Human secretaries were able to use their knowledge, agreed-upon rules and their best judgment to assess whether any unplanned event (visitor, phone call, mail) was worth disturbing your planning and workflow. To cut on expenses, they have been replaced by computers and software, which send you notifications for everything, without discernment. The result is you are disturbed all the time, you have to process huge amounts of info, so you only gained more stress and more work.</p> <p>The Virtual Secretary aims at making your ultra-connected office life less stressful, by cross-checking and pre-filtering information for you, such that you get a digest of only the important/urgent information, and can deal with less important information later, at your own pace.</p> <p>The framework provides an high-level Python API allowing you to efficiently write simple and advanced email filters, unleashing the full power of the Python programming language, along with its powerful ecosystem of packages for data mining, machine learning, regular expression search, etc.</p> <p>The Virtual Secretary works standalone:\u00a0it doesn\u2019t need any intermediate piece of software, but connects directly to servers. It can therefore be used on any typical mailbox supporting IMAP v4 (Gmail, Outlook/Office365, self-hosted and custom servers). It can work alongside your usual desktop mail client (Microsoft Outlook, Mozilla Thunderbird, web clients like Zimbra, Horde, SoGo, etc.).</p>"},{"location":"index.html#features","title":"Features","text":"<p>Internally, it provides low-level features exposed through a nice programming interface allowing to write clean and elegant filters:</p> <ul> <li>connection to IMAP and SMTP\u00a0mail servers through SSL and StartTLS (tested with Hotmail/Live, Gmail, Dovecot and Exim):<ul> <li>fetch, parse, and inject email content, headers and attachments into arbitrary Python scripts,</li> <li>tag and sort/move emails into IMAP folders based on manual filters (Python scripts) or AI classifiers,</li> <li>send automatic replies (auto-responder).</li> </ul> </li> <li>HTML, PDF and XML crawling, parsing and indexing with OCR :<ul> <li>crawl websites recursively, for content and links,</li> <li>easily create natural language corpora to train specialized AI language models that speak your business slang,</li> <li>create specialized search engines aggregating internal and external resources you need for your job (reference implementation of a server-side interface: Chantal),</li> </ul> </li> <li>connection to Instagram through their OAuth2 and Rest API (read-only),</li> <li>duplicate comments into your mailbox to centralize,</li> <li>email authentication (spam detection) through SPF, DKIM and ARC,<ul> <li>basic SPF and DKIM checks get rid of 90% of spam emails without relying on flimsy machine learning. Go figure why mail providers don\u2019t do it\u2026</li> </ul> </li> <li>connection to CardDAV address book servers (tested with SabreDAV, used by NextCloud  and Owncloud servers) (read-only),<ul> <li>reuse your contact info to label and sort emails</li> </ul> </li> <li>Machine-learning language classifier (SVM and decision trees on top of word2vec), allowing to train your own AI\u00a0against your own emails in your own language and perform automatic tagging and sorting based on content,<ul> <li>perform topic recognition in emails or documents,</li> <li>find relevant resources from a query (search engine) or an email body (auto-responder).</li> </ul> </li> <li>works on server or desktop, on demand or as a Cron job. A locking mechanism prevents more than one instance to process each mailbox. AI classifiers can be trained locally on desktop and sent to run on the server,</li> <li>an overridable internal logging mechanism prevents emails from being processed more than once, so automatic actions that are manually reverted are not performed again on the next run.</li> </ul>"},{"location":"index.html#examples-of-applications","title":"Examples of applications","text":"<p>The Virtual Secretary can, for example :</p> <ul> <li>move emails into IMAP folders or tag them based on their sender and content,</li> <li>detect spoofed emails and send them to the spam box,</li> <li>mirror your Instagram comments into an IMAP folder, respecting their original date, author, and threading, and attaching the original media,</li> <li>create advanced autoresponders, based on timeframes and number of unread messages in your mailbox,</li> <li>detect the language of an email.</li> </ul> <p>In the future, it will be able to :</p> <ul> <li>add an \u201curgent\u201d tag to emails sent by people with whom you have an appointment scheduled in the next 48 hours,</li> <li>add a \u201cclient\u201d tag to emails sent by someone who bought your product (connecting to the MySQL database of your Prestashop or WordPress WooCommerce website).</li> </ul>"},{"location":"index.html#quick-demos","title":"Quick demos","text":"<p>Filters are written in full Python and entirely modular. The quick demos after demonstrate how filters are coded.</p>"},{"location":"index.html#detect-spams-and-remove-them","title":"Detect spams and remove them","text":"<p>We use spoofing detection through SPF, DKIM and ARC, then check if the sender is already known in the CardDAV server, and finally use SpamAssassin headers if any.</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\ncarddav = protocols[\"carddav\"] if \"carddav\" in protocols else None\n\ndef filter(email) -&gt; bool:\n  # If email is spoofed or authentication is forged, exit immediately\n  if not email.is_authentic():\n    return True\n\n  names, addresses = email.get_sender()\n\n  # Email is in the address book :\u00a0exit early\n  if carddav.connection_inited:\n    for address in addresses:\n      if address in carddav.emails:\n        return False\n\n  # SpamAssassin headers if any\n  if email.has_header(\"X-Spam-Flag\"):\n    if email[\"X-Spam-Flag\"] == \"YES\":\n      return True\n\n  return False\n\ndef action(email):\n  email.spam(email.server.junk)\n\nimap.get_objects(\"INBOX\")\nimap.run_filters(filter, action)\n</code></pre>"},{"location":"index.html#sort-emails-in-folders-by-carddav-category","title":"Sort emails in folders by CardDAV category","text":"<p>Assuming your CardDAV contacts have a category (like \u201cclient\u201d, \u201cfamily\u201d, \u201cfriends\u201d), we can move their emails directly to a corresponding sub-folder of a <code>People</code> parent folder, or simply move them to <code>People</code> if they have no category:</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\ncarddav = protocols[\"carddav\"]\n\ndef filter(email) -&gt; bool:\n  global carddav\n  names, addresses = email.get_sender()\n\n  # Search if the sender is in the CardDAV address book\n  if len(addresses) &gt; 0:\n    results = carddav.search_by_email(addresses[0])\n\n    if results:\n      for vcard in results:\n        # Current sender can be found in multiple VCards on server, try them all\n        if len(vcard[\"categories\"]) &gt; 0:\n          # If more than one category is found, just take the first\n          category = vcard[\"categories\"][0]\n          folder_tree = [\"INBOX\", \"People\", category]\n          target_path = email.server.build_subfolder_name(folder_tree)\n          email.move(target_path)\n          return True\n\n      # We have no category but this contact is known\n      folder_tree = [\"INBOX\", \"People\"]\n      target_path = email.server.build_subfolder_name(folder_tree)\n      email.move(target_path)\n      return True\n\n  return False\n\nimap.get_objects(\"INBOX\")\nimap.run_filters(filter, None)\n</code></pre> <p>Note</p> <p>The true power of the Virtual Secretary here is mail folders and subfolders will be created dynamically for each contact category, meaning that:</p> <ul> <li>you don\u2019t need to know beforehand what the contact categories will be,</li> <li>you can add more contact categories anytime in the future, with no additional work on the filter,</li> <li>you don\u2019t need to manually map one filter with one folder, as with conventional mail filters.</li> </ul>"},{"location":"index.html#sort-github-emails-by-organizationproject","title":"Sort Github emails by organization/project","text":"<p>Github can generate a lot of notifications, but since all emails subjects start with <code>[organization/repository]</code>, we can use this to sort all emails into <code>Github/Organization/Repository</code> folders and dynamically replace <code>Organization</code> and <code>Repository</code> by their actual value from each email. We will unleash here the power of regular expressions:</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\n\ndef filter(email) -&gt; bool:\n  if email.is_in(\"notifications@github.com\", \"From\"):\n    import re\n    # find orga and repo in \"[orga/repo] blablabla\"\n    match = re.search(r\"\\[(.*?)\\/(.*?)\\]\", email[\"Subject\"])\n\n    if match:\n      # Replace . by _ in orga names containing .org\n      # because . is IMAP subfolder separator on Dovecot and Gmail.\n      organisation = match.groups()[0].replace(\".\", \"_\")\n      repository = match.groups()[1].replace(\".\", \"_\")\n      folder_tree = [\"INBOX\", \"Github\", organisation, repository]\n      target_path = email.server.build_subfolder_name(folder_tree)\n      email.move(target_path)\n      return True\n    else:\n      email.move(\"INBOX.Github\")\n      return True\n\n  return False\n\nimap.get_objects(\"INBOX\")\nimap.run_filters(filter, None)\n</code></pre> <p>Note</p> <p>This is not possible to achieve with conventional email filters because they don\u2019t allow to use the content to dynamically change the action performed on the email. So you would have to write one filter per repository, like <code>if email.subject contains(\"orga/repo\") then move(email, to=\"Github/Orga/Repo\"</code>, in addition of manually creating each <code>Github/Orga/Repo</code> subfolder.</p>"},{"location":"index.html#train-an-ai-to-classify-emails-for-you","title":"Train an AI\u00a0to classify emails for you","text":"<p>Assuming you have manually sorted your emails into folders and sub-folders, in a way that makes sense, we can try to find patterns in the content of those emails like this:</p> <pre><code>import pickle\nimport os\n\nprotocols = globals()\nimap = protocols[\"imap\"]\nemails_set = []\n\ndef filter(email) -&gt; bool:\n  global emails_set, folder\n  # Create a tuple with the content (subject and body) of the email and its foldername as label for training\n  # You can use any other meaningful textual property in place of the foldername.\n  emails_set.append((email[\"Subject\"] + \"\\r\\n\\r\\n\" + \\\n    email.get_body(preferencelist='plain'), folder))\n  return False\n\n# Create the database of emails\nfor folder in imap.folders:\n  imap.get_objects(folder, n_messages=500)\n  imap.run_filters(filter, None)\n\n# Train the word embedding and SVM classifier against the database\ntraining_set = [imap.nlp.Data(post[0], post[1]) for post in emails_set]\nembedding_set = [post[0] for post in emails_set]\nmodel = imap.nlp.Classifier(training_set, embedding_set, 'classifier.joblib', True)\n</code></pre> <p>To use the trained model in a filter that will move emails in the relevant folder for you, it\u2019s as simple as:</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\n\ndef filter(email) -&gt; bool:\n  global imap\n  model = imap.nlp.Classifier([], [], 'classifier.joblib', False)\n  content = email[\"Subject\"] + \"\\r\\n\\r\\n\" + email.get_body(preferencelist='plain')\n  result = model.prob_classify_many([content]))\n\n  if result[1] &gt; 0.5:\n    # Do something only if we found a label with at least 50 % confidence\n    # The label is directly the folder/subfolder\n    email.move(result[0])\n\n  return False\n\nimap.get_objects(\"INBOX\")\nimap.run_filters(filter, None)\n</code></pre> <p>The model will be stored in a file named <code>classifier.joblib</code> that can be saved and shared between computers. On a training sample of 6500 emails mixing both French and English, I\u00a0get a predictive accuracy between 87 and 90 %.</p> <p>Note</p> <p>Training your own AI makes sure it uses your language(s) and it knows the specific vocabulary of your particular business (including slang, trademarks and company names). This training is done on your own computer or server, not on a third-party cloud, which solves one of the major data privacy concerns of AI in its current SaaS approach.</p>"},{"location":"index.html#extensible-by-design","title":"Extensible by design","text":"<p>Protocols are managed through an abstract class. To implement your own connector for protocol <code>xyz</code>, you only need to inherit the <code>Server</code> and <code>Content</code> abstract classes from <code>src/core/connectors.py</code>, then put your children classes in a file named <code>xyz_server.py</code>, into the <code>src/protocols</code> folder. It will then be automatically loaded by the framework and will be accessible from the filters through:</p> <pre><code>protocols = globals()\nxyz = protocols[\"xyz\"]\n</code></pre>"},{"location":"Filter%20API/Instagram.html","title":"Instagram","text":"<p>Instagram has grown into a major pain in the ass since it went from a casual Instamatix-like microblogging app to a de-facto business-oriented influencer/influencee advertising platform that still wants to rely 100% on the mobile app paradigm. While this makes it practically a must-have for businesses and brands online presence, managing customers through IG direct messages diverts your work into one more channel, which is, by design, not integrated into the rest of your tools. Mix that with Twitter and YouTube and you have all you need to spend the day answering messages without getting anything done.</p> <p>While Instagram allows you to get emails notifications for new comments, you can\u2019t get direct messages (DM) as well (which makes zero sense from an usability perspective). I\u00a0will present here a Virtual Secretary workflow to duplicate comments and DM from Instagram into your IMAP\u00a0mailbox.</p>"},{"location":"Filter%20API/Instagram.html#requirements","title":"Requirements","text":"<p>This uses the Facebook Graph API for Instagram, which imposes that:</p> <ol> <li>You have one Instagram Business or Creator account (aka not the regular customer account),</li> <li>You have a Facebook page for your business,</li> <li>You have a Facebook account that has admin rights on that page,</li> <li>Your Facebook business page is linked to your Instagram Businesse/Creator account,</li> <li>You need to create an app for your business in order to allow the members of your organization to use it (it will not be published for general use):<ol> <li>Under \u201ctype\u201d, choose \u201cBusiness\u201d (as of 2022, API v15). In any case, you need the account type allowing access to Graph API for Instagram and Messenger,</li> <li>Choose the Business account linked to your Facebook page,</li> <li>Write down the app ID, which will be used in the next step inside the config assistant.</li> </ol> </li> <li>You need to enable the message control by connected apps in the Instagramm app, under Settings -&gt; Privacy -&gt; Messages.</li> </ol> <p>Note that:</p> <ol> <li>Accessing your IG DMs is actually done through your FB account, so you need both accounts alive,</li> <li>Linking your FB page to your IG account can only be achieved through the IG mobile app, not on the website,</li> <li>Linking your IG account to your FB page from your FB page doesn\u2019t work, it needs to be done on the IG mobile app.</li> <li>\u201cCreating an app\u201d is the FB/IG way of saying \u201cgetting a Rest API key\u201d, because they assume that you want the first if you are requesting the other. All we need is to generate an oAuth token by which you can grab IG user data from IG from a script. Facebook will nag you about getting your app reviewed assuming you want to publish it for general use (that is, grabbing other user\u2019s data), but as long as only the \u201capp\u201d owner uses it to access his own account, there is no need.</li> </ol>"},{"location":"Filter%20API/Instagram.html#set-up","title":"Set-up","text":"<ol> <li>Install the Python dependencies for the config assistant):  <pre><code>pip install PySide6 oauthlib requests_oauthlib\n</code></pre></li> <li>Call <code>python src/facebook-login.py</code>. This will prompt an user-friendly config assistant allowing you to connect to your FB account to grant access to your app and setup permissions. You need to grant all requested permissions on your pages for your app. This practically means getting an authentification token, along with the ID of your FB page and IG account. </li> <li>If everything goes well, a new config file called <code>instagram.ini</code> will be created in the same folder as your <code>src/facebook-login.py</code>, with everything we need, looking like :</li> </ol> <pre><code>[instagram]\naccess_token = XXXXXXX\ndata_access_expiration_time = 1675362365\nexpires_in = 0\nstate = EWZw0pzq8bLmC6PqQ4B20L66MHH7vg\nexpires_at = 1667586366.0085597\nclient_id = XXXXXX\nbusiness_id = XXXXXXX\ninstagram_business_account = XXXXXX\n</code></pre> <ol> <li>Copy and paste the content of the <code>instagram.ini</code> file above in the <code>settings.ini</code> file of the relevant email directory. If your Virtual Secretary instance runs on a remote server, you can generate the token locally on your desktop and copy-paste the IG credentials on your server. </li> <li>This token is valid only for 4 months (typically), so you will need to renew it in the future following the same procedure.</li> </ol> <p>Notes:</p> <ul> <li>The config assistant aims at providing a basic yet user-friendly way of retrieving the IDs of accounts. It does not check all possible corner cases, does not protect users from themselves and works only if all the requirements above have been met. If it doesn\u2019t work, you have messed up somewhere and need to check that your FB account has the proper management permissions over the FB business page, the FB business page is properly linked to an IG business account, etc.</li> <li>The config assistant runs locally on your device, fetching authentification token for your app with your account. Everything happens between you and Facebook, no third-party service provider or server is involved.</li> <li>The access token fetched by the assistant is like a password, you need to store it securely, not post it in forums, questions or post screenshots containing it.</li> <li>If there is already an <code>instagram.ini</code> file along the script <code>/src/facebook-login.php</code> and the authentification token is still valid, the config assistant uses it by default. If you need to configure another account, you will need to rename, move or delete this file.</li> </ul>"},{"location":"Filter%20API/Instagram.html#use","title":"Use","text":"<p>TODO.</p>"},{"location":"Filter%20API/carddav.html","title":"CardDAV server","text":"<p>CardDAV servers manage address books through devices. They can be used in email filters to map filtering behaviour on sender\u2019s info, for example CardDAV category, or whether or not the sender is in the address book already.</p>"},{"location":"Filter%20API/emails.html","title":"Emails","text":"<p>The emails handling is split in 2 parts:</p> <ul> <li>the server part, dealing with IMAP connection, fetching/adding mailboxes folders etc.</li> <li>the object part, dealing with the email content per-se.s</li> </ul>"},{"location":"Filter%20API/emails.html#preamble","title":"Preamble","text":"<p>There are a couple of oddities with IMAP servers that you should be aware of.</p>"},{"location":"Filter%20API/emails.html#standards-are-for-idiots-who-believe-in-them","title":"Standards are for idiots who believe in them","text":"<p>Gmail and Outlook seem to take an ill-placed pleasure in ignoring IMAP standards (the RFCs), that are otherwise well supported by open-source server software like Dovecot. As such, there are several things we can\u2019t do \u201cthe simple way\u201d because we have to account for those discrepancies. Methods are provided in the <code>imap_server</code> class to get data (like the names of the default mailboxes) properly, no matter the server implementation. You are advised to always use the provided methods to get mailboxes data for your filters, because they take care of the discrepancies internally and allow you to write portable filters.</p>"},{"location":"Filter%20API/emails.html#mail-account-vs-mailbox","title":"Mail account vs. mailbox","text":"<p>A mail account has usually (always ?) a root, main, default, top-level mailbox named <code>INBOX</code> or <code>Inbox</code>, depending on servers (it\u2019s case-sensitive). That\u2019s where incoming emails end up. Then, this one has subfolders, like <code>Sent</code>, <code>Junk</code>, etc., also named mailboxes by IMAP\u00a0specification. That can be confusing, so I\u00a0always refer to them here as \u201cfolders\u201d and \u201csubfolders\u201d.</p> <p>IMAP servers only let you grab emails from one mailbox at a time, in a non-recursive fashion. It means that we will need to iterate over the list of known folders and subfolders to fetch all emails from a mail account. This list can be found in protocols.imap_server.Server.folders.</p>"},{"location":"Filter%20API/emails.html#emails-have-no-truly-unique-id","title":"Emails have no truly unique ID","text":"<p>The IMAP UID of an email is only the positional order of reception of the email in the current mailbox. When moving emails to another mailbox, their UID will actually change. But moving emails to another mailbox and back to their original mailbox will not give them back their original UID either, as it is an index that can only be incremented.</p> <p>The RFC 822 defines the <code>Message-ID</code> header, that is indeed an unique identifier set when sending an email, like <code>abcdef@mailserver.com</code>, where <code>abcdef</code> is a random hash. The problem is this ID is set at the discretion of the email sender, and spam/spoofed emails don\u2019t have one.</p> <p>To circumvent this issue, the protocols.imap_object.EMail.create_hash <code>protocols.imap_object.EMail.create_hash</code> method creates a truly unique and persistent hash, using the data available in the email, like its date, sender and <code>Message-ID</code> header, in order to identify emails in logs through their moves between mailboxes.</p> <p>Unfortunately, IMAP actions still have to use the IMAP UID.</p>"},{"location":"Filter%20API/your-own-protocols.html","title":"Implementing your own protocels","text":"<p>The whole logic of the Virtual Secretary is meant to allow low-overhead extensibility. Adding new filters is as easy as adding new Python files, provided they respect the proper naming pattern, and so is adding new connectors for new protocols.</p>"},{"location":"Filter%20API/your-own-protocols.html#a-foreword-on-the-main-logic","title":"A foreword on the main logic","text":"<p>The structure of the program follows the dependency inversion principle where the interface layer is the <code>src/core/connectors.py</code> module which declares the abstract classes to be implemented by all protocol connectors.</p> <p>The <code>main.py</code> script is the entry point of the program and will build the list of all filters detected in subfolders that match the required naming patterns. For now, only patterns matching <code>00-protocol-filtername.py</code> or <code>LEARN-protocol-filtername.py</code> (where <code>protocol</code> is the actual protocol you are using, like <code>imap</code>, <code>smtp</code>, <code>carddav</code>, etc.) are recognized and will be run respectively by passing <code>process</code> or <code>learn</code> mode arguments to the bash call <code>$ python main.py config/ mode</code>. Adding more options to select a different set of filters is possible in the future simply by changing the filename patterns used to build the dictionary of filters at the very beginning of the program, the core will remain unchanged.</p> <p>With this list of filters to process, <code>main.py</code> calls the main manager <code>Secretary</code>, which is the class in <code>src/secretary.py</code>. This manager loads every module in <code>src/protocols</code> that has a filename matching the pattern <code>protocol_server.py</code> (where, again, <code>protocol</code> is the actual protocol you are using, like <code>imap</code>, <code>smtp</code>, <code>carddav</code>, etc.), start the corresponding servers, dispatch the credentials from the <code>settings.ini</code> configuration file, trigger the logins, loop through all the planned filters and then close the servers.</p> <p>The <code>Secretary</code> manager knows protocol implementations only through their abstract base classes in the interface layer <code>src/core/connectors.py</code> and loads all the matching modules blindly. This means that the connectors must properly inherit the <code>Server</code> and <code>Content</code> base classes and implement all of their mandatory abstract methods. Connectors must also be declared in <code>src/protocols</code> within files matching the pattern <code>protocol_server.py</code>. In return, this rigid logic allows to add as many connectors as you want by just adding new modules in <code>/src/protocols</code>, and the base files take care of starting, connecting and wiring everything in about 50 LoC each.</p> <p>The <code>Secretary</code> manager tracks the list of all active servers/protocols in its <code>Secretary.protocols</code> dictionary, which is passed as a global variable to all filters, allowing users to dispatch events between servers/protocols.</p>"},{"location":"Filter%20API/your-own-protocols.html#adding-your-own-protocol","title":"Adding your own protocol","text":"<p>Let\u2019s say you want to add the support for FTP protocol. First, you need to add an <code>ftp</code> section in <code>settings.ini</code>, like:</p> <pre><code>[ftp]\nserver = ftp.server.com\nuser = me\npassword = XXXXXXXX\n</code></pre> <p>Then, you need to create <code>ftp_server.py</code> and <code>ftp_object</code> in <code>src/protocols</code>. <code>ftp_server.py</code> will need to contain at very least: <pre><code>from protocols import ftp_object\nimport connectors\n\nclass Server(connectors.Server[ftp_object.Content]):\n\n    def get_objects(self):\n        # Fetch objects on server and populate the list\n        i = 0\n        for element in server_queue:\n            self.objects.append(ftp_object.Content(element, i, self))\n            i += 1\n\n    def run_filters(self, filter, action, runs=1):\n        # Define the log file as an hidden file inheriting the filter filename\n        filtername = self.calling_file()\n        directory = os.path.dirname(filtername)\n        basename = os.path.basename(filtername)\n        filter_logfile = os.path.join(directory, \".\" + basename + \".log\")\n        # Do what you will with the logfile to track what happened here\n\n        # Loop over objects to apply filters and actions\n        for object in self.objects:\n            try:\n                filter_on = filter(object)\n            except:\n                filter_on = False\n\n            if filter_on:\n                try:\n                    action(object)\n                except:\n                    pass\n\n    def init_connection(self, params: dict):\n        # High-level method to login to a server in one shot\n        # `params` are dispatched from the `[ftp]` section of `settings.ini` file by `Secretary`\n        password = params[\"password\"]\n        server = params[\"server\"]\n        user = params[\"user\"]\n\n        # Open connection, probably through some low-level Python lib\n\n        # Notify that we have a server with an active connection\n        self.connection_inited = True\n\n    def close_connection(self):\n        # High-level method to logout from a server\n</code></pre></p> <p>For more information, look into the <code>Server</code> class in <code>src/core/connectors.py</code>. Filters running on triggers which server has <code>Server.connection_inited</code> set to <code>False</code> are automatically discarded, so don\u2019t forget to set it but only after you got the server response to login commands. If you forgot the implement some mandatory methods, the program will throw an error before even starting a connection. You can also define read-only or write-only filters, for example the SMTP protocol (emails sending) is write-only and would have no <code>get_objects()</code> method and its <code>run_filters()</code> method would only contain <code>pass</code>.</p> <p>Then, the <code>ftp_object.py</code> file will need to contain at very least: <pre><code>import connectors\n\nclass Content(connectors.Content):\n  def __init__(self, raw_content, index:int, server) -&gt; None:\n    super().__init__(raw_content, index, server)\n    # Unpack, parse, decode the raw_content\n</code></pre></p> <p>The <code>Content</code> class is the one that is presented to the user when writing filters, so it should contain all the relevant data pre-parsed in members as well as proxy methods to trigger usual operations from the server on this particular object, like move, delete, tag. Then, the <code>Server</code> class should wrap low-level libraries into nice, specialized, user actions.</p> <p>The <code>Content</code> objects are meant to represent the basic individual data unit from the protocol you use, for example, for IMAP, it is emails, for CardDAV, it is contacts, for CalDAV, it is events, for FTP, it is files, etc. Then, in the <code>Server.objects</code> list, we aggregate them per folder, or per category, etc.</p> <p>From there, you can directly start writing filters. Nothing more is needed. For nicer filter debugging, you may want to manage exceptions and print appropriate warnings and errors.</p> <p>For a complete example, look at the <code>imap</code> protocol implementation.</p> <p>Note that protocols must have the same lowercase name everywhere to load modules, servers and settings properly.</p>"},{"location":"api/index.html","title":"API Reference","text":""},{"location":"api/index.html#core","title":"core","text":""},{"location":"api/index.html#batching","title":"batching","text":"<p>High-performance, paralellized high-level methods to process large corpora of documents.</p>"},{"location":"api/index.html#connectors","title":"connectors","text":"<p>Provide the abstract classes for server and content, that need to be implemented for each protocol.</p>"},{"location":"api/index.html#crawler","title":"crawler","text":"<p>Module containing utilities to crawl websites for HTML, XML and PDF pages for their text content. PDF can be read from their text content if any, or through optical characters recognition for scans. Websites can be crawled from a <code>sitemap.xml</code> file or by following internal links recursively from and index page. Each page is aggregated on a list of core.types.web_page objects, meant to be used as input to train natural language AI models and to index and rank for search engines.</p>"},{"location":"api/index.html#database","title":"database","text":"<p>Create an SQLite database of <code>web_pages</code> to be used by a search engine.</p>"},{"location":"api/index.html#deduplicator","title":"deduplicator","text":"<p>Find and remove duplicates and near-duplicates in a list of core.types.web_page</p>"},{"location":"api/index.html#facebook-login","title":"facebook-login","text":""},{"location":"api/index.html#language","title":"language","text":""},{"location":"api/index.html#network","title":"network","text":""},{"location":"api/index.html#nlp","title":"nlp","text":"<p>High-level natural language processing module for message-like (emails, comments, posts) input.</p>"},{"location":"api/index.html#parser","title":"parser","text":""},{"location":"api/index.html#patterns","title":"patterns","text":"<p>Contains global regular expression patterns re-used in the app.</p>"},{"location":"api/index.html#pdf","title":"pdf","text":"<p>PDF parsing utils, including OCR.</p>"},{"location":"api/index.html#search","title":"search","text":""},{"location":"api/index.html#secretary","title":"secretary","text":"<p>High level manager of all server connectors and bank of filters:</p>"},{"location":"api/index.html#types","title":"types","text":""},{"location":"api/index.html#utils","title":"utils","text":"<p>Logging and filter finding utilities.</p>"},{"location":"api/index.html#protocols","title":"protocols","text":""},{"location":"api/index.html#carddav_server","title":"carddav_server","text":""},{"location":"api/index.html#imap_object","title":"imap_object","text":""},{"location":"api/index.html#imap_server","title":"imap_server","text":""},{"location":"api/index.html#instagram_server","title":"instagram_server","text":""},{"location":"api/index.html#smtp_server","title":"smtp_server","text":""},{"location":"api/SUMMARY.html","title":"SUMMARY","text":"<ul> <li>API Reference</li> <li>core<ul> <li>batching</li> <li>connectors</li> <li>crawler</li> <li>database</li> <li>deduplicator</li> <li>facebook-login</li> <li>language</li> <li>network</li> <li>nlp</li> <li>parser</li> <li>patterns</li> <li>pdf</li> <li>search</li> <li>secretary</li> <li>types</li> <li>utils</li> </ul> </li> <li>protocols<ul> <li>carddav_server</li> <li>imap_object</li> <li>imap_server</li> <li>instagram_server</li> <li>smtp_server</li> </ul> </li> </ul>"},{"location":"api/core/batching.html","title":"core.batching","text":""},{"location":"api/core/batching.html#core.batching","title":"core.batching","text":"<p>High-performance, paralellized high-level methods to process large corpora of documents.</p> <p>Interfaces NLP processing with database entries, for efficient RAM management.</p> <p>Database structure is hard-coded and expects conformation to data structures defined in core.database and core.types</p> <p>\u00a9 2026 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/batching.html#core.batching-attributes","title":"Attributes","text":""},{"location":"api/core/batching.html#core.batching.LANG_MAP","title":"core.batching.LANG_MAP  <code>module-attribute</code>","text":"<pre><code>LANG_MAP = {\n    \"en\": \"english\",\n    \"fr\": \"french\",\n    \"de\": \"german\",\n    \"es\": \"spanish\",\n    \"it\": \"italian\",\n    \"pt\": \"portuguese\",\n    \"nl\": \"dutch\",\n    \"sv\": \"swedish\",\n    \"no\": \"norwegian\",\n    \"da\": \"danish\",\n    \"fi\": \"finnish\",\n    \"ru\": \"russian\",\n    \"ro\": \"romanian\",\n    \"hu\": \"hungarian\",\n    \"tr\": \"turkish\",\n}\n</code></pre> <p>Map ISO 639-1 language codes of supported languages to their full-name, as used by pre-trained corpora</p>"},{"location":"api/core/batching.html#core.batching.LANG_MAP_REVERSE","title":"core.batching.LANG_MAP_REVERSE  <code>module-attribute</code>","text":"<pre><code>LANG_MAP_REVERSE = {v: k for k, v in (LANG_MAP.items())}\n</code></pre> <p>Map the full-name of supported languages, as used by pre-trained corpora, to ISO 639-1 language codes</p>"},{"location":"api/core/batching.html#core.batching.STOPWORDS_DICT","title":"core.batching.STOPWORDS_DICT  <code>module-attribute</code>","text":"<pre><code>STOPWORDS_DICT = {\n    language: (set(STOPWORDS_DICT[language])) for language in STOPWORDS_DICT\n}\n</code></pre> <p>Dictionnary of stopwords (as sets values) mapped to full language names (as keys)</p>"},{"location":"api/core/batching.html#core.batching.regex_starter","title":"core.batching.regex_starter  <code>module-attribute</code>","text":"<pre><code>regex_starter = '(?&lt;=^|\\\\s|\\\\[|\\\\(|\\\\{|\\\\&lt;|\\\\\\'|\\\\\"|`|;|\\\\&gt;)'\n</code></pre> <p>Start of line, or start of document, or start of markup</p>"},{"location":"api/core/batching.html#core.batching.regex_stopper","title":"core.batching.regex_stopper  <code>module-attribute</code>","text":"<pre><code>regex_stopper = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|\\\\&lt;)'\n</code></pre> <p>End of line, or end of document, or end of markup</p>"},{"location":"api/core/batching.html#core.batching.end_of_word","title":"core.batching.end_of_word  <code>module-attribute</code>","text":"<pre><code>end_of_word = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|:|,|\\\\?|\\\\!|\\\\.|\\\\&lt;)'\n</code></pre> <p>End of word, or end of line, or end of document, or end of markup</p>"},{"location":"api/core/batching.html#core.batching.regex_algebra","title":"core.batching.regex_algebra  <code>module-attribute</code>","text":"<pre><code>regex_algebra = '[\\\\+\\\\-\\\\=\\\\\u2260\\\\\u00b1]'\n</code></pre> <p>Algebraic signs</p>"},{"location":"api/core/batching.html#core.batching.IP_PATTERN","title":"core.batching.IP_PATTERN  <code>module-attribute</code>","text":"<pre><code>IP_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_ip, regex_stopper), re.IGNORECASE\n)\n</code></pre> <p>IPv4 and IPv6 patterns where the whole IP is captured in the first group.</p>"},{"location":"api/core/batching.html#core.batching.EMAIL_PATTERN","title":"core.batching.EMAIL_PATTERN  <code>module-attribute</code>","text":"<pre><code>EMAIL_PATTERN = re.compile(\n    \"&lt;?([0-9a-z\\\\-\\\\_\\\\+\\\\.]+?@[0-9a-z\\\\-\\\\_\\\\+]+(\\\\.[0-9a-z\\\\_\\\\-]{2,})+)&gt;?\",\n    re.IGNORECASE,\n)\n</code></pre> <p>Emails patterns like <code>&lt;me@mail.com&gt;</code> or <code>me@mail.com</code> where the whole address is captured in the first group.</p>"},{"location":"api/core/batching.html#core.batching.URL_PATTERN","title":"core.batching.URL_PATTERN  <code>module-attribute</code>","text":"<pre><code>URL_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_url, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>URL patterns like <code>http(s)://domain.ext/page/subpage?q=x&amp;r=0:1#anchor</code> or <code>//domain.ext/page</code>. URL must follow RFC3986, meaning query parameters should be before anchors, if any. Relying on this assumption allows a faster regex parsing.</p> <ul> <li>the protocol (ftp, ftps, http, https) is captured as the first group,</li> <li><code>domain.ext</code> is captured as the second group,</li> <li><code>/page/etc</code> is the third group, including leading and trailing <code>/</code>,</li> <li>page query parameters <code>?s=x&amp;r=0</code>, including <code>?</code>, is the fourth group if the URL declares <code>...?params#anchor</code>,</li> <li>anchor <code>#anchor</code> is the fifth group, including <code>#</code>, if the URL declares <code>...?params#anchor</code>.</li> </ul> <p>URLs are captured if they are:</p> <ul> <li>alone on their own line,</li> <li>enclosed in <code>{}</code>, <code>[]</code>, <code>()</code></li> <li>enclosed in whitespaces.</li> </ul> <p>Warning: URLs enclosed in <code>()</code>, <code>[]</code> and <code>{}</code> may retain the closing sign  as part of the page name since <code>()</code> and <code>[]</code> are valid in URL pathes  and parameters. This pattern will work on plain text only: Markdown, XML, HTML and JSON  will need to be parsed ahead.</p>"},{"location":"api/core/batching.html#core.batching.MEMBERS_PATTERN","title":"core.batching.MEMBERS_PATTERN  <code>module-attribute</code>","text":"<pre><code>MEMBERS_PATTERN = re.compile('(?&lt;=[a-z])(\\\\.)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Domain patterns without leading protocol like <code>cdn.company.com</code> or class members in object-oriented programming languages like <code>params.cookies.client</code>.</p>"},{"location":"api/core/batching.html#core.batching.DATE_PATTERN","title":"core.batching.DATE_PATTERN  <code>module-attribute</code>","text":"<pre><code>DATE_PATTERN = re.compile(date_regex, re.IGNORECASE)\n</code></pre> <p>Dates like <code>2022-12-01</code>, <code>01-12-2022</code>, <code>01-12-22</code>, <code>01/12/2022</code>, <code>01/12/22</code> where the whole date is captured in the first group, then each group of digits is captured in the order of appearance, in the next 3 groups</p>"},{"location":"api/core/batching.html#core.batching.TIME_PATTERN","title":"core.batching.TIME_PATTERN  <code>module-attribute</code>","text":"<pre><code>TIME_PATTERN = re.compile(time_regex, re.IGNORECASE)\n</code></pre> <p>Identify more or less standard time patterns, like :</p> <ul> <li>12h15</li> <li>12:15</li> <li>12:15:00</li> <li>12am</li> <li>12 am</li> <li>12 h</li> <li>12:15:00Z</li> <li>12:15:00+01</li> <li>12:15:00 UTC+1</li> <li>11:27:45+0000</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>1- or 2-digits hour,</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>hour/minutes separator or half-day marker among <code>[\"h\", \":\", \"am\", \"pm\"]</code> (case-insensitive)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2-digits minutes, if any, or <code>None</code></p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>2-digits seconds, if any.</p> <p> TYPE: <code>str</code> </p> <code>4</code> <p>hour marker (<code>h</code> or <code>H</code>), half-day marker (case-insensitive <code>[\"am\", \"pm\"]</code>), or time zone marker (case-sensitive <code>[\"Z\", \"UTC\"]</code>)</p> <p> TYPE: <code>str</code> </p> <code>5</code> <p>1-or 2-digits signed integer timezone shift (referred to UTC).</p> <p> TYPE: <code>str</code> </p> <p>Examples:</p> <p>see https://regex101.com/r/QNtZAK/2</p> <p>see <code>src/tests/test-patterns.py</code></p>"},{"location":"api/core/batching.html#core.batching.DOMAIN_PATTERN","title":"core.batching.DOMAIN_PATTERN  <code>module-attribute</code>","text":"<pre><code>DOMAIN_PATTERN = re.compile(\n    \"from ((?:[a-z0-9\\\\-_]{0,61}\\\\.)+[a-z]{2,})\", re.IGNORECASE\n)\n</code></pre> <p>Matches patterns like <code>from (domain.ext)</code> from RFC-822 <code>Received</code> header in emails.</p>"},{"location":"api/core/batching.html#core.batching.UID_PATTERN","title":"core.batching.UID_PATTERN  <code>module-attribute</code>","text":"<pre><code>UID_PATTERN = re.compile('UID ([0-9]+)')\n</code></pre> <p>Matches email integer UID from IMAP headers.</p>"},{"location":"api/core/batching.html#core.batching.FLAGS_PATTERN","title":"core.batching.FLAGS_PATTERN  <code>module-attribute</code>","text":"<pre><code>FLAGS_PATTERN = re.compile('FLAGS \\\\((.*?)\\\\)')\n</code></pre> <p>Matches email flags from IMAP headers.</p>"},{"location":"api/core/batching.html#core.batching.PATH_PATTERN","title":"core.batching.PATH_PATTERN  <code>module-attribute</code>","text":"<pre><code>PATH_PATTERN = re.compile('%s%s%s' % (regex_starter, path_regex, end_of_word))\n</code></pre> <p>File path pattern like <code>~/file</code>, <code>/home/file</code>, <code>./file</code> or <code>C:\\windows</code></p>"},{"location":"api/core/batching.html#core.batching.PARTIAL_PATH_REGEX","title":"core.batching.PARTIAL_PATH_REGEX  <code>module-attribute</code>","text":"<pre><code>PARTIAL_PATH_REGEX = re.compile(\n    \"%s%s%s\" % (regex_starter, partial_path_regex, end_of_word)\n)\n</code></pre> <p>Partial, invalid path patterns missing the leading root, like <code>home/user/stuff</code>. We start capturing after at least two folder separators (slash or backslash).</p> <p>Warning</p> <p>this will collide with date detection, so run it after in the pipeline.</p>"},{"location":"api/core/batching.html#core.batching.RESOLUTION_PATTERN","title":"core.batching.RESOLUTION_PATTERN  <code>module-attribute</code>","text":"<pre><code>RESOLUTION_PATTERN = re.compile('\\\\d+(?:\u00d7|x|X)\\\\d+')\n</code></pre> <p>Pixel resolution like 10x20 or 10\u00d720. Units are discarded.</p>"},{"location":"api/core/batching.html#core.batching.NUMBER_PATTERN","title":"core.batching.NUMBER_PATTERN  <code>module-attribute</code>","text":"<pre><code>NUMBER_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_number, regex_stopper)\n)\n</code></pre> <p>Signed integers and decimals, fractions and numeric IDs with interal dashes and underscores. Numbers with starting or trailing units are not considered. Lazy decimals (.1 and 1.) are considered.</p>"},{"location":"api/core/batching.html#core.batching.HASH_PATTERN","title":"core.batching.HASH_PATTERN  <code>module-attribute</code>","text":"<pre><code>HASH_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_hash, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>Cryptographic hexadecimal hashes and fingerprints, of a min length of 8 characters.</p>"},{"location":"api/core/batching.html#core.batching.MULTIPLE_LINES","title":"core.batching.MULTIPLE_LINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_LINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]{2,} ?)+')\n</code></pre> <p>Detect more than 2 newlines and tab, possibly mixed with spaces</p>"},{"location":"api/core/batching.html#core.batching.MULTIPLE_NEWLINES","title":"core.batching.MULTIPLE_NEWLINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_NEWLINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]+ ?){2,}')\n</code></pre> <p>Detect broken sequences of newlines and spaces.</p>"},{"location":"api/core/batching.html#core.batching.INTERNAL_NEWLINE","title":"core.batching.INTERNAL_NEWLINE  <code>module-attribute</code>","text":"<pre><code>INTERNAL_NEWLINE = re.compile('(?&lt;=\\\\w)[\\\\n\\\\t\\\\r]{1}(?=\\\\w)')\n</code></pre> <p>Detect single newline characters nested inside text. Mostly useful for parsed PDF where line wrapping is quite literal ( used instead of space).</p>"},{"location":"api/core/batching.html#core.batching.EXPOSURE","title":"core.batching.EXPOSURE  <code>module-attribute</code>","text":"<pre><code>EXPOSURE = re.compile(\n    \"%s%s%s\" % (regex_starter, exposure_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/batching.html#core.batching.PHOTOSPEED","title":"core.batching.PHOTOSPEED  <code>module-attribute</code>","text":"<pre><code>PHOTOSPEED = re.compile(\n    \"%s%s%s\" % (regex_starter, photospeed_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/batching.html#core.batching.SENSIBILITY","title":"core.batching.SENSIBILITY  <code>module-attribute</code>","text":"<pre><code>SENSIBILITY = re.compile(\n    \"%s%s%s\" % (regex_starter, sensibility_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Photographic sensibility in ISO or ASA</p>"},{"location":"api/core/batching.html#core.batching.LUMINANCE","title":"core.batching.LUMINANCE  <code>module-attribute</code>","text":"<pre><code>LUMINANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, luminance_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Luminance/radiance in nits or Cd/m\u00b2</p>"},{"location":"api/core/batching.html#core.batching.DIAPHRAGM","title":"core.batching.DIAPHRAGM  <code>module-attribute</code>","text":"<pre><code>DIAPHRAGM = re.compile(\n    \"%s%s\" % (regex_starter, diaphragm_regex), flags=re.IGNORECASE\n)\n</code></pre> <p>Photographic diaph aperture values like f/2.8 or f/11</p>"},{"location":"api/core/batching.html#core.batching.GAIN","title":"core.batching.GAIN  <code>module-attribute</code>","text":"<pre><code>GAIN = re.compile(\n    \"%s%s%s\" % (regex_starter, gain_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Gain, attenuation and PSNR in dB</p>"},{"location":"api/core/batching.html#core.batching.FILE_SIZE","title":"core.batching.FILE_SIZE  <code>module-attribute</code>","text":"<pre><code>FILE_SIZE = re.compile(\n    \"%s%s%s\" % (regex_starter, filesize_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>File and memory size in bit, byte, or octet and their multiples</p>"},{"location":"api/core/batching.html#core.batching.DISTANCE","title":"core.batching.DISTANCE  <code>module-attribute</code>","text":"<pre><code>DISTANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, distance_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Distance in meter, inch, foot and their multiples</p>"},{"location":"api/core/batching.html#core.batching.PERCENT","title":"core.batching.PERCENT  <code>module-attribute</code>","text":"<pre><code>PERCENT = re.compile('%s%s%s' % (regex_starter, percent_regex, end_of_word))\n</code></pre> <p>Number followed by %</p>"},{"location":"api/core/batching.html#core.batching.WEIGHT","title":"core.batching.WEIGHT  <code>module-attribute</code>","text":"<pre><code>WEIGHT = re.compile(\n    \"%s%s%s\" % (regex_starter, weight_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Weight (mass) in British and SI units and their multiples</p>"},{"location":"api/core/batching.html#core.batching.ANGLE","title":"core.batching.ANGLE  <code>module-attribute</code>","text":"<pre><code>ANGLE = re.compile(\n    \"%s%s%s\" % (regex_starter, angle_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Angles in radians, degrees and steradians</p>"},{"location":"api/core/batching.html#core.batching.TEMPERATURE","title":"core.batching.TEMPERATURE  <code>module-attribute</code>","text":"<pre><code>TEMPERATURE = re.compile(\n    \"%s%s%s\" % (regex_starter, temperature_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Temperatures in \u00b0C, \u00b0F and K</p>"},{"location":"api/core/batching.html#core.batching.FREQUENCY","title":"core.batching.FREQUENCY  <code>module-attribute</code>","text":"<pre><code>FREQUENCY = re.compile(\n    \"%s%s%s\" % (regex_starter, frequency_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Frequencies in hertz and multiples</p>"},{"location":"api/core/batching.html#core.batching.TEXT_DATES","title":"core.batching.TEXT_DATES  <code>module-attribute</code>","text":"<pre><code>TEXT_DATES = re.compile(\n    \"([0-9]{1,2})? (jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec|jan|f\u00e9v|mar|avr|mai|jui|jui|aou|sep|oct|nov|d\u00e9c|janvier|f\u00e9vrier|mars|avril|mai|juin|juillet|ao\u00fbt|septembre|octobre|novembre|d\u00e9cembre|january|february|march|april|may|june|july|august|september|october|november|december)\\\\.?( [0-9]{1,2})?( [0-9]{2,4})(?!\\\\:)\",\n    flags=re.IGNORECASE | re.MULTILINE,\n)\n</code></pre> <p>Find textual dates formats:</p> <ul> <li>English dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> <li>French dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>month (full-form or abbreviated)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>4 digits (full year)</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/batching.html#core.batching.BASE_64","title":"core.batching.BASE_64  <code>module-attribute</code>","text":"<pre><code>BASE_64 = re.compile(\n    \"((?:[A-Za-z0-9+\\\\/]{4}){64,}(?:[A-Za-z0-9+\\\\/]{2}==|[A-Za-z0-9+\\\\/]{3}=)?)\"\n)\n</code></pre> <p>Identifies base64 encoding</p>"},{"location":"api/core/batching.html#core.batching.BB_CODE","title":"core.batching.BB_CODE  <code>module-attribute</code>","text":"<pre><code>BB_CODE = re.compile('\\\\[(img|quote)[a-zA-Z0-9 =\\\\\"]*?\\\\].*?\\\\[\\\\/\\\\1\\\\]')\n</code></pre> <p>Identifies left-over BB code markup <code>[img]</code> and <code>[quote]</code></p>"},{"location":"api/core/batching.html#core.batching.MARKUP","title":"core.batching.MARKUP  <code>module-attribute</code>","text":"<pre><code>MARKUP = re.compile('(?:\\\\[|\\\\{|\\\\&lt;)([^\\\\n\\\\r]+?)(?:\\\\]|\\\\}|\\\\&gt;)')\n</code></pre> <p>Identifies left-over HTML and Markdown markup, like <code>&lt;...&gt;</code>, <code>{...}</code>, <code>[...]</code></p>"},{"location":"api/core/batching.html#core.batching.USER","title":"core.batching.USER  <code>module-attribute</code>","text":"<pre><code>USER = re.compile('([\\\\w\\\\-\\\\+\\\\.]+)?@([\\\\w\\\\-\\\\+\\\\.]+)|(user\\\\-?\\\\d+)')\n</code></pre> <p>Identifies user handles or emails</p>"},{"location":"api/core/batching.html#core.batching.REPEATED_CHARACTERS","title":"core.batching.REPEATED_CHARACTERS  <code>module-attribute</code>","text":"<pre><code>REPEATED_CHARACTERS = re.compile('(.)\\\\1{9,}')\n</code></pre> <p>Identifies any character repeated more than 9 times</p>"},{"location":"api/core/batching.html#core.batching.UNFINISHED_SENTENCES","title":"core.batching.UNFINISHED_SENTENCES  <code>module-attribute</code>","text":"<pre><code>UNFINISHED_SENTENCES = re.compile('(?&lt;![?!.;:])\\\\n\\\\n|\\\\r\\\\n')\n</code></pre> <p>Identifies sentences finishing with 2 newlines characters without having ending punctuations</p>"},{"location":"api/core/batching.html#core.batching.MULTIPLE_DOTS","title":"core.batching.MULTIPLE_DOTS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DOTS = re.compile('\\\\.{2,}')\n</code></pre> <p>Identifies dots repeated more than twice</p>"},{"location":"api/core/batching.html#core.batching.MULTIPLE_DASHES","title":"core.batching.MULTIPLE_DASHES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DASHES = re.compile('[-~]{1,}')\n</code></pre> <p>Identifies dashes repeated more than once</p>"},{"location":"api/core/batching.html#core.batching.MULTIPLE_QUESTIONS","title":"core.batching.MULTIPLE_QUESTIONS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_QUESTIONS = re.compile('\\\\?{1,}')\n</code></pre> <p>Identifies question marks repeated more than once</p>"},{"location":"api/core/batching.html#core.batching.ORDINAL_FR","title":"core.batching.ORDINAL_FR  <code>module-attribute</code>","text":"<pre><code>ORDINAL_FR = re.compile('n\u00b0 ?([0-9]+)')\n</code></pre> <p>French ordinal numbers (num\u00e9ros n\u00b0)</p>"},{"location":"api/core/batching.html#core.batching.FRANCAIS","title":"core.batching.FRANCAIS  <code>module-attribute</code>","text":"<pre><code>FRANCAIS = re.compile(\n    \"%s(j|t|s|d|qu|lorsqu|quelqu|jusqu|m|c|n)\\\\'(?=[aeiouy\u00e9\u00e8\u00e0\u00ea\u00e2\u00ee\u00f4\u00fb\u00ef\u00fc\u00e4\u00eb\u00f6h][\\\\w\\\\s])\"\n    % regex_starter,\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>French contractions of pronouns and determinants</p>"},{"location":"api/core/batching.html#core.batching.DASHES","title":"core.batching.DASHES  <code>module-attribute</code>","text":"<pre><code>DASHES = re.compile('(?&lt;=\\\\w)(-|_|=)+(?=\\\\w)', re.IGNORECASE)\n</code></pre> <p>Dashes in the middle of ASCII/Latin compounded words. Will not work if accented or Unicode characters are immediately surrounding the dash.</p>"},{"location":"api/core/batching.html#core.batching.ALTERNATIVES","title":"core.batching.ALTERNATIVES  <code>module-attribute</code>","text":"<pre><code>ALTERNATIVES = re.compile('(?&lt;=[a-z])(\\\\/)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Slash-separated word alternatives like <code>and/or</code> <code>mr/mrs</code></p>"},{"location":"api/core/batching.html#core.batching.PLURAL_S","title":"core.batching.PLURAL_S  <code>module-attribute</code>","text":"<pre><code>PLURAL_S = re.compile('(?&lt;=[a-zA-Z]{4,})s?e{0,2}s%s' % end_of_word)\n</code></pre> <p>Identify plural form of nouns (French and English), adjectives (French) and third-person present verbs (English) and second-person verbs (French) in -s.</p>"},{"location":"api/core/batching.html#core.batching.FEMININE_E","title":"core.batching.FEMININE_E  <code>module-attribute</code>","text":"<pre><code>FEMININE_E = re.compile('(?&lt;=\\\\w{4,})e{1,2}%s' % end_of_word)\n</code></pre> <p>Identify feminine form of adjectives (French) in -e.</p>"},{"location":"api/core/batching.html#core.batching.DOUBLE_CONSONANTS","title":"core.batching.DOUBLE_CONSONANTS  <code>module-attribute</code>","text":"<pre><code>DOUBLE_CONSONANTS = re.compile(\n    \"(?&lt;=\\\\w{2,})([bcfghjklmnpqrstvwxz])\\\\1\", re.IGNORECASE\n)\n</code></pre> <p>Identify double consonants in the middle of words.</p>"},{"location":"api/core/batching.html#core.batching.FEMININE_TRICE","title":"core.batching.FEMININE_TRICE  <code>module-attribute</code>","text":"<pre><code>FEMININE_TRICE = re.compile('(?&lt;=\\\\w{4,})t(rice|eur|or)%s' % end_of_word)\n</code></pre> <p>Identify French feminine nouns in -trice.</p>"},{"location":"api/core/batching.html#core.batching.ADVERB_MENT","title":"core.batching.ADVERB_MENT  <code>module-attribute</code>","text":"<pre><code>ADVERB_MENT = re.compile('(?&lt;=\\\\w{4,})e?ment%s' % end_of_word)\n</code></pre> <p>Identify French adverbs and English nouns ending en -ment</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_TION","title":"core.batching.SUBSTANTIVE_TION  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_TION = re.compile('(?&lt;=\\\\w{4,})(t|s)ion%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from verbs by adding -tion and -sion</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_AT","title":"core.batching.SUBSTANTIVE_AT  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_AT = re.compile('(?&lt;=\\\\w{4,})at%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from other nouns by adding -at</p>"},{"location":"api/core/batching.html#core.batching.PARTICIPLE_ING","title":"core.batching.PARTICIPLE_ING  <code>module-attribute</code>","text":"<pre><code>PARTICIPLE_ING = re.compile('(?&lt;=\\\\w{4,})ing%s' % end_of_word)\n</code></pre> <p>Identify English substantives and present participles formed from verbs by adding -ing</p>"},{"location":"api/core/batching.html#core.batching.ADJECTIVE_ED","title":"core.batching.ADJECTIVE_ED  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_ED = re.compile('(?&lt;=\\\\w{4,})ed%s' % end_of_word)\n</code></pre> <p>Identify English adjectives formed from verbs by adding -ed</p>"},{"location":"api/core/batching.html#core.batching.ADJECTIVE_TIF","title":"core.batching.ADJECTIVE_TIF  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_TIF = re.compile('(?&lt;=\\\\w{2,})ti(f|v)%s' % end_of_word)\n</code></pre> <p>Identify English and French adjectives formed from verbs by adding -tif or -tive</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_Y","title":"core.batching.SUBSTANTIVE_Y  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_Y = re.compile('(?&lt;=\\\\w{3,})y%s' % end_of_word)\n</code></pre> <p>Identify English substantives ending in -y</p>"},{"location":"api/core/batching.html#core.batching.VERB_IZ","title":"core.batching.VERB_IZ  <code>module-attribute</code>","text":"<pre><code>VERB_IZ = re.compile('(?&lt;=\\\\w{4,})(i|y)z%s' % end_of_word)\n</code></pre> <p>Identify American verbs ending in -iz that French and Brits write in -is</p>"},{"location":"api/core/batching.html#core.batching.STUFF_ER","title":"core.batching.STUFF_ER  <code>module-attribute</code>","text":"<pre><code>STUFF_ER = re.compile('(?&lt;=\\\\w{5,})er%s' % end_of_word)\n</code></pre> <p>Identify French 1st group verb (infinitive) and English substantives ending in -er</p>"},{"location":"api/core/batching.html#core.batching.BRITISH_OUR","title":"core.batching.BRITISH_OUR  <code>module-attribute</code>","text":"<pre><code>BRITISH_OUR = re.compile('(?&lt;=\\\\w{3,})our%s' % end_of_word)\n</code></pre> <p>Identify British spelling ending in -our (colour, behaviour).</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_ITY","title":"core.batching.SUBSTANTIVE_ITY  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_ITY = re.compile('(?&lt;=\\\\w{4,})it(y|e)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ity (English) and -ite (French).</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_IST","title":"core.batching.SUBSTANTIVE_IST  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IST = re.compile('(?&lt;=\\\\w{3,})is(t|m)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ist and -ism.</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_IQU","title":"core.batching.SUBSTANTIVE_IQU  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IQU = re.compile('(?&lt;=\\\\w{3,})i(qu|c)%s' % end_of_word)\n</code></pre> <p>Identify French substantives in -iqu</p>"},{"location":"api/core/batching.html#core.batching.SUBSTANTIVE_EUR","title":"core.batching.SUBSTANTIVE_EUR  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_EUR = re.compile('(?&lt;=\\\\w{3,})eur%s' % end_of_word)\n</code></pre> <p>Identify French substantives -eur</p>"},{"location":"api/core/batching.html#core.batching.HYPHENIZED","title":"core.batching.HYPHENIZED  <code>module-attribute</code>","text":"<pre><code>HYPHENIZED = re.compile('(?&lt;=\\\\w{3,})[-\u2013\u2014]+ *[\\\\n\\\\r]{1,2}(?=\\\\w)')\n</code></pre> <p>Detect hyphenized words at the end of a PDF text line.</p>"},{"location":"api/core/batching.html#core.batching.WAYBACK_RE","title":"core.batching.WAYBACK_RE  <code>module-attribute</code>","text":"<pre><code>WAYBACK_RE = re.compile('https?://web\\\\.archive\\\\.org/web/[^/]+/(https?://.+)')\n</code></pre> <p>Find the canonical URL from web.archive.org (Wayback Machine) URLs</p>"},{"location":"api/core/batching.html#core.batching-classes","title":"Classes","text":""},{"location":"api/core/batching.html#core.batching.Lexicon","title":"core.batching.Lexicon  <code>dataclass</code>","text":"<pre><code>Lexicon(counts: Counter[str] = Counter())\n</code></pre> <p>Mutable token frequency index with canonicalization helpers for: - malformed n-grams, - merged/split variants, - plural compound normalization.</p> <p>Examples:</p> <p>liber_tarian  -&gt; libertarian etres_humains -&gt; etre_humain</p>"},{"location":"api/core/batching.html#core.batching.Lexicon-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.Lexicon.update","title":"core.batching.Lexicon.update","text":"<pre><code>update(corpus: Iterable[Iterable[str]]) -&gt; None\n</code></pre> <p>Update token frequencies from a corpus of tokenized sentences.</p> PARAMETER DESCRIPTION <code>corpus</code> <p>Iterable of tokenized sentences: [     [\u201cthis\u201d, \u201cis\u201d, \u201ca\u201d, \u201csentence\u201d],     [\u201canother\u201d, \u201csentence\u201d] ]</p> <p> TYPE: <code>Iterable[Iterable[str]]</code> </p>"},{"location":"api/core/batching.html#core.batching.Lexicon.frequency","title":"core.batching.Lexicon.frequency","text":"<pre><code>frequency(token: str) -&gt; int\n</code></pre> <p>Return token frequency.</p>"},{"location":"api/core/batching.html#core.batching.Lexicon.exists","title":"core.batching.Lexicon.exists","text":"<pre><code>exists(token: str) -&gt; bool\n</code></pre> <p>Check whether a token exists in the lexicon.</p>"},{"location":"api/core/batching.html#core.batching.Lexicon.prune","title":"core.batching.Lexicon.prune","text":"<pre><code>prune(min_count: int = 10) -&gt; None\n</code></pre> <p>Remove all entries whose frequency is lower than <code>min_count</code>.</p> PARAMETER DESCRIPTION <code>min_count</code> <p>Minimum frequency to keep.</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p>"},{"location":"api/core/batching.html#core.batching.Lexicon.resolve_token","title":"core.batching.Lexicon.resolve_token","text":"<pre><code>resolve_token(token: str, separator: str = '_', min_ratio: float = 1.0) -&gt; str\n</code></pre> <p>Attempt to canonicalize malformed n-grams.</p> <p>Operations: 1. malformed n-grams:     liber_tarian -&gt; libertarian</p> <ol> <li>plural compound reduction:     etres_humains -&gt; etre_humain</li> </ol> <p>Strategy: - if token exists already -&gt; keep it - otherwise:     - remove separators,     - check if merged variant exists,     - compare frequencies,     - prefer merged form if sufficiently frequent.</p> PARAMETER DESCRIPTION <code>token</code> <p>Token to canonicalize.</p> <p> TYPE: <code>str</code> </p> <code>separator</code> <p>N-gram separator.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'_'</code> </p> <code>min_ratio</code> <p>Require merged token frequency to be at least <code>min_ratio</code> times the split variant frequency.</p> <p>Helps avoid false positives.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Canonicalized token.</p>"},{"location":"api/core/batching.html#core.batching.Lexicon.canonicalize_sentence","title":"core.batching.Lexicon.canonicalize_sentence","text":"<pre><code>canonicalize_sentence(\n    sentence: list[str], separator: str = \"_\", min_ratio: float = 1.0\n) -&gt; list[str]\n</code></pre> <p>Canonicalize all tokens in a sentence.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer","title":"core.batching.Tokenizer","text":"<pre><code>Tokenizer(\n    meta_tokens: dict[re.Pattern, str] | None = None,\n    abbreviations: dict[str, str] | None = None,\n    replacements: dict[str, str] | None = None,\n    stopwords: set[str] | None = None,\n    lang_stopwords: dict[str, set[str]] | None = None,\n    backend: str = \"blingfire\",\n)\n</code></pre> <p>Pre-processing pipeline and tokenizer.</p> <p>Splits a string into normalized word tokens after applying a series of configurable text transformations.</p> PARAMETER DESCRIPTION <code>meta_tokens</code> <p>Pipeline of regular-expression substitutions used to replace document fragments with meta-tokens.</p> <p>Keys must be compiled <code>re.Pattern</code> objects and values must be meta-token strings, typically enclosed in underscores.</p> <p>Transformations are applied in declaration order. This relies on Python\u2019s ordered dictionaries (Python 3.7+).</p> <p>If not provided, a default pipeline suitable for bilingual English/French technical documents is used.</p> <p> TYPE: <code>dict[re.Pattern, str] | None</code> DEFAULT: <code>None</code> </p> <code>abbreviations</code> <p>Pipeline of abbreviation replacements as a <code>{to_replace: replacement}</code> dictionary.</p> <p>Replacements are applied in declaration order.</p> <p> TYPE: <code>dict[str, str] | None</code> DEFAULT: <code>None</code> </p> <code>replacements</code> <p>Dictionary of token-level substitutions applied as <code>{key: value}</code> string replacements.</p> <p> TYPE: <code>dict[str, str] | None</code> DEFAULT: <code>None</code> </p> <code>stopwords</code> <p>Language-agnostic stopwords to remove from the token stream.</p> <p> TYPE: <code>set[str] | None</code> DEFAULT: <code>None</code> </p> <code>lang_stopwords</code> <p>Language-specific stopwords.</p> <p>Keys must be ISO 639-1 language codes and values must be sets of stopwords associated with each language.</p> <p> TYPE: <code>dict[str, set[str]] | None</code> DEFAULT: <code>None</code> </p> <code>backend</code> <p>Tokenization backend to use.</p> <p>Supported values are:</p> <ul> <li><code>\"blingfire\"</code>: Microsoft BlingFire tokenizer (pattern-based).</li> <li><code>\"nltk\"</code>: NLTK Punkt tokenizer.</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'blingfire'</code> </p>"},{"location":"api/core/batching.html#core.batching.Tokenizer-attributes","title":"Attributes","text":""},{"location":"api/core/batching.html#core.batching.Tokenizer.characters_cleanup","title":"core.batching.Tokenizer.characters_cleanup  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>characters_cleanup: dict[(re.Pattern) : str] = {\n    MULTIPLE_DOTS: \"...\",\n    MULTIPLE_DASHES: \"-\",\n    MULTIPLE_QUESTIONS: \"?\",\n    REPEATED_CHARACTERS: \" \",\n    BB_CODE: \" \",\n    MARKUP: \" \\\\1 \",\n    BASE_64: \" \",\n}\n</code></pre> <p>Dictionnary of regular expressions (keys) to find and replace by the provided strings (values). Cleanup repeated characters, including ellipses and question marks, leftover BBcode and XML markup, base64-encoded strings and French pronominal contractions (e.g \u201cme + a\u201d contracted into \u201cm\u2019a\u201d).</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.internal_meta_tokens","title":"core.batching.Tokenizer.internal_meta_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>internal_meta_tokens: dict[(re.Pattern) : str] = {\n    HASH_PATTERN_FAST: \"_HASH_\",\n    NUMBER_PATTERN_FAST: \"_NUMBER_\",\n}\n</code></pre> <p>Dictionnary of regular expressions (keys) to find in full-tokens and replace by meta-tokens. Use simplified regex patterns for performance.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.abbreviations","title":"core.batching.Tokenizer.abbreviations  <code>instance-attribute</code>","text":"<pre><code>abbreviations = abbreviations\n</code></pre> <p>Abbreviations and contractions to replace in full documents</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.replacements","title":"core.batching.Tokenizer.replacements  <code>instance-attribute</code>","text":"<pre><code>replacements = replacements\n</code></pre> <p>Arbitrary string replacements in single tokens</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.stopwords","title":"core.batching.Tokenizer.stopwords  <code>instance-attribute</code>","text":"<pre><code>stopwords = set(stopwords) if stopwords else None\n</code></pre> <p>Language-agnostic stopwords</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.lang_stopwords","title":"core.batching.Tokenizer.lang_stopwords  <code>instance-attribute</code>","text":"<pre><code>lang_stopwords = lang_stopwords\n</code></pre> <p>Language-specific stopwords</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.supports_ngrams","title":"core.batching.Tokenizer.supports_ngrams  <code>instance-attribute</code>","text":"<pre><code>supports_ngrams: bool = False\n</code></pre> <p>Whether or not the tokenizer has an embedded n-grams model</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.ngrams_trie","title":"core.batching.Tokenizer.ngrams_trie  <code>instance-attribute</code>","text":"<pre><code>ngrams_trie = {}\n</code></pre> <p>Prefix tree of known n-grams for efficient lookups</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.vocabulary","title":"core.batching.Tokenizer.vocabulary  <code>instance-attribute</code>","text":"<pre><code>vocabulary: Lexicon = Lexicon()\n</code></pre> <p>Known tokens, if trained for n-grams.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.Tokenizer.prefilter","title":"core.batching.Tokenizer.prefilter","text":"<pre><code>prefilter(string: str, meta_tokens: bool = True) -&gt; str\n</code></pre> <p>Tokenizers split words based on unsupervised machine-learned models. Sometimes, they work weird. For example, in emails and user handles like <code>@user</code>, they would split <code>@</code> and <code>user</code> as 2 different tokens, making it impossible to detect usernames in single tokens later.</p> <p>To avoid that, we replace data of interest by meta-tokens before the tokenization, with regular expressions.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.lemmatize","title":"core.batching.Tokenizer.lemmatize","text":"<pre><code>lemmatize(word: str) -&gt; str\n</code></pre> <p>Find the root (lemma) of words to help topical generalization.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.normalize_text","title":"core.batching.Tokenizer.normalize_text","text":"<pre><code>normalize_text(document: str) -&gt; str\n</code></pre> <p>Prepare text for tokenization by converting it to lowercase ASCII characters.</p> <p>This will loose accents, diacritics and capitals, which means some nuance will be lost at the benefit of generality. In case this does not suit your usecase, you may inherit the <code>Tokenizer</code> class, build a child class and re-implement this method</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.normalize_token","title":"core.batching.Tokenizer.normalize_token","text":"<pre><code>normalize_token(\n    word: str,\n    language: str | None,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; str | None\n</code></pre> <p>Return normalized, lemmatized and stemmed word tokens, where dates, times, digits, monetary units  and URLs have their actual value replaced by meta-tokens designating their type.  Stopwords (\u201cthe\u201d, \u201ca\u201d, etc.), punctuation etc. is replaced by <code>None</code>, which should be filtered out at the next step.</p> PARAMETER DESCRIPTION <code>word</code> <p>tokenized word in lower case only.</p> <p> TYPE: <code>str</code> </p> <code>language</code> <p>the ISO 369-1 language code used to remove typical stopwords.</p> <p> TYPE: <code>str</code> </p> <code>normalize</code> <p>remove punctuation and leading/trailing symbols.</p> <p> TYPE: <code>str</code> DEFAULT: <code>True</code> </p> <code>meta_tokens</code> <p>replace string patterns by meta_tokens</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>stem</code> <p>remove word suffixes, double consonnants, etc.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>remove_stopwords</code> <p>remove stopwords</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> NOTE <p>Tokenization is non-destructive (full sentences can be reconstructed entirely from token lists) if <code>normalize=False</code>, <code>meta_tokens=False</code>, <code>stem=False</code> and <code>remove_stopwords=False</code>. In this setting, only 1:1 token replacements defined in <code>self.replacements</code> will be applied, which can allow to replace abbreviations or accronyms. Other modes start generalizing semantics by removing meaning.</p> <p>Examples:</p> <p>Meta-tokens:     <code>10:00</code> or <code>10 h</code> or <code>10am</code> or <code>10 am</code> will all be replaced by a <code>_TIME_</code> meta-token.     <code>feb</code>, <code>February</code>, <code>feb.</code>, <code>monday</code> will all be replaced by a <code>_DATE_</code> meta-token.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.tokenize_text","title":"core.batching.Tokenizer.tokenize_text","text":"<pre><code>tokenize_text(\n    sentence: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[str]\n</code></pre> <p>Split text into normalized word tokens and meta-tokens.</p> <p>No sentence or paragraph boundary detection is performed.</p> PARAMETER DESCRIPTION <code>sentence</code> <p>Input text to tokenize.</p> <p> TYPE: <code>str</code> </p> <code>n_grams</code> <p>Whether to detect and collapse n-grams.</p> <p>Requires a trained n-gram model generated with <code>train_ngrams()</code>.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Note <p>The parameters <code>language</code>, <code>normalize</code>, <code>meta_tokens</code>, <code>stem</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>list[str]</code> <p>List of normalized tokens represented as a bag of words.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.post_filter_tokens","title":"core.batching.Tokenizer.post_filter_tokens","text":"<pre><code>post_filter_tokens(\n    tokens: list[str],\n    language: str | None = None,\n    meta_tokens: bool = True,\n    stem: bool = False,\n    normalize: bool = False,\n    remove_stopwords: bool = False,\n) -&gt; list[str]\n</code></pre> <p>Apply post-processing operations to an existing token stream.</p> <p>This method applies token normalization, stemming, stopword removal, and meta-token handling without performing tokenization.</p> PARAMETER DESCRIPTION <code>tokens</code> <p>List of input tokens to process.</p> <p> TYPE: <code>list[str]</code> </p> Note <p>The parameters <code>language</code>, <code>meta_tokens</code>, <code>stem</code>, <code>normalize</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>list[str]</code> <p>List of processed tokens.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.tokenize_document_flat","title":"core.batching.Tokenizer.tokenize_document_flat","text":"<pre><code>tokenize_document_flat(\n    document: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[str]\n</code></pre> <p>Cleanup and tokenize a document or a sentence as an atomic element, meaning we don\u2019t split it into sentences.  Use this either for search-engine purposes (into a document\u2019s body) or if the document is already split into sentences.  The document text needs to have been prepared and cleaned, which means :</p> <ul> <li>lowercased (optional but recommended) with <code>str.lower()</code>,</li> <li>translated from Unicode to ASCII (optional but recommended) with core.utils.typography_undo,</li> <li>cleaned up for sequences of whitespaces with core.utils.clean_whitespaces</li> </ul> Note <p>the language is detected internally if not provided as an optional argument. When processing a single sentence extracted from a document, instead of the whole document, it is more accurate to run the language detection on the whole document, ahead of calling this method, and pass on the result here.</p> PARAMETER DESCRIPTION <code>document</code> <p>the text of the document to tokenize</p> <p> TYPE: <code>str</code> </p> <code>n_grams</code> <p>see core.nlp.Tokenizer.tokenize_text</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Note <p>The parameters <code>language</code>, <code>meta_tokens</code>, <code>stem</code>, <code>normalize</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>tokens</code> <p>a 1D list of normalized tokens and meta-tokens.</p> <p> TYPE: <code>list[str]</code> </p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.tokenize_document_per_sentence","title":"core.batching.Tokenizer.tokenize_document_per_sentence","text":"<pre><code>tokenize_document_per_sentence(\n    document: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[list[str]]\n</code></pre> <p>Cleanup and tokenize a whole document as a list of sentences, meaning we split it into sentences before tokenizing.  Use this to train a Word2Vec (embedding) model so each token is properly embedded into its syntactic context.  The document text needs to have been prepared and cleaned, which means :</p> <ul> <li>lowercased (optional but recommended) with <code>str.lower()</code>,</li> <li>translated from Unicode to ASCII (optional but recommended) with core.utils.typography_undo,</li> <li>cleaned up for sequences of whitespaces with core.utils.clean_whitespaces</li> </ul> PARAMETER DESCRIPTION <code>document</code> <p>the text of the document to tokenize</p> <p> TYPE: <code>str</code> </p> <code>n_grams</code> <p>see core.nlp.Tokenizer.tokenize_text</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Note <p>The parameters <code>language</code>, <code>meta_tokens</code>, <code>stem</code>, <code>normalize</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>tokens</code> <p>a 2D list of sentences (1st axis), each containing a list of normalized tokens and meta-tokens (2nd axis).</p> <p> TYPE: <code>list[list[str]]</code> </p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.tokenize_document_per_paragraph","title":"core.batching.Tokenizer.tokenize_document_per_paragraph","text":"<pre><code>tokenize_document_per_paragraph(\n    document: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[list[str]]\n</code></pre> <p>Cleanup and tokenize a whole document as a list of paragraphs, meaning we split it on `</p> <p><code>or</code> ` before tokenizing.          Use this to train a Word2Vec (embedding) model so each token is properly embedded into its syntactic context.          The document text needs to have been prepared and cleaned, which means :</p> <pre><code>    - lowercased (optional but recommended) with `str.lower()`,\n    - translated from Unicode to ASCII (optional but recommended) with [core.utils.typography_undo][],\n    - cleaned up for sequences of whitespaces with [core.utils.clean_whitespaces][]\n\n    Arguments:\n        document (str): the text of the document to tokenize\n        n_grams (bool): see [core.nlp.Tokenizer.tokenize_text][]\n        others: see [core.nlp.Tokenizer.normalize_token][] arguments\n\n    Note:\n        the language is detected internally if not provided. The text is prefiltered with [self.prefilter][]\n\n    Returns:\n        tokens: a 2D list of paragraphs (1st axis), each containing a list of normalized tokens and meta-tokens (2nd axis).\n</code></pre>"},{"location":"api/core/batching.html#core.batching.Tokenizer.load","title":"core.batching.Tokenizer.load  <code>classmethod</code>","text":"<pre><code>load(name: str)\n</code></pre> <p>Load an existing trained model by its name from the <code>../models</code> folder.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.members_from_ngram","title":"core.batching.Tokenizer.members_from_ngram","text":"<pre><code>members_from_ngram(token: str | None) -&gt; list[str] | None\n</code></pre> <p>Recover n-grams members from a single tokenized phrase, separated with <code>_</code>. This expects lower-case tokens, except for meta-tokens which are expected capitalized.</p> RETURNS DESCRIPTION <code>list[str] | None</code> <p>the list of n-gram members, or None if the token was not an n-gram but a singleton.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.train_ngrams","title":"core.batching.Tokenizer.train_ngrams","text":"<pre><code>train_ngrams(\n    sentences: list[str],\n    connector_words: str = \"\",\n    min_count: int = 10,\n    threshold: float = 0.7,\n    scoring: str = \"npmi\",\n)\n</code></pre> <p>Train an n-gram model (bigrams and trigrams).</p> <p>Detects common phrases such as \u201cNew York City\u201d and merges them into single tokens using a statistical phrase model.</p> PARAMETER DESCRIPTION <code>sentences</code> <p>Training corpus. Must be a list of tokenized sentences.</p> <p> TYPE: <code>list[str]</code> </p> <code>connector_words</code> <p>Space-separated list of connector words allowed inside phrases (e.g. \u201cby\u201d in \u201cpiece by piece\u201d).</p> <p>These words are treated as valid bridges when forming n-grams.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> <code>min_count</code> <p>Minimum number of occurrences required for a phrase to be considered.</p> <p>See <code>gensim.models.phrases.Phrases</code> for details.</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> <code>threshold</code> <p>Phrase detection sensitivity threshold.</p> <p>See <code>gensim.models.phrases.Phrases</code>.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.7</code> </p> <code>scoring</code> <p>Scoring function used for phrase detection.</p> <p>See <code>gensim.models.phrases.Phrases</code>.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'npmi'</code> </p> Warning <p>N-gram training must be performed on lightly processed tokenized sentences. Do not apply stemming, stopword removal, or punctuation stripping before training.</p> <p>See <code>Tokenizer.normalize_token()</code> for required preprocessing options.</p> Note <ul> <li>Writes an <code>ngrams</code> log file in the models directory containing discovered phrases.</li> <li>Can be executed multiple times (e.g. per language); results are appended to the existing model.</li> </ul>"},{"location":"api/core/batching.html#core.batching.Tokenizer.compile_ngrams","title":"core.batching.Tokenizer.compile_ngrams","text":"<pre><code>compile_ngrams(ngrams: list[str])\n</code></pre> <p>Build a nested n-grams dictionnary for efficient querying, like: <pre><code>{\n    \"new\": {\n        \"york\": {\n            \"__value__\": \"new_york\",\n            \"city\": {\n                \"__value__\": \"new_york_city\"\n            }\n        }\n    }\n}\n</code></pre></p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.replace_ngrams","title":"core.batching.Tokenizer.replace_ngrams","text":"<pre><code>replace_ngrams(tokens: list[str]) -&gt; list[str]\n</code></pre> <p>Identify n-grams among tokens and collapse them into single tokens. N-grams should have been trained before, with core.nlp.Tokenizer.train_ngrams.</p> RETURNS DESCRIPTION <code>list[str]</code> <p>the collapsed list of strings, or the original list if no n-grams</p> <code>list[str]</code> <p>was found or the n-grams model has not been trained.</p>"},{"location":"api/core/batching.html#core.batching.Tokenizer.lookup_ngram","title":"core.batching.Tokenizer.lookup_ngram","text":"<pre><code>lookup_ngram(members: list[str] | tuple[str, ...]) -&gt; str | None\n</code></pre> <p>Lookup an n-gram in the trie from its token members.</p> PARAMETER DESCRIPTION <code>members</code> <p>the tokens iterable</p> <p> TYPE: <code>list[str] | tuple[str, ...]</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>the collapsed n-gram if found in the trie, or <code>None</code> if the input members match</p> <code>str | None</code> <p>no known n-gram.</p> Example <p>lookup_ngram((\u201cnew\u201d, \u201cyork\u201d)) -&gt; \u201cnew_york\u201d</p> <p>lookup_ngram((\u201cnew\u201d, \u201cyork\u201d, \u201ccity\u201d)) -&gt; \u201cnew_york_city\u201d</p> <p>lookup_ngram((\u201cfoo\u201d, \u201cbar\u201d)) -&gt; None</p>"},{"location":"api/core/batching.html#core.batching.Data","title":"core.batching.Data","text":"<pre><code>Data(text: str, label: str)\n</code></pre> <p>Represent an item of tagged training data.</p> PARAMETER DESCRIPTION <code>text</code> <p>the content to label, which will be vectorized</p> <p> TYPE: <code>str</code> </p> <code>label</code> <p>the category of the content, which will be predicted by the model</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/batching.html#core.batching.LossLogger","title":"core.batching.LossLogger","text":"<pre><code>LossLogger()\n</code></pre> <p>               Bases: <code>CallbackAny2Vec</code></p> <p>Output loss at each epoch</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec","title":"core.batching.Word2Vec","text":"<pre><code>Word2Vec(\n    documents: list[list[str]],\n    name: str = \"word2vec\",\n    vector_size: int = 300,\n    epochs: int = 200,\n    window: int = 5,\n    min_count: int = 5,\n    sample: float = 0.0005,\n    tokenizer: Tokenizer = None,\n    compute_idf: bool = False,\n    **kwargs: dict[str, Any]\n)\n</code></pre> <p>               Bases: <code>gensim.models.Word2Vec</code></p> <p>Train, re-train, or load a Word2Vec embedding model.</p> <p>If a model with the given <code>name</code> already exists, it is automatically loaded instead of re-trained. Note that in this case, <code>vector_size</code> will be overridden by the saved model configuration.</p> PARAMETER DESCRIPTION <code>documents</code> <p>Pre-tokenized training corpus.</p> <p>Structure: - outer list: documents - inner list: tokenized sentences</p> <p> TYPE: <code>list[list[str]]</code> </p> <code>name</code> <p>Name of the model file used for saving/loading.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'word2vec'</code> </p> <code>vector_size</code> <p>Dimensionality of word embeddings.</p> <p> TYPE: <code>int</code> DEFAULT: <code>300</code> </p> <code>epochs</code> <p>Number of training iterations.</p> <p>Higher values improve quality on small corpora but increase training time.</p> <p> TYPE: <code>int</code> DEFAULT: <code>200</code> </p> <code>window</code> <p>Context window size for word co-occurrence.</p> <p> TYPE: <code>int</code> DEFAULT: <code>5</code> </p> <code>min_count</code> <p>Minimum frequency threshold for vocabulary filtering.</p> <p> TYPE: <code>int</code> DEFAULT: <code>5</code> </p> <code>sample</code> <p>Subsampling rate for frequent words.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.0005</code> </p> <code>tokenizer</code> <p>Tokenizer instance used for preprocessing (if applicable).</p> <p> TYPE: <code>Tokenizer</code> DEFAULT: <code>None</code> </p> <code>compute_idf</code> <p>Whether to compute and store IDF statistics for SIF weighting.</p> <p>Disable to reduce model size when SIF is not used.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>**kwargs</code> <p>Additional parameters forwarded directly to <code>gensim.models.Word2Vec</code>.</p> <p> TYPE: <code>dict[str, Any]</code> DEFAULT: <code>{}</code> </p>"},{"location":"api/core/batching.html#core.batching.Word2Vec-attributes","title":"Attributes","text":""},{"location":"api/core/batching.html#core.batching.Word2Vec.tokenizer","title":"core.batching.Word2Vec.tokenizer  <code>instance-attribute</code>","text":"<pre><code>tokenizer = tokenizer if tokenizer is not None else Tokenizer()\n</code></pre> <p>Tokenizer used to train the model. We store it to be sure to use the same when using it.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.N_docs","title":"core.batching.Word2Vec.N_docs  <code>instance-attribute</code>","text":"<pre><code>N_docs = len(documents)\n</code></pre> <p>Number of documents in the training corpus</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.N_sentences","title":"core.batching.Word2Vec.N_sentences  <code>instance-attribute</code>","text":"<pre><code>N_sentences = len(sentences)\n</code></pre> <p>Number of sentences in the training corpus</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.N_words","title":"core.batching.Word2Vec.N_words  <code>instance-attribute</code>","text":"<pre><code>N_words = len(words)\n</code></pre> <p>Number of words (tokens) in the training corpus</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.N_terms","title":"core.batching.Word2Vec.N_terms  <code>instance-attribute</code>","text":"<pre><code>N_terms = len(counts)\n</code></pre> <p>Number of terms (unique words) in the training corpus</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.idf","title":"core.batching.Word2Vec.idf  <code>instance-attribute</code>","text":"<pre><code>idf: dict[str, float] | None = None\n</code></pre> <p>Inverse Document Frequency, used only for SIF weighting when enabled.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.avg_doc_len","title":"core.batching.Word2Vec.avg_doc_len  <code>instance-attribute</code>","text":"<pre><code>avg_doc_len: float | None = None\n</code></pre> <p>Average number of words in documents of the training corpus, available with IDF stats.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.Word2Vec.compute_idf","title":"core.batching.Word2Vec.compute_idf","text":"<pre><code>compute_idf(documents: list[list[str]]) -&gt; None\n</code></pre> <p>Compute and store IDF statistics from a tokenized document corpus.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.update_idf","title":"core.batching.Word2Vec.update_idf","text":"<pre><code>update_idf(documents: list[list[str]]) -&gt; None\n</code></pre> <p>Update IDF statistics and corpus-dependent metadata with new documents.</p> PARAMETER DESCRIPTION <code>documents</code> <p>New pre-tokenized documents.</p> <p> TYPE: <code>list[list[str]]</code> </p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.prune_idf","title":"core.batching.Word2Vec.prune_idf","text":"<pre><code>prune_idf()\n</code></pre> <p>Prune IDF entries to the actual model vocabulary (remove tokens that were filtered out by gensim during <code>super().__init__</code>).</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.load_model","title":"core.batching.Word2Vec.load_model  <code>classmethod</code>","text":"<pre><code>load_model(name: str)\n</code></pre> <p>Load a trained model saved in <code>models</code> folders</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.get_word","title":"core.batching.Word2Vec.get_word","text":"<pre><code>get_word(word: str) -&gt; str | None\n</code></pre> <p>Find out if word is in dictionary, optionnaly attempting spell-checking if not found.</p> PARAMETER DESCRIPTION <code>word</code> <p>word to find</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>str | None</code> <ul> <li>the original word if found in dictionnary,</li> <li><code>None</code> if both previous conditions were not matched.</li> </ul>"},{"location":"api/core/batching.html#core.batching.Word2Vec.get_wordvec","title":"core.batching.Word2Vec.get_wordvec","text":"<pre><code>get_wordvec(\n    word: str, embed: str = \"IN\", normalize: bool = True\n) -&gt; np.ndarray[np.float32] | None\n</code></pre> <p>Return the vector associated to a word, through a dictionnary of words.</p> PARAMETER DESCRIPTION <code>word</code> <p>the word to convert to a vector.</p> <p> TYPE: <code>str</code> </p> <code>embed</code> <ul> <li><code>IN</code> uses the input embedding matrix gensim.models.word2vec.Word2Vec.wv, useful to vectorize queries and documents for classification training.</li> <li><code>OUT</code> uses the output embedding matrix gensim.models.word2vec.Word2Vec, useful for the dual-space embedding scheme, to train search engines. [^1]</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'IN'</code> </p> <ol> <li> <p>A Dual Embedding Space Model for Document Ranking (2016), Bhaskar Mitra, Eric Nalisnick, Nick Craswell, Rich Caruana https://arxiv.org/pdf/1602.01137.pdf \u21a9</p> </li> </ol> RETURNS DESCRIPTION <code>np.ndarray[np.float32] | None</code> <p>the nD vector if the word was found in the dictionnary, or <code>None</code>.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.get_features","title":"core.batching.Word2Vec.get_features","text":"<pre><code>get_features(\n    tokens: list[str],\n    embed: str = \"IN\",\n    use_sif: bool = False,\n    sif_smoothing: float = 0.001,\n) -&gt; np.ndarray[np.float32]\n</code></pre> <p>Calls core.nlp.Word2Vec.get_wordvec over a list of tokens and returns a single vector representing the whole list.</p> PARAMETER DESCRIPTION <code>tokens</code> <p>list of text tokens.</p> <p> TYPE: <code>list[str]</code> </p> <code>embed</code> <p>see core.nlp.Word2Vec.get_wordvec</p> <p> TYPE: <code>str</code> DEFAULT: <code>'IN'</code> </p> <code>use_sif</code> <p>Use SIF weighting on each term when embedding a full sentence or document. See core.nlp.Word2Vec.SIF.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>sif_smoothing</code> <p>The SIF smoothing coefficient.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.001</code> </p> RETURNS DESCRIPTION <code>np.ndarray[np.float32]</code> <p>the normalized centroid of word embedding vectors associated with the input tokens </p> <code>np.ndarray[np.float32]</code> <p>(aka the average vector), or the null vector if no word from the list was found in dictionnary.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.SIF","title":"core.batching.Word2Vec.SIF","text":"<pre><code>SIF(token: str, a: float = 0.001) -&gt; float\n</code></pre> <p>Smooth inverse frequency weighting</p> <p>Taken from A simple but tough-to-beat baseline for sentence embeddings, Sanjeev Arora, Yingyu Liang, Tengyu Ma. https://openreview.net/pdf?id=SyK00v5xx</p> <p>This helps refining semantics by under-weighting stopwords, however it\u2019s unsuited for File Information Retrieval (search engines) because it over-smoothen the embedding space geometry and hinders relevance discrimination with regard to a query.</p> PARAMETER DESCRIPTION <code>token</code> <p>the token to weight. It should be in the model vocabulary.</p> <p> TYPE: <code>str</code> </p> Return <p>The SIF weight associated with the token or 0. if the token was not found in the vocabulary.</p>"},{"location":"api/core/batching.html#core.batching.Word2Vec.tokens_to_indices","title":"core.batching.Word2Vec.tokens_to_indices","text":"<pre><code>tokens_to_indices(tokens: list[str]) -&gt; np.ndarray[np.int32]\n</code></pre> <p>Convert a list of tokens to a list of their index number in the Word2Vec vocabulary. This yields a more compact, albeit purely symbolic, representation of a tokenized document as a series of integers.</p> <p>The conversion is reversible and the original token can be found with <code>self.wv.index_to_key[i]</code>, where <code>i</code> is the index number output (for each token) from here.</p> Return <p>the list of indices as 32 bits integers, meaning the Word2Vec vocabulary needs to contain fewer than 4.29 billions words.</p>"},{"location":"api/core/batching.html#core.batching.Classifier","title":"core.batching.Classifier","text":"<pre><code>Classifier(\n    training_set: list[Data],\n    name: str,\n    word2vec: Word2Vec,\n    validate: bool = True,\n    variant: str = \"svm\",\n)\n</code></pre> <p>               Bases: <code>nltk.classify.SklearnClassifier</code></p> <p>Initialize a Word2Vec + SVM classification pipeline.</p> <p>This class wraps a Word2Vec embedding model with a downstream machine-learning classifier (SVM or alternatives).</p> PARAMETER DESCRIPTION <code>training_set</code> <p>List of <code>Data</code> samples used for training.</p> <p>If empty, the system will attempt to load a pre-trained model using <code>name</code>.</p> <p> TYPE: <code>list[Data]</code> </p> <code>name</code> <p>Identifier used to save and reload the trained model.</p> <p> TYPE: <code>str</code> </p> <code>word2vec</code> <p>Word embedding model used to generate feature vectors.</p> <p> TYPE: <code>Word2Vec</code> </p> <code>validate</code> <p>If True, splits the dataset into training (95%) and testing (5%) subsets and prints evaluation metrics.</p> <p>Useful for classifier selection and sanity checking.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>variant</code> <p>Type of classifier to use:</p> <ul> <li> <p><code>svm</code>: RBF-kernel Support Vector Machine (default). Robust and stable across general datasets.</p> </li> <li> <p><code>linear svm</code>: Linear Support Vector Machine. Faster and often better for high-dimensional features.</p> </li> <li> <p><code>forest</code>: Random Forest classifier. Faster than linear SVM in some cases, but produces larger models.</p> </li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'svm'</code> </p> Note <p>The previous documentation mentioned <code>path</code> and <code>features</code>, but these are not part of the current signature and were removed.</p>"},{"location":"api/core/batching.html#core.batching.Classifier-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.Classifier.get_features_parallel","title":"core.batching.Classifier.get_features_parallel","text":"<pre><code>get_features_parallel(post: Data) -&gt; tuple[str, str]\n</code></pre> <p>Thread-safe call to <code>.get_features()</code> to be called in multiprocessing.Pool map</p>"},{"location":"api/core/batching.html#core.batching.Classifier.load","title":"core.batching.Classifier.load  <code>classmethod</code>","text":"<pre><code>load(name: str)\n</code></pre> <p>Load an existing trained model by its name from the <code>../models</code> folder.</p>"},{"location":"api/core/batching.html#core.batching.Classifier.classify","title":"core.batching.Classifier.classify","text":"<pre><code>classify(post: str) -&gt; str\n</code></pre> <p>Apply a label on a post based on the trained model.</p>"},{"location":"api/core/batching.html#core.batching.Classifier.prob_classify","title":"core.batching.Classifier.prob_classify","text":"<pre><code>prob_classify(post: str) -&gt; tuple[str, float]\n</code></pre> <p>Apply a label on a post based on the trained model and output the probability too.</p>"},{"location":"api/core/batching.html#core.batching.StemTokenIndex","title":"core.batching.StemTokenIndex","text":"<pre><code>StemTokenIndex(db: sqlite3.Connection, tokenizer: Tokenizer)\n</code></pre> <p>Reverse normalized stem -&gt; token lookup index.</p>"},{"location":"api/core/batching.html#core.batching.StemTokenIndex-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.StemTokenIndex.most_probable_token","title":"core.batching.StemTokenIndex.most_probable_token","text":"<pre><code>most_probable_token(db: sqlite3.Connection, stem: str) -&gt; str\n</code></pre> <p>Return the most probable original token associated to the stem. If the stem doesn\u2019t exist in the database, it is returned as-is.</p>"},{"location":"api/core/batching.html#core.batching.StemTokenIndex.most_probable_tokens","title":"core.batching.StemTokenIndex.most_probable_tokens","text":"<pre><code>most_probable_tokens(db: sqlite3.Connection, stems: list[str]) -&gt; list[str]\n</code></pre> <p>Return the most probable original token for each stem.</p> <p>Stems not found in DB are returned unchanged.</p>"},{"location":"api/core/batching.html#core.batching.SQLitePageCorpus","title":"core.batching.SQLitePageCorpus","text":"<pre><code>SQLitePageCorpus(\n    db,\n    query,\n    params=(),\n    atomic_types=(str, bytes),\n    max_depth=None,\n    yield_rows=False,\n)\n</code></pre> <p>Lazily stream rows from an SQLite request, avoiding full copy.</p> Example <p><pre><code>    corpus = SQLitePageCorpus(\n        db,\n        \"\"\"\n        SELECT tokenized\n        FROM pages\n        WHERE lang IN ('fr', 'en')\n        \"\"\",\n        max_depth=0\n    )\n</code></pre> - <code>max_depth=0</code> will not flatten the content, so it will return   the original <code>list[list[str]]</code> (list of sentences, aka list of list of words), - <code>max_depth=1</code> flattens documents, to it will return   <code>list[str]</code> (list of words)</p>"},{"location":"api/core/batching.html#core.batching.Deduplicator","title":"core.batching.Deduplicator","text":"<pre><code>Deduplicator(\n    threshold: float = 0.9,\n    distance: int = 50,\n    discard_params: bool = True,\n    n_min: int = 0,\n    fix_urls: bool = True,\n)\n</code></pre> <p>Instanciate a depduplicator object.</p> <p>The duplicates factorizing takes a list of core.types.web_page</p> <p>Duplication detection is done using canonical URLs (removing query parameters and anchors) and lowercased, ASCII-converted content.</p> <p>You can edit (append or replace) the list of URLs to ignore core.deduplicator.Deduplicator.urls_to_ignore before doing the actual process.</p> <p>Optionaly, near-duplicates are detected too by computing the Levenshtein distance between pages contents (lowercased and ASCII-converted). This brings a significant performance penalty on large datasets.</p> PARAMETER DESCRIPTION <code>threshold</code> <p>the minimum Levenshtein distance ratio between 2 pages contents for those pages to be considered near-duplicates and be factorized. If set to 1.0, the near-duplicates detection is bypassed which results in a huge speed up.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.9</code> </p> <code>distance</code> <p>the near-duplicates search is performed on the nearest elements after the core.types.web_page list has been ordered alphabetically by URL, for performance, assuming near-duplicates will most likely be found on the same domain and at a resembling path. The distance parameters defines how many elements ahead we will look into.</p> <p> TYPE: <code>int</code> DEFAULT: <code>50</code> </p> <code>discard_params</code> <p>on modern CMS that enable \u201cpretty URLs\u201d (URL rewriting), pages will be indexed by a <code>domain/section/subsection/page</code> and URL query parameters will most likely be used my meaningless pages like social sharing links or search results page so this parameter can be set to <code>True</code> to discard those. On Rest-API-driven websites, streaming websites and old CMS using \u201cugly URLS\u201d, pages will be indexed by <code>domain?content=id</code> and the query parameters need to be kept by setting this parameter to <code>False</code></p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>n_min</code> <p>domains that have a number of indexed pages below this threshold will be discarded entirely. This avoids indexing random dude\u2019s website, under the assumption that relevant and reliable domains will have several pages indexed.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> <code>fix_urls</code> <p>attempt to convert <code>http</code> to <code>https</code> URLs and remove leading <code>www.</code>. This sends DNS requests to assess if the <code>https</code> and <code>www.</code>-less variants can be reached, which takes a most 2 s per URL. Set to <code>False</code> to speed things up.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p>"},{"location":"api/core/batching.html#core.batching.Deduplicator-attributes","title":"Attributes","text":""},{"location":"api/core/batching.html#core.batching.Deduplicator.urls_to_ignore","title":"core.batching.Deduplicator.urls_to_ignore  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>urls_to_ignore: list[str] = [\n    \"/tag/\",\n    \"/tags/\",\n    \"/category/\",\n    \"/categories/\",\n    \"/author/\",\n    \"/authors/\",\n    \"/profil/\",\n    \"/profiles/\",\n    \"/user/\",\n    \"/users/\",\n    \"/login/\",\n    \"/signup/\",\n    \"/member/\",\n    \"/members/\",\n    \"/cart/\",\n    \"/shop/\",\n    \"/register\",\n]\n</code></pre> <p>URL substrings to find in URLs and remove matching web pages: mostly WordPress archive pages, user profiles and login pages.</p>"},{"location":"api/core/batching.html#core.batching.Deduplicator-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.Deduplicator.get_unique_urls","title":"core.batching.Deduplicator.get_unique_urls","text":"<pre><code>get_unique_urls(posts: list[web_page]) -&gt; list[web_page]\n</code></pre> <p>Pick the most recent, or otherwise the longer, candidate for each canonical URL.</p>"},{"location":"api/core/batching.html#core.batching.Deduplicator.run_on_db","title":"core.batching.Deduplicator.run_on_db","text":"<pre><code>run_on_db(db: sqlite3.Connection, chunksize: int = 4096) -&gt; None\n</code></pre> <p>Deduplicate the <code>pages</code> table in-place, matching the full <code>__call__</code> pipeline.</p> <p>The method runs four sequential phases that mirror <code>__call__</code>:</p> <ol> <li>URL canonicalization \u2013 stream every row through    :meth:<code>prepare_posts_parallel</code> (threaded, I/O-bound), normalise URLs,    compute a SHA-1 content hash, and write results to the temporary    <code>_prepared</code> table.</li> <li>URL deduplication \u2013 for each canonical URL keep the single best row    using SQL window functions with :attr:<code>_ELECTION_ORDER</code>.</li> <li>Exact-content deduplication \u2013 among URL winners, collapse rows that    share the same SHA-1 hash using the same election order.</li> <li>Near-duplicate removal (skipped when <code>threshold == 1.0</code>) \u2013 load the    survivors into memory, run the Levenshtein window scan with parallelised    comparisons (threaded; <code>python-Levenshtein</code> releases the GIL), write    the final winner set back to a temp table.</li> </ol> <p>The <code>pages</code> table is atomically replaced by the winner set at the end. All intermediate <code>_prepared / _url_winners / _content_winners / _near_winners</code> temp tables are cleaned up on success.</p> <p>Assumptions: - <code>pages</code> has at least the columns: <code>url, title, content, date,   datetime, parsed, category</code>. - <code>datetime</code> values, when present, are ISO-8601 strings (SQLite TEXT).   NULL is treated as \u201coldest possible\u201d in the election. - The <code>external</code> category label means the page was crawled by following   external links and contains the full <code>&lt;body&gt;</code>; any other category means   it was crawled from a sitemap / REST-API and contains cleaner markup.   Non-external therefore wins over external in the election.</p> PARAMETER DESCRIPTION <code>db</code> <p>Open <code>sqlite3.Connection</code> to the database.</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>chunksize</code> <p>Number of rows fetched per batch during Phase 1.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4096</code> </p>"},{"location":"api/core/batching.html#core.batching.Deduplicator.add_content_hash_column","title":"core.batching.Deduplicator.add_content_hash_column  <code>staticmethod</code>","text":"<pre><code>add_content_hash_column(db: sqlite3.Connection) -&gt; None\n</code></pre> <p>Add (or refresh) a <code>content_hash</code> column on the <code>pages</code> table.</p> <p>Computes a SHA-1 digest of each row\u2019s <code>parsed</code> field and stores it in <code>content_hash</code>.  The column is created if it does not yet exist.  Rows with a NULL <code>parsed</code> value are skipped and left with a NULL hash.</p> <p>A covering index <code>idx_pages_content_hash</code> is created (or left in place) after the update so that subsequent deduplication queries are cheap.</p> <p>This method is a standalone maintenance utility.  The deduplication pipeline (:meth:<code>run_on_db</code>) computes hashes inline during Phase 1 and does not require this method to be called first.</p> <p>Assumption: <code>parsed</code> values fit in memory individually (they are fetched one batch at a time, not all at once).</p> PARAMETER DESCRIPTION <code>db</code> <p>Open <code>sqlite3.Connection</code> to the target database.</p> <p> TYPE: <code>sqlite3.Connection</code> </p>"},{"location":"api/core/batching.html#core.batching.Deduplicator.get_unique_content","title":"core.batching.Deduplicator.get_unique_content","text":"<pre><code>get_unique_content(posts: list[web_page]) -&gt; list[web_page]\n</code></pre> <p>Pick the most recent candidate for each canonical content.</p> Return <p><code>canonical content: web_page</code> dictionnary</p>"},{"location":"api/core/batching.html#core.batching.Deduplicator.get_close_content","title":"core.batching.Deduplicator.get_close_content","text":"<pre><code>get_close_content(\n    posts: list[web_page], threshold: float = 0.9, distance: float = 50\n) -&gt; list[web_page]\n</code></pre> <p>Find near-duplicate by computing the Levenshtein distance between pages contents.</p> PARAMETER DESCRIPTION <code>posts</code> <p>dictionnary mapping an unused key to a liste of core.types.web_page</p> <p> TYPE: <code>list[web_page]</code> </p> <code>threshold</code> <p>the minimum distance ratio of Lenvenshtein metric for 2 contents to be assumed duplicates</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.9</code> </p> <code>distance</code> <p>for efficiency, the list of web_page is first sorted alphabetically by URL, assuming duplicates will share at least the beginning of their URL. From there, duplicates are searched ahead in the list up to this distance.</p> <p> TYPE: <code>float</code> DEFAULT: <code>50</code> </p>"},{"location":"api/core/batching.html#core.batching.Deduplicator.run_on_list","title":"core.batching.Deduplicator.run_on_list","text":"<pre><code>run_on_list(posts: list[web_page]) -&gt; list[web_page]\n</code></pre> <p>Deduplicate an in-memory list of web pages, matching the full pipeline.</p> <p>This is the list-based counterpart to :meth:<code>run_on_db</code>.  The two methods are kept symmetrical: both run the same four phases (URL canonicalization, exact-URL deduplication, exact-content deduplication, optional near-duplicate removal) and honour the same election rules.</p> Note <p><code>posts</code> is consumed and partially destroyed during processing to avoid keeping two copies in memory simultaneously.</p> PARAMETER DESCRIPTION <code>posts</code> <p>Flat list of :class:<code>core.types.web_page</code> dicts.  The list    is modified in-place; callers should not rely on its contents    after this call returns.</p> <p> TYPE: <code>list[web_page]</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>Deduplicated list of sanitised :class:<code>core.types.web_page</code> dicts,</p> <code>list[web_page]</code> <p>ready for downstream use (<code>to_db=False</code>).  Also writes a</p> <code>list[web_page]</code> <p><code>domains</code> frequency file via :func:<code>~core.utils.get_models_folder</code>.</p>"},{"location":"api/core/batching.html#core.batching-functions","title":"Functions","text":""},{"location":"api/core/batching.html#core.batching.parse_lang_to_iso639_1","title":"core.batching.parse_lang_to_iso639_1","text":"<pre><code>parse_lang_to_iso639_1(value: str | None) -&gt; str | None\n</code></pre> <p>Normalize language identifier to ISO 639-1.</p>"},{"location":"api/core/batching.html#core.batching.guess_language","title":"core.batching.guess_language","text":"<pre><code>guess_language(\n    string: str,\n    stopwords_threshold: float = 0.05,\n    letters_threshold: float = 0.8,\n) -&gt; str | None\n</code></pre> <p>Basic language guesser based on stopwords detection.</p> <p>Stopwords are the most common words of a language: for each language, we count how many stopwords we found and return the language having the most matches. It is accurate for paragraphs and long documents, not so much for short sentences.</p> PARAMETER DESCRIPTION <code>string</code> <p>the string to analyze. Needs to be lowercased but to retain accents and diacritics.</p> <p> TYPE: <code>str</code> </p> <code>stopwords_threshold</code> <p>the minimum ratio of stopwords divided by total words in strings to be found to conclude on a language. For example, Japanese companies often have technical reports written in Japanese but still containing some English. If less than 5% of the words are known English stopwords, we could conclude it\u2019s not English.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.05</code> </p> <code>letters_threshold</code> <p>the minimum ratio of roman (latin) characters among all characters (including numbers, symbols and non-latin alphabets) to be found to conclude on a language.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.8</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>ISO 639-1 language code. Defaults to \u201cen\u201d if nothing found.</p>"},{"location":"api/core/batching.html#core.batching.detect_language","title":"core.batching.detect_language","text":"<pre><code>detect_language(text: str) -&gt; str | None\n</code></pre> <p>Detect language from arbitrary text safely.</p> RETURNS DESCRIPTION <code>str | None</code> <p>ISO 639-1 language code.</p>"},{"location":"api/core/batching.html#core.batching.tokenize_document_to_words","title":"core.batching.tokenize_document_to_words","text":"<pre><code>tokenize_document_to_words(\n    text: str, language: str | None = None, backend: str = \"blingfire\"\n) -&gt; list[str]\n</code></pre> <p>Split a text into single words</p> PARAMETER DESCRIPTION <code>language</code> <p>ISO 639-1 language code.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[str]</code> <p>Bag of words for the whole document. Sentence delimiters are removed.</p>"},{"location":"api/core/batching.html#core.batching.split_document_to_sentences","title":"core.batching.split_document_to_sentences","text":"<pre><code>split_document_to_sentences(\n    text: str, language: str | None = None, backend: str = \"blingfire\"\n) -&gt; list[str]\n</code></pre> <p>Split a text into a list of sentences.</p> PARAMETER DESCRIPTION <code>language</code> <p>ISO 639-1 language code.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[str]</code> <p>List of sentences as full text.</p>"},{"location":"api/core/batching.html#core.batching.tokenize_document_to_sentences","title":"core.batching.tokenize_document_to_sentences","text":"<pre><code>tokenize_document_to_sentences(\n    text: str, language: str | None = None, backend: str = \"blingfire\"\n) -&gt; list[list[str]]\n</code></pre> <p>Split a text into single words as a list of lists</p> PARAMETER DESCRIPTION <code>language</code> <p>ISO 639-1 language code.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[list[str]]</code> <p>List of sentences, each sentence is itself a list of words.</p>"},{"location":"api/core/batching.html#core.batching.split_url","title":"core.batching.split_url","text":"<pre><code>split_url(url: str) -&gt; tuple[str, str, str, str, str] | None\n</code></pre> <p>Split a well-formed URL following RFC3986 into base elements.</p> RETURNS DESCRIPTION <code>tuple[str, str, str, str, str] | None</code> <p>a tuple of <code>(protocol, domain, page, parameters, anchor)</code>. </p> <code>tuple[str, str, str, str, str] | None</code> <p>Empty/missing fields are inited with empty strings so there is no need for individual <code>None</code> checks.</p> <code>tuple[str, str, str, str, str] | None</code> <p>If the <code>url</code> input doesn\u2019t match an URL format, return <code>None</code>.</p>"},{"location":"api/core/batching.html#core.batching.adapt_array","title":"core.batching.adapt_array","text":"<pre><code>adapt_array(arr: np.ndarray)\n</code></pre> <p>http://stackoverflow.com/a/31312102/190597 (SoulNibbler)</p>"},{"location":"api/core/batching.html#core.batching.create_db","title":"core.batching.create_db","text":"<pre><code>create_db(name: str) -&gt; sqlite3.Connection\n</code></pre> <p>Create the <code>pages</code> table if needed and add any missing columns. This doesn\u2019t destroy existing tables, rows or columns, so it\u2019s safe to run on any database.</p> Warning <p>Columns are inferred directly from <code>web_page.__annotations__</code>. Existing columns are preserved unchanged.</p> <p>The <code>url</code> column is used as the PRIMARY KEY.</p>"},{"location":"api/core/batching.html#core.batching.create_temp_db","title":"core.batching.create_temp_db","text":"<pre><code>create_temp_db(\n    min_free: float = 2.0, filename: str | None = None\n) -&gt; sqlite3.Connection\n</code></pre> <p>Create a temporary SQLite database file (in /dev/shm when available) and initialize the <code>pages</code> table according to <code>web_page</code> annotations.</p> PARAMETER DESCRIPTION <code>min_free</code> <p>minimum available disk space in GiB required to create the temporary database. This is checked at runtime and the function will raise an error if the condition is not met.</p> <p> TYPE: <code>float</code> DEFAULT: <code>2.0</code> </p> <code>filename</code> <p>the full path and filename to save the temporary database, if it needs to be reused at some point.    </p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>sqlite3.Connection</code> <p>the sqlite3.Connection opened in bulk mode.</p> WARNING <p>the temporary SQLite database doesn\u2019t use <code>web_page</code> URL as primary key, to allow later deduplication.</p>"},{"location":"api/core/batching.html#core.batching.delete_temp_db","title":"core.batching.delete_temp_db","text":"<pre><code>delete_temp_db(db: sqlite3.Connection)\n</code></pre> <p>Close and delete a temporary database in one shot.</p>"},{"location":"api/core/batching.html#core.batching.open_db","title":"core.batching.open_db","text":"<pre><code>open_db(name: str, mode: str = 'rw') -&gt; sqlite3.Connection\n</code></pre> <p>Open an SQLite database with workload-specific optimizations.</p> PARAMETER DESCRIPTION <code>name</code> <p>Database identifier/path passed to <code>get_models_folder()</code>.</p> <p> TYPE: <code>str</code> </p> <code>mode</code> <ul> <li>\u201crw\u201d: Generic read/write mode.</li> <li>\u201cro\u201d: Read-only immutable mode optimized for serving/search workloads.</li> <li>\u201cbulk\u201d: Bulk-ingestion mode optimized for large batch writes.</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'rw'</code> </p> RETURNS DESCRIPTION <code>sqlite3.Connection</code> <p>sqlite3.Connection</p>"},{"location":"api/core/batching.html#core.batching.compress_db","title":"core.batching.compress_db","text":"<pre><code>compress_db(\n    db: sqlite3.Connection,\n    delete_query: str | None = None,\n    delete_params: tuple | None = None,\n    delete_columns: list[str] | None = None,\n)\n</code></pre> <p>Optionally delete rows, then reclaim SQLite disk space.</p> PARAMETER DESCRIPTION <code>db</code> <p>SQLite connection</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>delete_query</code> <p>full DELETE SQL query</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>delete_params</code> <p>optional SQL parameters</p> <p> TYPE: <code>tuple | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/core/batching.html#core.batching.is_primary_key","title":"core.batching.is_primary_key","text":"<pre><code>is_primary_key(db: sqlite3.Connection, table: str, column: str) -&gt; bool\n</code></pre> <p>Check whether <code>column</code> is part of the PRIMARY KEY of <code>table</code>.</p>"},{"location":"api/core/batching.html#core.batching.populate_db","title":"core.batching.populate_db","text":"<pre><code>populate_db(\n    db: sqlite3.Connection, pages: list[web_page], batch_size: int = 4096\n)\n</code></pre> <p>Insert or update <code>web_page</code> records into the SQLite database.</p> <p>Existing rows are matched using the PRIMARY KEY <code>url</code>.</p> Warning <p>Array-like Python values are converted to <code>bytearray</code> then to <code>bytes</code> in order to be handled as <code>BLOB</code> by SQLite.</p>"},{"location":"api/core/batching.html#core.batching.db_to_list","title":"core.batching.db_to_list","text":"<pre><code>db_to_list(db: sqlite3.Connection) -&gt; list[web_page]\n</code></pre> <p>Extract all <code>web_page</code> rows from the <code>pages</code> table in <code>db</code> as a list of <code>web_page</code></p>"},{"location":"api/core/batching.html#core.batching.migrate_url_to_primary_key","title":"core.batching.migrate_url_to_primary_key","text":"<pre><code>migrate_url_to_primary_key(db: sqlite3.Connection)\n</code></pre> <p>Rebuild the <code>pages</code> table using <code>url</code> as PRIMARY KEY for older databases that didn\u2019t use a primary key.</p>"},{"location":"api/core/batching.html#core.batching.merge_databases","title":"core.batching.merge_databases","text":"<pre><code>merge_databases(old_db: sqlite3.Connection, new_db: sqlite3.Connection)\n</code></pre> <p>Merge two <code>pages</code> databases.</p> <p>Rows from <code>old_db</code> are inserted into <code>new_db</code> only if their URL does not already exist.</p> <p>Existing rows in <code>new_db</code> are preserved unchanged.</p> <p>Only columns existing in BOTH databases are copied.</p>"},{"location":"api/core/batching.html#core.batching.update_pages_from_database","title":"core.batching.update_pages_from_database","text":"<pre><code>update_pages_from_database(\n    target_db: sqlite3.Connection, source_db: sqlite3.Connection\n) -&gt; list[str]\n</code></pre> <p>Update rows in <code>target_db.pages</code> from <code>source_db.pages</code> using <code>url</code> as PRIMARY KEY.</p> <p>Only shared columns are updated.</p> <p>Returns     missing_urls: URLs present in target_db but absent from source_db.</p>"},{"location":"api/core/batching.html#core.batching.import_pages","title":"core.batching.import_pages","text":"<pre><code>import_pages(\n    source_db: str | sqlite3.Connection,\n    destination_db: str | sqlite3.Connection,\n    where_clause: str = \"1=1\",\n    params: tuple = (),\n) -&gt; int\n</code></pre> <p>Import rows from one SQLite database into another.</p> <p>Both source_db and destination_db may be either a filesystem path (str) or an active <code>sqlite3.Connection</code> handle.  Passing a Connection is the only way to target a <code>:memory:</code> database, since those cannot be addressed by path.</p> <p>Connection lifecycle     - Path supplied \u2013 the function opens, commits, and closes the       connection itself (original behaviour).     - Connection supplied \u2013 the caller retains full control; the       connection is neither committed nor closed here, so the import       can participate in a larger transaction.</p> <p>Rows are copied from <code>source.pages</code> into <code>destination.pages</code>. Existing rows are updated on conflict of the <code>url</code> primary key. Columns present in the destination but absent from the source receive NULL.  Both schemas are discovered at runtime, so the function adapts automatically if either evolves.</p> PARAMETER DESCRIPTION <code>source_db</code> <p>Path to, or an open connection for, the source SQLite database.</p> <p> TYPE: <code>str | sqlite3.Connection</code> </p> <code>destination_db</code> <p>Path to, or an open connection for, the destination SQLite database.</p> <p> TYPE: <code>str | sqlite3.Connection</code> </p> <code>where_clause</code> <p>SQL WHERE clause applied to <code>source.pages</code>. Example: <code>\"domain = ? AND date &gt;= ?\"</code></p> <p> TYPE: <code>str</code> DEFAULT: <code>'1=1'</code> </p> <code>params</code> <p>Positional parameters bound to where_clause.</p> <p> TYPE: <code>tuple</code> DEFAULT: <code>()</code> </p> RETURNS DESCRIPTION <code>int</code> <p>Number of affected rows.</p> <p>Examples::</p> <pre><code># File \u2192 file (unchanged from before)\nimport_pages(\"old.db\", \"new.db\", \"domain = ?\", (\"example.com\",))\n\n# In-memory source \u2192 file destination\nimport_pages(mem_conn, \"new.db\")\n\n# File source \u2192 in-memory destination (e.g. for tests)\nimport_pages(\"prod.db\", mem_conn, \"date &gt;= ?\", (\"2024-01-01\",))\n\n# Both in-memory\nimport_pages(src_conn, dst_conn)\n</code></pre>"},{"location":"api/core/batching.html#core.batching.inspect_db","title":"core.batching.inspect_db","text":"<pre><code>inspect_db(db: sqlite3.Connection, message: str = '') -&gt; None\n</code></pre> <p>Print useful metadata and statistics about a SQLite database.</p> PARAMETER DESCRIPTION <code>db</code> <p>active database connection</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>message</code> <p>optional additional message to indentify several inspections if any.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p>"},{"location":"api/core/batching.html#core.batching.sanitize_web_page","title":"core.batching.sanitize_web_page","text":"<pre><code>sanitize_web_page(page: web_page) -&gt; web_page\n</code></pre> <p>Ensure existence and validity of <code>web_page</code> keys/values.</p>"},{"location":"api/core/batching.html#core.batching.batch_guess_dates","title":"core.batching.batch_guess_dates","text":"<pre><code>batch_guess_dates(db: sqlite3.Connection, chunksize: int = 2048)\n</code></pre> <p>High-throughput parallel datetime parsing.</p>"},{"location":"api/core/batching.html#core.batching.batch_parse_web_page","title":"core.batching.batch_parse_web_page","text":"<pre><code>batch_parse_web_page(\n    documents: sqlite3.Connection,\n    tokenizer: Tokenizer,\n    chunksize: int = 512,\n    cores: int | None = None,\n)\n</code></pre> <p>High-performance parallel parsing for core.types.web_page objects</p> <p>This function is meant to cleanup text encoding issues and multi-spacings in <code>web_page</code> title and content. It prepares the <code>web_page[\"parsed\"]</code> field from title and content for the next stages of tokenization, and updates language (using declared ISO code or machine-learned detection).</p> <p>It is needed to call it before core.deduplicator.Deduplicator, so the content duplication has a clean parsed version to compare web pages.</p> PARAMETER DESCRIPTION <code>documents</code> <p>any database having core.types.web_page rows stored in a <code>pages</code> table and stored on the filesystem. It cannot be a memory-hosted database: each parallel worker will open its own copy by file path.</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>tokenizer</code> <p>we only use it for the the core.nlp.Tokenizer.normalize_text method</p> <p> TYPE: <code>Tokenizer</code> </p> <code>chunksize</code> <p>number of SQLite rows to process at once, too many is not helpful since some batches may take longer than others, depending on text length.</p> <p> TYPE: <code>int</code> DEFAULT: <code>512</code> </p> <code>cores</code> <p>CPU cores to use for parallel processing.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/core/batching.html#core.batching.batch_tokenize","title":"core.batching.batch_tokenize","text":"<pre><code>batch_tokenize(\n    db: sqlite3.Connection,\n    tokenizer: Tokenizer,\n    chunksize: int = 512,\n    urls: list[str] | None = None,\n    only_none: bool = True,\n)\n</code></pre> <p>Tokenize a list of <code>web_pages</code> in a non-destructive way, in parallel, in a RAM-friendly way, directly in database.</p> <p>Populate the <code>tokenized</code> database column from the <code>parsed</code> column. This needs to run after core.batching.batch_parse_web_page and prepares n-gram training if any, or stemming.</p> Note <p>The tokenization is forced non-destructive and doesn\u2019t apply stemming, stopwords removal, normalization, or n-grams. Original sentences can be reconstructed from joining back the list of tokens.</p> PARAMETER DESCRIPTION <code>urls</code> <p>list of URLs to tokenize. If None, the whole database is processed.</p> <p> TYPE: <code>list[str] | None</code> DEFAULT: <code>None</code> </p> <code>only_none</code> <p>stem only the new entries that have not been tokenized already. If <code>False</code>, force-update the whole database. It has no effect when <code>urls</code> are explicitely specified</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p>"},{"location":"api/core/batching.html#core.batching.batch_stem","title":"core.batching.batch_stem","text":"<pre><code>batch_stem(\n    db: sqlite3.Connection,\n    tokenizer: Tokenizer,\n    chunksize: int = 512,\n    urls: list[str] | None = None,\n    only_none: bool = True,\n)\n</code></pre> <p>Tokenize and stem a list of <code>web_pages</code> in parallel, in a RAM-friendly way, directly in database.</p> <p>Populate the <code>stemmed</code> database column from the <code>tokenized</code> column. This needs to run after core.batching.batch_tokenize. The tokenization is destructive and apply stemming, stopwords removal, normalization and n-grams if available.</p> PARAMETER DESCRIPTION <code>urls</code> <p>list of URLs to tokenize. If None, the whole database is processed.</p> <p> TYPE: <code>list[str] | None</code> DEFAULT: <code>None</code> </p> <code>only_none</code> <p>stem only the new entries that have not been stemmed already. If <code>False</code>, force-update the whole database. It has no effect when <code>urls</code> are explicitely specified</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p>"},{"location":"api/core/batching.html#core.batching.batch_vectorize","title":"core.batching.batch_vectorize","text":"<pre><code>batch_vectorize(\n    db: sqlite3.Connection, word2vec: Word2Vec, chunksize: int = 256\n)\n</code></pre> <p>Vectorize a column of the <code>db</code> database using the provided <code>word2vec</code> model using all available cores.</p> <p>Works on the <code>tokenized</code> column of the database and writes the <code>vectorized</code> column. Vectors are normalized as per <code>nlp.Word2Vec.get_features()</code> output.</p>"},{"location":"api/core/connectors.html","title":"core.connectors","text":""},{"location":"api/core/connectors.html#core.connectors","title":"core.connectors","text":"<p>Provide the abstract classes for server and content, that need to be implemented for each protocol.</p> <p>Filters and actions are applied by the <code>Server</code> by looping over each <code>Content</code> object and executing the user-defined functions.</p> <p>The core of the framework knows only these abstract classes.</p> <p>\u00a9 2022-2023 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/connectors.html#core.connectors-classes","title":"Classes","text":""},{"location":"api/core/connectors.html#core.connectors.Content","title":"core.connectors.Content","text":"<pre><code>Content(raw_content, server: Server)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>The <code>Content</code> class manages the decoding and parsing of individual data objects (such as posts, emails, contacts, events, etc.), as well as the data representation to be used in filters. It provides proxy methods to the <code>Server</code> class that can be used to perform server-side actions on the current <code>Content</code> object.</p> PARAMETER DESCRIPTION <code>raw_content</code> <p>data dump from server that should be decoded, parsed and split into properties.</p> <p> TYPE: <code>any</code> </p> <code>server</code> <p>back-reference to the <code>connectors.Server</code> instance to which this <code>Content</code> belongs.</p> <p> TYPE: <code>Server</code> </p>"},{"location":"api/core/connectors.html#core.connectors.Content-attributes","title":"Attributes","text":""},{"location":"api/core/connectors.html#core.connectors.Content.server","title":"core.connectors.Content.server  <code>instance-attribute</code>","text":"<pre><code>server: Server = server\n</code></pre> <p>Back-reference to the <code>Server</code> object to which the current <code>Content</code> belongs. In filters, users only access the <code>Content</code> object, so the <code>Content.server</code> reference is their only entry-point to the server. Use that with care and preferably wrap relevant server-side operations as <code>Content</code> methods, with proper arguments mapping.</p>"},{"location":"api/core/connectors.html#core.connectors.Server","title":"core.connectors.Server","text":"<pre><code>Server(logfile, secretary: 'secretary.Secretary')\n</code></pre> <p>               Bases: <code>ABC</code>, <code>ServerType</code></p> <p>The <code>Server</code> class manages the connection to a distant host with URL, port, login and password. Then it loads a list of individual <code>objects</code>, like emails or contacts, as <code>Content</code> type.</p>"},{"location":"api/core/connectors.html#core.connectors.Server-attributes","title":"Attributes","text":""},{"location":"api/core/connectors.html#core.connectors.Server.nlp","title":"core.connectors.Server.nlp  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>nlp = nlp\n</code></pre> <p>Reference the natural language processing module for reuse in filters without imports.</p>"},{"location":"api/core/connectors.html#core.connectors.Server.utils","title":"core.connectors.Server.utils  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>utils = utils\n</code></pre> <p>Reference the utility module for reuse in filters without imports.</p>"},{"location":"api/core/connectors.html#core.connectors.Server.secretary","title":"core.connectors.Server.secretary  <code>instance-attribute</code>","text":"<pre><code>secretary = secretary\n</code></pre> <p>Back-reference to the main core.secretary.Secretary handler</p>"},{"location":"api/core/connectors.html#core.connectors.Server.connection_inited","title":"core.connectors.Server.connection_inited  <code>instance-attribute</code>","text":"<pre><code>connection_inited: bool = False\n</code></pre> <p>Set to <code>True</code> in server implementation when a connection to the server has been inited, that is the DNS/IP has been resolved, server has been contacted and responded, credentials have been sent, and valid login session is active.</p>"},{"location":"api/core/connectors.html#core.connectors.Server.std_out","title":"core.connectors.Server.std_out  <code>instance-attribute</code>","text":"<pre><code>std_out: list = []\n</code></pre> <p>Cast status/success messages under the form <code>[\"OK\", \"details/data\"]</code></p>"},{"location":"api/core/connectors.html#core.connectors.Server.objects","title":"core.connectors.Server.objects  <code>instance-attribute</code>","text":"<pre><code>objects: list[ContentType] = []\n</code></pre> <p>The iterable list of <code>connectors.Content</code> object fetched at runtime, upon which filters will be run in sequence.</p>"},{"location":"api/core/connectors.html#core.connectors.Server.connection_timestamp","title":"core.connectors.Server.connection_timestamp  <code>instance-attribute</code>","text":"<pre><code>connection_timestamp = 0\n</code></pre> <p>Timestamp of the last successful login, useful to keep connections alive.</p> Todo <p>implement it</p>"},{"location":"api/core/connectors.html#core.connectors.Server-functions","title":"Functions","text":""},{"location":"api/core/connectors.html#core.connectors.Server.calling_file","title":"core.connectors.Server.calling_file","text":"<pre><code>calling_file() -&gt; str\n</code></pre> <p>Output the name of the calling filter from the stack. Allows to find the path of the folder containing filter, to perform actions like writing logs.</p> <p>It needs to be called from filter script through object proxy method, pointing to the server method, otherwise the index 2 will be wrong.</p>"},{"location":"api/core/connectors.html#core.connectors.Server.init_connection","title":"core.connectors.Server.init_connection  <code>abstractmethod</code>","text":"<pre><code>init_connection(params: dict) -&gt; None\n</code></pre> <p>Init a connection with the server and login.</p> <p>This should set <code>self.connection_inited = True</code> on success.</p>"},{"location":"api/core/connectors.html#core.connectors.Server.close_connection","title":"core.connectors.Server.close_connection  <code>abstractmethod</code>","text":"<pre><code>close_connection() -&gt; None\n</code></pre> <p>Close the server connection</p>"},{"location":"api/core/connectors.html#core.connectors.Server.run_filters","title":"core.connectors.Server.run_filters  <code>abstractmethod</code>","text":"<pre><code>run_filters(\n    filter: typing.Callable, action: typing.Callable, runs: int = 1, **kwargs\n) -&gt; None\n</code></pre> <p>Loop over <code>self.objects</code> to execute <code>filter()</code> and <code>action()</code> on each.</p>"},{"location":"api/core/crawler.html","title":"core.crawler","text":""},{"location":"api/core/crawler.html#core.crawler","title":"core.crawler","text":"<p>Module containing utilities to crawl websites for HTML, XML and PDF pages for their text content. PDF can be read from their text content if any, or through optical characters recognition for scans. Websites can be crawled from a <code>sitemap.xml</code> file or by following internal links recursively from and index page. Each page is aggregated on a list of core.types.web_page objects, meant to be used as input to train natural language AI models and to index and rank for search engines.</p> <p>\u00a9 2023-2024 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/crawler.html#core.crawler-classes","title":"Classes","text":""},{"location":"api/core/crawler.html#core.crawler.Crawler","title":"core.crawler.Crawler","text":"<pre><code>Crawler(\n    delay: float = 1.0,\n    no_follow: list[str] = [],\n    known_urls: dict[str, datetime.datetime] | None = None,\n    since: datetime.datetime | None = None,\n)\n</code></pre> <p>               Bases: <code>DelayedClass</code></p> <p>Crawl a website from its sitemap or by following internal links recusively from an index page. This class needs therefore to be used within a <code>with</code> statement that will take care of resources allocations and releases in background.</p> PARAMETER DESCRIPTION <code>delay</code> <p>time in seconds to wait before 2 HTTP requests. The right delay will prevent the crawler from being throttled by anti-DoS rules while making it as fast as possible. Set to <code>0.0</code> if you are crawling your own servers and they have no DoS protection.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>no_follow</code> <p>list of URL parts to completely ignore, that is not index them but not even crawl them for internal links.</p> <p> TYPE: <code>list[str]</code> DEFAULT: <code>[]</code> </p> <code>known_urls</code> <p>mapping of <code>url \u2192 last crawled datetime</code> for pages already in the index. When provided, crawling methods use it to skip pages that have not changed since they were last indexed.  Populate it conveniently with load_known_urls.</p> <p> TYPE: <code>dict[str, datetime.datetime] | None</code> DEFAULT: <code>None</code> </p> <code>since</code> <p>global freshness cut-off for recursive crawling.  Any URL present in known_urls and last crawled on or after this datetime will be skipped entirely. Has no effect when known_urls is empty or when a URL is not yet known. For sitemap crawling, the sitemap\u2019s own <code>&lt;lastmod&gt;</code> field takes precedence; since is only used as a fallback for entries that have no <code>&lt;lastmod&gt;</code>.</p> <p> TYPE: <code>datetime.datetime | None</code> DEFAULT: <code>None</code> </p> Example <pre><code>db = database.open_db(\"my-engine.db\")\n\nwith crawler.Crawler(delay=1.0) as cr:\n    cr.load_known_urls(db)      # populate incremental-update map\n    cr.since = datetime.datetime(2025, 1, 1, tzinfo=datetime.timezone.utc)\n\n    # Only re-fetches pages whose &lt;lastmod&gt; is newer than the stored crawl date.\n    pages = cr.get_website_from_sitemap(\"https://domain.com\", \"en\")\n\n    # Only re-fetches pages not yet in the index, or crawled before since.\n    pages += cr.get_website_from_crawling(\"https://forum.domain.com\", \"en\")\n</code></pre>"},{"location":"api/core/crawler.html#core.crawler.Crawler-attributes","title":"Attributes","text":""},{"location":"api/core/crawler.html#core.crawler.Crawler.no_follow","title":"core.crawler.Crawler.no_follow  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>no_follow: list[str] = [\n    \"api.whatsapp.com/share\",\n    \"api.whatsapp.com/send\",\n    \"pinterest.fr/pin/create\",\n    \"pinterest.com/pin/create\",\n    \"facebook.com/sharer\",\n    \"twitter.com/intent/tweet\",\n    \"twitter.com/share\",\n    \"x.com/share\",\n    \"reddit.com/submit\",\n    \"t.me/share\",\n    \"linkedin.com/share\",\n    \"vk.com/share.php\",\n    \"bufferapp.com/add\",\n    \"getpocket.com/edit\",\n    \"tumblr.com/share\",\n    \"translate.google.com/translate\",\n    \"flickr.com\",\n    \"instagram.com\",\n    \"mailto:\",\n    \"/profile/\",\n    \"/login/\",\n    \"/signup/\",\n    \"/login?\",\n    \"/signup?/user/\",\n    \"/member/\",\n    \"/register?\",\n    \"?share=\",\n    \".css\",\n    \".js\",\n    \".json\",\n    \".jpg\",\n    \".png\",\n    \".jpeg\",\n    \".gif\",\n    \".webp\",\n    \".heif\",\n    \".tif\",\n]\n</code></pre> <p>List of URLs sub-strings that will disable crawling if they are found in URLs. Mostly social networks sharing links.</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.crawled_URL","title":"core.crawler.Crawler.crawled_URL  <code>instance-attribute</code>","text":"<pre><code>crawled_URL: list[str] = []\n</code></pre> <p>List of { URL + category } hashes already visited. Websites crawled from sitemap and also following internal links recursively will tag recursively-crawled pages with an <code>external</code> category, which will later be considered by core.deduplicator.Deduplicator with a lower priority than any other category. Sitemap crawling may restrict content to selected HTML tags and produce better-quality data, with less noise. So we need to keep crawling everything from sitemap, whether or not it was already crawled from internal links earlier, and dedup will sort it out.</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.crawled_content","title":"core.crawler.Crawler.crawled_content  <code>instance-attribute</code>","text":"<pre><code>crawled_content: list[str] = []\n</code></pre> <p>List of hashes of content already known</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.known_urls","title":"core.crawler.Crawler.known_urls  <code>instance-attribute</code>","text":"<pre><code>known_urls: dict[str, datetime.datetime] = (\n    dict(known_urls) if known_urls else {}\n)\n</code></pre> <p>Mapping of URL \u2192 last-crawled datetime for incremental updates. Populated at construction time or via load_known_urls.</p> Note <p>We strip leading and trailing <code>/</code> for generality, in URL keys.</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.since","title":"core.crawler.Crawler.since  <code>instance-attribute</code>","text":"<pre><code>since: datetime.datetime | None = since\n</code></pre> <p>Global freshness cut-off for recursive and API-based crawling. Pages in known_urls last crawled on or after this datetime are skipped.</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.errors","title":"core.crawler.Crawler.errors  <code>instance-attribute</code>","text":"<pre><code>errors = []\n</code></pre> <p>URLs that couldn\u2019t be accessed due to blocking or throttling</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.notfound","title":"core.crawler.Crawler.notfound  <code>instance-attribute</code>","text":"<pre><code>notfound = []\n</code></pre> <p>URLs returning error 404 - not found</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler-functions","title":"Functions","text":""},{"location":"api/core/crawler.html#core.crawler.Crawler.load_known_urls","title":"core.crawler.Crawler.load_known_urls","text":"<pre><code>load_known_urls(db: sqlite3.Connection) -&gt; int\n</code></pre> <p>Populate the incremental-update map from an existing index database.</p> <p>After calling this, all crawling methods will skip pages whose stored crawl timestamp indicates they are still fresh (see <code>self.since</code> and the <code>&lt;lastmod&gt;</code> logic in get_website_from_sitemap).</p> PARAMETER DESCRIPTION <code>db</code> <p>an open SQLite connection to a Virtual Secretary database (as returned by core.database.open_db or core.database.create_db).</p> <p> TYPE: <code>sqlite3.Connection</code> </p> RETURNS DESCRIPTION <code>int</code> <p>Number of URL entries loaded.</p> Example <pre><code>db  = database.open_db(\"my-engine.db\")\nwith crawler.Crawler(delay=1.0) as cr:\n    cr.load_known_urls(db)\n    cr.since = datetime.datetime(2025, 6, 1, tzinfo=datetime.timezone.utc)\n    pages = cr.get_website_from_sitemap(\"https://domain.com\", \"en\")\ndb.close()\n</code></pre>"},{"location":"api/core/crawler.html#core.crawler.Crawler.discard_link","title":"core.crawler.Crawler.discard_link","text":"<pre><code>discard_link(url)\n</code></pre> <p>Returns True if the url is found in the <code>self.no_follow</code> list</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.get_immediate_links","title":"core.crawler.Crawler.get_immediate_links","text":"<pre><code>get_immediate_links(\n    links: list[str],\n    domain,\n    default_lang,\n    langs,\n    category,\n    contains_str,\n    internal_links: str = \"any\",\n    mine_pdf=False,\n) -&gt; list[web_page]\n</code></pre> <p>Follow internal and external links contained in a webpage only to one recursivity level, including PDF files and HTML pages. This is useful to index references docs linked from a page.</p> PARAMETER DESCRIPTION <code>internal_links</code> <p>defines what to do with links found inside the HTML page body/content: - <code>any</code>: follow and include all links found in page content, no matter what domain they point to, - <code>internal</code>: follow and include links found in page content only if they point to the same domain as the current page, - <code>external</code>: follow and include links found in page content only if they point to a different domain than the current page, - <code>ignore</code>: don\u2019t follow links found in page content.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'any'</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>list of links targets content</p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.update_link","title":"core.crawler.Crawler.update_link","text":"<pre><code>update_link(\n    old_link: str, new_link: str | None, category: str, status_code: int\n) -&gt; str\n</code></pre> <p>Update target link with possible HTTP redirections</p> PARAMETER DESCRIPTION <code>old_link</code> <p>original URL followed, found in HTML</p> <p> TYPE: <code>str</code> </p> <code>new_link</code> <p>destination URL retrieved, possibly after HTTP redirections.</p> <p> TYPE: <code>str | None</code> </p> <code>category</code> <p>tagged category of the page</p> <p> TYPE: <code>str</code> </p> <code>status_code</code> <p>HTML returned status code</p> <p> TYPE: <code>int</code> </p>"},{"location":"api/core/crawler.html#core.crawler.Crawler.get_website_from_crawling","title":"core.crawler.Crawler.get_website_from_crawling","text":"<pre><code>get_website_from_crawling(\n    website: str,\n    default_lang: str = \"en\",\n    child: str = \"/\",\n    langs: tuple = (\"en\", \"fr\"),\n    markup: str = \"body\",\n    contains_str: str | list[str] = \"\",\n    max_recurse_level: int = -1,\n    category: str = \"\",\n    restrict_section: bool = False,\n    mine_pdf: bool = False,\n    _recursion_level: int = 0,\n    _mainthread: bool = True,\n) -&gt; list[web_page]\n</code></pre> <p>Recursively crawl all pages of a website from internal links found starting from the <code>child</code> page. This applies to all HTML pages hosted on the domain of <code>website</code> and to PDF documents either from the current domain or from external domains but referenced on HTML pages of the current domain.</p> PARAMETER DESCRIPTION <code>website</code> <p>root of the website, including <code>https://</code> or <code>http://</code> without trailing slash.</p> <p> TYPE: <code>str</code> </p> <code>default_lang</code> <p>provided or guessed main language of the website content. Not used internally.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'en'</code> </p> <code>child</code> <p>page of the website to use as index to start crawling for internal links.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'/'</code> </p> <code>langs</code> <p>ISO-something 2-letters code of the languages for which we attempt to fetch the translation  if available, looking for the HTML <code>&lt;link rel=\"alternate\" hreflang=\"...\"&gt;</code> tag.</p> <p> TYPE: <code>tuple</code> DEFAULT: <code>('en', 'fr')</code> </p> <code>contains_str</code> <p>a string or a list of strings that should be contained in a page URL for the page to be indexed. On a forum, you could for example restrict pages to URLs containing <code>\"discussion\"</code> to get only the threads and avoid user profiles or archive pages.</p> <p> TYPE: <code>str | list[str]</code> DEFAULT: <code>''</code> </p> <code>markup</code> <p>see core.parser.ParsedHTML.get_page_markup</p> <p> TYPE: <code>str</code> DEFAULT: <code>'body'</code> </p> <code>max_recurse_level</code> <p>this method will call itself recursively on each internal link found in the current page,  starting from the <code>website/child</code> page. The <code>max_recursion_level</code> defines how many times  it calls itself until it is stopped, if it is stopped. When set to <code>-1</code>, it stops when all  the internal links have been crawled.</p> <p> TYPE: <code>int</code> DEFAULT: <code>-1</code> </p> <code>category</code> <p>arbitrary category or label set by user for classification. Will be automatically set to <code>external</code> for URLs followed outside of the main domain.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> <code>restrict_section</code> <p>set to <code>True</code> to limit crawling to the website section defined by <code>://website/child/*</code>.  This is useful when indexing parts of very large websites when you are only interested in a small subset.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>mine_pdf</code> <p>set to <code>True</code> to aggressively try to crawl PDF linked on external HTML pages.  This may increase RAM consumption dramatically.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>_recursion_level</code> <p>DON\u2019T USE IT. Everytime this method calls itself recursively,  it increments this variable internally, and recursion stops when the level is equal to the <code>max_recurse_level</code>.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>a list of all valid pages found. Invalid pages (wrong markup, empty HTML response, 404 errors) will be silently ignored.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from core import crawler\n&gt;&gt;&gt; cr = crawler.Crawler()\n&gt;&gt;&gt; pages = cr.get_website_from_crawling(\"https://aurelienpierre.com\", default_lang=\"fr\", markup=(\"div\", { \"class\": \"post-content\" }))\n</code></pre>"},{"location":"api/core/crawler.html#core.crawler.Crawler.get_website_from_sitemap","title":"core.crawler.Crawler.get_website_from_sitemap","text":"<pre><code>get_website_from_sitemap(\n    website: str,\n    default_lang: str,\n    sitemap: str = \"/sitemap.xml\",\n    langs: tuple[str] = (\"en\", \"fr\"),\n    markup: str | tuple[str] = \"body\",\n    category: str = \"\",\n    contains_str: str | list[str] = \"\",\n    internal_links: str = \"any\",\n    mine_pdf: bool = False,\n    _recursion_level: int = 0,\n) -&gt; list[web_page]\n</code></pre> <p>Recursively crawl all pages of a website from links found in a sitemap.  This applies to all HTML pages hosted on the domain of <code>website</code> and to PDF documents either from  the current domain or from external domains but referenced on HTML pages of the current domain.  Sitemaps of sitemaps are followed recursively.</p> PARAMETER DESCRIPTION <code>website</code> <p>root of the website, including <code>https://</code> or <code>http://</code> without trailing slash.</p> <p> TYPE: <code>str</code> </p> <code>default_lang</code> <p>provided or guessed main language of the website content. Not used internally.</p> <p> TYPE: <code>str</code> </p> <code>sitemap</code> <p>relative path of the XML sitemap.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'/sitemap.xml'</code> </p> <code>langs</code> <p>ISO-something 2-letters code of the languages for which we attempt to fetch the translation if available,  looking for the HTML <code>&lt;link rel=\"alternate\" hreflang=\"...\"&gt;</code> tag.</p> <p> TYPE: <code>tuple[str]</code> DEFAULT: <code>('en', 'fr')</code> </p> <code>markup</code> <p>see core.parser.ParsedHTML.get_page_markup</p> <p> TYPE: <code>str | tuple[str]</code> DEFAULT: <code>'body'</code> </p> <code>category</code> <p>arbitrary category or label</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> <code>contains_str</code> <p>limit recursive crawling from sitemap-defined pages to pages containing this string or list of strings.  Will get passed as-is to core.crawler.Crawler.get_website_from_crawling.</p> <p> TYPE: <code>str | list[str]</code> DEFAULT: <code>''</code> </p> <code>internal_links</code> <p>defines what to do with links found inside the HTML page body/content. - <code>any</code>: follow and include all links found in page, no matter what domain they point to, - <code>internal</code>: follow and include links found in page only if they point to the same domain as the current page, - <code>external</code>: follow and include links found in page only if they point to a different domain than the current page, - <code>ignore</code>: don\u2019t follow internal links</p> <p> TYPE: <code>str</code> DEFAULT: <code>'any'</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>a list of all valid pages found. Invalid pages (wrong markup, empty HTML response, 404 errors) will be silently ignored.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from core import crawler\n&gt;&gt;&gt; cr = crawler.Crawler()\n&gt;&gt;&gt; pages = cr.get_website_from_sitemap(\"https://aurelienpierre.com\", default_lang=\"fr\", markup=(\"div\", { \"class\": \"post-content\" }))\n</code></pre>"},{"location":"api/core/crawler.html#core.crawler.Crawler.get_unique_internal_url","title":"core.crawler.Crawler.get_unique_internal_url","text":"<pre><code>get_unique_internal_url(\n    page: ParsedHTML, domain: str, currentURL: str\n) -&gt; list[str]\n</code></pre> <p>Grab the internal links found in page, except PDF, and return only the ones we don\u2019t already know</p>"},{"location":"api/core/crawler.html#core.crawler-functions","title":"Functions","text":""},{"location":"api/core/crawler.html#core.crawler.get_content_type","title":"core.crawler.get_content_type","text":"<pre><code>get_content_type(\n    url: str, delay: DelayedClass, bypass_robots_txt=False\n) -&gt; tuple[str, bool, str, dict | None, int]\n</code></pre> <p>Probe an URL for HTTP headers only to see what type of content it returns. Try to sanitize partly-invalid URLs, like when protocols are not handled/redirected (<code>http</code> vs <code>https</code>), or invalid trailing characters, URL parameters and anchors are passed.</p> PARAMETER DESCRIPTION <code>url</code> <p>fully-formed link to try (including protocol)</p> <p> TYPE: <code>str</code> </p> <code>delay</code> <p>time to wait before re-trying different sanitized URLs</p> <p> TYPE: <code>DelayedClass</code> </p> RETURNS DESCRIPTION <code>type</code> <p>the type of content, like <code>plain/html</code>, <code>application/pdf</code>, etc.</p> <p> TYPE: <code>str</code> </p> <code>status</code> <p>the state flag:</p> <ul> <li><code>True</code> if the URL exists, but it might unreachable or forbidden,</li> <li><code>False</code> if the URL raises an HTTP 404 error (not found).</li> </ul> <p> TYPE: <code>bool</code> </p> <code>response_url</code> <p>the actual target URL, sanitized and possibly redirected.</p> <p> TYPE: <code>str</code> </p> <code>states</code> <p>HTTP return code</p> <p> TYPE: <code>int</code> </p>"},{"location":"api/core/crawler.html#core.crawler.relative_to_absolute","title":"core.crawler.relative_to_absolute","text":"<pre><code>relative_to_absolute(URL: str, domain: str, current_page: str) -&gt; str\n</code></pre> <p>Convert a relative URL to absolute by prepending the domain.</p> PARAMETER DESCRIPTION <code>URL</code> <p>the URL string to normalize to absolute,</p> <p> TYPE: <code>str</code> </p> <code>domain</code> <p>the domain name of the website, without protocol (<code>http://</code>) nor trailing slash. It will be appended to the relative links starting by <code>/</code>.</p> <p> TYPE: <code>str</code> </p> <code>current_page</code> <p>the URL of the page from which we analyze links. It will be appended to the relative links starting by <code>./</code>.</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>str</code> <p>The normalized, absolute URL on this website.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; relative_to_absolute(\"folder/page\", \"me.com\")\n\"://me.com/folder/page\"\n</code></pre>"},{"location":"api/core/crawler.html#core.crawler.radical_url","title":"core.crawler.radical_url","text":"<pre><code>radical_url(URL: str) -&gt; str\n</code></pre> <p>Trim an URL to the page (radical) part, removing anchors if any (internal links)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; radical_url(\"http://me.com/page#section-1\")\n\"http://me.com/page\"\n</code></pre>"},{"location":"api/core/crawler.html#core.crawler.get_page_content","title":"core.crawler.get_page_content","text":"<pre><code>get_page_content(\n    url: str | None,\n    delay: DelayedClass,\n    content: str | None = None,\n    custom_header={},\n) -&gt; tuple[ParsedHTML | None, str | None, int]\n</code></pre> <p>Request an (x)HTML page through the network with HTTP GET and feed its response to a ParsedHTML handler. This needs a functionnal network connection.</p> <p>The DOM is pre-filtered as follow to keep only natural language and avoid duplicate strings:</p> <ul> <li>media tags are removed (<code>&lt;iframe&gt;</code>, <code>&lt;embed&gt;</code>, <code>&lt;img&gt;</code>, <code>&lt;svg&gt;</code>, <code>&lt;audio&gt;</code>, <code>&lt;video&gt;</code>, etc.),</li> <li>code and machine language tags are removed (<code>&lt;script&gt;</code>, <code>&lt;style&gt;</code>, <code>&lt;math&gt;</code>),</li> <li>menus and sidebars are removed (<code>&lt;nav&gt;</code>, <code>&lt;aside&gt;</code>),</li> <li>forms, fields and buttons are removed(<code>&lt;select&gt;</code>, <code>&lt;input&gt;</code>, <code>&lt;button&gt;</code>, <code>&lt;textarea&gt;</code>, etc.)</li> </ul> <p>The HTML is un-minified to help end-of-sentences detections in cases where sentences don\u2019t end with punctuation (e.g. in titles).</p> PARAMETER DESCRIPTION <code>url</code> <p>a valid URL that can be fetched with an HTTP GET request.</p> <p> TYPE: <code>str | None</code> </p> <code>content</code> <p>a string buffer used as HTML source. If this argument is passed, we don\u2019t fetch <code>url</code> from network and directly use this input.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>tuple[ParsedHTML | None, str | None, int]</code> <p>a tuple with: 1. core.parser.ParsedHTML object initialized with the page DOM for further text mining. <code>None</code> if the HTML response was empty or the URL could not be reached. The list of URLs found in page before removing meaningless markup is stored as a list of strings in the <code>object.links</code> member. <code>object.h1</code> and <code>object.h2</code> contain a set of headers 1 and 2 found in the page before removing any markup. <code>object.date</code> contains the best-guess for the date. 2. the final URL of the retrieved page, which might be different from the input URL if HTTP redirections happened,</p>"},{"location":"api/core/crawler.html#core.crawler.parse_page","title":"core.crawler.parse_page","text":"<pre><code>parse_page(\n    page: ParsedHTML,\n    url: str,\n    lang: str | None,\n    markup: str | tuple | list[str] | list[tuple] | None,\n    date: str | None = None,\n    category: str | None = None,\n) -&gt; list[web_page]\n</code></pre> <p>Get the requested markup from the requested page URL.</p> <p>This chains in a single call:</p> <ul> <li>core.parser.ParsedHTML.get_page_markup</li> <li>core.parser.ParsedHTML.get_date</li> <li>core.parser.ParsedHTML.get_excerpt</li> </ul> PARAMETER DESCRIPTION <code>page</code> <p>a core.parser.ParsedHTML handler with pre-filtered DOM,</p> <p> TYPE: <code>ParsedHTML</code> </p> <code>url</code> <p>the valid URL accessible by HTTP GET request of the page</p> <p> TYPE: <code>str</code> </p> <code>lang</code> <p>the provided or guessed language of the page,</p> <p> TYPE: <code>str | None</code> </p> <code>markup</code> <p>the markup to search for. See core.parser.ParsedHTML.get_page_markup for details.</p> <p> TYPE: <code>str | tuple | list[str] | list[tuple] | None</code> </p> <code>date</code> <p>if the page was retrieved from a sitemap, usually the date is available in ISO format (<code>yyyy-mm-ddTHH:MM:SS</code>) and can be passed directly here. Otherwise, several attempts will be made to extract it from the page content (see core.parser.ParsedHTML.get_date).</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>category</code> <p>arbitrary category or label defined by user</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>The content of the page, including metadata, in a core.types.web_page singleton.</p>"},{"location":"api/core/crawler.html#core.crawler.hash_with_category","title":"core.crawler.hash_with_category","text":"<pre><code>hash_with_category(data: str, category: str) -&gt; str\n</code></pre> <p>Produce a unique identifier mixing data and category.</p>"},{"location":"api/core/database.html","title":"core.database","text":""},{"location":"api/core/database.html#core.database","title":"core.database","text":"<p>Create an SQLite database of <code>web_pages</code> to be used by a search engine.</p> <p>\u00a9 2024 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/database.html#core.database-attributes","title":"Attributes","text":""},{"location":"api/core/database.html#core.database.regex_starter","title":"core.database.regex_starter  <code>module-attribute</code>","text":"<pre><code>regex_starter = '(?&lt;=^|\\\\s|\\\\[|\\\\(|\\\\{|\\\\&lt;|\\\\\\'|\\\\\"|`|;|\\\\&gt;)'\n</code></pre> <p>Start of line, or start of document, or start of markup</p>"},{"location":"api/core/database.html#core.database.regex_stopper","title":"core.database.regex_stopper  <code>module-attribute</code>","text":"<pre><code>regex_stopper = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|\\\\&lt;)'\n</code></pre> <p>End of line, or end of document, or end of markup</p>"},{"location":"api/core/database.html#core.database.end_of_word","title":"core.database.end_of_word  <code>module-attribute</code>","text":"<pre><code>end_of_word = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|:|,|\\\\?|\\\\!|\\\\.|\\\\&lt;)'\n</code></pre> <p>End of word, or end of line, or end of document, or end of markup</p>"},{"location":"api/core/database.html#core.database.regex_algebra","title":"core.database.regex_algebra  <code>module-attribute</code>","text":"<pre><code>regex_algebra = '[\\\\+\\\\-\\\\=\\\\\u2260\\\\\u00b1]'\n</code></pre> <p>Algebraic signs</p>"},{"location":"api/core/database.html#core.database.IP_PATTERN","title":"core.database.IP_PATTERN  <code>module-attribute</code>","text":"<pre><code>IP_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_ip, regex_stopper), re.IGNORECASE\n)\n</code></pre> <p>IPv4 and IPv6 patterns where the whole IP is captured in the first group.</p>"},{"location":"api/core/database.html#core.database.EMAIL_PATTERN","title":"core.database.EMAIL_PATTERN  <code>module-attribute</code>","text":"<pre><code>EMAIL_PATTERN = re.compile(\n    \"&lt;?([0-9a-z\\\\-\\\\_\\\\+\\\\.]+?@[0-9a-z\\\\-\\\\_\\\\+]+(\\\\.[0-9a-z\\\\_\\\\-]{2,})+)&gt;?\",\n    re.IGNORECASE,\n)\n</code></pre> <p>Emails patterns like <code>&lt;me@mail.com&gt;</code> or <code>me@mail.com</code> where the whole address is captured in the first group.</p>"},{"location":"api/core/database.html#core.database.URL_PATTERN","title":"core.database.URL_PATTERN  <code>module-attribute</code>","text":"<pre><code>URL_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_url, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>URL patterns like <code>http(s)://domain.ext/page/subpage?q=x&amp;r=0:1#anchor</code> or <code>//domain.ext/page</code>. URL must follow RFC3986, meaning query parameters should be before anchors, if any. Relying on this assumption allows a faster regex parsing.</p> <ul> <li>the protocol (ftp, ftps, http, https) is captured as the first group,</li> <li><code>domain.ext</code> is captured as the second group,</li> <li><code>/page/etc</code> is the third group, including leading and trailing <code>/</code>,</li> <li>page query parameters <code>?s=x&amp;r=0</code>, including <code>?</code>, is the fourth group if the URL declares <code>...?params#anchor</code>,</li> <li>anchor <code>#anchor</code> is the fifth group, including <code>#</code>, if the URL declares <code>...?params#anchor</code>.</li> </ul> <p>URLs are captured if they are:</p> <ul> <li>alone on their own line,</li> <li>enclosed in <code>{}</code>, <code>[]</code>, <code>()</code></li> <li>enclosed in whitespaces.</li> </ul> <p>Warning: URLs enclosed in <code>()</code>, <code>[]</code> and <code>{}</code> may retain the closing sign  as part of the page name since <code>()</code> and <code>[]</code> are valid in URL pathes  and parameters. This pattern will work on plain text only: Markdown, XML, HTML and JSON  will need to be parsed ahead.</p>"},{"location":"api/core/database.html#core.database.MEMBERS_PATTERN","title":"core.database.MEMBERS_PATTERN  <code>module-attribute</code>","text":"<pre><code>MEMBERS_PATTERN = re.compile('(?&lt;=[a-z])(\\\\.)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Domain patterns without leading protocol like <code>cdn.company.com</code> or class members in object-oriented programming languages like <code>params.cookies.client</code>.</p>"},{"location":"api/core/database.html#core.database.DATE_PATTERN","title":"core.database.DATE_PATTERN  <code>module-attribute</code>","text":"<pre><code>DATE_PATTERN = re.compile(date_regex, re.IGNORECASE)\n</code></pre> <p>Dates like <code>2022-12-01</code>, <code>01-12-2022</code>, <code>01-12-22</code>, <code>01/12/2022</code>, <code>01/12/22</code> where the whole date is captured in the first group, then each group of digits is captured in the order of appearance, in the next 3 groups</p>"},{"location":"api/core/database.html#core.database.TIME_PATTERN","title":"core.database.TIME_PATTERN  <code>module-attribute</code>","text":"<pre><code>TIME_PATTERN = re.compile(time_regex, re.IGNORECASE)\n</code></pre> <p>Identify more or less standard time patterns, like :</p> <ul> <li>12h15</li> <li>12:15</li> <li>12:15:00</li> <li>12am</li> <li>12 am</li> <li>12 h</li> <li>12:15:00Z</li> <li>12:15:00+01</li> <li>12:15:00 UTC+1</li> <li>11:27:45+0000</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>1- or 2-digits hour,</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>hour/minutes separator or half-day marker among <code>[\"h\", \":\", \"am\", \"pm\"]</code> (case-insensitive)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2-digits minutes, if any, or <code>None</code></p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>2-digits seconds, if any.</p> <p> TYPE: <code>str</code> </p> <code>4</code> <p>hour marker (<code>h</code> or <code>H</code>), half-day marker (case-insensitive <code>[\"am\", \"pm\"]</code>), or time zone marker (case-sensitive <code>[\"Z\", \"UTC\"]</code>)</p> <p> TYPE: <code>str</code> </p> <code>5</code> <p>1-or 2-digits signed integer timezone shift (referred to UTC).</p> <p> TYPE: <code>str</code> </p> <p>Examples:</p> <p>see https://regex101.com/r/QNtZAK/2</p> <p>see <code>src/tests/test-patterns.py</code></p>"},{"location":"api/core/database.html#core.database.DOMAIN_PATTERN","title":"core.database.DOMAIN_PATTERN  <code>module-attribute</code>","text":"<pre><code>DOMAIN_PATTERN = re.compile(\n    \"from ((?:[a-z0-9\\\\-_]{0,61}\\\\.)+[a-z]{2,})\", re.IGNORECASE\n)\n</code></pre> <p>Matches patterns like <code>from (domain.ext)</code> from RFC-822 <code>Received</code> header in emails.</p>"},{"location":"api/core/database.html#core.database.UID_PATTERN","title":"core.database.UID_PATTERN  <code>module-attribute</code>","text":"<pre><code>UID_PATTERN = re.compile('UID ([0-9]+)')\n</code></pre> <p>Matches email integer UID from IMAP headers.</p>"},{"location":"api/core/database.html#core.database.FLAGS_PATTERN","title":"core.database.FLAGS_PATTERN  <code>module-attribute</code>","text":"<pre><code>FLAGS_PATTERN = re.compile('FLAGS \\\\((.*?)\\\\)')\n</code></pre> <p>Matches email flags from IMAP headers.</p>"},{"location":"api/core/database.html#core.database.PATH_PATTERN","title":"core.database.PATH_PATTERN  <code>module-attribute</code>","text":"<pre><code>PATH_PATTERN = re.compile('%s%s%s' % (regex_starter, path_regex, end_of_word))\n</code></pre> <p>File path pattern like <code>~/file</code>, <code>/home/file</code>, <code>./file</code> or <code>C:\\windows</code></p>"},{"location":"api/core/database.html#core.database.PARTIAL_PATH_REGEX","title":"core.database.PARTIAL_PATH_REGEX  <code>module-attribute</code>","text":"<pre><code>PARTIAL_PATH_REGEX = re.compile(\n    \"%s%s%s\" % (regex_starter, partial_path_regex, end_of_word)\n)\n</code></pre> <p>Partial, invalid path patterns missing the leading root, like <code>home/user/stuff</code>. We start capturing after at least two folder separators (slash or backslash).</p> <p>Warning</p> <p>this will collide with date detection, so run it after in the pipeline.</p>"},{"location":"api/core/database.html#core.database.RESOLUTION_PATTERN","title":"core.database.RESOLUTION_PATTERN  <code>module-attribute</code>","text":"<pre><code>RESOLUTION_PATTERN = re.compile('\\\\d+(?:\u00d7|x|X)\\\\d+')\n</code></pre> <p>Pixel resolution like 10x20 or 10\u00d720. Units are discarded.</p>"},{"location":"api/core/database.html#core.database.NUMBER_PATTERN","title":"core.database.NUMBER_PATTERN  <code>module-attribute</code>","text":"<pre><code>NUMBER_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_number, regex_stopper)\n)\n</code></pre> <p>Signed integers and decimals, fractions and numeric IDs with interal dashes and underscores. Numbers with starting or trailing units are not considered. Lazy decimals (.1 and 1.) are considered.</p>"},{"location":"api/core/database.html#core.database.HASH_PATTERN","title":"core.database.HASH_PATTERN  <code>module-attribute</code>","text":"<pre><code>HASH_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_hash, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>Cryptographic hexadecimal hashes and fingerprints, of a min length of 8 characters.</p>"},{"location":"api/core/database.html#core.database.MULTIPLE_LINES","title":"core.database.MULTIPLE_LINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_LINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]{2,} ?)+')\n</code></pre> <p>Detect more than 2 newlines and tab, possibly mixed with spaces</p>"},{"location":"api/core/database.html#core.database.MULTIPLE_NEWLINES","title":"core.database.MULTIPLE_NEWLINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_NEWLINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]+ ?){2,}')\n</code></pre> <p>Detect broken sequences of newlines and spaces.</p>"},{"location":"api/core/database.html#core.database.INTERNAL_NEWLINE","title":"core.database.INTERNAL_NEWLINE  <code>module-attribute</code>","text":"<pre><code>INTERNAL_NEWLINE = re.compile('(?&lt;=\\\\w)[\\\\n\\\\t\\\\r]{1}(?=\\\\w)')\n</code></pre> <p>Detect single newline characters nested inside text. Mostly useful for parsed PDF where line wrapping is quite literal ( used instead of space).</p>"},{"location":"api/core/database.html#core.database.EXPOSURE","title":"core.database.EXPOSURE  <code>module-attribute</code>","text":"<pre><code>EXPOSURE = re.compile(\n    \"%s%s%s\" % (regex_starter, exposure_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/database.html#core.database.PHOTOSPEED","title":"core.database.PHOTOSPEED  <code>module-attribute</code>","text":"<pre><code>PHOTOSPEED = re.compile(\n    \"%s%s%s\" % (regex_starter, photospeed_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/database.html#core.database.SENSIBILITY","title":"core.database.SENSIBILITY  <code>module-attribute</code>","text":"<pre><code>SENSIBILITY = re.compile(\n    \"%s%s%s\" % (regex_starter, sensibility_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Photographic sensibility in ISO or ASA</p>"},{"location":"api/core/database.html#core.database.LUMINANCE","title":"core.database.LUMINANCE  <code>module-attribute</code>","text":"<pre><code>LUMINANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, luminance_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Luminance/radiance in nits or Cd/m\u00b2</p>"},{"location":"api/core/database.html#core.database.DIAPHRAGM","title":"core.database.DIAPHRAGM  <code>module-attribute</code>","text":"<pre><code>DIAPHRAGM = re.compile(\n    \"%s%s\" % (regex_starter, diaphragm_regex), flags=re.IGNORECASE\n)\n</code></pre> <p>Photographic diaph aperture values like f/2.8 or f/11</p>"},{"location":"api/core/database.html#core.database.GAIN","title":"core.database.GAIN  <code>module-attribute</code>","text":"<pre><code>GAIN = re.compile(\n    \"%s%s%s\" % (regex_starter, gain_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Gain, attenuation and PSNR in dB</p>"},{"location":"api/core/database.html#core.database.FILE_SIZE","title":"core.database.FILE_SIZE  <code>module-attribute</code>","text":"<pre><code>FILE_SIZE = re.compile(\n    \"%s%s%s\" % (regex_starter, filesize_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>File and memory size in bit, byte, or octet and their multiples</p>"},{"location":"api/core/database.html#core.database.DISTANCE","title":"core.database.DISTANCE  <code>module-attribute</code>","text":"<pre><code>DISTANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, distance_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Distance in meter, inch, foot and their multiples</p>"},{"location":"api/core/database.html#core.database.PERCENT","title":"core.database.PERCENT  <code>module-attribute</code>","text":"<pre><code>PERCENT = re.compile('%s%s%s' % (regex_starter, percent_regex, end_of_word))\n</code></pre> <p>Number followed by %</p>"},{"location":"api/core/database.html#core.database.WEIGHT","title":"core.database.WEIGHT  <code>module-attribute</code>","text":"<pre><code>WEIGHT = re.compile(\n    \"%s%s%s\" % (regex_starter, weight_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Weight (mass) in British and SI units and their multiples</p>"},{"location":"api/core/database.html#core.database.ANGLE","title":"core.database.ANGLE  <code>module-attribute</code>","text":"<pre><code>ANGLE = re.compile(\n    \"%s%s%s\" % (regex_starter, angle_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Angles in radians, degrees and steradians</p>"},{"location":"api/core/database.html#core.database.TEMPERATURE","title":"core.database.TEMPERATURE  <code>module-attribute</code>","text":"<pre><code>TEMPERATURE = re.compile(\n    \"%s%s%s\" % (regex_starter, temperature_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Temperatures in \u00b0C, \u00b0F and K</p>"},{"location":"api/core/database.html#core.database.FREQUENCY","title":"core.database.FREQUENCY  <code>module-attribute</code>","text":"<pre><code>FREQUENCY = re.compile(\n    \"%s%s%s\" % (regex_starter, frequency_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Frequencies in hertz and multiples</p>"},{"location":"api/core/database.html#core.database.TEXT_DATES","title":"core.database.TEXT_DATES  <code>module-attribute</code>","text":"<pre><code>TEXT_DATES = re.compile(\n    \"([0-9]{1,2})? (jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec|jan|f\u00e9v|mar|avr|mai|jui|jui|aou|sep|oct|nov|d\u00e9c|janvier|f\u00e9vrier|mars|avril|mai|juin|juillet|ao\u00fbt|septembre|octobre|novembre|d\u00e9cembre|january|february|march|april|may|june|july|august|september|october|november|december)\\\\.?( [0-9]{1,2})?( [0-9]{2,4})(?!\\\\:)\",\n    flags=re.IGNORECASE | re.MULTILINE,\n)\n</code></pre> <p>Find textual dates formats:</p> <ul> <li>English dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> <li>French dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>month (full-form or abbreviated)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>4 digits (full year)</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/database.html#core.database.BASE_64","title":"core.database.BASE_64  <code>module-attribute</code>","text":"<pre><code>BASE_64 = re.compile(\n    \"((?:[A-Za-z0-9+\\\\/]{4}){64,}(?:[A-Za-z0-9+\\\\/]{2}==|[A-Za-z0-9+\\\\/]{3}=)?)\"\n)\n</code></pre> <p>Identifies base64 encoding</p>"},{"location":"api/core/database.html#core.database.BB_CODE","title":"core.database.BB_CODE  <code>module-attribute</code>","text":"<pre><code>BB_CODE = re.compile('\\\\[(img|quote)[a-zA-Z0-9 =\\\\\"]*?\\\\].*?\\\\[\\\\/\\\\1\\\\]')\n</code></pre> <p>Identifies left-over BB code markup <code>[img]</code> and <code>[quote]</code></p>"},{"location":"api/core/database.html#core.database.MARKUP","title":"core.database.MARKUP  <code>module-attribute</code>","text":"<pre><code>MARKUP = re.compile('(?:\\\\[|\\\\{|\\\\&lt;)([^\\\\n\\\\r]+?)(?:\\\\]|\\\\}|\\\\&gt;)')\n</code></pre> <p>Identifies left-over HTML and Markdown markup, like <code>&lt;...&gt;</code>, <code>{...}</code>, <code>[...]</code></p>"},{"location":"api/core/database.html#core.database.USER","title":"core.database.USER  <code>module-attribute</code>","text":"<pre><code>USER = re.compile('([\\\\w\\\\-\\\\+\\\\.]+)?@([\\\\w\\\\-\\\\+\\\\.]+)|(user\\\\-?\\\\d+)')\n</code></pre> <p>Identifies user handles or emails</p>"},{"location":"api/core/database.html#core.database.REPEATED_CHARACTERS","title":"core.database.REPEATED_CHARACTERS  <code>module-attribute</code>","text":"<pre><code>REPEATED_CHARACTERS = re.compile('(.)\\\\1{9,}')\n</code></pre> <p>Identifies any character repeated more than 9 times</p>"},{"location":"api/core/database.html#core.database.UNFINISHED_SENTENCES","title":"core.database.UNFINISHED_SENTENCES  <code>module-attribute</code>","text":"<pre><code>UNFINISHED_SENTENCES = re.compile('(?&lt;![?!.;:])\\\\n\\\\n|\\\\r\\\\n')\n</code></pre> <p>Identifies sentences finishing with 2 newlines characters without having ending punctuations</p>"},{"location":"api/core/database.html#core.database.MULTIPLE_DOTS","title":"core.database.MULTIPLE_DOTS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DOTS = re.compile('\\\\.{2,}')\n</code></pre> <p>Identifies dots repeated more than twice</p>"},{"location":"api/core/database.html#core.database.MULTIPLE_DASHES","title":"core.database.MULTIPLE_DASHES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DASHES = re.compile('[-~]{1,}')\n</code></pre> <p>Identifies dashes repeated more than once</p>"},{"location":"api/core/database.html#core.database.MULTIPLE_QUESTIONS","title":"core.database.MULTIPLE_QUESTIONS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_QUESTIONS = re.compile('\\\\?{1,}')\n</code></pre> <p>Identifies question marks repeated more than once</p>"},{"location":"api/core/database.html#core.database.ORDINAL_FR","title":"core.database.ORDINAL_FR  <code>module-attribute</code>","text":"<pre><code>ORDINAL_FR = re.compile('n\u00b0 ?([0-9]+)')\n</code></pre> <p>French ordinal numbers (num\u00e9ros n\u00b0)</p>"},{"location":"api/core/database.html#core.database.FRANCAIS","title":"core.database.FRANCAIS  <code>module-attribute</code>","text":"<pre><code>FRANCAIS = re.compile(\n    \"%s(j|t|s|d|qu|lorsqu|quelqu|jusqu|m|c|n)\\\\'(?=[aeiouy\u00e9\u00e8\u00e0\u00ea\u00e2\u00ee\u00f4\u00fb\u00ef\u00fc\u00e4\u00eb\u00f6h][\\\\w\\\\s])\"\n    % regex_starter,\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>French contractions of pronouns and determinants</p>"},{"location":"api/core/database.html#core.database.DASHES","title":"core.database.DASHES  <code>module-attribute</code>","text":"<pre><code>DASHES = re.compile('(?&lt;=\\\\w)(-|_|=)+(?=\\\\w)', re.IGNORECASE)\n</code></pre> <p>Dashes in the middle of ASCII/Latin compounded words. Will not work if accented or Unicode characters are immediately surrounding the dash.</p>"},{"location":"api/core/database.html#core.database.ALTERNATIVES","title":"core.database.ALTERNATIVES  <code>module-attribute</code>","text":"<pre><code>ALTERNATIVES = re.compile('(?&lt;=[a-z])(\\\\/)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Slash-separated word alternatives like <code>and/or</code> <code>mr/mrs</code></p>"},{"location":"api/core/database.html#core.database.PLURAL_S","title":"core.database.PLURAL_S  <code>module-attribute</code>","text":"<pre><code>PLURAL_S = re.compile('(?&lt;=[a-zA-Z]{4,})s?e{0,2}s%s' % end_of_word)\n</code></pre> <p>Identify plural form of nouns (French and English), adjectives (French) and third-person present verbs (English) and second-person verbs (French) in -s.</p>"},{"location":"api/core/database.html#core.database.FEMININE_E","title":"core.database.FEMININE_E  <code>module-attribute</code>","text":"<pre><code>FEMININE_E = re.compile('(?&lt;=\\\\w{4,})e{1,2}%s' % end_of_word)\n</code></pre> <p>Identify feminine form of adjectives (French) in -e.</p>"},{"location":"api/core/database.html#core.database.DOUBLE_CONSONANTS","title":"core.database.DOUBLE_CONSONANTS  <code>module-attribute</code>","text":"<pre><code>DOUBLE_CONSONANTS = re.compile(\n    \"(?&lt;=\\\\w{2,})([bcfghjklmnpqrstvwxz])\\\\1\", re.IGNORECASE\n)\n</code></pre> <p>Identify double consonants in the middle of words.</p>"},{"location":"api/core/database.html#core.database.FEMININE_TRICE","title":"core.database.FEMININE_TRICE  <code>module-attribute</code>","text":"<pre><code>FEMININE_TRICE = re.compile('(?&lt;=\\\\w{4,})t(rice|eur|or)%s' % end_of_word)\n</code></pre> <p>Identify French feminine nouns in -trice.</p>"},{"location":"api/core/database.html#core.database.ADVERB_MENT","title":"core.database.ADVERB_MENT  <code>module-attribute</code>","text":"<pre><code>ADVERB_MENT = re.compile('(?&lt;=\\\\w{4,})e?ment%s' % end_of_word)\n</code></pre> <p>Identify French adverbs and English nouns ending en -ment</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_TION","title":"core.database.SUBSTANTIVE_TION  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_TION = re.compile('(?&lt;=\\\\w{4,})(t|s)ion%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from verbs by adding -tion and -sion</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_AT","title":"core.database.SUBSTANTIVE_AT  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_AT = re.compile('(?&lt;=\\\\w{4,})at%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from other nouns by adding -at</p>"},{"location":"api/core/database.html#core.database.PARTICIPLE_ING","title":"core.database.PARTICIPLE_ING  <code>module-attribute</code>","text":"<pre><code>PARTICIPLE_ING = re.compile('(?&lt;=\\\\w{4,})ing%s' % end_of_word)\n</code></pre> <p>Identify English substantives and present participles formed from verbs by adding -ing</p>"},{"location":"api/core/database.html#core.database.ADJECTIVE_ED","title":"core.database.ADJECTIVE_ED  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_ED = re.compile('(?&lt;=\\\\w{4,})ed%s' % end_of_word)\n</code></pre> <p>Identify English adjectives formed from verbs by adding -ed</p>"},{"location":"api/core/database.html#core.database.ADJECTIVE_TIF","title":"core.database.ADJECTIVE_TIF  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_TIF = re.compile('(?&lt;=\\\\w{2,})ti(f|v)%s' % end_of_word)\n</code></pre> <p>Identify English and French adjectives formed from verbs by adding -tif or -tive</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_Y","title":"core.database.SUBSTANTIVE_Y  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_Y = re.compile('(?&lt;=\\\\w{3,})y%s' % end_of_word)\n</code></pre> <p>Identify English substantives ending in -y</p>"},{"location":"api/core/database.html#core.database.VERB_IZ","title":"core.database.VERB_IZ  <code>module-attribute</code>","text":"<pre><code>VERB_IZ = re.compile('(?&lt;=\\\\w{4,})(i|y)z%s' % end_of_word)\n</code></pre> <p>Identify American verbs ending in -iz that French and Brits write in -is</p>"},{"location":"api/core/database.html#core.database.STUFF_ER","title":"core.database.STUFF_ER  <code>module-attribute</code>","text":"<pre><code>STUFF_ER = re.compile('(?&lt;=\\\\w{5,})er%s' % end_of_word)\n</code></pre> <p>Identify French 1st group verb (infinitive) and English substantives ending in -er</p>"},{"location":"api/core/database.html#core.database.BRITISH_OUR","title":"core.database.BRITISH_OUR  <code>module-attribute</code>","text":"<pre><code>BRITISH_OUR = re.compile('(?&lt;=\\\\w{3,})our%s' % end_of_word)\n</code></pre> <p>Identify British spelling ending in -our (colour, behaviour).</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_ITY","title":"core.database.SUBSTANTIVE_ITY  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_ITY = re.compile('(?&lt;=\\\\w{4,})it(y|e)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ity (English) and -ite (French).</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_IST","title":"core.database.SUBSTANTIVE_IST  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IST = re.compile('(?&lt;=\\\\w{3,})is(t|m)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ist and -ism.</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_IQU","title":"core.database.SUBSTANTIVE_IQU  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IQU = re.compile('(?&lt;=\\\\w{3,})i(qu|c)%s' % end_of_word)\n</code></pre> <p>Identify French substantives in -iqu</p>"},{"location":"api/core/database.html#core.database.SUBSTANTIVE_EUR","title":"core.database.SUBSTANTIVE_EUR  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_EUR = re.compile('(?&lt;=\\\\w{3,})eur%s' % end_of_word)\n</code></pre> <p>Identify French substantives -eur</p>"},{"location":"api/core/database.html#core.database.HYPHENIZED","title":"core.database.HYPHENIZED  <code>module-attribute</code>","text":"<pre><code>HYPHENIZED = re.compile('(?&lt;=\\\\w{3,})[-\u2013\u2014]+ *[\\\\n\\\\r]{1,2}(?=\\\\w)')\n</code></pre> <p>Detect hyphenized words at the end of a PDF text line.</p>"},{"location":"api/core/database.html#core.database.WAYBACK_RE","title":"core.database.WAYBACK_RE  <code>module-attribute</code>","text":"<pre><code>WAYBACK_RE = re.compile('https?://web\\\\.archive\\\\.org/web/[^/]+/(https?://.+)')\n</code></pre> <p>Find the canonical URL from web.archive.org (Wayback Machine) URLs</p>"},{"location":"api/core/database.html#core.database-classes","title":"Classes","text":""},{"location":"api/core/database.html#core.database.SQLitePageCorpus","title":"core.database.SQLitePageCorpus","text":"<pre><code>SQLitePageCorpus(\n    db,\n    query,\n    params=(),\n    atomic_types=(str, bytes),\n    max_depth=None,\n    yield_rows=False,\n)\n</code></pre> <p>Lazily stream rows from an SQLite request, avoiding full copy.</p> Example <p><pre><code>    corpus = SQLitePageCorpus(\n        db,\n        \"\"\"\n        SELECT tokenized\n        FROM pages\n        WHERE lang IN ('fr', 'en')\n        \"\"\",\n        max_depth=0\n    )\n</code></pre> - <code>max_depth=0</code> will not flatten the content, so it will return   the original <code>list[list[str]]</code> (list of sentences, aka list of list of words), - <code>max_depth=1</code> flattens documents, to it will return   <code>list[str]</code> (list of words)</p>"},{"location":"api/core/database.html#core.database-functions","title":"Functions","text":""},{"location":"api/core/database.html#core.database.split_url","title":"core.database.split_url","text":"<pre><code>split_url(url: str) -&gt; tuple[str, str, str, str, str] | None\n</code></pre> <p>Split a well-formed URL following RFC3986 into base elements.</p> RETURNS DESCRIPTION <code>tuple[str, str, str, str, str] | None</code> <p>a tuple of <code>(protocol, domain, page, parameters, anchor)</code>. </p> <code>tuple[str, str, str, str, str] | None</code> <p>Empty/missing fields are inited with empty strings so there is no need for individual <code>None</code> checks.</p> <code>tuple[str, str, str, str, str] | None</code> <p>If the <code>url</code> input doesn\u2019t match an URL format, return <code>None</code>.</p>"},{"location":"api/core/database.html#core.database.adapt_array","title":"core.database.adapt_array","text":"<pre><code>adapt_array(arr: np.ndarray)\n</code></pre> <p>http://stackoverflow.com/a/31312102/190597 (SoulNibbler)</p>"},{"location":"api/core/database.html#core.database.create_db","title":"core.database.create_db","text":"<pre><code>create_db(name: str) -&gt; sqlite3.Connection\n</code></pre> <p>Create the <code>pages</code> table if needed and add any missing columns. This doesn\u2019t destroy existing tables, rows or columns, so it\u2019s safe to run on any database.</p> Warning <p>Columns are inferred directly from <code>web_page.__annotations__</code>. Existing columns are preserved unchanged.</p> <p>The <code>url</code> column is used as the PRIMARY KEY.</p>"},{"location":"api/core/database.html#core.database.create_temp_db","title":"core.database.create_temp_db","text":"<pre><code>create_temp_db(\n    min_free: float = 2.0, filename: str | None = None\n) -&gt; sqlite3.Connection\n</code></pre> <p>Create a temporary SQLite database file (in /dev/shm when available) and initialize the <code>pages</code> table according to <code>web_page</code> annotations.</p> PARAMETER DESCRIPTION <code>min_free</code> <p>minimum available disk space in GiB required to create the temporary database. This is checked at runtime and the function will raise an error if the condition is not met.</p> <p> TYPE: <code>float</code> DEFAULT: <code>2.0</code> </p> <code>filename</code> <p>the full path and filename to save the temporary database, if it needs to be reused at some point.    </p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>sqlite3.Connection</code> <p>the sqlite3.Connection opened in bulk mode.</p> WARNING <p>the temporary SQLite database doesn\u2019t use <code>web_page</code> URL as primary key, to allow later deduplication.</p>"},{"location":"api/core/database.html#core.database.delete_temp_db","title":"core.database.delete_temp_db","text":"<pre><code>delete_temp_db(db: sqlite3.Connection)\n</code></pre> <p>Close and delete a temporary database in one shot.</p>"},{"location":"api/core/database.html#core.database.open_db","title":"core.database.open_db","text":"<pre><code>open_db(name: str, mode: str = 'rw') -&gt; sqlite3.Connection\n</code></pre> <p>Open an SQLite database with workload-specific optimizations.</p> PARAMETER DESCRIPTION <code>name</code> <p>Database identifier/path passed to <code>get_models_folder()</code>.</p> <p> TYPE: <code>str</code> </p> <code>mode</code> <ul> <li>\u201crw\u201d: Generic read/write mode.</li> <li>\u201cro\u201d: Read-only immutable mode optimized for serving/search workloads.</li> <li>\u201cbulk\u201d: Bulk-ingestion mode optimized for large batch writes.</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'rw'</code> </p> RETURNS DESCRIPTION <code>sqlite3.Connection</code> <p>sqlite3.Connection</p>"},{"location":"api/core/database.html#core.database.compress_db","title":"core.database.compress_db","text":"<pre><code>compress_db(\n    db: sqlite3.Connection,\n    delete_query: str | None = None,\n    delete_params: tuple | None = None,\n    delete_columns: list[str] | None = None,\n)\n</code></pre> <p>Optionally delete rows, then reclaim SQLite disk space.</p> PARAMETER DESCRIPTION <code>db</code> <p>SQLite connection</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>delete_query</code> <p>full DELETE SQL query</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>delete_params</code> <p>optional SQL parameters</p> <p> TYPE: <code>tuple | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/core/database.html#core.database.is_primary_key","title":"core.database.is_primary_key","text":"<pre><code>is_primary_key(db: sqlite3.Connection, table: str, column: str) -&gt; bool\n</code></pre> <p>Check whether <code>column</code> is part of the PRIMARY KEY of <code>table</code>.</p>"},{"location":"api/core/database.html#core.database.populate_db","title":"core.database.populate_db","text":"<pre><code>populate_db(\n    db: sqlite3.Connection, pages: list[web_page], batch_size: int = 4096\n)\n</code></pre> <p>Insert or update <code>web_page</code> records into the SQLite database.</p> <p>Existing rows are matched using the PRIMARY KEY <code>url</code>.</p> Warning <p>Array-like Python values are converted to <code>bytearray</code> then to <code>bytes</code> in order to be handled as <code>BLOB</code> by SQLite.</p>"},{"location":"api/core/database.html#core.database.db_to_list","title":"core.database.db_to_list","text":"<pre><code>db_to_list(db: sqlite3.Connection) -&gt; list[web_page]\n</code></pre> <p>Extract all <code>web_page</code> rows from the <code>pages</code> table in <code>db</code> as a list of <code>web_page</code></p>"},{"location":"api/core/database.html#core.database.migrate_url_to_primary_key","title":"core.database.migrate_url_to_primary_key","text":"<pre><code>migrate_url_to_primary_key(db: sqlite3.Connection)\n</code></pre> <p>Rebuild the <code>pages</code> table using <code>url</code> as PRIMARY KEY for older databases that didn\u2019t use a primary key.</p>"},{"location":"api/core/database.html#core.database.merge_databases","title":"core.database.merge_databases","text":"<pre><code>merge_databases(old_db: sqlite3.Connection, new_db: sqlite3.Connection)\n</code></pre> <p>Merge two <code>pages</code> databases.</p> <p>Rows from <code>old_db</code> are inserted into <code>new_db</code> only if their URL does not already exist.</p> <p>Existing rows in <code>new_db</code> are preserved unchanged.</p> <p>Only columns existing in BOTH databases are copied.</p>"},{"location":"api/core/database.html#core.database.update_pages_from_database","title":"core.database.update_pages_from_database","text":"<pre><code>update_pages_from_database(\n    target_db: sqlite3.Connection, source_db: sqlite3.Connection\n) -&gt; list[str]\n</code></pre> <p>Update rows in <code>target_db.pages</code> from <code>source_db.pages</code> using <code>url</code> as PRIMARY KEY.</p> <p>Only shared columns are updated.</p> <p>Returns     missing_urls: URLs present in target_db but absent from source_db.</p>"},{"location":"api/core/database.html#core.database.import_pages","title":"core.database.import_pages","text":"<pre><code>import_pages(\n    source_db: str | sqlite3.Connection,\n    destination_db: str | sqlite3.Connection,\n    where_clause: str = \"1=1\",\n    params: tuple = (),\n) -&gt; int\n</code></pre> <p>Import rows from one SQLite database into another.</p> <p>Both source_db and destination_db may be either a filesystem path (str) or an active <code>sqlite3.Connection</code> handle.  Passing a Connection is the only way to target a <code>:memory:</code> database, since those cannot be addressed by path.</p> <p>Connection lifecycle     - Path supplied \u2013 the function opens, commits, and closes the       connection itself (original behaviour).     - Connection supplied \u2013 the caller retains full control; the       connection is neither committed nor closed here, so the import       can participate in a larger transaction.</p> <p>Rows are copied from <code>source.pages</code> into <code>destination.pages</code>. Existing rows are updated on conflict of the <code>url</code> primary key. Columns present in the destination but absent from the source receive NULL.  Both schemas are discovered at runtime, so the function adapts automatically if either evolves.</p> PARAMETER DESCRIPTION <code>source_db</code> <p>Path to, or an open connection for, the source SQLite database.</p> <p> TYPE: <code>str | sqlite3.Connection</code> </p> <code>destination_db</code> <p>Path to, or an open connection for, the destination SQLite database.</p> <p> TYPE: <code>str | sqlite3.Connection</code> </p> <code>where_clause</code> <p>SQL WHERE clause applied to <code>source.pages</code>. Example: <code>\"domain = ? AND date &gt;= ?\"</code></p> <p> TYPE: <code>str</code> DEFAULT: <code>'1=1'</code> </p> <code>params</code> <p>Positional parameters bound to where_clause.</p> <p> TYPE: <code>tuple</code> DEFAULT: <code>()</code> </p> RETURNS DESCRIPTION <code>int</code> <p>Number of affected rows.</p> <p>Examples::</p> <pre><code># File \u2192 file (unchanged from before)\nimport_pages(\"old.db\", \"new.db\", \"domain = ?\", (\"example.com\",))\n\n# In-memory source \u2192 file destination\nimport_pages(mem_conn, \"new.db\")\n\n# File source \u2192 in-memory destination (e.g. for tests)\nimport_pages(\"prod.db\", mem_conn, \"date &gt;= ?\", (\"2024-01-01\",))\n\n# Both in-memory\nimport_pages(src_conn, dst_conn)\n</code></pre>"},{"location":"api/core/database.html#core.database.inspect_db","title":"core.database.inspect_db","text":"<pre><code>inspect_db(db: sqlite3.Connection, message: str = '') -&gt; None\n</code></pre> <p>Print useful metadata and statistics about a SQLite database.</p> PARAMETER DESCRIPTION <code>db</code> <p>active database connection</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>message</code> <p>optional additional message to indentify several inspections if any.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p>"},{"location":"api/core/deduplicator.html","title":"core.deduplicator","text":""},{"location":"api/core/deduplicator.html#core.deduplicator","title":"core.deduplicator","text":"<p>Find and remove duplicates and near-duplicates in a list of core.types.web_page</p> <p>\u00a9 2024 - Aur\u00e9lien Pierre.</p>"},{"location":"api/core/deduplicator.html#core.deduplicator-classes","title":"Classes","text":""},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator","title":"core.deduplicator.Deduplicator","text":"<pre><code>Deduplicator(\n    threshold: float = 0.9,\n    distance: int = 50,\n    discard_params: bool = True,\n    n_min: int = 0,\n    fix_urls: bool = True,\n)\n</code></pre> <p>Instanciate a depduplicator object.</p> <p>The duplicates factorizing takes a list of core.types.web_page</p> <p>Duplication detection is done using canonical URLs (removing query parameters and anchors) and lowercased, ASCII-converted content.</p> <p>You can edit (append or replace) the list of URLs to ignore core.deduplicator.Deduplicator.urls_to_ignore before doing the actual process.</p> <p>Optionaly, near-duplicates are detected too by computing the Levenshtein distance between pages contents (lowercased and ASCII-converted). This brings a significant performance penalty on large datasets.</p> PARAMETER DESCRIPTION <code>threshold</code> <p>the minimum Levenshtein distance ratio between 2 pages contents for those pages to be considered near-duplicates and be factorized. If set to 1.0, the near-duplicates detection is bypassed which results in a huge speed up.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.9</code> </p> <code>distance</code> <p>the near-duplicates search is performed on the nearest elements after the core.types.web_page list has been ordered alphabetically by URL, for performance, assuming near-duplicates will most likely be found on the same domain and at a resembling path. The distance parameters defines how many elements ahead we will look into.</p> <p> TYPE: <code>int</code> DEFAULT: <code>50</code> </p> <code>discard_params</code> <p>on modern CMS that enable \u201cpretty URLs\u201d (URL rewriting), pages will be indexed by a <code>domain/section/subsection/page</code> and URL query parameters will most likely be used my meaningless pages like social sharing links or search results page so this parameter can be set to <code>True</code> to discard those. On Rest-API-driven websites, streaming websites and old CMS using \u201cugly URLS\u201d, pages will be indexed by <code>domain?content=id</code> and the query parameters need to be kept by setting this parameter to <code>False</code></p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>n_min</code> <p>domains that have a number of indexed pages below this threshold will be discarded entirely. This avoids indexing random dude\u2019s website, under the assumption that relevant and reliable domains will have several pages indexed.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> <code>fix_urls</code> <p>attempt to convert <code>http</code> to <code>https</code> URLs and remove leading <code>www.</code>. This sends DNS requests to assess if the <code>https</code> and <code>www.</code>-less variants can be reached, which takes a most 2 s per URL. Set to <code>False</code> to speed things up.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator-attributes","title":"Attributes","text":""},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.urls_to_ignore","title":"core.deduplicator.Deduplicator.urls_to_ignore  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>urls_to_ignore: list[str] = [\n    \"/tag/\",\n    \"/tags/\",\n    \"/category/\",\n    \"/categories/\",\n    \"/author/\",\n    \"/authors/\",\n    \"/profil/\",\n    \"/profiles/\",\n    \"/user/\",\n    \"/users/\",\n    \"/login/\",\n    \"/signup/\",\n    \"/member/\",\n    \"/members/\",\n    \"/cart/\",\n    \"/shop/\",\n    \"/register\",\n]\n</code></pre> <p>URL substrings to find in URLs and remove matching web pages: mostly WordPress archive pages, user profiles and login pages.</p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator-functions","title":"Functions","text":""},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.get_unique_urls","title":"core.deduplicator.Deduplicator.get_unique_urls","text":"<pre><code>get_unique_urls(posts: list[web_page]) -&gt; list[web_page]\n</code></pre> <p>Pick the most recent, or otherwise the longer, candidate for each canonical URL.</p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.run_on_db","title":"core.deduplicator.Deduplicator.run_on_db","text":"<pre><code>run_on_db(db: sqlite3.Connection, chunksize: int = 4096) -&gt; None\n</code></pre> <p>Deduplicate the <code>pages</code> table in-place, matching the full <code>__call__</code> pipeline.</p> <p>The method runs four sequential phases that mirror <code>__call__</code>:</p> <ol> <li>URL canonicalization \u2013 stream every row through    :meth:<code>prepare_posts_parallel</code> (threaded, I/O-bound), normalise URLs,    compute a SHA-1 content hash, and write results to the temporary    <code>_prepared</code> table.</li> <li>URL deduplication \u2013 for each canonical URL keep the single best row    using SQL window functions with :attr:<code>_ELECTION_ORDER</code>.</li> <li>Exact-content deduplication \u2013 among URL winners, collapse rows that    share the same SHA-1 hash using the same election order.</li> <li>Near-duplicate removal (skipped when <code>threshold == 1.0</code>) \u2013 load the    survivors into memory, run the Levenshtein window scan with parallelised    comparisons (threaded; <code>python-Levenshtein</code> releases the GIL), write    the final winner set back to a temp table.</li> </ol> <p>The <code>pages</code> table is atomically replaced by the winner set at the end. All intermediate <code>_prepared / _url_winners / _content_winners / _near_winners</code> temp tables are cleaned up on success.</p> <p>Assumptions: - <code>pages</code> has at least the columns: <code>url, title, content, date,   datetime, parsed, category</code>. - <code>datetime</code> values, when present, are ISO-8601 strings (SQLite TEXT).   NULL is treated as \u201coldest possible\u201d in the election. - The <code>external</code> category label means the page was crawled by following   external links and contains the full <code>&lt;body&gt;</code>; any other category means   it was crawled from a sitemap / REST-API and contains cleaner markup.   Non-external therefore wins over external in the election.</p> PARAMETER DESCRIPTION <code>db</code> <p>Open <code>sqlite3.Connection</code> to the database.</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>chunksize</code> <p>Number of rows fetched per batch during Phase 1.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4096</code> </p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.add_content_hash_column","title":"core.deduplicator.Deduplicator.add_content_hash_column  <code>staticmethod</code>","text":"<pre><code>add_content_hash_column(db: sqlite3.Connection) -&gt; None\n</code></pre> <p>Add (or refresh) a <code>content_hash</code> column on the <code>pages</code> table.</p> <p>Computes a SHA-1 digest of each row\u2019s <code>parsed</code> field and stores it in <code>content_hash</code>.  The column is created if it does not yet exist.  Rows with a NULL <code>parsed</code> value are skipped and left with a NULL hash.</p> <p>A covering index <code>idx_pages_content_hash</code> is created (or left in place) after the update so that subsequent deduplication queries are cheap.</p> <p>This method is a standalone maintenance utility.  The deduplication pipeline (:meth:<code>run_on_db</code>) computes hashes inline during Phase 1 and does not require this method to be called first.</p> <p>Assumption: <code>parsed</code> values fit in memory individually (they are fetched one batch at a time, not all at once).</p> PARAMETER DESCRIPTION <code>db</code> <p>Open <code>sqlite3.Connection</code> to the target database.</p> <p> TYPE: <code>sqlite3.Connection</code> </p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.get_unique_content","title":"core.deduplicator.Deduplicator.get_unique_content","text":"<pre><code>get_unique_content(posts: list[web_page]) -&gt; list[web_page]\n</code></pre> <p>Pick the most recent candidate for each canonical content.</p> Return <p><code>canonical content: web_page</code> dictionnary</p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.get_close_content","title":"core.deduplicator.Deduplicator.get_close_content","text":"<pre><code>get_close_content(\n    posts: list[web_page], threshold: float = 0.9, distance: float = 50\n) -&gt; list[web_page]\n</code></pre> <p>Find near-duplicate by computing the Levenshtein distance between pages contents.</p> PARAMETER DESCRIPTION <code>posts</code> <p>dictionnary mapping an unused key to a liste of core.types.web_page</p> <p> TYPE: <code>list[web_page]</code> </p> <code>threshold</code> <p>the minimum distance ratio of Lenvenshtein metric for 2 contents to be assumed duplicates</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.9</code> </p> <code>distance</code> <p>for efficiency, the list of web_page is first sorted alphabetically by URL, assuming duplicates will share at least the beginning of their URL. From there, duplicates are searched ahead in the list up to this distance.</p> <p> TYPE: <code>float</code> DEFAULT: <code>50</code> </p>"},{"location":"api/core/deduplicator.html#core.deduplicator.Deduplicator.run_on_list","title":"core.deduplicator.Deduplicator.run_on_list","text":"<pre><code>run_on_list(posts: list[web_page]) -&gt; list[web_page]\n</code></pre> <p>Deduplicate an in-memory list of web pages, matching the full pipeline.</p> <p>This is the list-based counterpart to :meth:<code>run_on_db</code>.  The two methods are kept symmetrical: both run the same four phases (URL canonicalization, exact-URL deduplication, exact-content deduplication, optional near-duplicate removal) and honour the same election rules.</p> Note <p><code>posts</code> is consumed and partially destroyed during processing to avoid keeping two copies in memory simultaneously.</p> PARAMETER DESCRIPTION <code>posts</code> <p>Flat list of :class:<code>core.types.web_page</code> dicts.  The list    is modified in-place; callers should not rely on its contents    after this call returns.</p> <p> TYPE: <code>list[web_page]</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>Deduplicated list of sanitised :class:<code>core.types.web_page</code> dicts,</p> <code>list[web_page]</code> <p>ready for downstream use (<code>to_db=False</code>).  Also writes a</p> <code>list[web_page]</code> <p><code>domains</code> frequency file via :func:<code>~core.utils.get_models_folder</code>.</p>"},{"location":"api/core/deduplicator.html#core.deduplicator-functions","title":"Functions","text":""},{"location":"api/core/facebook-login.html","title":"core.facebook-login","text":""},{"location":"api/core/facebook-login.html#core.facebook-login","title":"core.facebook-login","text":""},{"location":"api/core/language.html","title":"core.language","text":""},{"location":"api/core/language.html#core.language","title":"core.language","text":""},{"location":"api/core/language.html#core.language-attributes","title":"Attributes","text":""},{"location":"api/core/language.html#core.language.LANG_MAP","title":"core.language.LANG_MAP  <code>module-attribute</code>","text":"<pre><code>LANG_MAP = {\n    \"en\": \"english\",\n    \"fr\": \"french\",\n    \"de\": \"german\",\n    \"es\": \"spanish\",\n    \"it\": \"italian\",\n    \"pt\": \"portuguese\",\n    \"nl\": \"dutch\",\n    \"sv\": \"swedish\",\n    \"no\": \"norwegian\",\n    \"da\": \"danish\",\n    \"fi\": \"finnish\",\n    \"ru\": \"russian\",\n    \"ro\": \"romanian\",\n    \"hu\": \"hungarian\",\n    \"tr\": \"turkish\",\n}\n</code></pre> <p>Map ISO 639-1 language codes of supported languages to their full-name, as used by pre-trained corpora</p>"},{"location":"api/core/language.html#core.language.LANG_MAP_REVERSE","title":"core.language.LANG_MAP_REVERSE  <code>module-attribute</code>","text":"<pre><code>LANG_MAP_REVERSE = {v: k for k, v in (LANG_MAP.items())}\n</code></pre> <p>Map the full-name of supported languages, as used by pre-trained corpora, to ISO 639-1 language codes</p>"},{"location":"api/core/language.html#core.language.STOPWORDS_DICT","title":"core.language.STOPWORDS_DICT  <code>module-attribute</code>","text":"<pre><code>STOPWORDS_DICT = {\n    language: (set(STOPWORDS_DICT[language])) for language in STOPWORDS_DICT\n}\n</code></pre> <p>Dictionnary of stopwords (as sets values) mapped to full language names (as keys)</p>"},{"location":"api/core/language.html#core.language-functions","title":"Functions","text":""},{"location":"api/core/network.html","title":"core.network","text":""},{"location":"api/core/network.html#core.network","title":"core.network","text":""},{"location":"api/core/network.html#core.network-classes","title":"Classes","text":""},{"location":"api/core/network.html#core.network.DelayedClass","title":"core.network.DelayedClass","text":"<pre><code>DelayedClass(protocol: str, domain: str, delay: float, timeout: float)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract class for any implementation having an internal timer that won\u2019t trigger a given action more often than <code>delay</code> seconds</p>"},{"location":"api/core/network.html#core.network.DelayedClass-attributes","title":"Attributes","text":""},{"location":"api/core/network.html#core.network.DelayedClass.last_requests","title":"core.network.DelayedClass.last_requests  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>last_requests: dict[str, float] = {}\n</code></pre> <p>Dictionnary of domain/timestamp for the last request sent to a domain</p>"},{"location":"api/core/network.html#core.network.DelayedClass.domain_thresholds","title":"core.network.DelayedClass.domain_thresholds  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>domain_thresholds: dict[str, float] = {}\n</code></pre> <p>Dictionnary of known domains remembering the robots.txt rate thresholds</p>"},{"location":"api/core/network.html#core.network.DelayedClass.main_domain","title":"core.network.DelayedClass.main_domain  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>main_domain: str | None = domain\n</code></pre> <p>Main domain from where we crawl, either the one holding the sitemap or the one at the root of the recursion.</p>"},{"location":"api/core/network.html#core.network.DelayedClass.robots_txt","title":"core.network.DelayedClass.robots_txt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>robots_txt: Protego | None = self.get_robots_txt(protocol, domain, timeout)\n</code></pre> <p>robots.txt file associated to the main_domain</p>"},{"location":"api/core/network.html#core.network.DelayedClass.delay","title":"core.network.DelayedClass.delay  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>delay: float = self.get_crawling_rate(domain, self.robots_txt)\n</code></pre> <p>Timeout between two requests</p>"},{"location":"api/core/network.html#core.network.DelayedClass-functions","title":"Functions","text":""},{"location":"api/core/network.html#core.network.DelayedClass.sleep","title":"core.network.DelayedClass.sleep","text":"<pre><code>sleep(domain: str, overwrite: float | None = None)\n</code></pre> <p>Sleep for at most the remaining timeout time.</p> PARAMETER DESCRIPTION <code>overwrite</code> <p>temporary timeout overwrite</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/core/network.html#core.network.HTTPResponse","title":"core.network.HTTPResponse  <code>dataclass</code>","text":"<pre><code>HTTPResponse(\n    url: str,\n    status_code: int,\n    headers: dict,\n    content: bytes | None,\n    encoding: str,\n    apparent_encoding: str,\n    text: str,\n    raw_response: object,\n)\n</code></pre>"},{"location":"api/core/network.html#core.network.HTTPResponse-attributes","title":"Attributes","text":""},{"location":"api/core/network.html#core.network.HTTPResponse.url","title":"core.network.HTTPResponse.url  <code>instance-attribute</code>","text":"<pre><code>url: str\n</code></pre> <p>Redirected URL, if redirection, else initial URL</p>"},{"location":"api/core/network.html#core.network.HTTPResponse.status_code","title":"core.network.HTTPResponse.status_code  <code>instance-attribute</code>","text":"<pre><code>status_code: int\n</code></pre> <p>HTTP response return code</p>"},{"location":"api/core/network.html#core.network.HTTPResponse.headers","title":"core.network.HTTPResponse.headers  <code>instance-attribute</code>","text":"<pre><code>headers: dict\n</code></pre> <p>Server response HTTP headers</p>"},{"location":"api/core/network.html#core.network.HTTPResponse.content","title":"core.network.HTTPResponse.content  <code>instance-attribute</code>","text":"<pre><code>content: bytes | None\n</code></pre> <p>Page content as bytes</p>"},{"location":"api/core/network.html#core.network.HTTPResponse.text","title":"core.network.HTTPResponse.text  <code>instance-attribute</code>","text":"<pre><code>text: str\n</code></pre> <p>Page content as text, if possible</p>"},{"location":"api/core/network.html#core.network.HTTPResponse.raw_response","title":"core.network.HTTPResponse.raw_response  <code>instance-attribute</code>","text":"<pre><code>raw_response: object\n</code></pre> <p>Original curl_cffi or httpx Response object</p>"},{"location":"api/core/network.html#core.network-functions","title":"Functions","text":""},{"location":"api/core/network.html#core.network.wrap_response","title":"core.network.wrap_response","text":"<pre><code>wrap_response(r: requests.Response | httpx.Response) -&gt; HTTPResponse\n</code></pre> <p>Unify responses from HTTPx and cURL into uniform types</p>"},{"location":"api/core/network.html#core.network.request","title":"core.network.request","text":"<pre><code>request(method, url, timeout=30, headers=None) -&gt; HTTPResponse\n</code></pre> <p>Try curl_cffi first, fallback to httpx on ANY transport-level failure.</p>"},{"location":"api/core/network.html#core.network.try_url","title":"core.network.try_url","text":"<pre><code>try_url(\n    url,\n    delay: DelayedClass,\n    timeout: int | float = 30,\n    bypass_robots_txt: bool = False,\n) -&gt; tuple[HTTPResponse | None, dict | None, str]\n</code></pre> <p>Probe the URL head, without getting the content.</p> This will <ol> <li>resolve redirections</li> <li>check with robots.txt (if any) if we have permission to crawl and at what rate,</li> <li>fallback to web.archive.org if hitting 404 (not found) error,</li> <li>handle requests rate thresholding,</li> <li>find out what headers spoofing combination is accepted by the server (or by fucking Cloudflare), when robots.txt didn\u2019t block us explicitely, but server/proxy returned 403 (unauthorized) error.</li> </ol> PARAMETER DESCRIPTION <code>delay</code> <p>class holding a thresholding timer/delay method,</p> <p> TYPE: <code>DelayedClass</code> </p> <code>timeout</code> <p>abort any connection that takes longer than this (in seconds) to finish. That might cancel loading  large PDFs if too small.</p> <p> TYPE: <code>int | float</code> DEFAULT: <code>30</code> </p> <code>bypass_robots_txt</code> <p>don\u2019t check if robots.txt allows us to crawl the current page. That makes us spare some requests. When crawling pages from <code>sitemap.xml</code>, we can safely assume that all pages there are allowed or the webmaster is an idiot.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>response</code> <p>the HTTP response object,</p> <p> TYPE: <code>HTTPResponse | None</code> </p> <code>headers</code> <p>the HTTP client headers that succeeded in spoofing the server, if any,</p> <p> TYPE: <code>dict | None</code> </p> <code>url</code> <p>the final, redirected, URL (can be the same as the input one).</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/network.html#core.network.get_url","title":"core.network.get_url","text":"<pre><code>get_url(\n    url: str, delay: DelayedClass, timeout=60, custom_header={}\n) -&gt; tuple[bytes | None, str, int, str, str]\n</code></pre> <p>Get the content of an URL using requests. <code>.pdf</code>, <code>.xml</code> and <code>.txt</code> URLs always use requests.</p> Return <p>content: the raw DOM, url: the final URL after possible redirections status: the HTTP code (integer) encoding:  apparent encoding:</p>"},{"location":"api/core/nlp.html","title":"core.nlp","text":""},{"location":"api/core/nlp.html#core.nlp","title":"core.nlp","text":"<p>High-level natural language processing module for message-like (emails, comments, posts) input.</p> <p>Supports automatic language detection, word tokenization and stemming for <code>'danish', 'dutch', 'english', 'finnish', 'french', 'german', 'italian', 'norwegian', 'portuguese', 'spanish', 'swedish'</code>.</p> <p>\u00a9 2023 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/nlp.html#core.nlp-attributes","title":"Attributes","text":""},{"location":"api/core/nlp.html#core.nlp.regex_starter","title":"core.nlp.regex_starter  <code>module-attribute</code>","text":"<pre><code>regex_starter = '(?&lt;=^|\\\\s|\\\\[|\\\\(|\\\\{|\\\\&lt;|\\\\\\'|\\\\\"|`|;|\\\\&gt;)'\n</code></pre> <p>Start of line, or start of document, or start of markup</p>"},{"location":"api/core/nlp.html#core.nlp.regex_stopper","title":"core.nlp.regex_stopper  <code>module-attribute</code>","text":"<pre><code>regex_stopper = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|\\\\&lt;)'\n</code></pre> <p>End of line, or end of document, or end of markup</p>"},{"location":"api/core/nlp.html#core.nlp.end_of_word","title":"core.nlp.end_of_word  <code>module-attribute</code>","text":"<pre><code>end_of_word = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|:|,|\\\\?|\\\\!|\\\\.|\\\\&lt;)'\n</code></pre> <p>End of word, or end of line, or end of document, or end of markup</p>"},{"location":"api/core/nlp.html#core.nlp.regex_algebra","title":"core.nlp.regex_algebra  <code>module-attribute</code>","text":"<pre><code>regex_algebra = '[\\\\+\\\\-\\\\=\\\\\u2260\\\\\u00b1]'\n</code></pre> <p>Algebraic signs</p>"},{"location":"api/core/nlp.html#core.nlp.IP_PATTERN","title":"core.nlp.IP_PATTERN  <code>module-attribute</code>","text":"<pre><code>IP_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_ip, regex_stopper), re.IGNORECASE\n)\n</code></pre> <p>IPv4 and IPv6 patterns where the whole IP is captured in the first group.</p>"},{"location":"api/core/nlp.html#core.nlp.EMAIL_PATTERN","title":"core.nlp.EMAIL_PATTERN  <code>module-attribute</code>","text":"<pre><code>EMAIL_PATTERN = re.compile(\n    \"&lt;?([0-9a-z\\\\-\\\\_\\\\+\\\\.]+?@[0-9a-z\\\\-\\\\_\\\\+]+(\\\\.[0-9a-z\\\\_\\\\-]{2,})+)&gt;?\",\n    re.IGNORECASE,\n)\n</code></pre> <p>Emails patterns like <code>&lt;me@mail.com&gt;</code> or <code>me@mail.com</code> where the whole address is captured in the first group.</p>"},{"location":"api/core/nlp.html#core.nlp.URL_PATTERN","title":"core.nlp.URL_PATTERN  <code>module-attribute</code>","text":"<pre><code>URL_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_url, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>URL patterns like <code>http(s)://domain.ext/page/subpage?q=x&amp;r=0:1#anchor</code> or <code>//domain.ext/page</code>. URL must follow RFC3986, meaning query parameters should be before anchors, if any. Relying on this assumption allows a faster regex parsing.</p> <ul> <li>the protocol (ftp, ftps, http, https) is captured as the first group,</li> <li><code>domain.ext</code> is captured as the second group,</li> <li><code>/page/etc</code> is the third group, including leading and trailing <code>/</code>,</li> <li>page query parameters <code>?s=x&amp;r=0</code>, including <code>?</code>, is the fourth group if the URL declares <code>...?params#anchor</code>,</li> <li>anchor <code>#anchor</code> is the fifth group, including <code>#</code>, if the URL declares <code>...?params#anchor</code>.</li> </ul> <p>URLs are captured if they are:</p> <ul> <li>alone on their own line,</li> <li>enclosed in <code>{}</code>, <code>[]</code>, <code>()</code></li> <li>enclosed in whitespaces.</li> </ul> <p>Warning: URLs enclosed in <code>()</code>, <code>[]</code> and <code>{}</code> may retain the closing sign  as part of the page name since <code>()</code> and <code>[]</code> are valid in URL pathes  and parameters. This pattern will work on plain text only: Markdown, XML, HTML and JSON  will need to be parsed ahead.</p>"},{"location":"api/core/nlp.html#core.nlp.MEMBERS_PATTERN","title":"core.nlp.MEMBERS_PATTERN  <code>module-attribute</code>","text":"<pre><code>MEMBERS_PATTERN = re.compile('(?&lt;=[a-z])(\\\\.)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Domain patterns without leading protocol like <code>cdn.company.com</code> or class members in object-oriented programming languages like <code>params.cookies.client</code>.</p>"},{"location":"api/core/nlp.html#core.nlp.DATE_PATTERN","title":"core.nlp.DATE_PATTERN  <code>module-attribute</code>","text":"<pre><code>DATE_PATTERN = re.compile(date_regex, re.IGNORECASE)\n</code></pre> <p>Dates like <code>2022-12-01</code>, <code>01-12-2022</code>, <code>01-12-22</code>, <code>01/12/2022</code>, <code>01/12/22</code> where the whole date is captured in the first group, then each group of digits is captured in the order of appearance, in the next 3 groups</p>"},{"location":"api/core/nlp.html#core.nlp.TIME_PATTERN","title":"core.nlp.TIME_PATTERN  <code>module-attribute</code>","text":"<pre><code>TIME_PATTERN = re.compile(time_regex, re.IGNORECASE)\n</code></pre> <p>Identify more or less standard time patterns, like :</p> <ul> <li>12h15</li> <li>12:15</li> <li>12:15:00</li> <li>12am</li> <li>12 am</li> <li>12 h</li> <li>12:15:00Z</li> <li>12:15:00+01</li> <li>12:15:00 UTC+1</li> <li>11:27:45+0000</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>1- or 2-digits hour,</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>hour/minutes separator or half-day marker among <code>[\"h\", \":\", \"am\", \"pm\"]</code> (case-insensitive)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2-digits minutes, if any, or <code>None</code></p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>2-digits seconds, if any.</p> <p> TYPE: <code>str</code> </p> <code>4</code> <p>hour marker (<code>h</code> or <code>H</code>), half-day marker (case-insensitive <code>[\"am\", \"pm\"]</code>), or time zone marker (case-sensitive <code>[\"Z\", \"UTC\"]</code>)</p> <p> TYPE: <code>str</code> </p> <code>5</code> <p>1-or 2-digits signed integer timezone shift (referred to UTC).</p> <p> TYPE: <code>str</code> </p> <p>Examples:</p> <p>see https://regex101.com/r/QNtZAK/2</p> <p>see <code>src/tests/test-patterns.py</code></p>"},{"location":"api/core/nlp.html#core.nlp.DOMAIN_PATTERN","title":"core.nlp.DOMAIN_PATTERN  <code>module-attribute</code>","text":"<pre><code>DOMAIN_PATTERN = re.compile(\n    \"from ((?:[a-z0-9\\\\-_]{0,61}\\\\.)+[a-z]{2,})\", re.IGNORECASE\n)\n</code></pre> <p>Matches patterns like <code>from (domain.ext)</code> from RFC-822 <code>Received</code> header in emails.</p>"},{"location":"api/core/nlp.html#core.nlp.UID_PATTERN","title":"core.nlp.UID_PATTERN  <code>module-attribute</code>","text":"<pre><code>UID_PATTERN = re.compile('UID ([0-9]+)')\n</code></pre> <p>Matches email integer UID from IMAP headers.</p>"},{"location":"api/core/nlp.html#core.nlp.FLAGS_PATTERN","title":"core.nlp.FLAGS_PATTERN  <code>module-attribute</code>","text":"<pre><code>FLAGS_PATTERN = re.compile('FLAGS \\\\((.*?)\\\\)')\n</code></pre> <p>Matches email flags from IMAP headers.</p>"},{"location":"api/core/nlp.html#core.nlp.PATH_PATTERN","title":"core.nlp.PATH_PATTERN  <code>module-attribute</code>","text":"<pre><code>PATH_PATTERN = re.compile('%s%s%s' % (regex_starter, path_regex, end_of_word))\n</code></pre> <p>File path pattern like <code>~/file</code>, <code>/home/file</code>, <code>./file</code> or <code>C:\\windows</code></p>"},{"location":"api/core/nlp.html#core.nlp.PARTIAL_PATH_REGEX","title":"core.nlp.PARTIAL_PATH_REGEX  <code>module-attribute</code>","text":"<pre><code>PARTIAL_PATH_REGEX = re.compile(\n    \"%s%s%s\" % (regex_starter, partial_path_regex, end_of_word)\n)\n</code></pre> <p>Partial, invalid path patterns missing the leading root, like <code>home/user/stuff</code>. We start capturing after at least two folder separators (slash or backslash).</p> <p>Warning</p> <p>this will collide with date detection, so run it after in the pipeline.</p>"},{"location":"api/core/nlp.html#core.nlp.RESOLUTION_PATTERN","title":"core.nlp.RESOLUTION_PATTERN  <code>module-attribute</code>","text":"<pre><code>RESOLUTION_PATTERN = re.compile('\\\\d+(?:\u00d7|x|X)\\\\d+')\n</code></pre> <p>Pixel resolution like 10x20 or 10\u00d720. Units are discarded.</p>"},{"location":"api/core/nlp.html#core.nlp.NUMBER_PATTERN","title":"core.nlp.NUMBER_PATTERN  <code>module-attribute</code>","text":"<pre><code>NUMBER_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_number, regex_stopper)\n)\n</code></pre> <p>Signed integers and decimals, fractions and numeric IDs with interal dashes and underscores. Numbers with starting or trailing units are not considered. Lazy decimals (.1 and 1.) are considered.</p>"},{"location":"api/core/nlp.html#core.nlp.HASH_PATTERN","title":"core.nlp.HASH_PATTERN  <code>module-attribute</code>","text":"<pre><code>HASH_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_hash, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>Cryptographic hexadecimal hashes and fingerprints, of a min length of 8 characters.</p>"},{"location":"api/core/nlp.html#core.nlp.MULTIPLE_LINES","title":"core.nlp.MULTIPLE_LINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_LINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]{2,} ?)+')\n</code></pre> <p>Detect more than 2 newlines and tab, possibly mixed with spaces</p>"},{"location":"api/core/nlp.html#core.nlp.MULTIPLE_NEWLINES","title":"core.nlp.MULTIPLE_NEWLINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_NEWLINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]+ ?){2,}')\n</code></pre> <p>Detect broken sequences of newlines and spaces.</p>"},{"location":"api/core/nlp.html#core.nlp.INTERNAL_NEWLINE","title":"core.nlp.INTERNAL_NEWLINE  <code>module-attribute</code>","text":"<pre><code>INTERNAL_NEWLINE = re.compile('(?&lt;=\\\\w)[\\\\n\\\\t\\\\r]{1}(?=\\\\w)')\n</code></pre> <p>Detect single newline characters nested inside text. Mostly useful for parsed PDF where line wrapping is quite literal ( used instead of space).</p>"},{"location":"api/core/nlp.html#core.nlp.EXPOSURE","title":"core.nlp.EXPOSURE  <code>module-attribute</code>","text":"<pre><code>EXPOSURE = re.compile(\n    \"%s%s%s\" % (regex_starter, exposure_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/nlp.html#core.nlp.PHOTOSPEED","title":"core.nlp.PHOTOSPEED  <code>module-attribute</code>","text":"<pre><code>PHOTOSPEED = re.compile(\n    \"%s%s%s\" % (regex_starter, photospeed_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/nlp.html#core.nlp.SENSIBILITY","title":"core.nlp.SENSIBILITY  <code>module-attribute</code>","text":"<pre><code>SENSIBILITY = re.compile(\n    \"%s%s%s\" % (regex_starter, sensibility_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Photographic sensibility in ISO or ASA</p>"},{"location":"api/core/nlp.html#core.nlp.LUMINANCE","title":"core.nlp.LUMINANCE  <code>module-attribute</code>","text":"<pre><code>LUMINANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, luminance_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Luminance/radiance in nits or Cd/m\u00b2</p>"},{"location":"api/core/nlp.html#core.nlp.DIAPHRAGM","title":"core.nlp.DIAPHRAGM  <code>module-attribute</code>","text":"<pre><code>DIAPHRAGM = re.compile(\n    \"%s%s\" % (regex_starter, diaphragm_regex), flags=re.IGNORECASE\n)\n</code></pre> <p>Photographic diaph aperture values like f/2.8 or f/11</p>"},{"location":"api/core/nlp.html#core.nlp.GAIN","title":"core.nlp.GAIN  <code>module-attribute</code>","text":"<pre><code>GAIN = re.compile(\n    \"%s%s%s\" % (regex_starter, gain_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Gain, attenuation and PSNR in dB</p>"},{"location":"api/core/nlp.html#core.nlp.FILE_SIZE","title":"core.nlp.FILE_SIZE  <code>module-attribute</code>","text":"<pre><code>FILE_SIZE = re.compile(\n    \"%s%s%s\" % (regex_starter, filesize_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>File and memory size in bit, byte, or octet and their multiples</p>"},{"location":"api/core/nlp.html#core.nlp.DISTANCE","title":"core.nlp.DISTANCE  <code>module-attribute</code>","text":"<pre><code>DISTANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, distance_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Distance in meter, inch, foot and their multiples</p>"},{"location":"api/core/nlp.html#core.nlp.PERCENT","title":"core.nlp.PERCENT  <code>module-attribute</code>","text":"<pre><code>PERCENT = re.compile('%s%s%s' % (regex_starter, percent_regex, end_of_word))\n</code></pre> <p>Number followed by %</p>"},{"location":"api/core/nlp.html#core.nlp.WEIGHT","title":"core.nlp.WEIGHT  <code>module-attribute</code>","text":"<pre><code>WEIGHT = re.compile(\n    \"%s%s%s\" % (regex_starter, weight_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Weight (mass) in British and SI units and their multiples</p>"},{"location":"api/core/nlp.html#core.nlp.ANGLE","title":"core.nlp.ANGLE  <code>module-attribute</code>","text":"<pre><code>ANGLE = re.compile(\n    \"%s%s%s\" % (regex_starter, angle_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Angles in radians, degrees and steradians</p>"},{"location":"api/core/nlp.html#core.nlp.TEMPERATURE","title":"core.nlp.TEMPERATURE  <code>module-attribute</code>","text":"<pre><code>TEMPERATURE = re.compile(\n    \"%s%s%s\" % (regex_starter, temperature_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Temperatures in \u00b0C, \u00b0F and K</p>"},{"location":"api/core/nlp.html#core.nlp.FREQUENCY","title":"core.nlp.FREQUENCY  <code>module-attribute</code>","text":"<pre><code>FREQUENCY = re.compile(\n    \"%s%s%s\" % (regex_starter, frequency_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Frequencies in hertz and multiples</p>"},{"location":"api/core/nlp.html#core.nlp.TEXT_DATES","title":"core.nlp.TEXT_DATES  <code>module-attribute</code>","text":"<pre><code>TEXT_DATES = re.compile(\n    \"([0-9]{1,2})? (jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec|jan|f\u00e9v|mar|avr|mai|jui|jui|aou|sep|oct|nov|d\u00e9c|janvier|f\u00e9vrier|mars|avril|mai|juin|juillet|ao\u00fbt|septembre|octobre|novembre|d\u00e9cembre|january|february|march|april|may|june|july|august|september|october|november|december)\\\\.?( [0-9]{1,2})?( [0-9]{2,4})(?!\\\\:)\",\n    flags=re.IGNORECASE | re.MULTILINE,\n)\n</code></pre> <p>Find textual dates formats:</p> <ul> <li>English dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> <li>French dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>month (full-form or abbreviated)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>4 digits (full year)</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/nlp.html#core.nlp.BASE_64","title":"core.nlp.BASE_64  <code>module-attribute</code>","text":"<pre><code>BASE_64 = re.compile(\n    \"((?:[A-Za-z0-9+\\\\/]{4}){64,}(?:[A-Za-z0-9+\\\\/]{2}==|[A-Za-z0-9+\\\\/]{3}=)?)\"\n)\n</code></pre> <p>Identifies base64 encoding</p>"},{"location":"api/core/nlp.html#core.nlp.BB_CODE","title":"core.nlp.BB_CODE  <code>module-attribute</code>","text":"<pre><code>BB_CODE = re.compile('\\\\[(img|quote)[a-zA-Z0-9 =\\\\\"]*?\\\\].*?\\\\[\\\\/\\\\1\\\\]')\n</code></pre> <p>Identifies left-over BB code markup <code>[img]</code> and <code>[quote]</code></p>"},{"location":"api/core/nlp.html#core.nlp.MARKUP","title":"core.nlp.MARKUP  <code>module-attribute</code>","text":"<pre><code>MARKUP = re.compile('(?:\\\\[|\\\\{|\\\\&lt;)([^\\\\n\\\\r]+?)(?:\\\\]|\\\\}|\\\\&gt;)')\n</code></pre> <p>Identifies left-over HTML and Markdown markup, like <code>&lt;...&gt;</code>, <code>{...}</code>, <code>[...]</code></p>"},{"location":"api/core/nlp.html#core.nlp.USER","title":"core.nlp.USER  <code>module-attribute</code>","text":"<pre><code>USER = re.compile('([\\\\w\\\\-\\\\+\\\\.]+)?@([\\\\w\\\\-\\\\+\\\\.]+)|(user\\\\-?\\\\d+)')\n</code></pre> <p>Identifies user handles or emails</p>"},{"location":"api/core/nlp.html#core.nlp.REPEATED_CHARACTERS","title":"core.nlp.REPEATED_CHARACTERS  <code>module-attribute</code>","text":"<pre><code>REPEATED_CHARACTERS = re.compile('(.)\\\\1{9,}')\n</code></pre> <p>Identifies any character repeated more than 9 times</p>"},{"location":"api/core/nlp.html#core.nlp.UNFINISHED_SENTENCES","title":"core.nlp.UNFINISHED_SENTENCES  <code>module-attribute</code>","text":"<pre><code>UNFINISHED_SENTENCES = re.compile('(?&lt;![?!.;:])\\\\n\\\\n|\\\\r\\\\n')\n</code></pre> <p>Identifies sentences finishing with 2 newlines characters without having ending punctuations</p>"},{"location":"api/core/nlp.html#core.nlp.MULTIPLE_DOTS","title":"core.nlp.MULTIPLE_DOTS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DOTS = re.compile('\\\\.{2,}')\n</code></pre> <p>Identifies dots repeated more than twice</p>"},{"location":"api/core/nlp.html#core.nlp.MULTIPLE_DASHES","title":"core.nlp.MULTIPLE_DASHES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DASHES = re.compile('[-~]{1,}')\n</code></pre> <p>Identifies dashes repeated more than once</p>"},{"location":"api/core/nlp.html#core.nlp.MULTIPLE_QUESTIONS","title":"core.nlp.MULTIPLE_QUESTIONS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_QUESTIONS = re.compile('\\\\?{1,}')\n</code></pre> <p>Identifies question marks repeated more than once</p>"},{"location":"api/core/nlp.html#core.nlp.ORDINAL_FR","title":"core.nlp.ORDINAL_FR  <code>module-attribute</code>","text":"<pre><code>ORDINAL_FR = re.compile('n\u00b0 ?([0-9]+)')\n</code></pre> <p>French ordinal numbers (num\u00e9ros n\u00b0)</p>"},{"location":"api/core/nlp.html#core.nlp.FRANCAIS","title":"core.nlp.FRANCAIS  <code>module-attribute</code>","text":"<pre><code>FRANCAIS = re.compile(\n    \"%s(j|t|s|d|qu|lorsqu|quelqu|jusqu|m|c|n)\\\\'(?=[aeiouy\u00e9\u00e8\u00e0\u00ea\u00e2\u00ee\u00f4\u00fb\u00ef\u00fc\u00e4\u00eb\u00f6h][\\\\w\\\\s])\"\n    % regex_starter,\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>French contractions of pronouns and determinants</p>"},{"location":"api/core/nlp.html#core.nlp.DASHES","title":"core.nlp.DASHES  <code>module-attribute</code>","text":"<pre><code>DASHES = re.compile('(?&lt;=\\\\w)(-|_|=)+(?=\\\\w)', re.IGNORECASE)\n</code></pre> <p>Dashes in the middle of ASCII/Latin compounded words. Will not work if accented or Unicode characters are immediately surrounding the dash.</p>"},{"location":"api/core/nlp.html#core.nlp.ALTERNATIVES","title":"core.nlp.ALTERNATIVES  <code>module-attribute</code>","text":"<pre><code>ALTERNATIVES = re.compile('(?&lt;=[a-z])(\\\\/)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Slash-separated word alternatives like <code>and/or</code> <code>mr/mrs</code></p>"},{"location":"api/core/nlp.html#core.nlp.PLURAL_S","title":"core.nlp.PLURAL_S  <code>module-attribute</code>","text":"<pre><code>PLURAL_S = re.compile('(?&lt;=[a-zA-Z]{4,})s?e{0,2}s%s' % end_of_word)\n</code></pre> <p>Identify plural form of nouns (French and English), adjectives (French) and third-person present verbs (English) and second-person verbs (French) in -s.</p>"},{"location":"api/core/nlp.html#core.nlp.FEMININE_E","title":"core.nlp.FEMININE_E  <code>module-attribute</code>","text":"<pre><code>FEMININE_E = re.compile('(?&lt;=\\\\w{4,})e{1,2}%s' % end_of_word)\n</code></pre> <p>Identify feminine form of adjectives (French) in -e.</p>"},{"location":"api/core/nlp.html#core.nlp.DOUBLE_CONSONANTS","title":"core.nlp.DOUBLE_CONSONANTS  <code>module-attribute</code>","text":"<pre><code>DOUBLE_CONSONANTS = re.compile(\n    \"(?&lt;=\\\\w{2,})([bcfghjklmnpqrstvwxz])\\\\1\", re.IGNORECASE\n)\n</code></pre> <p>Identify double consonants in the middle of words.</p>"},{"location":"api/core/nlp.html#core.nlp.FEMININE_TRICE","title":"core.nlp.FEMININE_TRICE  <code>module-attribute</code>","text":"<pre><code>FEMININE_TRICE = re.compile('(?&lt;=\\\\w{4,})t(rice|eur|or)%s' % end_of_word)\n</code></pre> <p>Identify French feminine nouns in -trice.</p>"},{"location":"api/core/nlp.html#core.nlp.ADVERB_MENT","title":"core.nlp.ADVERB_MENT  <code>module-attribute</code>","text":"<pre><code>ADVERB_MENT = re.compile('(?&lt;=\\\\w{4,})e?ment%s' % end_of_word)\n</code></pre> <p>Identify French adverbs and English nouns ending en -ment</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_TION","title":"core.nlp.SUBSTANTIVE_TION  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_TION = re.compile('(?&lt;=\\\\w{4,})(t|s)ion%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from verbs by adding -tion and -sion</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_AT","title":"core.nlp.SUBSTANTIVE_AT  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_AT = re.compile('(?&lt;=\\\\w{4,})at%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from other nouns by adding -at</p>"},{"location":"api/core/nlp.html#core.nlp.PARTICIPLE_ING","title":"core.nlp.PARTICIPLE_ING  <code>module-attribute</code>","text":"<pre><code>PARTICIPLE_ING = re.compile('(?&lt;=\\\\w{4,})ing%s' % end_of_word)\n</code></pre> <p>Identify English substantives and present participles formed from verbs by adding -ing</p>"},{"location":"api/core/nlp.html#core.nlp.ADJECTIVE_ED","title":"core.nlp.ADJECTIVE_ED  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_ED = re.compile('(?&lt;=\\\\w{4,})ed%s' % end_of_word)\n</code></pre> <p>Identify English adjectives formed from verbs by adding -ed</p>"},{"location":"api/core/nlp.html#core.nlp.ADJECTIVE_TIF","title":"core.nlp.ADJECTIVE_TIF  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_TIF = re.compile('(?&lt;=\\\\w{2,})ti(f|v)%s' % end_of_word)\n</code></pre> <p>Identify English and French adjectives formed from verbs by adding -tif or -tive</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_Y","title":"core.nlp.SUBSTANTIVE_Y  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_Y = re.compile('(?&lt;=\\\\w{3,})y%s' % end_of_word)\n</code></pre> <p>Identify English substantives ending in -y</p>"},{"location":"api/core/nlp.html#core.nlp.VERB_IZ","title":"core.nlp.VERB_IZ  <code>module-attribute</code>","text":"<pre><code>VERB_IZ = re.compile('(?&lt;=\\\\w{4,})(i|y)z%s' % end_of_word)\n</code></pre> <p>Identify American verbs ending in -iz that French and Brits write in -is</p>"},{"location":"api/core/nlp.html#core.nlp.STUFF_ER","title":"core.nlp.STUFF_ER  <code>module-attribute</code>","text":"<pre><code>STUFF_ER = re.compile('(?&lt;=\\\\w{5,})er%s' % end_of_word)\n</code></pre> <p>Identify French 1st group verb (infinitive) and English substantives ending in -er</p>"},{"location":"api/core/nlp.html#core.nlp.BRITISH_OUR","title":"core.nlp.BRITISH_OUR  <code>module-attribute</code>","text":"<pre><code>BRITISH_OUR = re.compile('(?&lt;=\\\\w{3,})our%s' % end_of_word)\n</code></pre> <p>Identify British spelling ending in -our (colour, behaviour).</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_ITY","title":"core.nlp.SUBSTANTIVE_ITY  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_ITY = re.compile('(?&lt;=\\\\w{4,})it(y|e)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ity (English) and -ite (French).</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_IST","title":"core.nlp.SUBSTANTIVE_IST  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IST = re.compile('(?&lt;=\\\\w{3,})is(t|m)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ist and -ism.</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_IQU","title":"core.nlp.SUBSTANTIVE_IQU  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IQU = re.compile('(?&lt;=\\\\w{3,})i(qu|c)%s' % end_of_word)\n</code></pre> <p>Identify French substantives in -iqu</p>"},{"location":"api/core/nlp.html#core.nlp.SUBSTANTIVE_EUR","title":"core.nlp.SUBSTANTIVE_EUR  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_EUR = re.compile('(?&lt;=\\\\w{3,})eur%s' % end_of_word)\n</code></pre> <p>Identify French substantives -eur</p>"},{"location":"api/core/nlp.html#core.nlp.HYPHENIZED","title":"core.nlp.HYPHENIZED  <code>module-attribute</code>","text":"<pre><code>HYPHENIZED = re.compile('(?&lt;=\\\\w{3,})[-\u2013\u2014]+ *[\\\\n\\\\r]{1,2}(?=\\\\w)')\n</code></pre> <p>Detect hyphenized words at the end of a PDF text line.</p>"},{"location":"api/core/nlp.html#core.nlp.WAYBACK_RE","title":"core.nlp.WAYBACK_RE  <code>module-attribute</code>","text":"<pre><code>WAYBACK_RE = re.compile('https?://web\\\\.archive\\\\.org/web/[^/]+/(https?://.+)')\n</code></pre> <p>Find the canonical URL from web.archive.org (Wayback Machine) URLs</p>"},{"location":"api/core/nlp.html#core.nlp.LANG_MAP","title":"core.nlp.LANG_MAP  <code>module-attribute</code>","text":"<pre><code>LANG_MAP = {\n    \"en\": \"english\",\n    \"fr\": \"french\",\n    \"de\": \"german\",\n    \"es\": \"spanish\",\n    \"it\": \"italian\",\n    \"pt\": \"portuguese\",\n    \"nl\": \"dutch\",\n    \"sv\": \"swedish\",\n    \"no\": \"norwegian\",\n    \"da\": \"danish\",\n    \"fi\": \"finnish\",\n    \"ru\": \"russian\",\n    \"ro\": \"romanian\",\n    \"hu\": \"hungarian\",\n    \"tr\": \"turkish\",\n}\n</code></pre> <p>Map ISO 639-1 language codes of supported languages to their full-name, as used by pre-trained corpora</p>"},{"location":"api/core/nlp.html#core.nlp.LANG_MAP_REVERSE","title":"core.nlp.LANG_MAP_REVERSE  <code>module-attribute</code>","text":"<pre><code>LANG_MAP_REVERSE = {v: k for k, v in (LANG_MAP.items())}\n</code></pre> <p>Map the full-name of supported languages, as used by pre-trained corpora, to ISO 639-1 language codes</p>"},{"location":"api/core/nlp.html#core.nlp.STOPWORDS_DICT","title":"core.nlp.STOPWORDS_DICT  <code>module-attribute</code>","text":"<pre><code>STOPWORDS_DICT = {\n    language: (set(STOPWORDS_DICT[language])) for language in STOPWORDS_DICT\n}\n</code></pre> <p>Dictionnary of stopwords (as sets values) mapped to full language names (as keys)</p>"},{"location":"api/core/nlp.html#core.nlp-classes","title":"Classes","text":""},{"location":"api/core/nlp.html#core.nlp.Lexicon","title":"core.nlp.Lexicon  <code>dataclass</code>","text":"<pre><code>Lexicon(counts: Counter[str] = Counter())\n</code></pre> <p>Mutable token frequency index with canonicalization helpers for: - malformed n-grams, - merged/split variants, - plural compound normalization.</p> <p>Examples:</p> <p>liber_tarian  -&gt; libertarian etres_humains -&gt; etre_humain</p>"},{"location":"api/core/nlp.html#core.nlp.Lexicon-functions","title":"Functions","text":""},{"location":"api/core/nlp.html#core.nlp.Lexicon.update","title":"core.nlp.Lexicon.update","text":"<pre><code>update(corpus: Iterable[Iterable[str]]) -&gt; None\n</code></pre> <p>Update token frequencies from a corpus of tokenized sentences.</p> PARAMETER DESCRIPTION <code>corpus</code> <p>Iterable of tokenized sentences: [     [\u201cthis\u201d, \u201cis\u201d, \u201ca\u201d, \u201csentence\u201d],     [\u201canother\u201d, \u201csentence\u201d] ]</p> <p> TYPE: <code>Iterable[Iterable[str]]</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Lexicon.frequency","title":"core.nlp.Lexicon.frequency","text":"<pre><code>frequency(token: str) -&gt; int\n</code></pre> <p>Return token frequency.</p>"},{"location":"api/core/nlp.html#core.nlp.Lexicon.exists","title":"core.nlp.Lexicon.exists","text":"<pre><code>exists(token: str) -&gt; bool\n</code></pre> <p>Check whether a token exists in the lexicon.</p>"},{"location":"api/core/nlp.html#core.nlp.Lexicon.prune","title":"core.nlp.Lexicon.prune","text":"<pre><code>prune(min_count: int = 10) -&gt; None\n</code></pre> <p>Remove all entries whose frequency is lower than <code>min_count</code>.</p> PARAMETER DESCRIPTION <code>min_count</code> <p>Minimum frequency to keep.</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Lexicon.resolve_token","title":"core.nlp.Lexicon.resolve_token","text":"<pre><code>resolve_token(token: str, separator: str = '_', min_ratio: float = 1.0) -&gt; str\n</code></pre> <p>Attempt to canonicalize malformed n-grams.</p> <p>Operations: 1. malformed n-grams:     liber_tarian -&gt; libertarian</p> <ol> <li>plural compound reduction:     etres_humains -&gt; etre_humain</li> </ol> <p>Strategy: - if token exists already -&gt; keep it - otherwise:     - remove separators,     - check if merged variant exists,     - compare frequencies,     - prefer merged form if sufficiently frequent.</p> PARAMETER DESCRIPTION <code>token</code> <p>Token to canonicalize.</p> <p> TYPE: <code>str</code> </p> <code>separator</code> <p>N-gram separator.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'_'</code> </p> <code>min_ratio</code> <p>Require merged token frequency to be at least <code>min_ratio</code> times the split variant frequency.</p> <p>Helps avoid false positives.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Canonicalized token.</p>"},{"location":"api/core/nlp.html#core.nlp.Lexicon.canonicalize_sentence","title":"core.nlp.Lexicon.canonicalize_sentence","text":"<pre><code>canonicalize_sentence(\n    sentence: list[str], separator: str = \"_\", min_ratio: float = 1.0\n) -&gt; list[str]\n</code></pre> <p>Canonicalize all tokens in a sentence.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer","title":"core.nlp.Tokenizer","text":"<pre><code>Tokenizer(\n    meta_tokens: dict[re.Pattern, str] | None = None,\n    abbreviations: dict[str, str] | None = None,\n    replacements: dict[str, str] | None = None,\n    stopwords: set[str] | None = None,\n    lang_stopwords: dict[str, set[str]] | None = None,\n    backend: str = \"blingfire\",\n)\n</code></pre> <p>Pre-processing pipeline and tokenizer.</p> <p>Splits a string into normalized word tokens after applying a series of configurable text transformations.</p> PARAMETER DESCRIPTION <code>meta_tokens</code> <p>Pipeline of regular-expression substitutions used to replace document fragments with meta-tokens.</p> <p>Keys must be compiled <code>re.Pattern</code> objects and values must be meta-token strings, typically enclosed in underscores.</p> <p>Transformations are applied in declaration order. This relies on Python\u2019s ordered dictionaries (Python 3.7+).</p> <p>If not provided, a default pipeline suitable for bilingual English/French technical documents is used.</p> <p> TYPE: <code>dict[re.Pattern, str] | None</code> DEFAULT: <code>None</code> </p> <code>abbreviations</code> <p>Pipeline of abbreviation replacements as a <code>{to_replace: replacement}</code> dictionary.</p> <p>Replacements are applied in declaration order.</p> <p> TYPE: <code>dict[str, str] | None</code> DEFAULT: <code>None</code> </p> <code>replacements</code> <p>Dictionary of token-level substitutions applied as <code>{key: value}</code> string replacements.</p> <p> TYPE: <code>dict[str, str] | None</code> DEFAULT: <code>None</code> </p> <code>stopwords</code> <p>Language-agnostic stopwords to remove from the token stream.</p> <p> TYPE: <code>set[str] | None</code> DEFAULT: <code>None</code> </p> <code>lang_stopwords</code> <p>Language-specific stopwords.</p> <p>Keys must be ISO 639-1 language codes and values must be sets of stopwords associated with each language.</p> <p> TYPE: <code>dict[str, set[str]] | None</code> DEFAULT: <code>None</code> </p> <code>backend</code> <p>Tokenization backend to use.</p> <p>Supported values are:</p> <ul> <li><code>\"blingfire\"</code>: Microsoft BlingFire tokenizer (pattern-based).</li> <li><code>\"nltk\"</code>: NLTK Punkt tokenizer.</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'blingfire'</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer-attributes","title":"Attributes","text":""},{"location":"api/core/nlp.html#core.nlp.Tokenizer.characters_cleanup","title":"core.nlp.Tokenizer.characters_cleanup  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>characters_cleanup: dict[(re.Pattern) : str] = {\n    MULTIPLE_DOTS: \"...\",\n    MULTIPLE_DASHES: \"-\",\n    MULTIPLE_QUESTIONS: \"?\",\n    REPEATED_CHARACTERS: \" \",\n    BB_CODE: \" \",\n    MARKUP: \" \\\\1 \",\n    BASE_64: \" \",\n}\n</code></pre> <p>Dictionnary of regular expressions (keys) to find and replace by the provided strings (values). Cleanup repeated characters, including ellipses and question marks, leftover BBcode and XML markup, base64-encoded strings and French pronominal contractions (e.g \u201cme + a\u201d contracted into \u201cm\u2019a\u201d).</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.internal_meta_tokens","title":"core.nlp.Tokenizer.internal_meta_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>internal_meta_tokens: dict[(re.Pattern) : str] = {\n    HASH_PATTERN_FAST: \"_HASH_\",\n    NUMBER_PATTERN_FAST: \"_NUMBER_\",\n}\n</code></pre> <p>Dictionnary of regular expressions (keys) to find in full-tokens and replace by meta-tokens. Use simplified regex patterns for performance.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.abbreviations","title":"core.nlp.Tokenizer.abbreviations  <code>instance-attribute</code>","text":"<pre><code>abbreviations = abbreviations\n</code></pre> <p>Abbreviations and contractions to replace in full documents</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.replacements","title":"core.nlp.Tokenizer.replacements  <code>instance-attribute</code>","text":"<pre><code>replacements = replacements\n</code></pre> <p>Arbitrary string replacements in single tokens</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.stopwords","title":"core.nlp.Tokenizer.stopwords  <code>instance-attribute</code>","text":"<pre><code>stopwords = set(stopwords) if stopwords else None\n</code></pre> <p>Language-agnostic stopwords</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.lang_stopwords","title":"core.nlp.Tokenizer.lang_stopwords  <code>instance-attribute</code>","text":"<pre><code>lang_stopwords = lang_stopwords\n</code></pre> <p>Language-specific stopwords</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.supports_ngrams","title":"core.nlp.Tokenizer.supports_ngrams  <code>instance-attribute</code>","text":"<pre><code>supports_ngrams: bool = False\n</code></pre> <p>Whether or not the tokenizer has an embedded n-grams model</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.ngrams_trie","title":"core.nlp.Tokenizer.ngrams_trie  <code>instance-attribute</code>","text":"<pre><code>ngrams_trie = {}\n</code></pre> <p>Prefix tree of known n-grams for efficient lookups</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.vocabulary","title":"core.nlp.Tokenizer.vocabulary  <code>instance-attribute</code>","text":"<pre><code>vocabulary: Lexicon = Lexicon()\n</code></pre> <p>Known tokens, if trained for n-grams.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer-functions","title":"Functions","text":""},{"location":"api/core/nlp.html#core.nlp.Tokenizer.prefilter","title":"core.nlp.Tokenizer.prefilter","text":"<pre><code>prefilter(string: str, meta_tokens: bool = True) -&gt; str\n</code></pre> <p>Tokenizers split words based on unsupervised machine-learned models. Sometimes, they work weird. For example, in emails and user handles like <code>@user</code>, they would split <code>@</code> and <code>user</code> as 2 different tokens, making it impossible to detect usernames in single tokens later.</p> <p>To avoid that, we replace data of interest by meta-tokens before the tokenization, with regular expressions.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.lemmatize","title":"core.nlp.Tokenizer.lemmatize","text":"<pre><code>lemmatize(word: str) -&gt; str\n</code></pre> <p>Find the root (lemma) of words to help topical generalization.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.normalize_text","title":"core.nlp.Tokenizer.normalize_text","text":"<pre><code>normalize_text(document: str) -&gt; str\n</code></pre> <p>Prepare text for tokenization by converting it to lowercase ASCII characters.</p> <p>This will loose accents, diacritics and capitals, which means some nuance will be lost at the benefit of generality. In case this does not suit your usecase, you may inherit the <code>Tokenizer</code> class, build a child class and re-implement this method</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.normalize_token","title":"core.nlp.Tokenizer.normalize_token","text":"<pre><code>normalize_token(\n    word: str,\n    language: str | None,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; str | None\n</code></pre> <p>Return normalized, lemmatized and stemmed word tokens, where dates, times, digits, monetary units  and URLs have their actual value replaced by meta-tokens designating their type.  Stopwords (\u201cthe\u201d, \u201ca\u201d, etc.), punctuation etc. is replaced by <code>None</code>, which should be filtered out at the next step.</p> PARAMETER DESCRIPTION <code>word</code> <p>tokenized word in lower case only.</p> <p> TYPE: <code>str</code> </p> <code>language</code> <p>the ISO 369-1 language code used to remove typical stopwords.</p> <p> TYPE: <code>str</code> </p> <code>normalize</code> <p>remove punctuation and leading/trailing symbols.</p> <p> TYPE: <code>str</code> DEFAULT: <code>True</code> </p> <code>meta_tokens</code> <p>replace string patterns by meta_tokens</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>stem</code> <p>remove word suffixes, double consonnants, etc.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>remove_stopwords</code> <p>remove stopwords</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> NOTE <p>Tokenization is non-destructive (full sentences can be reconstructed entirely from token lists) if <code>normalize=False</code>, <code>meta_tokens=False</code>, <code>stem=False</code> and <code>remove_stopwords=False</code>. In this setting, only 1:1 token replacements defined in <code>self.replacements</code> will be applied, which can allow to replace abbreviations or accronyms. Other modes start generalizing semantics by removing meaning.</p> <p>Examples:</p> <p>Meta-tokens:     <code>10:00</code> or <code>10 h</code> or <code>10am</code> or <code>10 am</code> will all be replaced by a <code>_TIME_</code> meta-token.     <code>feb</code>, <code>February</code>, <code>feb.</code>, <code>monday</code> will all be replaced by a <code>_DATE_</code> meta-token.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.tokenize_text","title":"core.nlp.Tokenizer.tokenize_text","text":"<pre><code>tokenize_text(\n    sentence: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[str]\n</code></pre> <p>Split text into normalized word tokens and meta-tokens.</p> <p>No sentence or paragraph boundary detection is performed.</p> PARAMETER DESCRIPTION <code>sentence</code> <p>Input text to tokenize.</p> <p> TYPE: <code>str</code> </p> <code>n_grams</code> <p>Whether to detect and collapse n-grams.</p> <p>Requires a trained n-gram model generated with <code>train_ngrams()</code>.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Note <p>The parameters <code>language</code>, <code>normalize</code>, <code>meta_tokens</code>, <code>stem</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>list[str]</code> <p>List of normalized tokens represented as a bag of words.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.post_filter_tokens","title":"core.nlp.Tokenizer.post_filter_tokens","text":"<pre><code>post_filter_tokens(\n    tokens: list[str],\n    language: str | None = None,\n    meta_tokens: bool = True,\n    stem: bool = False,\n    normalize: bool = False,\n    remove_stopwords: bool = False,\n) -&gt; list[str]\n</code></pre> <p>Apply post-processing operations to an existing token stream.</p> <p>This method applies token normalization, stemming, stopword removal, and meta-token handling without performing tokenization.</p> PARAMETER DESCRIPTION <code>tokens</code> <p>List of input tokens to process.</p> <p> TYPE: <code>list[str]</code> </p> Note <p>The parameters <code>language</code>, <code>meta_tokens</code>, <code>stem</code>, <code>normalize</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>list[str]</code> <p>List of processed tokens.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.tokenize_document_flat","title":"core.nlp.Tokenizer.tokenize_document_flat","text":"<pre><code>tokenize_document_flat(\n    document: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[str]\n</code></pre> <p>Cleanup and tokenize a document or a sentence as an atomic element, meaning we don\u2019t split it into sentences.  Use this either for search-engine purposes (into a document\u2019s body) or if the document is already split into sentences.  The document text needs to have been prepared and cleaned, which means :</p> <ul> <li>lowercased (optional but recommended) with <code>str.lower()</code>,</li> <li>translated from Unicode to ASCII (optional but recommended) with core.utils.typography_undo,</li> <li>cleaned up for sequences of whitespaces with core.utils.clean_whitespaces</li> </ul> Note <p>the language is detected internally if not provided as an optional argument. When processing a single sentence extracted from a document, instead of the whole document, it is more accurate to run the language detection on the whole document, ahead of calling this method, and pass on the result here.</p> PARAMETER DESCRIPTION <code>document</code> <p>the text of the document to tokenize</p> <p> TYPE: <code>str</code> </p> <code>n_grams</code> <p>see core.nlp.Tokenizer.tokenize_text</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Note <p>The parameters <code>language</code>, <code>meta_tokens</code>, <code>stem</code>, <code>normalize</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>tokens</code> <p>a 1D list of normalized tokens and meta-tokens.</p> <p> TYPE: <code>list[str]</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.tokenize_document_per_sentence","title":"core.nlp.Tokenizer.tokenize_document_per_sentence","text":"<pre><code>tokenize_document_per_sentence(\n    document: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[list[str]]\n</code></pre> <p>Cleanup and tokenize a whole document as a list of sentences, meaning we split it into sentences before tokenizing.  Use this to train a Word2Vec (embedding) model so each token is properly embedded into its syntactic context.  The document text needs to have been prepared and cleaned, which means :</p> <ul> <li>lowercased (optional but recommended) with <code>str.lower()</code>,</li> <li>translated from Unicode to ASCII (optional but recommended) with core.utils.typography_undo,</li> <li>cleaned up for sequences of whitespaces with core.utils.clean_whitespaces</li> </ul> PARAMETER DESCRIPTION <code>document</code> <p>the text of the document to tokenize</p> <p> TYPE: <code>str</code> </p> <code>n_grams</code> <p>see core.nlp.Tokenizer.tokenize_text</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> Note <p>The parameters <code>language</code>, <code>meta_tokens</code>, <code>stem</code>, <code>normalize</code>, and <code>remove_stopwords</code> are forwarded to <code>normalize_token()</code> and have the same meaning.</p> RETURNS DESCRIPTION <code>tokens</code> <p>a 2D list of sentences (1st axis), each containing a list of normalized tokens and meta-tokens (2nd axis).</p> <p> TYPE: <code>list[list[str]]</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.tokenize_document_per_paragraph","title":"core.nlp.Tokenizer.tokenize_document_per_paragraph","text":"<pre><code>tokenize_document_per_paragraph(\n    document: str,\n    language: str | None = None,\n    n_grams: bool = True,\n    normalize: bool = True,\n    meta_tokens: bool = True,\n    stem: bool = True,\n    remove_stopwords: bool = True,\n) -&gt; list[list[str]]\n</code></pre> <p>Cleanup and tokenize a whole document as a list of paragraphs, meaning we split it on `</p> <p><code>or</code> ` before tokenizing.          Use this to train a Word2Vec (embedding) model so each token is properly embedded into its syntactic context.          The document text needs to have been prepared and cleaned, which means :</p> <pre><code>    - lowercased (optional but recommended) with `str.lower()`,\n    - translated from Unicode to ASCII (optional but recommended) with [core.utils.typography_undo][],\n    - cleaned up for sequences of whitespaces with [core.utils.clean_whitespaces][]\n\n    Arguments:\n        document (str): the text of the document to tokenize\n        n_grams (bool): see [core.nlp.Tokenizer.tokenize_text][]\n        others: see [core.nlp.Tokenizer.normalize_token][] arguments\n\n    Note:\n        the language is detected internally if not provided. The text is prefiltered with [self.prefilter][]\n\n    Returns:\n        tokens: a 2D list of paragraphs (1st axis), each containing a list of normalized tokens and meta-tokens (2nd axis).\n</code></pre>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.load","title":"core.nlp.Tokenizer.load  <code>classmethod</code>","text":"<pre><code>load(name: str)\n</code></pre> <p>Load an existing trained model by its name from the <code>../models</code> folder.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.members_from_ngram","title":"core.nlp.Tokenizer.members_from_ngram","text":"<pre><code>members_from_ngram(token: str | None) -&gt; list[str] | None\n</code></pre> <p>Recover n-grams members from a single tokenized phrase, separated with <code>_</code>. This expects lower-case tokens, except for meta-tokens which are expected capitalized.</p> RETURNS DESCRIPTION <code>list[str] | None</code> <p>the list of n-gram members, or None if the token was not an n-gram but a singleton.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.train_ngrams","title":"core.nlp.Tokenizer.train_ngrams","text":"<pre><code>train_ngrams(\n    sentences: list[str],\n    connector_words: str = \"\",\n    min_count: int = 10,\n    threshold: float = 0.7,\n    scoring: str = \"npmi\",\n)\n</code></pre> <p>Train an n-gram model (bigrams and trigrams).</p> <p>Detects common phrases such as \u201cNew York City\u201d and merges them into single tokens using a statistical phrase model.</p> PARAMETER DESCRIPTION <code>sentences</code> <p>Training corpus. Must be a list of tokenized sentences.</p> <p> TYPE: <code>list[str]</code> </p> <code>connector_words</code> <p>Space-separated list of connector words allowed inside phrases (e.g. \u201cby\u201d in \u201cpiece by piece\u201d).</p> <p>These words are treated as valid bridges when forming n-grams.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> <code>min_count</code> <p>Minimum number of occurrences required for a phrase to be considered.</p> <p>See <code>gensim.models.phrases.Phrases</code> for details.</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> <code>threshold</code> <p>Phrase detection sensitivity threshold.</p> <p>See <code>gensim.models.phrases.Phrases</code>.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.7</code> </p> <code>scoring</code> <p>Scoring function used for phrase detection.</p> <p>See <code>gensim.models.phrases.Phrases</code>.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'npmi'</code> </p> Warning <p>N-gram training must be performed on lightly processed tokenized sentences. Do not apply stemming, stopword removal, or punctuation stripping before training.</p> <p>See <code>Tokenizer.normalize_token()</code> for required preprocessing options.</p> Note <ul> <li>Writes an <code>ngrams</code> log file in the models directory containing discovered phrases.</li> <li>Can be executed multiple times (e.g. per language); results are appended to the existing model.</li> </ul>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.compile_ngrams","title":"core.nlp.Tokenizer.compile_ngrams","text":"<pre><code>compile_ngrams(ngrams: list[str])\n</code></pre> <p>Build a nested n-grams dictionnary for efficient querying, like: <pre><code>{\n    \"new\": {\n        \"york\": {\n            \"__value__\": \"new_york\",\n            \"city\": {\n                \"__value__\": \"new_york_city\"\n            }\n        }\n    }\n}\n</code></pre></p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.replace_ngrams","title":"core.nlp.Tokenizer.replace_ngrams","text":"<pre><code>replace_ngrams(tokens: list[str]) -&gt; list[str]\n</code></pre> <p>Identify n-grams among tokens and collapse them into single tokens. N-grams should have been trained before, with core.nlp.Tokenizer.train_ngrams.</p> RETURNS DESCRIPTION <code>list[str]</code> <p>the collapsed list of strings, or the original list if no n-grams</p> <code>list[str]</code> <p>was found or the n-grams model has not been trained.</p>"},{"location":"api/core/nlp.html#core.nlp.Tokenizer.lookup_ngram","title":"core.nlp.Tokenizer.lookup_ngram","text":"<pre><code>lookup_ngram(members: list[str] | tuple[str, ...]) -&gt; str | None\n</code></pre> <p>Lookup an n-gram in the trie from its token members.</p> PARAMETER DESCRIPTION <code>members</code> <p>the tokens iterable</p> <p> TYPE: <code>list[str] | tuple[str, ...]</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>the collapsed n-gram if found in the trie, or <code>None</code> if the input members match</p> <code>str | None</code> <p>no known n-gram.</p> Example <p>lookup_ngram((\u201cnew\u201d, \u201cyork\u201d)) -&gt; \u201cnew_york\u201d</p> <p>lookup_ngram((\u201cnew\u201d, \u201cyork\u201d, \u201ccity\u201d)) -&gt; \u201cnew_york_city\u201d</p> <p>lookup_ngram((\u201cfoo\u201d, \u201cbar\u201d)) -&gt; None</p>"},{"location":"api/core/nlp.html#core.nlp.Data","title":"core.nlp.Data","text":"<pre><code>Data(text: str, label: str)\n</code></pre> <p>Represent an item of tagged training data.</p> PARAMETER DESCRIPTION <code>text</code> <p>the content to label, which will be vectorized</p> <p> TYPE: <code>str</code> </p> <code>label</code> <p>the category of the content, which will be predicted by the model</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/nlp.html#core.nlp.LossLogger","title":"core.nlp.LossLogger","text":"<pre><code>LossLogger()\n</code></pre> <p>               Bases: <code>CallbackAny2Vec</code></p> <p>Output loss at each epoch</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec","title":"core.nlp.Word2Vec","text":"<pre><code>Word2Vec(\n    documents: list[list[str]],\n    name: str = \"word2vec\",\n    vector_size: int = 300,\n    epochs: int = 200,\n    window: int = 5,\n    min_count: int = 5,\n    sample: float = 0.0005,\n    tokenizer: Tokenizer = None,\n    compute_idf: bool = False,\n    **kwargs: dict[str, Any]\n)\n</code></pre> <p>               Bases: <code>gensim.models.Word2Vec</code></p> <p>Train, re-train, or load a Word2Vec embedding model.</p> <p>If a model with the given <code>name</code> already exists, it is automatically loaded instead of re-trained. Note that in this case, <code>vector_size</code> will be overridden by the saved model configuration.</p> PARAMETER DESCRIPTION <code>documents</code> <p>Pre-tokenized training corpus.</p> <p>Structure: - outer list: documents - inner list: tokenized sentences</p> <p> TYPE: <code>list[list[str]]</code> </p> <code>name</code> <p>Name of the model file used for saving/loading.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'word2vec'</code> </p> <code>vector_size</code> <p>Dimensionality of word embeddings.</p> <p> TYPE: <code>int</code> DEFAULT: <code>300</code> </p> <code>epochs</code> <p>Number of training iterations.</p> <p>Higher values improve quality on small corpora but increase training time.</p> <p> TYPE: <code>int</code> DEFAULT: <code>200</code> </p> <code>window</code> <p>Context window size for word co-occurrence.</p> <p> TYPE: <code>int</code> DEFAULT: <code>5</code> </p> <code>min_count</code> <p>Minimum frequency threshold for vocabulary filtering.</p> <p> TYPE: <code>int</code> DEFAULT: <code>5</code> </p> <code>sample</code> <p>Subsampling rate for frequent words.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.0005</code> </p> <code>tokenizer</code> <p>Tokenizer instance used for preprocessing (if applicable).</p> <p> TYPE: <code>Tokenizer</code> DEFAULT: <code>None</code> </p> <code>compute_idf</code> <p>Whether to compute and store IDF statistics for SIF weighting.</p> <p>Disable to reduce model size when SIF is not used.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>**kwargs</code> <p>Additional parameters forwarded directly to <code>gensim.models.Word2Vec</code>.</p> <p> TYPE: <code>dict[str, Any]</code> DEFAULT: <code>{}</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec-attributes","title":"Attributes","text":""},{"location":"api/core/nlp.html#core.nlp.Word2Vec.tokenizer","title":"core.nlp.Word2Vec.tokenizer  <code>instance-attribute</code>","text":"<pre><code>tokenizer = tokenizer if tokenizer is not None else Tokenizer()\n</code></pre> <p>Tokenizer used to train the model. We store it to be sure to use the same when using it.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.N_docs","title":"core.nlp.Word2Vec.N_docs  <code>instance-attribute</code>","text":"<pre><code>N_docs = len(documents)\n</code></pre> <p>Number of documents in the training corpus</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.N_sentences","title":"core.nlp.Word2Vec.N_sentences  <code>instance-attribute</code>","text":"<pre><code>N_sentences = len(sentences)\n</code></pre> <p>Number of sentences in the training corpus</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.N_words","title":"core.nlp.Word2Vec.N_words  <code>instance-attribute</code>","text":"<pre><code>N_words = len(words)\n</code></pre> <p>Number of words (tokens) in the training corpus</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.N_terms","title":"core.nlp.Word2Vec.N_terms  <code>instance-attribute</code>","text":"<pre><code>N_terms = len(counts)\n</code></pre> <p>Number of terms (unique words) in the training corpus</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.idf","title":"core.nlp.Word2Vec.idf  <code>instance-attribute</code>","text":"<pre><code>idf: dict[str, float] | None = None\n</code></pre> <p>Inverse Document Frequency, used only for SIF weighting when enabled.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.avg_doc_len","title":"core.nlp.Word2Vec.avg_doc_len  <code>instance-attribute</code>","text":"<pre><code>avg_doc_len: float | None = None\n</code></pre> <p>Average number of words in documents of the training corpus, available with IDF stats.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec-functions","title":"Functions","text":""},{"location":"api/core/nlp.html#core.nlp.Word2Vec.compute_idf","title":"core.nlp.Word2Vec.compute_idf","text":"<pre><code>compute_idf(documents: list[list[str]]) -&gt; None\n</code></pre> <p>Compute and store IDF statistics from a tokenized document corpus.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.update_idf","title":"core.nlp.Word2Vec.update_idf","text":"<pre><code>update_idf(documents: list[list[str]]) -&gt; None\n</code></pre> <p>Update IDF statistics and corpus-dependent metadata with new documents.</p> PARAMETER DESCRIPTION <code>documents</code> <p>New pre-tokenized documents.</p> <p> TYPE: <code>list[list[str]]</code> </p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.prune_idf","title":"core.nlp.Word2Vec.prune_idf","text":"<pre><code>prune_idf()\n</code></pre> <p>Prune IDF entries to the actual model vocabulary (remove tokens that were filtered out by gensim during <code>super().__init__</code>).</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.load_model","title":"core.nlp.Word2Vec.load_model  <code>classmethod</code>","text":"<pre><code>load_model(name: str)\n</code></pre> <p>Load a trained model saved in <code>models</code> folders</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.get_word","title":"core.nlp.Word2Vec.get_word","text":"<pre><code>get_word(word: str) -&gt; str | None\n</code></pre> <p>Find out if word is in dictionary, optionnaly attempting spell-checking if not found.</p> PARAMETER DESCRIPTION <code>word</code> <p>word to find</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>str | None</code> <ul> <li>the original word if found in dictionnary,</li> <li><code>None</code> if both previous conditions were not matched.</li> </ul>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.get_wordvec","title":"core.nlp.Word2Vec.get_wordvec","text":"<pre><code>get_wordvec(\n    word: str, embed: str = \"IN\", normalize: bool = True\n) -&gt; np.ndarray[np.float32] | None\n</code></pre> <p>Return the vector associated to a word, through a dictionnary of words.</p> PARAMETER DESCRIPTION <code>word</code> <p>the word to convert to a vector.</p> <p> TYPE: <code>str</code> </p> <code>embed</code> <ul> <li><code>IN</code> uses the input embedding matrix gensim.models.word2vec.Word2Vec.wv, useful to vectorize queries and documents for classification training.</li> <li><code>OUT</code> uses the output embedding matrix gensim.models.word2vec.Word2Vec, useful for the dual-space embedding scheme, to train search engines. [^1]</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'IN'</code> </p> <ol> <li> <p>A Dual Embedding Space Model for Document Ranking (2016), Bhaskar Mitra, Eric Nalisnick, Nick Craswell, Rich Caruana https://arxiv.org/pdf/1602.01137.pdf \u21a9</p> </li> </ol> RETURNS DESCRIPTION <code>np.ndarray[np.float32] | None</code> <p>the nD vector if the word was found in the dictionnary, or <code>None</code>.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.get_features","title":"core.nlp.Word2Vec.get_features","text":"<pre><code>get_features(\n    tokens: list[str],\n    embed: str = \"IN\",\n    use_sif: bool = False,\n    sif_smoothing: float = 0.001,\n) -&gt; np.ndarray[np.float32]\n</code></pre> <p>Calls core.nlp.Word2Vec.get_wordvec over a list of tokens and returns a single vector representing the whole list.</p> PARAMETER DESCRIPTION <code>tokens</code> <p>list of text tokens.</p> <p> TYPE: <code>list[str]</code> </p> <code>embed</code> <p>see core.nlp.Word2Vec.get_wordvec</p> <p> TYPE: <code>str</code> DEFAULT: <code>'IN'</code> </p> <code>use_sif</code> <p>Use SIF weighting on each term when embedding a full sentence or document. See core.nlp.Word2Vec.SIF.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>sif_smoothing</code> <p>The SIF smoothing coefficient.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.001</code> </p> RETURNS DESCRIPTION <code>np.ndarray[np.float32]</code> <p>the normalized centroid of word embedding vectors associated with the input tokens </p> <code>np.ndarray[np.float32]</code> <p>(aka the average vector), or the null vector if no word from the list was found in dictionnary.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.SIF","title":"core.nlp.Word2Vec.SIF","text":"<pre><code>SIF(token: str, a: float = 0.001) -&gt; float\n</code></pre> <p>Smooth inverse frequency weighting</p> <p>Taken from A simple but tough-to-beat baseline for sentence embeddings, Sanjeev Arora, Yingyu Liang, Tengyu Ma. https://openreview.net/pdf?id=SyK00v5xx</p> <p>This helps refining semantics by under-weighting stopwords, however it\u2019s unsuited for File Information Retrieval (search engines) because it over-smoothen the embedding space geometry and hinders relevance discrimination with regard to a query.</p> PARAMETER DESCRIPTION <code>token</code> <p>the token to weight. It should be in the model vocabulary.</p> <p> TYPE: <code>str</code> </p> Return <p>The SIF weight associated with the token or 0. if the token was not found in the vocabulary.</p>"},{"location":"api/core/nlp.html#core.nlp.Word2Vec.tokens_to_indices","title":"core.nlp.Word2Vec.tokens_to_indices","text":"<pre><code>tokens_to_indices(tokens: list[str]) -&gt; np.ndarray[np.int32]\n</code></pre> <p>Convert a list of tokens to a list of their index number in the Word2Vec vocabulary. This yields a more compact, albeit purely symbolic, representation of a tokenized document as a series of integers.</p> <p>The conversion is reversible and the original token can be found with <code>self.wv.index_to_key[i]</code>, where <code>i</code> is the index number output (for each token) from here.</p> Return <p>the list of indices as 32 bits integers, meaning the Word2Vec vocabulary needs to contain fewer than 4.29 billions words.</p>"},{"location":"api/core/nlp.html#core.nlp.Classifier","title":"core.nlp.Classifier","text":"<pre><code>Classifier(\n    training_set: list[Data],\n    name: str,\n    word2vec: Word2Vec,\n    validate: bool = True,\n    variant: str = \"svm\",\n)\n</code></pre> <p>               Bases: <code>nltk.classify.SklearnClassifier</code></p> <p>Initialize a Word2Vec + SVM classification pipeline.</p> <p>This class wraps a Word2Vec embedding model with a downstream machine-learning classifier (SVM or alternatives).</p> PARAMETER DESCRIPTION <code>training_set</code> <p>List of <code>Data</code> samples used for training.</p> <p>If empty, the system will attempt to load a pre-trained model using <code>name</code>.</p> <p> TYPE: <code>list[Data]</code> </p> <code>name</code> <p>Identifier used to save and reload the trained model.</p> <p> TYPE: <code>str</code> </p> <code>word2vec</code> <p>Word embedding model used to generate feature vectors.</p> <p> TYPE: <code>Word2Vec</code> </p> <code>validate</code> <p>If True, splits the dataset into training (95%) and testing (5%) subsets and prints evaluation metrics.</p> <p>Useful for classifier selection and sanity checking.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>variant</code> <p>Type of classifier to use:</p> <ul> <li> <p><code>svm</code>: RBF-kernel Support Vector Machine (default). Robust and stable across general datasets.</p> </li> <li> <p><code>linear svm</code>: Linear Support Vector Machine. Faster and often better for high-dimensional features.</p> </li> <li> <p><code>forest</code>: Random Forest classifier. Faster than linear SVM in some cases, but produces larger models.</p> </li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'svm'</code> </p> Note <p>The previous documentation mentioned <code>path</code> and <code>features</code>, but these are not part of the current signature and were removed.</p>"},{"location":"api/core/nlp.html#core.nlp.Classifier-functions","title":"Functions","text":""},{"location":"api/core/nlp.html#core.nlp.Classifier.get_features_parallel","title":"core.nlp.Classifier.get_features_parallel","text":"<pre><code>get_features_parallel(post: Data) -&gt; tuple[str, str]\n</code></pre> <p>Thread-safe call to <code>.get_features()</code> to be called in multiprocessing.Pool map</p>"},{"location":"api/core/nlp.html#core.nlp.Classifier.load","title":"core.nlp.Classifier.load  <code>classmethod</code>","text":"<pre><code>load(name: str)\n</code></pre> <p>Load an existing trained model by its name from the <code>../models</code> folder.</p>"},{"location":"api/core/nlp.html#core.nlp.Classifier.classify","title":"core.nlp.Classifier.classify","text":"<pre><code>classify(post: str) -&gt; str\n</code></pre> <p>Apply a label on a post based on the trained model.</p>"},{"location":"api/core/nlp.html#core.nlp.Classifier.prob_classify","title":"core.nlp.Classifier.prob_classify","text":"<pre><code>prob_classify(post: str) -&gt; tuple[str, float]\n</code></pre> <p>Apply a label on a post based on the trained model and output the probability too.</p>"},{"location":"api/core/nlp.html#core.nlp.StemTokenIndex","title":"core.nlp.StemTokenIndex","text":"<pre><code>StemTokenIndex(db: sqlite3.Connection, tokenizer: Tokenizer)\n</code></pre> <p>Reverse normalized stem -&gt; token lookup index.</p>"},{"location":"api/core/nlp.html#core.nlp.StemTokenIndex-functions","title":"Functions","text":""},{"location":"api/core/nlp.html#core.nlp.StemTokenIndex.most_probable_token","title":"core.nlp.StemTokenIndex.most_probable_token","text":"<pre><code>most_probable_token(db: sqlite3.Connection, stem: str) -&gt; str\n</code></pre> <p>Return the most probable original token associated to the stem. If the stem doesn\u2019t exist in the database, it is returned as-is.</p>"},{"location":"api/core/nlp.html#core.nlp.StemTokenIndex.most_probable_tokens","title":"core.nlp.StemTokenIndex.most_probable_tokens","text":"<pre><code>most_probable_tokens(db: sqlite3.Connection, stems: list[str]) -&gt; list[str]\n</code></pre> <p>Return the most probable original token for each stem.</p> <p>Stems not found in DB are returned unchanged.</p>"},{"location":"api/core/nlp.html#core.nlp-functions","title":"Functions","text":""},{"location":"api/core/nlp.html#core.nlp.split_url","title":"core.nlp.split_url","text":"<pre><code>split_url(url: str) -&gt; tuple[str, str, str, str, str] | None\n</code></pre> <p>Split a well-formed URL following RFC3986 into base elements.</p> RETURNS DESCRIPTION <code>tuple[str, str, str, str, str] | None</code> <p>a tuple of <code>(protocol, domain, page, parameters, anchor)</code>. </p> <code>tuple[str, str, str, str, str] | None</code> <p>Empty/missing fields are inited with empty strings so there is no need for individual <code>None</code> checks.</p> <code>tuple[str, str, str, str, str] | None</code> <p>If the <code>url</code> input doesn\u2019t match an URL format, return <code>None</code>.</p>"},{"location":"api/core/nlp.html#core.nlp.parse_lang_to_iso639_1","title":"core.nlp.parse_lang_to_iso639_1","text":"<pre><code>parse_lang_to_iso639_1(value: str | None) -&gt; str | None\n</code></pre> <p>Normalize language identifier to ISO 639-1.</p>"},{"location":"api/core/nlp.html#core.nlp.guess_language","title":"core.nlp.guess_language","text":"<pre><code>guess_language(\n    string: str,\n    stopwords_threshold: float = 0.05,\n    letters_threshold: float = 0.8,\n) -&gt; str | None\n</code></pre> <p>Basic language guesser based on stopwords detection.</p> <p>Stopwords are the most common words of a language: for each language, we count how many stopwords we found and return the language having the most matches. It is accurate for paragraphs and long documents, not so much for short sentences.</p> PARAMETER DESCRIPTION <code>string</code> <p>the string to analyze. Needs to be lowercased but to retain accents and diacritics.</p> <p> TYPE: <code>str</code> </p> <code>stopwords_threshold</code> <p>the minimum ratio of stopwords divided by total words in strings to be found to conclude on a language. For example, Japanese companies often have technical reports written in Japanese but still containing some English. If less than 5% of the words are known English stopwords, we could conclude it\u2019s not English.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.05</code> </p> <code>letters_threshold</code> <p>the minimum ratio of roman (latin) characters among all characters (including numbers, symbols and non-latin alphabets) to be found to conclude on a language.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.8</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>ISO 639-1 language code. Defaults to \u201cen\u201d if nothing found.</p>"},{"location":"api/core/nlp.html#core.nlp.detect_language","title":"core.nlp.detect_language","text":"<pre><code>detect_language(text: str) -&gt; str | None\n</code></pre> <p>Detect language from arbitrary text safely.</p> RETURNS DESCRIPTION <code>str | None</code> <p>ISO 639-1 language code.</p>"},{"location":"api/core/nlp.html#core.nlp.tokenize_document_to_words","title":"core.nlp.tokenize_document_to_words","text":"<pre><code>tokenize_document_to_words(\n    text: str, language: str | None = None, backend: str = \"blingfire\"\n) -&gt; list[str]\n</code></pre> <p>Split a text into single words</p> PARAMETER DESCRIPTION <code>language</code> <p>ISO 639-1 language code.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[str]</code> <p>Bag of words for the whole document. Sentence delimiters are removed.</p>"},{"location":"api/core/nlp.html#core.nlp.split_document_to_sentences","title":"core.nlp.split_document_to_sentences","text":"<pre><code>split_document_to_sentences(\n    text: str, language: str | None = None, backend: str = \"blingfire\"\n) -&gt; list[str]\n</code></pre> <p>Split a text into a list of sentences.</p> PARAMETER DESCRIPTION <code>language</code> <p>ISO 639-1 language code.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[str]</code> <p>List of sentences as full text.</p>"},{"location":"api/core/nlp.html#core.nlp.tokenize_document_to_sentences","title":"core.nlp.tokenize_document_to_sentences","text":"<pre><code>tokenize_document_to_sentences(\n    text: str, language: str | None = None, backend: str = \"blingfire\"\n) -&gt; list[list[str]]\n</code></pre> <p>Split a text into single words as a list of lists</p> PARAMETER DESCRIPTION <code>language</code> <p>ISO 639-1 language code.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[list[str]]</code> <p>List of sentences, each sentence is itself a list of words.</p>"},{"location":"api/core/parser.html","title":"core.parser","text":""},{"location":"api/core/parser.html#core.parser","title":"core.parser","text":""},{"location":"api/core/parser.html#core.parser-classes","title":"Classes","text":""},{"location":"api/core/parser.html#core.parser.ParsedHTML","title":"core.parser.ParsedHTML  <code>dataclass</code>","text":"<pre><code>ParsedHTML(\n    soup: BeautifulSoup,\n    links: list[str] = list(),\n    h1: set[str] = set(),\n    h2: set[str] = set(),\n    content: str | None = str(),\n    title: str | None = str(),\n    excerpt: str | None = str(),\n    date: str | None = str(),\n    lang: str | None = str(),\n    scripts: list[str] = list(),\n)\n</code></pre> <p>Wrapper around BeautifulSoup with precomputed crawler metadata.</p>"},{"location":"api/core/parser.html#core.parser.ParsedHTML-functions","title":"Functions","text":""},{"location":"api/core/parser.html#core.parser.ParsedHTML.from_html","title":"core.parser.ParsedHTML.from_html  <code>classmethod</code>","text":"<pre><code>from_html(html: str, parser: str = 'html5lib') -&gt; ParsedHTML\n</code></pre> <p>Build ParsedHTML from raw HTML string.</p>"},{"location":"api/core/parser.html#core.parser.ParsedHTML.get_page_markup","title":"core.parser.ParsedHTML.get_page_markup","text":"<pre><code>get_page_markup(\n    markup: str | tuple | list[str] | list[tuple] | None,\n) -&gt; str | None\n</code></pre> <p>Extract the text content of an HTML page DOM by targeting only the specific tags.</p> PARAMETER DESCRIPTION <code>markup</code> <p>any kind of tags supported by bs4.Tag.find_all:</p> <ul> <li>(str): the single tag to select. For example, <code>\"body\"</code> will select <code>&lt;body&gt;...&lt;/body&gt;</code>.</li> <li>(tuple): the tag and properties to select. For example, <code>(\"div\", { \"class\": \"right\" })</code> will select <code>&lt;div class=\"right\"&gt;...&lt;/div&gt;</code>.</li> <li>all combinations of the above can be chained in lists.</li> <li>None: don\u2019t parse the page internal content. Links, h1 and h2 headers will still be parsed.</li> </ul> <p> TYPE: <code>str | tuple | list[str] | list[tuple] | None</code> </p> RETURNS DESCRIPTION <code>str | None</code> <p>The text content of all instances of all tags in markup as a single string, if any, else an empty string.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; get_page_markup(page, \"article\")\n</code></pre> <pre><code>&gt;&gt;&gt; get_page_markup(page, [\"h1\", \"h2\", \"h3\", \"article\"])\n</code></pre> <pre><code>&gt;&gt;&gt; get_page_markup(page, [(\"div\", {\"id\": \"content\"}), \"details\", (\"div\", {\"class\": \"comment-reply\"})])\n</code></pre>"},{"location":"api/core/parser.html#core.parser.ParsedHTML.get_excerpt","title":"core.parser.ParsedHTML.get_excerpt","text":"<pre><code>get_excerpt() -&gt; str | None\n</code></pre> <p>Find HTML tags possibly containing the shortened version of the page content.</p> Looks for HTML tags <ul> <li><code>&lt;meta name=\"description\" content=\"...\"&gt;</code></li> <li><code>&lt;meta property=\"og:description\" content=\"...\"&gt;</code></li> </ul> RETURNS DESCRIPTION <code>str | None</code> <p>The content of the meta tag if any.</p>"},{"location":"api/core/parser.html#core.parser.ParsedHTML.get_date","title":"core.parser.ParsedHTML.get_date","text":"<pre><code>get_date() -&gt; str | None\n</code></pre> <p>Find HTML tags possibly containing the page date.</p> <p>Looks for HTML tags:</p> <ul> <li><code>&lt;meta name=\"date\" content=\"...\"&gt;</code></li> <li><code>&lt;meta name=\"publishDate\" content=\"...\"&gt;</code></li> <li><code>&lt;meta property=\"article:published_time\" content=\"...\"&gt;</code></li> <li><code>&lt;meta property=\"article:modified_time\" content=\"...\"&gt;</code></li> <li><code>&lt;meta name=\"dc.date\" content=\"...\"&gt;</code></li> <li><code>&lt;time datetime=\"...\"&gt;</code></li> <li><code>&lt;relative-time datetime=\"...\"&gt;</code></li> <li><code>&lt;div class=\"dateline\"&gt;...&lt;/div&gt;</code></li> <li><code>&lt;script type=\"application/ld+json\"&gt;{\"dateModified\":\"...\", }&lt;/script&gt;</code> (Wikipedia)</li> </ul> RETURNS DESCRIPTION <code>str | None</code> <p>The content of the meta tag if any.</p>"},{"location":"api/core/parser.html#core.parser.ParsedHTML.get_lang","title":"core.parser.ParsedHTML.get_lang","text":"<pre><code>get_lang() -&gt; str | None\n</code></pre> <p>Attempt to find the page language</p>"},{"location":"api/core/parser.html#core.parser-functions","title":"Functions","text":""},{"location":"api/core/patterns.html","title":"core.patterns","text":""},{"location":"api/core/patterns.html#core.patterns","title":"core.patterns","text":"<p>Contains global regular expression patterns re-used in the app. You can use https://regex101.com/ to test these conveniently.</p> <p>\u00a9 2023 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/patterns.html#core.patterns-attributes","title":"Attributes","text":""},{"location":"api/core/patterns.html#core.patterns.regex_starter","title":"core.patterns.regex_starter  <code>module-attribute</code>","text":"<pre><code>regex_starter = '(?&lt;=^|\\\\s|\\\\[|\\\\(|\\\\{|\\\\&lt;|\\\\\\'|\\\\\"|`|;|\\\\&gt;)'\n</code></pre> <p>Start of line, or start of document, or start of markup</p>"},{"location":"api/core/patterns.html#core.patterns.regex_stopper","title":"core.patterns.regex_stopper  <code>module-attribute</code>","text":"<pre><code>regex_stopper = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|\\\\&lt;)'\n</code></pre> <p>End of line, or end of document, or end of markup</p>"},{"location":"api/core/patterns.html#core.patterns.end_of_word","title":"core.patterns.end_of_word  <code>module-attribute</code>","text":"<pre><code>end_of_word = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|:|,|\\\\?|\\\\!|\\\\.|\\\\&lt;)'\n</code></pre> <p>End of word, or end of line, or end of document, or end of markup</p>"},{"location":"api/core/patterns.html#core.patterns.regex_algebra","title":"core.patterns.regex_algebra  <code>module-attribute</code>","text":"<pre><code>regex_algebra = '[\\\\+\\\\-\\\\=\\\\\u2260\\\\\u00b1]'\n</code></pre> <p>Algebraic signs</p>"},{"location":"api/core/patterns.html#core.patterns.IP_PATTERN","title":"core.patterns.IP_PATTERN  <code>module-attribute</code>","text":"<pre><code>IP_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_ip, regex_stopper), re.IGNORECASE\n)\n</code></pre> <p>IPv4 and IPv6 patterns where the whole IP is captured in the first group.</p>"},{"location":"api/core/patterns.html#core.patterns.EMAIL_PATTERN","title":"core.patterns.EMAIL_PATTERN  <code>module-attribute</code>","text":"<pre><code>EMAIL_PATTERN = re.compile(\n    \"&lt;?([0-9a-z\\\\-\\\\_\\\\+\\\\.]+?@[0-9a-z\\\\-\\\\_\\\\+]+(\\\\.[0-9a-z\\\\_\\\\-]{2,})+)&gt;?\",\n    re.IGNORECASE,\n)\n</code></pre> <p>Emails patterns like <code>&lt;me@mail.com&gt;</code> or <code>me@mail.com</code> where the whole address is captured in the first group.</p>"},{"location":"api/core/patterns.html#core.patterns.URL_PATTERN","title":"core.patterns.URL_PATTERN  <code>module-attribute</code>","text":"<pre><code>URL_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_url, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>URL patterns like <code>http(s)://domain.ext/page/subpage?q=x&amp;r=0:1#anchor</code> or <code>//domain.ext/page</code>. URL must follow RFC3986, meaning query parameters should be before anchors, if any. Relying on this assumption allows a faster regex parsing.</p> <ul> <li>the protocol (ftp, ftps, http, https) is captured as the first group,</li> <li><code>domain.ext</code> is captured as the second group,</li> <li><code>/page/etc</code> is the third group, including leading and trailing <code>/</code>,</li> <li>page query parameters <code>?s=x&amp;r=0</code>, including <code>?</code>, is the fourth group if the URL declares <code>...?params#anchor</code>,</li> <li>anchor <code>#anchor</code> is the fifth group, including <code>#</code>, if the URL declares <code>...?params#anchor</code>.</li> </ul> <p>URLs are captured if they are:</p> <ul> <li>alone on their own line,</li> <li>enclosed in <code>{}</code>, <code>[]</code>, <code>()</code></li> <li>enclosed in whitespaces.</li> </ul> <p>Warning: URLs enclosed in <code>()</code>, <code>[]</code> and <code>{}</code> may retain the closing sign  as part of the page name since <code>()</code> and <code>[]</code> are valid in URL pathes  and parameters. This pattern will work on plain text only: Markdown, XML, HTML and JSON  will need to be parsed ahead.</p>"},{"location":"api/core/patterns.html#core.patterns.MEMBERS_PATTERN","title":"core.patterns.MEMBERS_PATTERN  <code>module-attribute</code>","text":"<pre><code>MEMBERS_PATTERN = re.compile('(?&lt;=[a-z])(\\\\.)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Domain patterns without leading protocol like <code>cdn.company.com</code> or class members in object-oriented programming languages like <code>params.cookies.client</code>.</p>"},{"location":"api/core/patterns.html#core.patterns.DATE_PATTERN","title":"core.patterns.DATE_PATTERN  <code>module-attribute</code>","text":"<pre><code>DATE_PATTERN = re.compile(date_regex, re.IGNORECASE)\n</code></pre> <p>Dates like <code>2022-12-01</code>, <code>01-12-2022</code>, <code>01-12-22</code>, <code>01/12/2022</code>, <code>01/12/22</code> where the whole date is captured in the first group, then each group of digits is captured in the order of appearance, in the next 3 groups</p>"},{"location":"api/core/patterns.html#core.patterns.TIME_PATTERN","title":"core.patterns.TIME_PATTERN  <code>module-attribute</code>","text":"<pre><code>TIME_PATTERN = re.compile(time_regex, re.IGNORECASE)\n</code></pre> <p>Identify more or less standard time patterns, like :</p> <ul> <li>12h15</li> <li>12:15</li> <li>12:15:00</li> <li>12am</li> <li>12 am</li> <li>12 h</li> <li>12:15:00Z</li> <li>12:15:00+01</li> <li>12:15:00 UTC+1</li> <li>11:27:45+0000</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>1- or 2-digits hour,</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>hour/minutes separator or half-day marker among <code>[\"h\", \":\", \"am\", \"pm\"]</code> (case-insensitive)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2-digits minutes, if any, or <code>None</code></p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>2-digits seconds, if any.</p> <p> TYPE: <code>str</code> </p> <code>4</code> <p>hour marker (<code>h</code> or <code>H</code>), half-day marker (case-insensitive <code>[\"am\", \"pm\"]</code>), or time zone marker (case-sensitive <code>[\"Z\", \"UTC\"]</code>)</p> <p> TYPE: <code>str</code> </p> <code>5</code> <p>1-or 2-digits signed integer timezone shift (referred to UTC).</p> <p> TYPE: <code>str</code> </p> <p>Examples:</p> <p>see https://regex101.com/r/QNtZAK/2</p> <p>see <code>src/tests/test-patterns.py</code></p>"},{"location":"api/core/patterns.html#core.patterns.DOMAIN_PATTERN","title":"core.patterns.DOMAIN_PATTERN  <code>module-attribute</code>","text":"<pre><code>DOMAIN_PATTERN = re.compile(\n    \"from ((?:[a-z0-9\\\\-_]{0,61}\\\\.)+[a-z]{2,})\", re.IGNORECASE\n)\n</code></pre> <p>Matches patterns like <code>from (domain.ext)</code> from RFC-822 <code>Received</code> header in emails.</p>"},{"location":"api/core/patterns.html#core.patterns.UID_PATTERN","title":"core.patterns.UID_PATTERN  <code>module-attribute</code>","text":"<pre><code>UID_PATTERN = re.compile('UID ([0-9]+)')\n</code></pre> <p>Matches email integer UID from IMAP headers.</p>"},{"location":"api/core/patterns.html#core.patterns.FLAGS_PATTERN","title":"core.patterns.FLAGS_PATTERN  <code>module-attribute</code>","text":"<pre><code>FLAGS_PATTERN = re.compile('FLAGS \\\\((.*?)\\\\)')\n</code></pre> <p>Matches email flags from IMAP headers.</p>"},{"location":"api/core/patterns.html#core.patterns.PATH_PATTERN","title":"core.patterns.PATH_PATTERN  <code>module-attribute</code>","text":"<pre><code>PATH_PATTERN = re.compile('%s%s%s' % (regex_starter, path_regex, end_of_word))\n</code></pre> <p>File path pattern like <code>~/file</code>, <code>/home/file</code>, <code>./file</code> or <code>C:\\windows</code></p>"},{"location":"api/core/patterns.html#core.patterns.PARTIAL_PATH_REGEX","title":"core.patterns.PARTIAL_PATH_REGEX  <code>module-attribute</code>","text":"<pre><code>PARTIAL_PATH_REGEX = re.compile(\n    \"%s%s%s\" % (regex_starter, partial_path_regex, end_of_word)\n)\n</code></pre> <p>Partial, invalid path patterns missing the leading root, like <code>home/user/stuff</code>. We start capturing after at least two folder separators (slash or backslash).</p> <p>Warning</p> <p>this will collide with date detection, so run it after in the pipeline.</p>"},{"location":"api/core/patterns.html#core.patterns.RESOLUTION_PATTERN","title":"core.patterns.RESOLUTION_PATTERN  <code>module-attribute</code>","text":"<pre><code>RESOLUTION_PATTERN = re.compile('\\\\d+(?:\u00d7|x|X)\\\\d+')\n</code></pre> <p>Pixel resolution like 10x20 or 10\u00d720. Units are discarded.</p>"},{"location":"api/core/patterns.html#core.patterns.NUMBER_PATTERN","title":"core.patterns.NUMBER_PATTERN  <code>module-attribute</code>","text":"<pre><code>NUMBER_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_number, regex_stopper)\n)\n</code></pre> <p>Signed integers and decimals, fractions and numeric IDs with interal dashes and underscores. Numbers with starting or trailing units are not considered. Lazy decimals (.1 and 1.) are considered.</p>"},{"location":"api/core/patterns.html#core.patterns.HASH_PATTERN","title":"core.patterns.HASH_PATTERN  <code>module-attribute</code>","text":"<pre><code>HASH_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_hash, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>Cryptographic hexadecimal hashes and fingerprints, of a min length of 8 characters.</p>"},{"location":"api/core/patterns.html#core.patterns.MULTIPLE_LINES","title":"core.patterns.MULTIPLE_LINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_LINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]{2,} ?)+')\n</code></pre> <p>Detect more than 2 newlines and tab, possibly mixed with spaces</p>"},{"location":"api/core/patterns.html#core.patterns.MULTIPLE_NEWLINES","title":"core.patterns.MULTIPLE_NEWLINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_NEWLINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]+ ?){2,}')\n</code></pre> <p>Detect broken sequences of newlines and spaces.</p>"},{"location":"api/core/patterns.html#core.patterns.INTERNAL_NEWLINE","title":"core.patterns.INTERNAL_NEWLINE  <code>module-attribute</code>","text":"<pre><code>INTERNAL_NEWLINE = re.compile('(?&lt;=\\\\w)[\\\\n\\\\t\\\\r]{1}(?=\\\\w)')\n</code></pre> <p>Detect single newline characters nested inside text. Mostly useful for parsed PDF where line wrapping is quite literal ( used instead of space).</p>"},{"location":"api/core/patterns.html#core.patterns.EXPOSURE","title":"core.patterns.EXPOSURE  <code>module-attribute</code>","text":"<pre><code>EXPOSURE = re.compile(\n    \"%s%s%s\" % (regex_starter, exposure_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/patterns.html#core.patterns.PHOTOSPEED","title":"core.patterns.PHOTOSPEED  <code>module-attribute</code>","text":"<pre><code>PHOTOSPEED = re.compile(\n    \"%s%s%s\" % (regex_starter, photospeed_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/core/patterns.html#core.patterns.SENSIBILITY","title":"core.patterns.SENSIBILITY  <code>module-attribute</code>","text":"<pre><code>SENSIBILITY = re.compile(\n    \"%s%s%s\" % (regex_starter, sensibility_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Photographic sensibility in ISO or ASA</p>"},{"location":"api/core/patterns.html#core.patterns.LUMINANCE","title":"core.patterns.LUMINANCE  <code>module-attribute</code>","text":"<pre><code>LUMINANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, luminance_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Luminance/radiance in nits or Cd/m\u00b2</p>"},{"location":"api/core/patterns.html#core.patterns.DIAPHRAGM","title":"core.patterns.DIAPHRAGM  <code>module-attribute</code>","text":"<pre><code>DIAPHRAGM = re.compile(\n    \"%s%s\" % (regex_starter, diaphragm_regex), flags=re.IGNORECASE\n)\n</code></pre> <p>Photographic diaph aperture values like f/2.8 or f/11</p>"},{"location":"api/core/patterns.html#core.patterns.GAIN","title":"core.patterns.GAIN  <code>module-attribute</code>","text":"<pre><code>GAIN = re.compile(\n    \"%s%s%s\" % (regex_starter, gain_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Gain, attenuation and PSNR in dB</p>"},{"location":"api/core/patterns.html#core.patterns.FILE_SIZE","title":"core.patterns.FILE_SIZE  <code>module-attribute</code>","text":"<pre><code>FILE_SIZE = re.compile(\n    \"%s%s%s\" % (regex_starter, filesize_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>File and memory size in bit, byte, or octet and their multiples</p>"},{"location":"api/core/patterns.html#core.patterns.DISTANCE","title":"core.patterns.DISTANCE  <code>module-attribute</code>","text":"<pre><code>DISTANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, distance_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Distance in meter, inch, foot and their multiples</p>"},{"location":"api/core/patterns.html#core.patterns.PERCENT","title":"core.patterns.PERCENT  <code>module-attribute</code>","text":"<pre><code>PERCENT = re.compile('%s%s%s' % (regex_starter, percent_regex, end_of_word))\n</code></pre> <p>Number followed by %</p>"},{"location":"api/core/patterns.html#core.patterns.WEIGHT","title":"core.patterns.WEIGHT  <code>module-attribute</code>","text":"<pre><code>WEIGHT = re.compile(\n    \"%s%s%s\" % (regex_starter, weight_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Weight (mass) in British and SI units and their multiples</p>"},{"location":"api/core/patterns.html#core.patterns.ANGLE","title":"core.patterns.ANGLE  <code>module-attribute</code>","text":"<pre><code>ANGLE = re.compile(\n    \"%s%s%s\" % (regex_starter, angle_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Angles in radians, degrees and steradians</p>"},{"location":"api/core/patterns.html#core.patterns.TEMPERATURE","title":"core.patterns.TEMPERATURE  <code>module-attribute</code>","text":"<pre><code>TEMPERATURE = re.compile(\n    \"%s%s%s\" % (regex_starter, temperature_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Temperatures in \u00b0C, \u00b0F and K</p>"},{"location":"api/core/patterns.html#core.patterns.FREQUENCY","title":"core.patterns.FREQUENCY  <code>module-attribute</code>","text":"<pre><code>FREQUENCY = re.compile(\n    \"%s%s%s\" % (regex_starter, frequency_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Frequencies in hertz and multiples</p>"},{"location":"api/core/patterns.html#core.patterns.TEXT_DATES","title":"core.patterns.TEXT_DATES  <code>module-attribute</code>","text":"<pre><code>TEXT_DATES = re.compile(\n    \"([0-9]{1,2})? (jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec|jan|f\u00e9v|mar|avr|mai|jui|jui|aou|sep|oct|nov|d\u00e9c|janvier|f\u00e9vrier|mars|avril|mai|juin|juillet|ao\u00fbt|septembre|octobre|novembre|d\u00e9cembre|january|february|march|april|may|june|july|august|september|october|november|december)\\\\.?( [0-9]{1,2})?( [0-9]{2,4})(?!\\\\:)\",\n    flags=re.IGNORECASE | re.MULTILINE,\n)\n</code></pre> <p>Find textual dates formats:</p> <ul> <li>English dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> <li>French dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>month (full-form or abbreviated)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>4 digits (full year)</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/patterns.html#core.patterns.BASE_64","title":"core.patterns.BASE_64  <code>module-attribute</code>","text":"<pre><code>BASE_64 = re.compile(\n    \"((?:[A-Za-z0-9+\\\\/]{4}){64,}(?:[A-Za-z0-9+\\\\/]{2}==|[A-Za-z0-9+\\\\/]{3}=)?)\"\n)\n</code></pre> <p>Identifies base64 encoding</p>"},{"location":"api/core/patterns.html#core.patterns.BB_CODE","title":"core.patterns.BB_CODE  <code>module-attribute</code>","text":"<pre><code>BB_CODE = re.compile('\\\\[(img|quote)[a-zA-Z0-9 =\\\\\"]*?\\\\].*?\\\\[\\\\/\\\\1\\\\]')\n</code></pre> <p>Identifies left-over BB code markup <code>[img]</code> and <code>[quote]</code></p>"},{"location":"api/core/patterns.html#core.patterns.MARKUP","title":"core.patterns.MARKUP  <code>module-attribute</code>","text":"<pre><code>MARKUP = re.compile('(?:\\\\[|\\\\{|\\\\&lt;)([^\\\\n\\\\r]+?)(?:\\\\]|\\\\}|\\\\&gt;)')\n</code></pre> <p>Identifies left-over HTML and Markdown markup, like <code>&lt;...&gt;</code>, <code>{...}</code>, <code>[...]</code></p>"},{"location":"api/core/patterns.html#core.patterns.USER","title":"core.patterns.USER  <code>module-attribute</code>","text":"<pre><code>USER = re.compile('([\\\\w\\\\-\\\\+\\\\.]+)?@([\\\\w\\\\-\\\\+\\\\.]+)|(user\\\\-?\\\\d+)')\n</code></pre> <p>Identifies user handles or emails</p>"},{"location":"api/core/patterns.html#core.patterns.REPEATED_CHARACTERS","title":"core.patterns.REPEATED_CHARACTERS  <code>module-attribute</code>","text":"<pre><code>REPEATED_CHARACTERS = re.compile('(.)\\\\1{9,}')\n</code></pre> <p>Identifies any character repeated more than 9 times</p>"},{"location":"api/core/patterns.html#core.patterns.UNFINISHED_SENTENCES","title":"core.patterns.UNFINISHED_SENTENCES  <code>module-attribute</code>","text":"<pre><code>UNFINISHED_SENTENCES = re.compile('(?&lt;![?!.;:])\\\\n\\\\n|\\\\r\\\\n')\n</code></pre> <p>Identifies sentences finishing with 2 newlines characters without having ending punctuations</p>"},{"location":"api/core/patterns.html#core.patterns.MULTIPLE_DOTS","title":"core.patterns.MULTIPLE_DOTS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DOTS = re.compile('\\\\.{2,}')\n</code></pre> <p>Identifies dots repeated more than twice</p>"},{"location":"api/core/patterns.html#core.patterns.MULTIPLE_DASHES","title":"core.patterns.MULTIPLE_DASHES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DASHES = re.compile('[-~]{1,}')\n</code></pre> <p>Identifies dashes repeated more than once</p>"},{"location":"api/core/patterns.html#core.patterns.MULTIPLE_QUESTIONS","title":"core.patterns.MULTIPLE_QUESTIONS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_QUESTIONS = re.compile('\\\\?{1,}')\n</code></pre> <p>Identifies question marks repeated more than once</p>"},{"location":"api/core/patterns.html#core.patterns.ORDINAL_FR","title":"core.patterns.ORDINAL_FR  <code>module-attribute</code>","text":"<pre><code>ORDINAL_FR = re.compile('n\u00b0 ?([0-9]+)')\n</code></pre> <p>French ordinal numbers (num\u00e9ros n\u00b0)</p>"},{"location":"api/core/patterns.html#core.patterns.FRANCAIS","title":"core.patterns.FRANCAIS  <code>module-attribute</code>","text":"<pre><code>FRANCAIS = re.compile(\n    \"%s(j|t|s|d|qu|lorsqu|quelqu|jusqu|m|c|n)\\\\'(?=[aeiouy\u00e9\u00e8\u00e0\u00ea\u00e2\u00ee\u00f4\u00fb\u00ef\u00fc\u00e4\u00eb\u00f6h][\\\\w\\\\s])\"\n    % regex_starter,\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>French contractions of pronouns and determinants</p>"},{"location":"api/core/patterns.html#core.patterns.DASHES","title":"core.patterns.DASHES  <code>module-attribute</code>","text":"<pre><code>DASHES = re.compile('(?&lt;=\\\\w)(-|_|=)+(?=\\\\w)', re.IGNORECASE)\n</code></pre> <p>Dashes in the middle of ASCII/Latin compounded words. Will not work if accented or Unicode characters are immediately surrounding the dash.</p>"},{"location":"api/core/patterns.html#core.patterns.ALTERNATIVES","title":"core.patterns.ALTERNATIVES  <code>module-attribute</code>","text":"<pre><code>ALTERNATIVES = re.compile('(?&lt;=[a-z])(\\\\/)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Slash-separated word alternatives like <code>and/or</code> <code>mr/mrs</code></p>"},{"location":"api/core/patterns.html#core.patterns.PLURAL_S","title":"core.patterns.PLURAL_S  <code>module-attribute</code>","text":"<pre><code>PLURAL_S = re.compile('(?&lt;=[a-zA-Z]{4,})s?e{0,2}s%s' % end_of_word)\n</code></pre> <p>Identify plural form of nouns (French and English), adjectives (French) and third-person present verbs (English) and second-person verbs (French) in -s.</p>"},{"location":"api/core/patterns.html#core.patterns.FEMININE_E","title":"core.patterns.FEMININE_E  <code>module-attribute</code>","text":"<pre><code>FEMININE_E = re.compile('(?&lt;=\\\\w{4,})e{1,2}%s' % end_of_word)\n</code></pre> <p>Identify feminine form of adjectives (French) in -e.</p>"},{"location":"api/core/patterns.html#core.patterns.DOUBLE_CONSONANTS","title":"core.patterns.DOUBLE_CONSONANTS  <code>module-attribute</code>","text":"<pre><code>DOUBLE_CONSONANTS = re.compile(\n    \"(?&lt;=\\\\w{2,})([bcfghjklmnpqrstvwxz])\\\\1\", re.IGNORECASE\n)\n</code></pre> <p>Identify double consonants in the middle of words.</p>"},{"location":"api/core/patterns.html#core.patterns.FEMININE_TRICE","title":"core.patterns.FEMININE_TRICE  <code>module-attribute</code>","text":"<pre><code>FEMININE_TRICE = re.compile('(?&lt;=\\\\w{4,})t(rice|eur|or)%s' % end_of_word)\n</code></pre> <p>Identify French feminine nouns in -trice.</p>"},{"location":"api/core/patterns.html#core.patterns.ADVERB_MENT","title":"core.patterns.ADVERB_MENT  <code>module-attribute</code>","text":"<pre><code>ADVERB_MENT = re.compile('(?&lt;=\\\\w{4,})e?ment%s' % end_of_word)\n</code></pre> <p>Identify French adverbs and English nouns ending en -ment</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_TION","title":"core.patterns.SUBSTANTIVE_TION  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_TION = re.compile('(?&lt;=\\\\w{4,})(t|s)ion%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from verbs by adding -tion and -sion</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_AT","title":"core.patterns.SUBSTANTIVE_AT  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_AT = re.compile('(?&lt;=\\\\w{4,})at%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from other nouns by adding -at</p>"},{"location":"api/core/patterns.html#core.patterns.PARTICIPLE_ING","title":"core.patterns.PARTICIPLE_ING  <code>module-attribute</code>","text":"<pre><code>PARTICIPLE_ING = re.compile('(?&lt;=\\\\w{4,})ing%s' % end_of_word)\n</code></pre> <p>Identify English substantives and present participles formed from verbs by adding -ing</p>"},{"location":"api/core/patterns.html#core.patterns.ADJECTIVE_ED","title":"core.patterns.ADJECTIVE_ED  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_ED = re.compile('(?&lt;=\\\\w{4,})ed%s' % end_of_word)\n</code></pre> <p>Identify English adjectives formed from verbs by adding -ed</p>"},{"location":"api/core/patterns.html#core.patterns.ADJECTIVE_TIF","title":"core.patterns.ADJECTIVE_TIF  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_TIF = re.compile('(?&lt;=\\\\w{2,})ti(f|v)%s' % end_of_word)\n</code></pre> <p>Identify English and French adjectives formed from verbs by adding -tif or -tive</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_Y","title":"core.patterns.SUBSTANTIVE_Y  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_Y = re.compile('(?&lt;=\\\\w{3,})y%s' % end_of_word)\n</code></pre> <p>Identify English substantives ending in -y</p>"},{"location":"api/core/patterns.html#core.patterns.VERB_IZ","title":"core.patterns.VERB_IZ  <code>module-attribute</code>","text":"<pre><code>VERB_IZ = re.compile('(?&lt;=\\\\w{4,})(i|y)z%s' % end_of_word)\n</code></pre> <p>Identify American verbs ending in -iz that French and Brits write in -is</p>"},{"location":"api/core/patterns.html#core.patterns.STUFF_ER","title":"core.patterns.STUFF_ER  <code>module-attribute</code>","text":"<pre><code>STUFF_ER = re.compile('(?&lt;=\\\\w{5,})er%s' % end_of_word)\n</code></pre> <p>Identify French 1st group verb (infinitive) and English substantives ending in -er</p>"},{"location":"api/core/patterns.html#core.patterns.BRITISH_OUR","title":"core.patterns.BRITISH_OUR  <code>module-attribute</code>","text":"<pre><code>BRITISH_OUR = re.compile('(?&lt;=\\\\w{3,})our%s' % end_of_word)\n</code></pre> <p>Identify British spelling ending in -our (colour, behaviour).</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_ITY","title":"core.patterns.SUBSTANTIVE_ITY  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_ITY = re.compile('(?&lt;=\\\\w{4,})it(y|e)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ity (English) and -ite (French).</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_IST","title":"core.patterns.SUBSTANTIVE_IST  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IST = re.compile('(?&lt;=\\\\w{3,})is(t|m)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ist and -ism.</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_IQU","title":"core.patterns.SUBSTANTIVE_IQU  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IQU = re.compile('(?&lt;=\\\\w{3,})i(qu|c)%s' % end_of_word)\n</code></pre> <p>Identify French substantives in -iqu</p>"},{"location":"api/core/patterns.html#core.patterns.SUBSTANTIVE_EUR","title":"core.patterns.SUBSTANTIVE_EUR  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_EUR = re.compile('(?&lt;=\\\\w{3,})eur%s' % end_of_word)\n</code></pre> <p>Identify French substantives -eur</p>"},{"location":"api/core/patterns.html#core.patterns.HYPHENIZED","title":"core.patterns.HYPHENIZED  <code>module-attribute</code>","text":"<pre><code>HYPHENIZED = re.compile('(?&lt;=\\\\w{3,})[-\u2013\u2014]+ *[\\\\n\\\\r]{1,2}(?=\\\\w)')\n</code></pre> <p>Detect hyphenized words at the end of a PDF text line.</p>"},{"location":"api/core/patterns.html#core.patterns.WAYBACK_RE","title":"core.patterns.WAYBACK_RE  <code>module-attribute</code>","text":"<pre><code>WAYBACK_RE = re.compile('https?://web\\\\.archive\\\\.org/web/[^/]+/(https?://.+)')\n</code></pre> <p>Find the canonical URL from web.archive.org (Wayback Machine) URLs</p>"},{"location":"api/core/patterns.html#core.patterns-functions","title":"Functions","text":""},{"location":"api/core/patterns.html#core.patterns.split_url","title":"core.patterns.split_url","text":"<pre><code>split_url(url: str) -&gt; tuple[str, str, str, str, str] | None\n</code></pre> <p>Split a well-formed URL following RFC3986 into base elements.</p> RETURNS DESCRIPTION <code>tuple[str, str, str, str, str] | None</code> <p>a tuple of <code>(protocol, domain, page, parameters, anchor)</code>. </p> <code>tuple[str, str, str, str, str] | None</code> <p>Empty/missing fields are inited with empty strings so there is no need for individual <code>None</code> checks.</p> <code>tuple[str, str, str, str, str] | None</code> <p>If the <code>url</code> input doesn\u2019t match an URL format, return <code>None</code>.</p>"},{"location":"api/core/pdf.html","title":"core.pdf","text":""},{"location":"api/core/pdf.html#core.pdf","title":"core.pdf","text":"<p>PDF parsing utils, including OCR.</p> <p>\u00a9 2024 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/pdf.html#core.pdf-attributes","title":"Attributes","text":""},{"location":"api/core/pdf.html#core.pdf-classes","title":"Classes","text":""},{"location":"api/core/pdf.html#core.pdf-functions","title":"Functions","text":""},{"location":"api/core/pdf.html#core.pdf.ocr_pdf","title":"core.pdf.ocr_pdf","text":"<pre><code>ocr_pdf(\n    document: bytes,\n    output_images: bool = False,\n    path: str | None = None,\n    repair: int = 1,\n    upscale: int = 3,\n    contrast: float = 1.5,\n    sharpening: float = 1.2,\n    threshold: float = 0.4,\n    tesseract_lang: str = \"eng+fra+equ\",\n    tesseract_bin: str | None = None,\n) -&gt; str\n</code></pre> <p>Extract text from PDF using OCR through Tesseract. Both the binding Python package PyTesseract and the Tesseract binaries need to be installed.</p> <p>To run on a server where you don\u2019t have <code>sudo</code> access to install package, you will need to download the AppImage package and pass its path to the <code>tesseract_bin</code> argument.</p> <p>Tesseract uses machine-learning to identify words and needs the relevant language models to be installed on the system as well. Linux packaged version of Tesseract seem to generally ship French, English and equations (math) models by default. Other languages need to be installed manually,  see Tesseract docs for available packages. Use <code>pytesseract.get_languages(config='')</code> to list available language packages installed locally.</p> <p>The OCR is preceeded by an image processing step aiming at text reconstruction, by sharpening, increasing contrast and iteratively reconstructing holes in letters using an inpainting method in wavelets space. This is computationaly expensive, which may not be suitable to run on server.</p> PARAMETER DESCRIPTION <code>document</code> <p>the PDF document to open.</p> <p> TYPE: <code>bytes</code> </p> <code>output_images</code> <p>set to <code>True</code>, each page of the document is saved as PNG in the <code>path</code> directory before and after contrast enhancement. This is useful to tune the image contrast and sharpness enhancements, prior to OCR.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>repair</code> <p>number of iterations of enhancements (sharpening, contrast and inpainting) to perform. More iterations take longer, too many iterations might simplify their geometry (as if they were fluid and would drip, removing corners and pointy ends) in a way that actually degrades OCR.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>upscale</code> <p>upscaling factor to apply before enhancement. This can help recovering ink leaks but takes more memory and time to compute.</p> <p> TYPE: <code>int</code> DEFAULT: <code>3</code> </p> <code>contrast</code> <p><code>1.0</code> is the neutral value. Moves RGB values farther away from the threshold.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.5</code> </p> <code>sharpening</code> <p><code>1.0</code> is the neutral value. Increases sharpness. Values too high can produce ringing (replicated ghost edges).</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.2</code> </p> <code>threshold</code> <p>the reference value (fulcrum) for contrast enhancement. Good values are typically in the range 0.20-0.50.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.4</code> </p> <code>tesseract_lang</code> <p>the Tesseract command argument <code>-l</code> defining languages models to use for OCR. Languages are referenced by their 3-letters ISO-something code. See Tesseract doc for syntax and meaning. You can mix several languages by joining them with <code>+</code>.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'eng+fra+equ'</code> </p> <code>tesseract_bin</code> <p>the path to the Tesseract executable if it is not in the global CLI path. This is passed as-is to <code>pytesseract.pytesseract.tesseract_cmd</code> of the PyTesseract binding library.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>str</code> <p>All the retrieved text from all the PDF pages as a single string. No pagination is done.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>when using a language package is attempted while Tesseract has no such package installed.</p>"},{"location":"api/core/pdf.html#core.pdf.get_pdf_content","title":"core.pdf.get_pdf_content","text":"<pre><code>get_pdf_content(\n    url: str,\n    lang: str,\n    delay: DelayedClass,\n    file_path: str = None,\n    process_outline: bool = True,\n    category: str = None,\n    ocr: int = 1,\n    max_size: int = 20,\n    max_pages: int = 20,\n    custom_header: dict = {},\n    **kwargs: dict\n) -&gt; list[web_page]\n</code></pre> <p>Retrieve a PDF document through the network with HTTP GET or from the local filesystem, and parse its text content, using OCR if needed. This needs a functionnal network connection if <code>file_path</code> is not provided.</p> PARAMETER DESCRIPTION <code>url</code> <p>the online address of the document, or the downloading page if the doc  is not directly accessible from a GET request (for some old-schools website where downloads are inited from a  POST request to some PHP form handler, or publications behind a paywall).</p> <p> TYPE: <code>str</code> </p> <code>lang</code> <p>the ISO code of the language.</p> <p> TYPE: <code>str</code> </p> <code>file_path</code> <p>local path to the PDF file if the URL can\u2019t be directly fetched by GET request.  The content will be extracted from the local file but the original/remote URL  will still be referenced as the source.</p> <p> TYPE: <code>str</code> DEFAULT: <code>None</code> </p> <code>process_outline</code> <p>set to <code>True</code> to split the document according to its outline (table of content),  so each section will be in fact a document in itself. PDF pages are processed in full,  so sections are at least equal to a page length and there will be some overlapping.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>category</code> <p>arbitrary category or label set by user</p> <p> TYPE: <code>str</code> DEFAULT: <code>None</code> </p> <code>ocr</code> <ul> <li><code>0</code> disables any attempt at using OCR,</li> <li><code>1</code> enables OCR through Tesseract if no text was found in the PDF document</li> <li><code>2</code> forces OCR through Tesseract even when text was found in the PDF document. See core.pdf.ocr_pdf for info regarding the Tesseract environment. You will need to manually disable</li> </ul> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>max_size</code> <p>when attempting OCR on PDF files, files larger than this value (in MiB) will be ignored.</p> <p> TYPE: <code>int</code> DEFAULT: <code>20</code> </p> <code>max_pages</code> <p>when attempting OCR on PDF files, files having more pages than this value will be ignored.</p> <p> TYPE: <code>int</code> DEFAULT: <code>20</code> </p> <code>custom_header</code> <p>option HTTP headers to form the request that will download the PDF</p> <p> TYPE: <code>dict</code> DEFAULT: <code>{}</code> </p> PARAMETER DESCRIPTION <code>**kwargs</code> <p>directly passed-through to core.pdf.ocr_pdf. See this function documentation for more info.</p> <p> TYPE: <code>dict</code> </p> RETURNS DESCRIPTION <code>list[web_page]</code> <p>a list of core.types.web_page objects holding the text content and the PDF metadata</p>"},{"location":"api/core/search.html","title":"core.search","text":""},{"location":"api/core/search.html#core.search","title":"core.search","text":""},{"location":"api/core/search.html#core.search-classes","title":"Classes","text":""},{"location":"api/core/search.html#core.search.BM25PlusCSR","title":"core.search.BM25PlusCSR","text":"<pre><code>BM25PlusCSR(\n    corpus: list[list[int]],\n    word2vec,\n    k1: float = 1.7,\n    b: float = 0.3,\n    delta: float = 0.65,\n)\n</code></pre> <p>BM25+ with CSR inverted index: - doc_ids / tfs stored in contiguous arrays - indptr for token \u2192 posting list slicing - fully vectorized scoring</p>"},{"location":"api/core/search.html#core.search.search_methods","title":"core.search.search_methods","text":"<p>               Bases: <code>IntEnum</code></p> <p>Search methods available</p>"},{"location":"api/core/search.html#core.search.Indexer","title":"core.search.Indexer","text":"<pre><code>Indexer(\n    db: sqlite3.Connection,\n    name: str,\n    word2vec: Word2Vec,\n    strip_collocations: bool = False,\n    principal_components: int = 1,\n)\n</code></pre> <p>Search engine based on word similarity.</p> PARAMETER DESCRIPTION <code>db</code> <p>Opened SQLite database containing at least a <code>pages</code> table of core.types.web_page items saved as database.</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>name</code> <p>name under which the model will be saved for la ter reuse.</p> <p> TYPE: <code>str</code> </p> <code>word2vec</code> <p>the instance of word embedding model.</p> <p> TYPE: <code>Word2Vec</code> </p> <code>strip_collocations</code> <p>remove the matrix of collocations in documents, which is the list of word tokens represented by their index in the word2vec dictionnary. It is used for core.search.Indexer.find_query_pattern, which is optional and significatively slower (but not significatively better), so if you don\u2019t plan on using it, removing collocations saves some RAM and I/O.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>principal_components</code> <p>number of principal components to compute and remove from the index dataset.  This helps to make queries more selective and specific in the presence of boilerplate text and formatting language in the sampling.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> NOTE <p>The class is optimized to run online, on server: load fast when spawning a new server-side worker, use RAM sparingly.</p>"},{"location":"api/core/search.html#core.search.Indexer-attributes","title":"Attributes","text":""},{"location":"api/core/search.html#core.search.Indexer.sql","title":"core.search.Indexer.sql  <code>instance-attribute</code>","text":"<pre><code>sql: str = ''\n</code></pre> <p>Cache the previous SQL filtering conditions</p>"},{"location":"api/core/search.html#core.search.Indexer.word2vec","title":"core.search.Indexer.word2vec  <code>instance-attribute</code>","text":"<pre><code>word2vec: Word2Vec = word2vec\n</code></pre> <p>Word2Vec embedding language model</p>"},{"location":"api/core/search.html#core.search.Indexer.collocations","title":"core.search.Indexer.collocations  <code>instance-attribute</code>","text":"<pre><code>collocations: np.ndarray | None = None\n</code></pre> <p>Store the list of document tokens encoded by their index number in the Word2Vec vocabulary. Unknown tokens are discarded. This gives a symbolic and more compact representation of tokens collocations in documents (32 bits/token).</p> <p>Documents are on the first axis.</p>"},{"location":"api/core/search.html#core.search.Indexer.ranker","title":"core.search.Indexer.ranker  <code>instance-attribute</code>","text":"<pre><code>ranker: BM25PlusCSR = BM25PlusCSR(\n    corpus_token_indices, self.word2vec, k1=1.8, b=0.4, delta=0.8\n)\n</code></pre> <p>BM25+ CSR ranker (TF-IDF).</p>"},{"location":"api/core/search.html#core.search.Indexer.pc","title":"core.search.Indexer.pc  <code>instance-attribute</code>","text":"<pre><code>pc: np.ndarray = pca.components_\n</code></pre> <p>Principal component(s) of the dataset vectors (normalized)</p>"},{"location":"api/core/search.html#core.search.Indexer.vectors","title":"core.search.Indexer.vectors  <code>instance-attribute</code>","text":"<pre><code>vectors = self.normalize_pc(self.vectors)\n</code></pre> <p>Store the list of document-wise vector embeddings, where the vector represents the normalized centroid of tokens vectors contained the document. Documents are on the first axis.</p>"},{"location":"api/core/search.html#core.search.Indexer.index","title":"core.search.Indexer.index  <code>instance-attribute</code>","text":"<pre><code>index: list[str] = self.build_index(db)\n</code></pre> <p>LUT of document URLs as ordered when building the ranker, lazily loaded from the database.</p>"},{"location":"api/core/search.html#core.search.Indexer.url_to_index","title":"core.search.Indexer.url_to_index  <code>instance-attribute</code>","text":"<pre><code>url_to_index: dict[str, int] = self.build_index_reverse()\n</code></pre> <p>Reverse LUT of <code>self.index</code></p>"},{"location":"api/core/search.html#core.search.Indexer-functions","title":"Functions","text":""},{"location":"api/core/search.html#core.search.Indexer.init_search_table","title":"core.search.Indexer.init_search_table","text":"<pre><code>init_search_table(db: sqlite3.Connection)\n</code></pre> <p>Create or migrate the one-row search cache table.</p>"},{"location":"api/core/search.html#core.search.Indexer.init_stats_table","title":"core.search.Indexer.init_stats_table","text":"<pre><code>init_stats_table(db: sqlite3.Connection)\n</code></pre> <p>Create or migrate the plain-SQLite stats table.</p> <p>Scalar stats use <code>item = ''</code>. Grouped stats use <code>name</code> for the metric family and <code>item</code> for the domain/category/language key.</p>"},{"location":"api/core/search.html#core.search.Indexer.init_categories_table","title":"core.search.Indexer.init_categories_table","text":"<pre><code>init_categories_table(db: sqlite3.Connection)\n</code></pre> <p>Create a queryable catalog of all page categories.</p>"},{"location":"api/core/search.html#core.search.Indexer.init_pages_search_indexes","title":"core.search.Indexer.init_pages_search_indexes","text":"<pre><code>init_pages_search_indexes(db: sqlite3.Connection)\n</code></pre> <p>Add persistent indexes for the user-facing search filters.</p> <p>The URL primary key already gives us an index for URL lookups. The category indexes help equality filters and provide a stable ordering path for category-only queries. Substring filters such as <code>NOT LIKE '%github.com%'</code>, <code>instr(parsed, ?)</code>, and <code>REGEXP</code> cannot use a normal B-tree index, so <code>filter_contents()</code> narrows those to ranked candidate URLs before SQLite evaluates them.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_values","title":"core.search.Indexer.save_search_values","text":"<pre><code>save_search_values(db: sqlite3.Connection, values: dict)\n</code></pre> <p>Store one or more precomputed search cache values in the database.</p>"},{"location":"api/core/search.html#core.search.Indexer.clear_search_cache","title":"core.search.Indexer.clear_search_cache","text":"<pre><code>clear_search_cache(db: sqlite3.Connection)\n</code></pre> <p>Clear current heavy cache values before writing the refreshed cache.</p>"},{"location":"api/core/search.html#core.search.Indexer.get_search_values","title":"core.search.Indexer.get_search_values","text":"<pre><code>get_search_values(db: sqlite3.Connection, columns: list[str]) -&gt; any\n</code></pre> <p>Fetch one or more precomputed search cache values from the database.</p>"},{"location":"api/core/search.html#core.search.Indexer.get_legacy_search_values","title":"core.search.Indexer.get_legacy_search_values","text":"<pre><code>get_legacy_search_values(db: sqlite3.Connection, columns: list[str]) -&gt; any\n</code></pre> <p>Fetch legacy cache columns when loading an old DB before migration.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_index","title":"core.search.Indexer.save_search_index","text":"<pre><code>save_search_index(db: sqlite3.Connection, index: list[str])\n</code></pre> <p>Store the URL lookup table into <code>search.doc_index</code>.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_ranker","title":"core.search.Indexer.save_search_ranker","text":"<pre><code>save_search_ranker(db: sqlite3.Connection, ranker: BM25PlusCSR)\n</code></pre> <p>Store the BM25+ CSR ranker state into <code>search</code>.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_vectors","title":"core.search.Indexer.save_search_vectors","text":"<pre><code>save_search_vectors(db: sqlite3.Connection, vectors: np.ndarray)\n</code></pre> <p>Store the single global vector matrix into <code>search.vectors</code>.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_word2vec","title":"core.search.Indexer.save_search_word2vec","text":"<pre><code>save_search_word2vec(db: sqlite3.Connection)\n</code></pre> <p>Store Word2Vec vocabulary and embedding matrices into the search cache.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_tokenizer","title":"core.search.Indexer.save_search_tokenizer","text":"<pre><code>save_search_tokenizer(db: sqlite3.Connection)\n</code></pre> <p>Store the tokenizer\u2019s large n-gram state in the search cache.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_search_stats","title":"core.search.Indexer.save_search_stats","text":"<pre><code>save_search_stats(db: sqlite3.Connection, stats: dict)\n</code></pre> <p>Store cheap display and diagnostic metadata in plain SQLite rows.</p>"},{"location":"api/core/search.html#core.search.Indexer.save_categories_index","title":"core.search.Indexer.save_categories_index","text":"<pre><code>save_categories_index(db: sqlite3.Connection, category_counts: dict[str, int])\n</code></pre> <p>Store all existing non-empty categories and their page counts.</p>"},{"location":"api/core/search.html#core.search.Indexer.build_stats","title":"core.search.Indexer.build_stats","text":"<pre><code>build_stats(db: sqlite3.Connection) -&gt; dict\n</code></pre> <p>Compute index metadata while the database is writable/prepared.</p>"},{"location":"api/core/search.html#core.search.Indexer.array_to_raw","title":"core.search.Indexer.array_to_raw  <code>staticmethod</code>","text":"<pre><code>array_to_raw(array: np.ndarray) -&gt; sqlite3.Binary\n</code></pre> <p>Store arrays without the <code>.npy</code> wrapper used by SQLite converters. Raw contiguous blobs hydrate faster with <code>np.frombuffer()</code> at runtime.</p>"},{"location":"api/core/search.html#core.search.Indexer.normalize_pc","title":"core.search.Indexer.normalize_pc","text":"<pre><code>normalize_pc(vector: np.ndarray) -&gt; np.ndarray\n</code></pre> <p>Remove the principal component of the dataset to the vector. This helps removing stopwords, webpage boilerplates (menu, sidebars), formatting language and SEO junk, and makes cosine similarity between query and documents more specific.</p> <p>Taken from A simple but tough-to-beat baseline for sentence embeddings, Sanjeev Arora, Yingyu Liang, Tengyu Ma. https://openreview.net/pdf?id=SyK00v5xx</p> PARAMETER DESCRIPTION <code>vector</code> <p>can be a single vector (1D) or a document-wise stack of vectors (2D). We always consider the embedding vector to be on the last axis, document-wise vectors should be vertically stacked.</p> <p> TYPE: <code>np.ndarray</code> </p> <p>Return:     normalized vector</p>"},{"location":"api/core/search.html#core.search.Indexer.filter_contents","title":"core.search.Indexer.filter_contents","text":"<pre><code>filter_contents(\n    db: sqlite3.Connection,\n    sql_query: str = \"\",\n    sql_params: list[str] | None = None,\n    candidate_indices: np.ndarray | list[int] | None = None,\n) -&gt; list[int]\n</code></pre> <p>Filter pages by arbitrary SQL queries</p>"},{"location":"api/core/search.html#core.search.Indexer.load","title":"core.search.Indexer.load  <code>classmethod</code>","text":"<pre><code>load(name: str, db: sqlite3.Connection)\n</code></pre> <p>Load an existing trained model by its name from the <code>../models</code> folder.</p>"},{"location":"api/core/search.html#core.search.Indexer.tokenize_query","title":"core.search.Indexer.tokenize_query","text":"<pre><code>tokenize_query(\n    query: str,\n    language: str | None = None,\n    meta_tokens: bool = True,\n    n_grams: bool = True,\n) -&gt; list[str]\n</code></pre> <p>Tokenize a query string, returning only tokens known to our vocabulary.</p>"},{"location":"api/core/search.html#core.search.Indexer.vectorize_query","title":"core.search.Indexer.vectorize_query","text":"<pre><code>vectorize_query(tokenized_query: list[str]) -&gt; np.ndarray\n</code></pre> <p>Prepare a text search query: cleanup, tokenize and get the centroid vector.</p> RETURNS DESCRIPTION <code>np.ndarray</code> <p>tuple[vector, norm, tokens]</p>"},{"location":"api/core/search.html#core.search.Indexer.find_query_pattern","title":"core.search.Indexer.find_query_pattern","text":"<pre><code>find_query_pattern(\n    indexed_query: np.ndarray[np.int32],\n    documents: list[tuple[int, str, float]],\n    fast: bool = False,\n) -&gt; list[tuple[int, str, float]]\n</code></pre> <p>The rankers methods treat documents as continuous bag of words (CBOW). As such, they are good for topic extraction (aboutness), but they do not care about words colocations and ordering, therefore they loose syntactical meaning.</p> <p>This method adds an additional layer of detection using convolution filters that will detect word sequences, direct or reversed, and correct the similarity factor set by the other ranking methods using that collocation factor.</p> <p>Its major drawback is to be 100 to 500 times slower than the other rankers, due to 2D convolutions, which means it needs to run on a subset of the search index, after previous methods were tried, to refine a previous ranking.</p> PARAMETER DESCRIPTION <code>indexed_query</code> <p>the search query tokens translated into their integer indices in the Word2Vec vocabulary. Use core.nlp.Word2Vec.tokens_to_indices to convert the tokenized query.</p> <p> TYPE: <code>np.ndarray[np.int32]</code> </p> <code>documents</code> <p>a symbolic list of documents, as a <code>(index, url, similarity)</code> tuple.</p> <p> TYPE: <code>list[tuple[int, str, float]]</code> </p> <code>fast</code> <p>if <code>True</code>, uses a simplified variant that is 6 times faster and only uses local averages. Results from this method are rather inaccurate, for example, for a request like <code>token_1 token_2</code>, sentences repeating <code>token_1</code> twice will score as much as sentences containing the desired sequence <code>token_1 token_2</code>. If <code>False</code>, use the convolutional filter.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> References <p>Text Matching as Image Recognition, Liang Pang, Yanyan Lan, Jiafeng Guo, Jun Xu, Shengxian Wan, and Xueqi Cheng. (2016). https://arxiv.org/pdf/1602.06359.pdf</p>"},{"location":"api/core/search.html#core.search.Indexer.rrf","title":"core.search.Indexer.rrf","text":"<pre><code>rrf(ranks_1: np.ndarray, ranks_2: np.ndarray, coeff: float = 60) -&gt; np.ndarray\n</code></pre> <p>Reciprocal Rank Fusion</p> <p>Aggregate 2 sets of page rankings obtained from different semantic geometries and weighted differently.</p> <p>From Reciprocal rank fusion outperforms condorcet and individual rank learning methods, Gordon V. Cormack, Charles L A Clarke, Stefan Buettcher. https://dl.acm.org/doi/10.1145/1571941.1572114</p>"},{"location":"api/core/search.html#core.search.Indexer.rank","title":"core.search.Indexer.rank","text":"<pre><code>rank(\n    db: sqlite3.Connection,\n    tokens: list[str],\n    method: search_methods,\n    n_results: int = 500,\n    fine_search: bool = False,\n    sql_query: str = \"\",\n    sql_params: list[str] = [],\n) -&gt; list[tuple[int, str, float]]\n</code></pre> <p>Apply a label on a post based on the trained model.</p> PARAMETER DESCRIPTION <code>db</code> <p>the SQLite database holding the indexed set of document. This database must absolutely be up-to-date with the one used to instanciate this class, regarding row ordering of documents, otherwise rowid mismatches are to be expected between fuzzy, AI and regex searches. tokens: the tokenized query.</p> <p> TYPE: <code>sqlite3.Connection</code> </p> <code>method</code> <p><code>ai</code>, <code>fuzzy</code> or <code>grep</code>:     - <code>ai</code> use word embedding and meta-tokens with dual-embedding space,      - <code>fuzzy</code> uses meta-tokens with BM25Okapi stats model,      - <code>grep</code> uses direct string and regex search.</p> <p> TYPE: <code>search_methods</code> </p> <code>n_results</code> <p>number of results to retain</p> <p> TYPE: <code>int</code> DEFAULT: <code>500</code> </p> <code>fine_search</code> <p>optionally refine the search using a 2D interaction matrix. See [1]</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>sql_query</code> <p>SQL query to narrow-down the search, for example <code>WHERE field = value</code>. Supports PCRE regex with <code>WHERE field REGEXP 'pattern'</code>.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> <code>sql_params</code> <p>the SQL parameters such that: <pre><code>    cursor = db.execute(\n    f\"SELECT url FROM pages {sql_query}\",\n    sql_params\n)\n</code></pre> where each <code>sql_params</code> item is matched in the <code>sql_query</code> by a <code>?</code>. For example: <pre><code>    SELECT url              // imposed by the search API\n    FROM pages              // imposed by the search API\n    WHERE instr(url, ?) &gt; 0 // implementation-side `sql_query`\n    ORDER BY url            // imposed by the search API\n</code></pre> and <code>sql_params = ['google.com']</code> will filter all URLs from Google.</p> <p> TYPE: <code>list[str]</code> DEFAULT: <code>[]</code> </p> Note <p>Both SQL search into the database and Python filtering into the index are supported, and can be combined. The local index is a partial copy of the database and is already a Python object, so it will be faster to filter if you only need to parse the copied data to filter in/out.</p> RETURNS DESCRIPTION <code>list</code> <p>the list of best-matching results as (rank, url, similarity) tuples.</p> <p> TYPE: <code>list[tuple[int, str, float]]</code> </p>"},{"location":"api/core/search.html#core.search.Indexer.get_related","title":"core.search.Indexer.get_related","text":"<pre><code>get_related(tokens: list[str], n: int = 15, k: int = 5) -&gt; list\n</code></pre> <p>Get the n closest keywords from the query.</p>"},{"location":"api/core/search.html#core.search-functions","title":"Functions","text":""},{"location":"api/core/secretary.html","title":"core.secretary","text":""},{"location":"api/core/secretary.html#core.secretary","title":"core.secretary","text":"<p>High level manager of all server connectors and bank of filters:</p> <ul> <li>load available connector modules,</li> <li>pass them the content of the relevant <code>settings.ini</code> section,</li> <li>open and close the remote connections to servers by calling internal connector methods,</li> <li>process all filters of a given config subfolder in sequence by calling the internal <code>run_filters</code> method of each connector.</li> </ul> <p>\u00a9 2022-2023 Aur\u00e9lien Pierre</p>"},{"location":"api/core/secretary.html#core.secretary-classes","title":"Classes","text":""},{"location":"api/core/secretary.html#core.secretary.Secretary","title":"core.secretary.Secretary","text":"<pre><code>Secretary(subfolder_path: str, server_mode: bool, number: int, force: bool)\n</code></pre> <p>               Bases: <code>object</code></p> <p>Backbone class managing the collection of available connectors. It is called from <code>src/main.py</code>.</p> <p>Load configuration from <code>settings.ini</code> file in the current folder. Load all connector modules from <code>protocols</code>.</p> PARAMETER DESCRIPTION <code>subfolder_path</code> <p>the current folder of filters</p> <p> TYPE: <code>str</code> </p> <code>server_mode</code> <p>enable or disable the server mode, which makes processing slower to better share limited resources on a server.</p> <p> TYPE: <code>bool</code> </p> <code>number</code> <p>override the number of items to process defined in config file. <code>-1</code> honors config files settings.</p> <p> TYPE: <code>int</code> </p> <code>force</code> <p>ignore logs and reprocess items already processed.</p> <p> TYPE: <code>bool</code> </p> ATTRIBUTE DESCRIPTION <code>protocols</code> <p>connectors.Server): available core.connectors.Server implementations for server protocols. They are exposed to user filters in <code>globals()</code>.</p> <p> TYPE: <code>dict of str</code> </p> <code>config_file</code> <p>object handling the <code>settings.ini</code> content for the current config folder.</p> <p> TYPE: <code>configparser.ConfigParser</code> </p> <code>log_file</code> <p>object handling the main <code>sync.log</code> for the whole application, where every action on core.connectors.Content will be logged.</p> <p> TYPE: <code>io.TextIOWrapper</code> </p>"},{"location":"api/core/secretary.html#core.secretary.Secretary-functions","title":"Functions","text":""},{"location":"api/core/secretary.html#core.secretary.Secretary.load_connectors","title":"core.secretary.Secretary.load_connectors","text":"<pre><code>load_connectors()\n</code></pre> <p>Iterate over all connectors for which we have both an implementation and credentials in the congig file. Initialize them and append them to the dictionnary protocols</p>"},{"location":"api/core/secretary.html#core.secretary.Secretary.close_connectors","title":"core.secretary.Secretary.close_connectors","text":"<pre><code>close_connectors()\n</code></pre> <p>Iterate over all initialized connectors and close the connections. Then close the logfile.</p>"},{"location":"api/core/secretary.html#core.secretary.Secretary.filters","title":"core.secretary.Secretary.filters","text":"<pre><code>filters(filters: utils.filter_bank)\n</code></pre> <p>Process the loop over <code>Content</code> objects from the implementation of core.connectors.Server.run_filters for each filter.</p> PARAMETER DESCRIPTION <code>filters</code> <p>iterable of available filters. See core.utils.filter_bank.</p> <p> TYPE: <code>utils.filter_bank</code> </p>"},{"location":"api/core/secretary.html#core.secretary.Secretary.server_breathe","title":"core.secretary.Secretary.server_breathe","text":"<pre><code>server_breathe()\n</code></pre> <p>If server mode is enabled, introduce a 0.5 s timeout to let other processes run. This is useful where heavy text parsing happens</p>"},{"location":"api/core/types.html","title":"core.types","text":""},{"location":"api/core/types.html#core.types","title":"core.types","text":""},{"location":"api/core/types.html#core.types-classes","title":"Classes","text":""},{"location":"api/core/types.html#core.types.web_page","title":"core.types.web_page","text":"<p>               Bases: <code>TypedDict</code></p> <p>Typed dictionnary representing a web page and its metadata. It can also be used for any text document having an URL/URI.</p> <p>The database module automatically uses this dictionnary\u2019s keys to create DB columns when generating the <code>web_page</code> DB. Therefore, keys needs to be kept in sync along all the modules, that is modules should not add their own custom keys.</p> <p>PEP 705 adds the ability to declare read-only typed dictionnaries, which should be used here as soon as it gets merged to stable Python to forbid custom keys definitions in <code>web_pages</code> instances from other modules.</p>"},{"location":"api/core/types.html#core.types.web_page-attributes","title":"Attributes","text":""},{"location":"api/core/types.html#core.types.web_page.title","title":"core.types.web_page.title  <code>instance-attribute</code>","text":"<pre><code>title: str\n</code></pre> <p>Title of the page</p>"},{"location":"api/core/types.html#core.types.web_page.url","title":"core.types.web_page.url  <code>instance-attribute</code>","text":"<pre><code>url: str\n</code></pre> <p>Where to find the page on the network. Can be a local or distant URI, with or without protocol, or even an unique identifier.</p>"},{"location":"api/core/types.html#core.types.web_page.domain","title":"core.types.web_page.domain  <code>instance-attribute</code>","text":"<pre><code>domain: str\n</code></pre> <p>Domain of the url</p>"},{"location":"api/core/types.html#core.types.web_page.date","title":"core.types.web_page.date  <code>instance-attribute</code>","text":"<pre><code>date: str\n</code></pre> <p>Date of the last modification of the page, to assess relevance of the content, as a string.</p>"},{"location":"api/core/types.html#core.types.web_page.content","title":"core.types.web_page.content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>The actual content of the page in a human-readable way.</p>"},{"location":"api/core/types.html#core.types.web_page.excerpt","title":"core.types.web_page.excerpt  <code>instance-attribute</code>","text":"<pre><code>excerpt: str\n</code></pre> <p>Shortened version of the content for search results previews. Typically provided as <code>description</code> meta tag by websites.</p>"},{"location":"api/core/types.html#core.types.web_page.h1","title":"core.types.web_page.h1  <code>instance-attribute</code>","text":"<pre><code>h1: list\n</code></pre> <p>Title of the post if any. There should be only one h1 per page, matching title, but some templates wrongly use h1 for section titles.</p>"},{"location":"api/core/types.html#core.types.web_page.h2","title":"core.types.web_page.h2  <code>instance-attribute</code>","text":"<pre><code>h2: list\n</code></pre> <p>Section titles if any</p>"},{"location":"api/core/types.html#core.types.web_page.lang","title":"core.types.web_page.lang  <code>instance-attribute</code>","text":"<pre><code>lang: str\n</code></pre> <p>2-letters ISO code of the page language. Not used internally, it\u2019s important only if you need to use it in implementations.</p>"},{"location":"api/core/types.html#core.types.web_page.category","title":"core.types.web_page.category  <code>instance-attribute</code>","text":"<pre><code>category: str\n</code></pre> <p>Arbitrary category, tag or label set by user, to be reused for example in AI document tagging.</p>"},{"location":"api/core/types.html#core.types.web_page.datetime","title":"core.types.web_page.datetime  <code>instance-attribute</code>","text":"<pre><code>datetime: dt\n</code></pre> <p>The page date as a <code>datetime.datetime</code> object directly usable</p>"},{"location":"api/core/types.html#core.types.web_page.length","title":"core.types.web_page.length  <code>instance-attribute</code>","text":"<pre><code>length: int\n</code></pre> <p>Characters length of <code>content</code></p>"},{"location":"api/core/types.html#core.types.web_page.parsed","title":"core.types.web_page.parsed  <code>instance-attribute</code>","text":"<pre><code>parsed: str\n</code></pre> <p>The normalized content of the page (lowercase, possibly converted to simple ASCII characters) for machine view over the content.</p>"},{"location":"api/core/types.html#core.types.web_page.tokenized","title":"core.types.web_page.tokenized  <code>instance-attribute</code>","text":"<pre><code>tokenized: list\n</code></pre> <p>List of parsed content text tokens, including metatokens, if needed, as a list of sentences, where sentences are themselves a list of string tokens. This is a basic tokenization meant to train n-grams, so it should retain enough semantics.</p>"},{"location":"api/core/types.html#core.types.web_page.stemmed","title":"core.types.web_page.stemmed  <code>instance-attribute</code>","text":"<pre><code>stemmed: list\n</code></pre> <p>Same as tokenized but stemmed and normalized on top.</p>"},{"location":"api/core/types.html#core.types.web_page.vectorized","title":"core.types.web_page.vectorized  <code>instance-attribute</code>","text":"<pre><code>vectorized: np.ndarray\n</code></pre> <p>Precomputed vector representation of the tokenized content.</p>"},{"location":"api/core/types.html#core.types.web_page.wayback","title":"core.types.web_page.wayback  <code>instance-attribute</code>","text":"<pre><code>wayback: str\n</code></pre> <p>URL of the page accessed through web.archive.org/Wayback Machine, so we can store the canonical URL in the <code>url</code> key.</p>"},{"location":"api/core/types.html#core.types.web_page.crawled","title":"core.types.web_page.crawled  <code>instance-attribute</code>","text":"<pre><code>crawled: dt\n</code></pre> <p>Date and time of the last crawling for this page.</p>"},{"location":"api/core/types.html#core.types.web_page.content_hash","title":"core.types.web_page.content_hash  <code>instance-attribute</code>","text":"<pre><code>content_hash: str\n</code></pre> <p>SHA1 hash of the parsed (normalized) content.</p>"},{"location":"api/core/types.html#core.types-functions","title":"Functions","text":""},{"location":"api/core/types.html#core.types.sanitize_web_page","title":"core.types.sanitize_web_page","text":"<pre><code>sanitize_web_page(page: web_page) -&gt; web_page\n</code></pre> <p>Ensure existence and validity of <code>web_page</code> keys/values.</p>"},{"location":"api/core/types.html#core.types.db_row_to_web_page","title":"core.types.db_row_to_web_page","text":"<pre><code>db_row_to_web_page(row: list[tuple[any]]) -&gt; web_page\n</code></pre> <p>Turn an SQL extraction of a full row containing a <code>web_page</code>. Columns are matched to keys in the same order. The database needs to be saved with columns in the right order, call core.types.sanitize_web_page first</p>"},{"location":"api/core/types.html#core.types.get_web_page_ram","title":"core.types.get_web_page_ram","text":"<pre><code>get_web_page_ram(item: web_page) -&gt; int\n</code></pre> <p>Get RAM usage of a web_page in bytes</p>"},{"location":"api/core/types.html#core.types.get_web_pages_ram","title":"core.types.get_web_pages_ram","text":"<pre><code>get_web_pages_ram(web_pages: list[web_page]) -&gt; int\n</code></pre> <p>Get RAM usage of a list of web_pages in bytes</p>"},{"location":"api/core/utils.html","title":"core.utils","text":""},{"location":"api/core/utils.html#core.utils","title":"core.utils","text":"<p>Logging and filter finding utilities.</p> <p>\u00a9 2022-2023 - Aur\u00e9lien Pierre</p>"},{"location":"api/core/utils.html#core.utils-attributes","title":"Attributes","text":""},{"location":"api/core/utils.html#core.utils.filter_entry","title":"core.utils.filter_entry  <code>module-attribute</code>","text":"<pre><code>filter_entry = TypedDict(\n    \"filter_entry\", {\"path\": str, \"filter\": str, \"protocol\": str}\n)\n</code></pre> <p>Dictionnary type representating a Virtual Secretary filter</p> ATTRIBUTE DESCRIPTION <code>path</code> <p>absolute path of the filter path.</p> <p> TYPE: <code>str</code> </p> <code>filter</code> <p>name of the filter filter, aka name of the filter itself.</p> <p> TYPE: <code>str</code> </p> <code>protocol</code> <p>server protocol, matching the name of one of the protocols.</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/utils.html#core.utils.filter_bank","title":"core.utils.filter_bank  <code>module-attribute</code>","text":"<pre><code>filter_bank = dict[int, filter_entry]\n</code></pre> <p>Dictionnary type of core.utils.filter_entry elements associated with their priority in the bank.</p> ATTRIBUTE DESCRIPTION <code>key</code> <p>priority</p> <p> TYPE: <code>int</code> </p> <code>value</code> <p>filter data</p> <p> TYPE: <code>filter_entry</code> </p>"},{"location":"api/core/utils.html#core.utils-classes","title":"Classes","text":""},{"location":"api/core/utils.html#core.utils.filter_mode","title":"core.utils.filter_mode","text":"<p>               Bases: <code>Enum</code></p> <p>Available filter types</p>"},{"location":"api/core/utils.html#core.utils.filter_mode-attributes","title":"Attributes","text":""},{"location":"api/core/utils.html#core.utils.filter_mode.PROCESS","title":"core.utils.filter_mode.PROCESS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PROCESS = 'process'\n</code></pre> <p>Filter applying write, edit or move actions</p>"},{"location":"api/core/utils.html#core.utils.filter_mode.LEARN","title":"core.utils.filter_mode.LEARN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LEARN = 'learn'\n</code></pre> <p>Filter applying machine-learning or read-only actions</p>"},{"location":"api/core/utils.html#core.utils-functions","title":"Functions","text":""},{"location":"api/core/utils.html#core.utils.now","title":"core.utils.now","text":"<pre><code>now() -&gt; str\n</code></pre> <p>Return current time for log lines</p>"},{"location":"api/core/utils.html#core.utils.match_filter_name","title":"core.utils.match_filter_name","text":"<pre><code>match_filter_name(file: str, mode: filter_mode)\n</code></pre> <p>Check if the current filter file matches the requested mode.</p> PARAMETER DESCRIPTION <code>file</code> <p>filter file to test</p> <p> TYPE: <code>str</code> </p> <code>mode</code> <p>filter type</p> <p> TYPE: <code>filter_mode</code> </p> RETURNS DESCRIPTION <code>match</code> <p> TYPE: <code>re.Match.group</code> </p>"},{"location":"api/core/utils.html#core.utils.find_filters","title":"core.utils.find_filters","text":"<pre><code>find_filters(path: str, filters: filter_bank, mode: filter_mode) -&gt; filter_bank\n</code></pre> <p>Find all the filter files in directory (aka filenames matching filter name pattern)  and append them to the dictionnary of filters based on their priority.  If 2 similar priorities are found, the first-defined one gets precedence, the other is discarded.</p> PARAMETER DESCRIPTION <code>path</code> <p>the folder where to find filter files</p> <p> TYPE: <code>str</code> </p> <code>filters</code> <p>the dictionnary where we will append filters found here. This dictionnary will have the integer priority of filters (order of running) set as keys. If filters with the same priority are found in the current path, former filters are overriden.</p> <p> TYPE: <code>filter_bank</code> </p> <code>mode</code> <p>the type of filter.</p> <p> TYPE: <code>filter_mode</code> </p>"},{"location":"api/core/utils.html#core.utils.lock_subfolder","title":"core.utils.lock_subfolder","text":"<pre><code>lock_subfolder(lockfile: str)\n</code></pre> <p>Write a <code>.lock</code> text file in the subfolder being currently processed, with the PID of the current Virtual Secretary instance.</p> <p>Override the lock if it contains a PID that doesn\u2019t exist anymore on the system (Linux-only).</p> PARAMETER DESCRIPTION <code>lockfile</code> <p>absolute path of the target lockfile</p> <p> TYPE: <code>str</code> </p> Todo <p>Make it work for Windows PID too.</p>"},{"location":"api/core/utils.html#core.utils.unlock_subfolder","title":"core.utils.unlock_subfolder","text":"<pre><code>unlock_subfolder(lockfile: str)\n</code></pre> <p>Remove the <code>.lock</code> file in current subfolder.</p> PARAMETER DESCRIPTION <code>lockfile</code> <p>absolute path of the target lockfile</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/utils.html#core.utils.imap_encode","title":"core.utils.imap_encode","text":"<pre><code>imap_encode(value: str) -&gt; bytes\n</code></pre> <p>Encode Python string into IMAP-compliant UTF-7 bytes, as described in the RFC 3501.</p> <p>There are variations, specific to IMAP4rev1, therefore the built-in python UTF-7 codec can\u2019t be used. The main difference is the shift character, used to switch from ASCII to base64 encoding context. This is \u201c&amp;\u201d in that modified UTF-7 convention, since \u201c+\u201d is considered as mainly used in mailbox names. Full description at RFC 3501, section 5.1.3.</p> <p>Code from imap_tools/imap_utf7.py by ikvk under Apache 2.0 license.</p> PARAMETER DESCRIPTION <code>value</code> <p>IMAP mailbox path as string</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>path</code> <p>IMAP-encoded path as UTF-7</p> <p> TYPE: <code>bytes</code> </p>"},{"location":"api/core/utils.html#core.utils.imap_decode","title":"core.utils.imap_decode","text":"<pre><code>imap_decode(value: bytes) -&gt; str\n</code></pre> <p>Decode IMAP-compliant UTF-7 byte into Python string, as described in the RFC 3501.</p> <p>There are variations, specific to IMAP4rev1, therefore the built-in python UTF-7 codec can\u2019t be used. The main difference is the shift character, used to switch from ASCII to base64 encoding context. This is \u201c&amp;\u201d in that modified UTF-7 convention, since \u201c+\u201d is considered as mainly used in mailbox names. Full description at RFC 3501, section 5.1.3.</p> <p>Code from imap_tools/imap_utf7.py by ikvk under Apache 2.0 license.</p> PARAMETER DESCRIPTION <code>value</code> <p>IMAP-encoded path as UTF-7 modified for IMAP</p> <p> TYPE: <code>bytes</code> </p> RETURNS DESCRIPTION <code>path</code> <p>IMAP path encoded as Python string</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/utils.html#core.utils.typography_undo","title":"core.utils.typography_undo","text":"<pre><code>typography_undo(string: str) -&gt; str\n</code></pre> <p>Break correct typographic Unicode entities into dummy computer characters (ASCII) to produce computer-standard vocabulary and help word tokenizers to properly detect word boundaries.</p> <p>This is useful when parsing:</p> <pre><code>1. **properly composed** text, like the output of LaTeX or SmartyPants[^1]/WP Scholar[^2],\n2. text typed with Dvorak-like keyboard layouts (using proper Unicode entities where needed).\n</code></pre> <p>For example, the proper <code>\u2026</code> ellipsis entity (Unicode U+2026 symbol) will be converted into 3 regular dots <code>...</code>.</p> <ol> <li> <p>https://daringfireball.net/projects/smartypants/ \u21a9</p> </li> <li> <p>https://eng.aurelienpierre.com/wp-scholar/ \u21a9</p> </li> </ol>"},{"location":"api/core/utils.html#core.utils.clean_whitespaces","title":"core.utils.clean_whitespaces","text":"<pre><code>clean_whitespaces(string: str) -&gt; str\n</code></pre> <p>Collapse repeated spaces and newlines in text.</p>"},{"location":"api/core/utils.html#core.utils.sanitize_unicode","title":"core.utils.sanitize_unicode","text":"<pre><code>sanitize_unicode(text) -&gt; str\n</code></pre> <p>Normalize arbitrary string-like objects into safe Python UTF-8 text.</p>"},{"location":"api/core/utils.html#core.utils.guess_date","title":"core.utils.guess_date","text":"<pre><code>guess_date(string: str | datetime) -&gt; datetime | None\n</code></pre> <p>Best-effort datetime parsing.</p> Always returns <ul> <li>timezone-aware UTC datetime</li> <li>or None</li> </ul>"},{"location":"api/core/utils.html#core.utils.get_data_folder","title":"core.utils.get_data_folder","text":"<pre><code>get_data_folder(filename: str, scheme: str, ext: str) -&gt; str\n</code></pre> <p>Resolve the path of a training data saved under <code>filename</code>. These are stored in <code>../../data/</code>.</p> Warning <p>This does not check the existence of the file and root folder.</p>"},{"location":"api/core/utils.html#core.utils.save_data","title":"core.utils.save_data","text":"<pre><code>save_data(data: list[web_page] | sqlite3.Connection, filename: str)\n</code></pre> <p>Save scraped data to a compressed archive.</p> <p>The destination folder and file extension are handled automatically.</p> PARAMETER DESCRIPTION <code>data</code> <p>Data to save.</p> <p>Supported types:</p> <ul> <li><code>list[web_page]</code>: saved as a <code>.pickle.tar.gz</code> archive using   Python pickling.</li> <li><code>sqlite3.Connection</code>: saved as a <code>.sql.tar.gz</code> archive using   an SQLite SQL dump.</li> </ul> <p> TYPE: <code>list[web_page] | sqlite3.Connection</code> </p> <code>filename</code> <p>Base filename to use. The output extension is added automatically depending on the type of <code>data</code>.</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/core/utils.html#core.utils.open_data","title":"core.utils.open_data","text":"<pre><code>open_data(\n    filename: str, scheme: str = \"auto\"\n) -&gt; list[web_page] | sqlite3.Connection\n</code></pre> <p>Open data stored in a tar.gz archive. We probe for <code>sql</code> and <code>pickle</code> datasets,  in this order, and return the first we find. </p> PARAMETER DESCRIPTION <code>filename</code> <p>Extension-less name of the dataset (no path).</p> <p> TYPE: <code>str</code> </p> <code>scheme</code> <ul> <li><code>sql</code> for data saved as SQL dumps, </li> <li><code>pickle</code> for data saved as lists of <code>web_page</code>.</li> <li><code>auto</code> will probe both in this order and return the first one found.</li> </ul> <p> TYPE: <code>str</code> DEFAULT: <code>'auto'</code> </p> RETURNS DESCRIPTION <code>list[web_page] | sqlite3.Connection</code> <ul> <li>list of <code>web_pages</code> for pickle archives,</li> </ul> <code>list[web_page] | sqlite3.Connection</code> <ul> <li>sqlite3.Connection for database archives. The database lives in memory and </li> </ul> <code>list[web_page] | sqlite3.Connection</code> <p>will not be saved, so the caller needs to copy/dump it, and close the connection.</p> <p>If the archive does not exist, returns an empty list.</p>"},{"location":"api/core/utils.html#core.utils.get_data_mtime","title":"core.utils.get_data_mtime","text":"<pre><code>get_data_mtime(filename: str, scheme: str) -&gt; datetime | None\n</code></pre> <p>Return the modification date of the tar.gz archive.</p> RETURNS DESCRIPTION <code>datetime | None</code> <p>datetime of the archive modification time, or None if it does not exist.</p>"},{"location":"api/core/utils.html#core.utils.get_models_folder","title":"core.utils.get_models_folder","text":"<pre><code>get_models_folder(filename: str) -&gt; str\n</code></pre> <p>Resolve the path of a machine-learning model saved under <code>filename</code>. These are stored in <code>../../models/</code>.</p> Warning <p>This does not check the existence of the file and root folder.</p>"},{"location":"api/core/utils.html#core.utils.get_stopwords_file","title":"core.utils.get_stopwords_file","text":"<pre><code>get_stopwords_file(filename: str) -&gt; dict\n</code></pre> <p>Get a dictionnary file containing lines of \u201cword: frequency\u201d stored in <code>../../models/</code>. By default, core.nlp.Word2Vec stores a such file when the word embedding is learned. Manually-validated files can be used for search engine purposes, since stopwords add noise to the searches.</p>"},{"location":"api/core/utils.html#core.utils.timeit","title":"core.utils.timeit","text":"<pre><code>timeit(runs: int = 1)\n</code></pre> <p>Provide a <code>@timeit</code> decorator to profile the wall performance of a function.</p> PARAMETER DESCRIPTION <code>runs</code> <p>how many times the function should be re-executed. Runtimes will give average and standard deviation.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p>"},{"location":"api/core/utils.html#core.utils.exit_after","title":"core.utils.exit_after","text":"<pre><code>exit_after(s: int)\n</code></pre> <p>Define a decorator <code>exit_after(n)</code> that stops a function after <code>n</code> seconds.</p> <p>Mostly intended for text parsing functions that get fed unchecked text inputs from the web. In that case, some really bad XML or super-long log files can make the parsing loop hang forever. This decorator will skip them without breaking the loop.</p> PARAMETER DESCRIPTION <code>s</code> <p>number of seconds</p> <p> TYPE: <code>int</code> </p>"},{"location":"api/core/utils.html#core.utils.get_past_n_months","title":"core.utils.get_past_n_months","text":"<pre><code>get_past_n_months(n: int) -&gt; datetime\n</code></pre> <p>Get the date of now minus n months</p>"},{"location":"api/protocols/carddav_server.html","title":"protocols.carddav_server","text":""},{"location":"api/protocols/carddav_server.html#protocols.carddav_server","title":"protocols.carddav_server","text":""},{"location":"api/protocols/imap_object.html","title":"protocols.imap_object","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object","title":"protocols.imap_object","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object-attributes","title":"Attributes","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object.regex_starter","title":"protocols.imap_object.regex_starter  <code>module-attribute</code>","text":"<pre><code>regex_starter = '(?&lt;=^|\\\\s|\\\\[|\\\\(|\\\\{|\\\\&lt;|\\\\\\'|\\\\\"|`|;|\\\\&gt;)'\n</code></pre> <p>Start of line, or start of document, or start of markup</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.regex_stopper","title":"protocols.imap_object.regex_stopper  <code>module-attribute</code>","text":"<pre><code>regex_stopper = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|\\\\&lt;)'\n</code></pre> <p>End of line, or end of document, or end of markup</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.end_of_word","title":"protocols.imap_object.end_of_word  <code>module-attribute</code>","text":"<pre><code>end_of_word = '(?=$|\\\\s|\\\\]|\\\\)|\\\\}|\\\\&gt;|\\\\\\'|\\\\\"|`|;|:|,|\\\\?|\\\\!|\\\\.|\\\\&lt;)'\n</code></pre> <p>End of word, or end of line, or end of document, or end of markup</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.regex_algebra","title":"protocols.imap_object.regex_algebra  <code>module-attribute</code>","text":"<pre><code>regex_algebra = '[\\\\+\\\\-\\\\=\\\\\u2260\\\\\u00b1]'\n</code></pre> <p>Algebraic signs</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.IP_PATTERN","title":"protocols.imap_object.IP_PATTERN  <code>module-attribute</code>","text":"<pre><code>IP_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_ip, regex_stopper), re.IGNORECASE\n)\n</code></pre> <p>IPv4 and IPv6 patterns where the whole IP is captured in the first group.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMAIL_PATTERN","title":"protocols.imap_object.EMAIL_PATTERN  <code>module-attribute</code>","text":"<pre><code>EMAIL_PATTERN = re.compile(\n    \"&lt;?([0-9a-z\\\\-\\\\_\\\\+\\\\.]+?@[0-9a-z\\\\-\\\\_\\\\+]+(\\\\.[0-9a-z\\\\_\\\\-]{2,})+)&gt;?\",\n    re.IGNORECASE,\n)\n</code></pre> <p>Emails patterns like <code>&lt;me@mail.com&gt;</code> or <code>me@mail.com</code> where the whole address is captured in the first group.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.URL_PATTERN","title":"protocols.imap_object.URL_PATTERN  <code>module-attribute</code>","text":"<pre><code>URL_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_url, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>URL patterns like <code>http(s)://domain.ext/page/subpage?q=x&amp;r=0:1#anchor</code> or <code>//domain.ext/page</code>. URL must follow RFC3986, meaning query parameters should be before anchors, if any. Relying on this assumption allows a faster regex parsing.</p> <ul> <li>the protocol (ftp, ftps, http, https) is captured as the first group,</li> <li><code>domain.ext</code> is captured as the second group,</li> <li><code>/page/etc</code> is the third group, including leading and trailing <code>/</code>,</li> <li>page query parameters <code>?s=x&amp;r=0</code>, including <code>?</code>, is the fourth group if the URL declares <code>...?params#anchor</code>,</li> <li>anchor <code>#anchor</code> is the fifth group, including <code>#</code>, if the URL declares <code>...?params#anchor</code>.</li> </ul> <p>URLs are captured if they are:</p> <ul> <li>alone on their own line,</li> <li>enclosed in <code>{}</code>, <code>[]</code>, <code>()</code></li> <li>enclosed in whitespaces.</li> </ul> <p>Warning: URLs enclosed in <code>()</code>, <code>[]</code> and <code>{}</code> may retain the closing sign  as part of the page name since <code>()</code> and <code>[]</code> are valid in URL pathes  and parameters. This pattern will work on plain text only: Markdown, XML, HTML and JSON  will need to be parsed ahead.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MEMBERS_PATTERN","title":"protocols.imap_object.MEMBERS_PATTERN  <code>module-attribute</code>","text":"<pre><code>MEMBERS_PATTERN = re.compile('(?&lt;=[a-z])(\\\\.)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Domain patterns without leading protocol like <code>cdn.company.com</code> or class members in object-oriented programming languages like <code>params.cookies.client</code>.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.DATE_PATTERN","title":"protocols.imap_object.DATE_PATTERN  <code>module-attribute</code>","text":"<pre><code>DATE_PATTERN = re.compile(date_regex, re.IGNORECASE)\n</code></pre> <p>Dates like <code>2022-12-01</code>, <code>01-12-2022</code>, <code>01-12-22</code>, <code>01/12/2022</code>, <code>01/12/22</code> where the whole date is captured in the first group, then each group of digits is captured in the order of appearance, in the next 3 groups</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.TIME_PATTERN","title":"protocols.imap_object.TIME_PATTERN  <code>module-attribute</code>","text":"<pre><code>TIME_PATTERN = re.compile(time_regex, re.IGNORECASE)\n</code></pre> <p>Identify more or less standard time patterns, like :</p> <ul> <li>12h15</li> <li>12:15</li> <li>12:15:00</li> <li>12am</li> <li>12 am</li> <li>12 h</li> <li>12:15:00Z</li> <li>12:15:00+01</li> <li>12:15:00 UTC+1</li> <li>11:27:45+0000</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>1- or 2-digits hour,</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>hour/minutes separator or half-day marker among <code>[\"h\", \":\", \"am\", \"pm\"]</code> (case-insensitive)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2-digits minutes, if any, or <code>None</code></p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>2-digits seconds, if any.</p> <p> TYPE: <code>str</code> </p> <code>4</code> <p>hour marker (<code>h</code> or <code>H</code>), half-day marker (case-insensitive <code>[\"am\", \"pm\"]</code>), or time zone marker (case-sensitive <code>[\"Z\", \"UTC\"]</code>)</p> <p> TYPE: <code>str</code> </p> <code>5</code> <p>1-or 2-digits signed integer timezone shift (referred to UTC).</p> <p> TYPE: <code>str</code> </p> <p>Examples:</p> <p>see https://regex101.com/r/QNtZAK/2</p> <p>see <code>src/tests/test-patterns.py</code></p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.DOMAIN_PATTERN","title":"protocols.imap_object.DOMAIN_PATTERN  <code>module-attribute</code>","text":"<pre><code>DOMAIN_PATTERN = re.compile(\n    \"from ((?:[a-z0-9\\\\-_]{0,61}\\\\.)+[a-z]{2,})\", re.IGNORECASE\n)\n</code></pre> <p>Matches patterns like <code>from (domain.ext)</code> from RFC-822 <code>Received</code> header in emails.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.UID_PATTERN","title":"protocols.imap_object.UID_PATTERN  <code>module-attribute</code>","text":"<pre><code>UID_PATTERN = re.compile('UID ([0-9]+)')\n</code></pre> <p>Matches email integer UID from IMAP headers.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.FLAGS_PATTERN","title":"protocols.imap_object.FLAGS_PATTERN  <code>module-attribute</code>","text":"<pre><code>FLAGS_PATTERN = re.compile('FLAGS \\\\((.*?)\\\\)')\n</code></pre> <p>Matches email flags from IMAP headers.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.PATH_PATTERN","title":"protocols.imap_object.PATH_PATTERN  <code>module-attribute</code>","text":"<pre><code>PATH_PATTERN = re.compile('%s%s%s' % (regex_starter, path_regex, end_of_word))\n</code></pre> <p>File path pattern like <code>~/file</code>, <code>/home/file</code>, <code>./file</code> or <code>C:\\windows</code></p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.PARTIAL_PATH_REGEX","title":"protocols.imap_object.PARTIAL_PATH_REGEX  <code>module-attribute</code>","text":"<pre><code>PARTIAL_PATH_REGEX = re.compile(\n    \"%s%s%s\" % (regex_starter, partial_path_regex, end_of_word)\n)\n</code></pre> <p>Partial, invalid path patterns missing the leading root, like <code>home/user/stuff</code>. We start capturing after at least two folder separators (slash or backslash).</p> <p>Warning</p> <p>this will collide with date detection, so run it after in the pipeline.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.RESOLUTION_PATTERN","title":"protocols.imap_object.RESOLUTION_PATTERN  <code>module-attribute</code>","text":"<pre><code>RESOLUTION_PATTERN = re.compile('\\\\d+(?:\u00d7|x|X)\\\\d+')\n</code></pre> <p>Pixel resolution like 10x20 or 10\u00d720. Units are discarded.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.NUMBER_PATTERN","title":"protocols.imap_object.NUMBER_PATTERN  <code>module-attribute</code>","text":"<pre><code>NUMBER_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_number, regex_stopper)\n)\n</code></pre> <p>Signed integers and decimals, fractions and numeric IDs with interal dashes and underscores. Numbers with starting or trailing units are not considered. Lazy decimals (.1 and 1.) are considered.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.HASH_PATTERN","title":"protocols.imap_object.HASH_PATTERN  <code>module-attribute</code>","text":"<pre><code>HASH_PATTERN = re.compile(\n    \"%s%s%s\" % (regex_starter, regex_hash, end_of_word), re.IGNORECASE\n)\n</code></pre> <p>Cryptographic hexadecimal hashes and fingerprints, of a min length of 8 characters.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MULTIPLE_LINES","title":"protocols.imap_object.MULTIPLE_LINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_LINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]{2,} ?)+')\n</code></pre> <p>Detect more than 2 newlines and tab, possibly mixed with spaces</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MULTIPLE_NEWLINES","title":"protocols.imap_object.MULTIPLE_NEWLINES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_NEWLINES = re.compile('(?: ?[\\\\t\\\\r\\\\n]+ ?){2,}')\n</code></pre> <p>Detect broken sequences of newlines and spaces.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.INTERNAL_NEWLINE","title":"protocols.imap_object.INTERNAL_NEWLINE  <code>module-attribute</code>","text":"<pre><code>INTERNAL_NEWLINE = re.compile('(?&lt;=\\\\w)[\\\\n\\\\t\\\\r]{1}(?=\\\\w)')\n</code></pre> <p>Detect single newline characters nested inside text. Mostly useful for parsed PDF where line wrapping is quite literal ( used instead of space).</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EXPOSURE","title":"protocols.imap_object.EXPOSURE  <code>module-attribute</code>","text":"<pre><code>EXPOSURE = re.compile(\n    \"%s%s%s\" % (regex_starter, exposure_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.PHOTOSPEED","title":"protocols.imap_object.PHOTOSPEED  <code>module-attribute</code>","text":"<pre><code>PHOTOSPEED = re.compile(\n    \"%s%s%s\" % (regex_starter, photospeed_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Exposure values in EV or IL</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SENSIBILITY","title":"protocols.imap_object.SENSIBILITY  <code>module-attribute</code>","text":"<pre><code>SENSIBILITY = re.compile(\n    \"%s%s%s\" % (regex_starter, sensibility_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Photographic sensibility in ISO or ASA</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.LUMINANCE","title":"protocols.imap_object.LUMINANCE  <code>module-attribute</code>","text":"<pre><code>LUMINANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, luminance_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Luminance/radiance in nits or Cd/m\u00b2</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.DIAPHRAGM","title":"protocols.imap_object.DIAPHRAGM  <code>module-attribute</code>","text":"<pre><code>DIAPHRAGM = re.compile(\n    \"%s%s\" % (regex_starter, diaphragm_regex), flags=re.IGNORECASE\n)\n</code></pre> <p>Photographic diaph aperture values like f/2.8 or f/11</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.GAIN","title":"protocols.imap_object.GAIN  <code>module-attribute</code>","text":"<pre><code>GAIN = re.compile(\n    \"%s%s%s\" % (regex_starter, gain_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Gain, attenuation and PSNR in dB</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.FILE_SIZE","title":"protocols.imap_object.FILE_SIZE  <code>module-attribute</code>","text":"<pre><code>FILE_SIZE = re.compile(\n    \"%s%s%s\" % (regex_starter, filesize_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>File and memory size in bit, byte, or octet and their multiples</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.DISTANCE","title":"protocols.imap_object.DISTANCE  <code>module-attribute</code>","text":"<pre><code>DISTANCE = re.compile(\n    \"%s%s%s\" % (regex_starter, distance_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Distance in meter, inch, foot and their multiples</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.PERCENT","title":"protocols.imap_object.PERCENT  <code>module-attribute</code>","text":"<pre><code>PERCENT = re.compile('%s%s%s' % (regex_starter, percent_regex, end_of_word))\n</code></pre> <p>Number followed by %</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.WEIGHT","title":"protocols.imap_object.WEIGHT  <code>module-attribute</code>","text":"<pre><code>WEIGHT = re.compile(\n    \"%s%s%s\" % (regex_starter, weight_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Weight (mass) in British and SI units and their multiples</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.ANGLE","title":"protocols.imap_object.ANGLE  <code>module-attribute</code>","text":"<pre><code>ANGLE = re.compile(\n    \"%s%s%s\" % (regex_starter, angle_regex, end_of_word), flags=re.IGNORECASE\n)\n</code></pre> <p>Angles in radians, degrees and steradians</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.TEMPERATURE","title":"protocols.imap_object.TEMPERATURE  <code>module-attribute</code>","text":"<pre><code>TEMPERATURE = re.compile(\n    \"%s%s%s\" % (regex_starter, temperature_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Temperatures in \u00b0C, \u00b0F and K</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.FREQUENCY","title":"protocols.imap_object.FREQUENCY  <code>module-attribute</code>","text":"<pre><code>FREQUENCY = re.compile(\n    \"%s%s%s\" % (regex_starter, frequency_regex, end_of_word),\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>Frequencies in hertz and multiples</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.TEXT_DATES","title":"protocols.imap_object.TEXT_DATES  <code>module-attribute</code>","text":"<pre><code>TEXT_DATES = re.compile(\n    \"([0-9]{1,2})? (jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec|jan|f\u00e9v|mar|avr|mai|jui|jui|aou|sep|oct|nov|d\u00e9c|janvier|f\u00e9vrier|mars|avril|mai|juin|juillet|ao\u00fbt|septembre|octobre|novembre|d\u00e9cembre|january|february|march|april|may|june|july|august|september|october|november|december)\\\\.?( [0-9]{1,2})?( [0-9]{2,4})(?!\\\\:)\",\n    flags=re.IGNORECASE | re.MULTILINE,\n)\n</code></pre> <p>Find textual dates formats:</p> <ul> <li>English dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> <li>French dates like <code>01 Jan 20</code> or <code>01 Jan. 2020</code> but avoid capturing adjacent time like <code>12:08</code>.</li> </ul> RETURNS DESCRIPTION <code>0</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>1</code> <p>month (full-form or abbreviated)</p> <p> TYPE: <code>str</code> </p> <code>2</code> <p>2 digits (day number or year number, depending on language)</p> <p> TYPE: <code>str</code> </p> <code>3</code> <p>4 digits (full year)</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.BASE_64","title":"protocols.imap_object.BASE_64  <code>module-attribute</code>","text":"<pre><code>BASE_64 = re.compile(\n    \"((?:[A-Za-z0-9+\\\\/]{4}){64,}(?:[A-Za-z0-9+\\\\/]{2}==|[A-Za-z0-9+\\\\/]{3}=)?)\"\n)\n</code></pre> <p>Identifies base64 encoding</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.BB_CODE","title":"protocols.imap_object.BB_CODE  <code>module-attribute</code>","text":"<pre><code>BB_CODE = re.compile('\\\\[(img|quote)[a-zA-Z0-9 =\\\\\"]*?\\\\].*?\\\\[\\\\/\\\\1\\\\]')\n</code></pre> <p>Identifies left-over BB code markup <code>[img]</code> and <code>[quote]</code></p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MARKUP","title":"protocols.imap_object.MARKUP  <code>module-attribute</code>","text":"<pre><code>MARKUP = re.compile('(?:\\\\[|\\\\{|\\\\&lt;)([^\\\\n\\\\r]+?)(?:\\\\]|\\\\}|\\\\&gt;)')\n</code></pre> <p>Identifies left-over HTML and Markdown markup, like <code>&lt;...&gt;</code>, <code>{...}</code>, <code>[...]</code></p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.USER","title":"protocols.imap_object.USER  <code>module-attribute</code>","text":"<pre><code>USER = re.compile('([\\\\w\\\\-\\\\+\\\\.]+)?@([\\\\w\\\\-\\\\+\\\\.]+)|(user\\\\-?\\\\d+)')\n</code></pre> <p>Identifies user handles or emails</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.REPEATED_CHARACTERS","title":"protocols.imap_object.REPEATED_CHARACTERS  <code>module-attribute</code>","text":"<pre><code>REPEATED_CHARACTERS = re.compile('(.)\\\\1{9,}')\n</code></pre> <p>Identifies any character repeated more than 9 times</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.UNFINISHED_SENTENCES","title":"protocols.imap_object.UNFINISHED_SENTENCES  <code>module-attribute</code>","text":"<pre><code>UNFINISHED_SENTENCES = re.compile('(?&lt;![?!.;:])\\\\n\\\\n|\\\\r\\\\n')\n</code></pre> <p>Identifies sentences finishing with 2 newlines characters without having ending punctuations</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MULTIPLE_DOTS","title":"protocols.imap_object.MULTIPLE_DOTS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DOTS = re.compile('\\\\.{2,}')\n</code></pre> <p>Identifies dots repeated more than twice</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MULTIPLE_DASHES","title":"protocols.imap_object.MULTIPLE_DASHES  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_DASHES = re.compile('[-~]{1,}')\n</code></pre> <p>Identifies dashes repeated more than once</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.MULTIPLE_QUESTIONS","title":"protocols.imap_object.MULTIPLE_QUESTIONS  <code>module-attribute</code>","text":"<pre><code>MULTIPLE_QUESTIONS = re.compile('\\\\?{1,}')\n</code></pre> <p>Identifies question marks repeated more than once</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.ORDINAL_FR","title":"protocols.imap_object.ORDINAL_FR  <code>module-attribute</code>","text":"<pre><code>ORDINAL_FR = re.compile('n\u00b0 ?([0-9]+)')\n</code></pre> <p>French ordinal numbers (num\u00e9ros n\u00b0)</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.FRANCAIS","title":"protocols.imap_object.FRANCAIS  <code>module-attribute</code>","text":"<pre><code>FRANCAIS = re.compile(\n    \"%s(j|t|s|d|qu|lorsqu|quelqu|jusqu|m|c|n)\\\\'(?=[aeiouy\u00e9\u00e8\u00e0\u00ea\u00e2\u00ee\u00f4\u00fb\u00ef\u00fc\u00e4\u00eb\u00f6h][\\\\w\\\\s])\"\n    % regex_starter,\n    flags=re.IGNORECASE,\n)\n</code></pre> <p>French contractions of pronouns and determinants</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.DASHES","title":"protocols.imap_object.DASHES  <code>module-attribute</code>","text":"<pre><code>DASHES = re.compile('(?&lt;=\\\\w)(-|_|=)+(?=\\\\w)', re.IGNORECASE)\n</code></pre> <p>Dashes in the middle of ASCII/Latin compounded words. Will not work if accented or Unicode characters are immediately surrounding the dash.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.ALTERNATIVES","title":"protocols.imap_object.ALTERNATIVES  <code>module-attribute</code>","text":"<pre><code>ALTERNATIVES = re.compile('(?&lt;=[a-z])(\\\\/)(?=[a-z])', re.IGNORECASE)\n</code></pre> <p>Slash-separated word alternatives like <code>and/or</code> <code>mr/mrs</code></p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.PLURAL_S","title":"protocols.imap_object.PLURAL_S  <code>module-attribute</code>","text":"<pre><code>PLURAL_S = re.compile('(?&lt;=[a-zA-Z]{4,})s?e{0,2}s%s' % end_of_word)\n</code></pre> <p>Identify plural form of nouns (French and English), adjectives (French) and third-person present verbs (English) and second-person verbs (French) in -s.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.FEMININE_E","title":"protocols.imap_object.FEMININE_E  <code>module-attribute</code>","text":"<pre><code>FEMININE_E = re.compile('(?&lt;=\\\\w{4,})e{1,2}%s' % end_of_word)\n</code></pre> <p>Identify feminine form of adjectives (French) in -e.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.DOUBLE_CONSONANTS","title":"protocols.imap_object.DOUBLE_CONSONANTS  <code>module-attribute</code>","text":"<pre><code>DOUBLE_CONSONANTS = re.compile(\n    \"(?&lt;=\\\\w{2,})([bcfghjklmnpqrstvwxz])\\\\1\", re.IGNORECASE\n)\n</code></pre> <p>Identify double consonants in the middle of words.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.FEMININE_TRICE","title":"protocols.imap_object.FEMININE_TRICE  <code>module-attribute</code>","text":"<pre><code>FEMININE_TRICE = re.compile('(?&lt;=\\\\w{4,})t(rice|eur|or)%s' % end_of_word)\n</code></pre> <p>Identify French feminine nouns in -trice.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.ADVERB_MENT","title":"protocols.imap_object.ADVERB_MENT  <code>module-attribute</code>","text":"<pre><code>ADVERB_MENT = re.compile('(?&lt;=\\\\w{4,})e?ment%s' % end_of_word)\n</code></pre> <p>Identify French adverbs and English nouns ending en -ment</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_TION","title":"protocols.imap_object.SUBSTANTIVE_TION  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_TION = re.compile('(?&lt;=\\\\w{4,})(t|s)ion%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from verbs by adding -tion and -sion</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_AT","title":"protocols.imap_object.SUBSTANTIVE_AT  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_AT = re.compile('(?&lt;=\\\\w{4,})at%s' % end_of_word)\n</code></pre> <p>Identify French and English substantives formed from other nouns by adding -at</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.PARTICIPLE_ING","title":"protocols.imap_object.PARTICIPLE_ING  <code>module-attribute</code>","text":"<pre><code>PARTICIPLE_ING = re.compile('(?&lt;=\\\\w{4,})ing%s' % end_of_word)\n</code></pre> <p>Identify English substantives and present participles formed from verbs by adding -ing</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.ADJECTIVE_ED","title":"protocols.imap_object.ADJECTIVE_ED  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_ED = re.compile('(?&lt;=\\\\w{4,})ed%s' % end_of_word)\n</code></pre> <p>Identify English adjectives formed from verbs by adding -ed</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.ADJECTIVE_TIF","title":"protocols.imap_object.ADJECTIVE_TIF  <code>module-attribute</code>","text":"<pre><code>ADJECTIVE_TIF = re.compile('(?&lt;=\\\\w{2,})ti(f|v)%s' % end_of_word)\n</code></pre> <p>Identify English and French adjectives formed from verbs by adding -tif or -tive</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_Y","title":"protocols.imap_object.SUBSTANTIVE_Y  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_Y = re.compile('(?&lt;=\\\\w{3,})y%s' % end_of_word)\n</code></pre> <p>Identify English substantives ending in -y</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.VERB_IZ","title":"protocols.imap_object.VERB_IZ  <code>module-attribute</code>","text":"<pre><code>VERB_IZ = re.compile('(?&lt;=\\\\w{4,})(i|y)z%s' % end_of_word)\n</code></pre> <p>Identify American verbs ending in -iz that French and Brits write in -is</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.STUFF_ER","title":"protocols.imap_object.STUFF_ER  <code>module-attribute</code>","text":"<pre><code>STUFF_ER = re.compile('(?&lt;=\\\\w{5,})er%s' % end_of_word)\n</code></pre> <p>Identify French 1st group verb (infinitive) and English substantives ending in -er</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.BRITISH_OUR","title":"protocols.imap_object.BRITISH_OUR  <code>module-attribute</code>","text":"<pre><code>BRITISH_OUR = re.compile('(?&lt;=\\\\w{3,})our%s' % end_of_word)\n</code></pre> <p>Identify British spelling ending in -our (colour, behaviour).</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_ITY","title":"protocols.imap_object.SUBSTANTIVE_ITY  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_ITY = re.compile('(?&lt;=\\\\w{4,})it(y|e)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ity (English) and -ite (French).</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_IST","title":"protocols.imap_object.SUBSTANTIVE_IST  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IST = re.compile('(?&lt;=\\\\w{3,})is(t|m)%s' % end_of_word)\n</code></pre> <p>Identify substantives in -ist and -ism.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_IQU","title":"protocols.imap_object.SUBSTANTIVE_IQU  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_IQU = re.compile('(?&lt;=\\\\w{3,})i(qu|c)%s' % end_of_word)\n</code></pre> <p>Identify French substantives in -iqu</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.SUBSTANTIVE_EUR","title":"protocols.imap_object.SUBSTANTIVE_EUR  <code>module-attribute</code>","text":"<pre><code>SUBSTANTIVE_EUR = re.compile('(?&lt;=\\\\w{3,})eur%s' % end_of_word)\n</code></pre> <p>Identify French substantives -eur</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.HYPHENIZED","title":"protocols.imap_object.HYPHENIZED  <code>module-attribute</code>","text":"<pre><code>HYPHENIZED = re.compile('(?&lt;=\\\\w{3,})[-\u2013\u2014]+ *[\\\\n\\\\r]{1,2}(?=\\\\w)')\n</code></pre> <p>Detect hyphenized words at the end of a PDF text line.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.WAYBACK_RE","title":"protocols.imap_object.WAYBACK_RE  <code>module-attribute</code>","text":"<pre><code>WAYBACK_RE = re.compile('https?://web\\\\.archive\\\\.org/web/[^/]+/(https?://.+)')\n</code></pre> <p>Find the canonical URL from web.archive.org (Wayback Machine) URLs</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object-classes","title":"Classes","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail","title":"protocols.imap_object.EMail","text":"<pre><code>EMail(raw_message: list, server)\n</code></pre> <p>               Bases: <code>connectors.Content</code></p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail-attributes","title":"Attributes","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.urls","title":"protocols.imap_object.EMail.urls  <code>instance-attribute</code>","text":"<pre><code>urls = []\n</code></pre> <p><code>list[tuple[str]]</code>: List of URLs found in email body.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.ips","title":"protocols.imap_object.EMail.ips  <code>instance-attribute</code>","text":"<pre><code>ips = []\n</code></pre> <p><code>list[str]</code>: List of IPs found in the server delivery route (in <code>Received</code> headers)</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.domains","title":"protocols.imap_object.EMail.domains  <code>instance-attribute</code>","text":"<pre><code>domains = []\n</code></pre> <p><code>list[str]</code>: List of domains found in the server delivery route (in <code>Received</code> headers)</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.server","title":"protocols.imap_object.EMail.server  <code>instance-attribute</code>","text":"<pre><code>server: 'Server'\n</code></pre> <p><code>(Server)</code>: back-reference to the Server instance from which the current email is extracted.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.msg","title":"protocols.imap_object.EMail.msg  <code>instance-attribute</code>","text":"<pre><code>msg: email.message.EmailMessage = email.message_from_bytes(\n    self.raw, policy=policy.default\n)\n</code></pre> <p>Standard Python email object</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail-functions","title":"Functions","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.has_header","title":"protocols.imap_object.EMail.has_header","text":"<pre><code>has_header(header: str) -&gt; bool\n</code></pre> <p>Check if the case-insensitive <code>header</code> exists in the email headers.</p> PARAMETER DESCRIPTION <code>header</code> <p>the RFC 822 email header.</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>presence of the header</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.get_sender","title":"protocols.imap_object.EMail.get_sender","text":"<pre><code>get_sender() -&gt; list[list, list]\n</code></pre> <p>Get the full list of senders of the email, using the <code>From</code> header, splitting their name (if any) apart from their address.</p> RETURNS DESCRIPTION <code>list[list, list]</code> <p><code>list[0]</code> contains the list of names, rarely used, <code>list[1]</code> is the list of email addresses.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.parse_urls","title":"protocols.imap_object.EMail.parse_urls","text":"<pre><code>parse_urls(input: str) -&gt; list[tuple]\n</code></pre> <p>Update <code>self.urls</code> with a list of all URLs found in <code>input</code>, split as <code>(domain, page)</code> tuples.</p> <p>Examples:</p> <p>Each result in the list is a tuple (domain, page), for example :</p> <ul> <li><code>google.com/index.php</code> is broken into <code>('google.com', '/index.php')</code></li> <li><code>google.com/</code> is broken into <code>('google.com', '/')</code></li> <li><code>google.com/login.php?id=xxx</code> is broken into <code>('google.com', '/login.php')</code></li> </ul>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.get_body","title":"protocols.imap_object.EMail.get_body","text":"<pre><code>get_body(preferencelist=('related', 'html', 'plain')) -&gt; str\n</code></pre> <p>Get the body of the email.</p> PARAMETER DESCRIPTION <code>preferencelist</code> <p>sequence of candidate properties in which to pick the email body, by order of priority. If set to <code>\"plain\"</code>, return either the plain-text variant of the email if any, or build one by removing (x)HTML markup from the HTML variant if no plain-text variant is available.</p> <p> TYPE: <code>tuple | str</code> DEFAULT: <code>('related', 'html', 'plain')</code> </p> Note <p>Emails using <code>quoted-printable</code> transfer encoding but not UTF-8 charset are not handled. This weird combination has been met only in spam messages written in Russian, so far, and should not affect legit emails.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_in","title":"protocols.imap_object.EMail.is_in","text":"<pre><code>is_in(\n    query_list: list[str] | str,\n    field: str,\n    case_sensitive: bool = False,\n    mode: str = \"any\",\n) -&gt; bool\n</code></pre> <p>Check if any or all of the elements in the <code>query_list</code> is in the email <code>field</code>.</p> PARAMETER DESCRIPTION <code>query_list</code> <p>list of keywords or unique keyword to find in <code>field</code>.</p> <p> TYPE: <code>list[str] | str</code> </p> <code>field</code> <p>any RFC 822 header or <code>\"body\"</code>.</p> <p> TYPE: <code>str</code> </p> <code>case_sensitive</code> <p><code>True</code> if the search should be case-sensitive. This has no effect if <code>field</code> is a RFC 822 header, it only applies to the email body.</p> <p> TYPE: <code>str</code> DEFAULT: <code>False</code> </p> <code>mode</code> <p><code>\"any\"</code> if any element in <code>query_list</code> should be found in <code>field</code> to return <code>True</code>. <code>\"all\"</code> if all elements in <code>query_list</code> should be found in <code>field</code> to return <code>True</code>.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'any'</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> if any or all elements (depending on <code>mode</code>) of <code>query_list</code> have been found in <code>field</code>.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.tag","title":"protocols.imap_object.EMail.tag","text":"<pre><code>tag(keyword: str)\n</code></pre> <p>Add any arbitrary IMAP tag (aka label), standard or not, to the current email.</p> Warning <p>In Mozilla Thunderbird, labels/tags need to be configured first in the preferences (by mapping the label string to a color) to properly appear in the GUI. Otherwise, any undefined tag will be identified as \u201cImportant\u201d (associated with red), no matter its actual string.</p> <p>Horde, Roundcube and Nextcloud mail (based on Horde) treat those properly.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.untag","title":"protocols.imap_object.EMail.untag","text":"<pre><code>untag(keyword: str)\n</code></pre> <p>Remove any arbitrary IMAP tag (aka label), standard or not, to the current email.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.delete","title":"protocols.imap_object.EMail.delete","text":"<pre><code>delete()\n</code></pre> <p>Delete the current email directly without using the trash bin. It will not be recoverable.</p> <p>Use EMail.move to move the email to the trash folder to get a last chance at reviewing what will be deleted.</p> Note <p>As per IMAP standard, this only add the <code>\\Deleted</code> flag to the current email. Emails will be actually deleted when the <code>expunge</code> server command is launched, which is done automatically at the end of Server.run_filters.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.spam","title":"protocols.imap_object.EMail.spam","text":"<pre><code>spam(spam_folder='INBOX.spam')\n</code></pre> <p>Mark the current email as spam, adding Mozilla Thunderbird <code>Junk</code> flag, and move it to the spam/junk folder.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.move","title":"protocols.imap_object.EMail.move","text":"<pre><code>move(folder: str)\n</code></pre> <p>Move the current email to the target <code>folder</code>, that will be created recursively if it does not exist. <code>folder</code> will be internally encoded to IMAP-custom UTF-7 with Server.encode_imap_folder.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.mark_as_important","title":"protocols.imap_object.EMail.mark_as_important","text":"<pre><code>mark_as_important(mode: str)\n</code></pre> <p>Flag or unflag an email as important</p> PARAMETER DESCRIPTION <code>mode</code> <p><code>add</code> to add the <code>\\Flagged</code> IMAP tag to the current email, <code>remove</code> to remove it.</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.mark_as_read","title":"protocols.imap_object.EMail.mark_as_read","text":"<pre><code>mark_as_read(mode: str)\n</code></pre> <p>Flag or unflag an email as read (seen).</p> PARAMETER DESCRIPTION <code>mode</code> <p><code>add</code> to add the <code>\\Seen</code> IMAP tag to the current email, <code>remove</code> to remove it.</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.mark_as_answered","title":"protocols.imap_object.EMail.mark_as_answered","text":"<pre><code>mark_as_answered(mode: str)\n</code></pre> <p>Flag or unflag an email as answered.</p> PARAMETER DESCRIPTION <code>mode</code> <p><code>add</code> to add the <code>\\Answered</code> IMAP tag to the current email, <code>remove</code> to remove it.</p> <p> TYPE: <code>str</code> </p> Note <p>if you answer programmatically, you need to manually pass the Message-ID of the original email to the In-Reply-To and Referencess of the answer to get threaded messages. In-Reply-To gets only the immediate previous email, References get the whole thread.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_read","title":"protocols.imap_object.EMail.is_read","text":"<pre><code>is_read() -&gt; bool\n</code></pre> <p>Check if this email has been opened and read.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_unread","title":"protocols.imap_object.EMail.is_unread","text":"<pre><code>is_unread() -&gt; bool\n</code></pre> <p>Check if this email has not been yet opened and read.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_recent","title":"protocols.imap_object.EMail.is_recent","text":"<pre><code>is_recent() -&gt; bool\n</code></pre> <p>Check if this session is the first one to get this email. It doesn\u2019t mean user read it.</p> Note <p>this flag cannot be set by client, only by server. It\u2019s read-only app-wise.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_draft","title":"protocols.imap_object.EMail.is_draft","text":"<pre><code>is_draft() -&gt; bool\n</code></pre> <p>Check if this email is maked as draft.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_answered","title":"protocols.imap_object.EMail.is_answered","text":"<pre><code>is_answered() -&gt; bool\n</code></pre> <p>Check if this email has been answered.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_important","title":"protocols.imap_object.EMail.is_important","text":"<pre><code>is_important() -&gt; bool\n</code></pre> <p>Check if this email has been flagged as important.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_mailing_list","title":"protocols.imap_object.EMail.is_mailing_list","text":"<pre><code>is_mailing_list() -&gt; bool\n</code></pre> <p>Check if this email has the typical mailing-list headers.</p> Warning <p>The headers checked for hints here are not standard and not systematically used.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_newsletter","title":"protocols.imap_object.EMail.is_newsletter","text":"<pre><code>is_newsletter() -&gt; bool\n</code></pre> <p>Check if this email has the typical newsletter headers.</p> Warning <p>The headers checked for hints here are not standard and not systematically used.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.spf_pass","title":"protocols.imap_object.EMail.spf_pass","text":"<pre><code>spf_pass() -&gt; int\n</code></pre> <p>Check if any of the servers listed in the <code>Received</code> email headers is authorized by the DNS SPF rules to send emails on behalf of the email address set in <code>Return-Path</code>.</p> RETURNS DESCRIPTION <code>score</code> <ul> <li><code>= 0</code>: neutral result, no explicit success or fail, or server configuration could not be retrieved/interpreted.</li> <li><code>&gt; 0</code>: success, server is explicitly authorized or SPF rules are deliberately permissive.</li> <li><code>&lt; 0</code>: fail, server is unauthorized.</li> <li><code>= 2</code>: explicit success, server is authorized.</li> <li><code>= -2</code>: explicit fail, server is forbidden, the email is a deliberate spoofing attempt.</li> </ul> <p> TYPE: <code>int</code> </p> Note <p>The <code>Return-Path</code> header is set by any proper mail client to the mailbox collecting bounces (notice of undelivered emails), and, while it is optional, the RFC 4408 states that it is the one from which the SPF domain will be inferred. In practice, it is missing only in certain spam messages, so its absence is treated as an explicit fail.</p> Warning <p>Emails older than 6 months will at least get a score of <code>0</code> and will therefore never fail the SPF check. This is because DNS configuration may have changed since the email was sent, and it could have been valid at the time of sending.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.dkim_pass","title":"protocols.imap_object.EMail.dkim_pass","text":"<pre><code>dkim_pass() -&gt; int\n</code></pre> <p>Check the authenticity of the DKIM signature.</p> Note <p>The DKIM signature uses an asymetric key scheme, where the private key is set on the SMTP server and the public key is set in DNS records of the mailserver. The signature is a cryptographic hash of the email headers (not their content). A valid signature means the private key used to hash headers matches the public key in the DNS records AND the headers have not been tampered with since sending.</p> RETURNS DESCRIPTION <code>score</code> <ul> <li><code>= 0</code>: there is no DKIM signature.</li> <li><code>= 1</code>: the DKIM signature is valid but outdated. This means the public key in DNS records has been updated since they email was sent.</li> <li><code>= 2</code>: the DKIM signature is valid and up-to-date.</li> <li><code>= -2</code>: the DKIM signature is invalid. Either the headers have been tampered or the DKIM signature is entirely forged (happens a lot in spam emails).</li> </ul> <p> TYPE: <code>int</code> </p> Warning <p>Emails older than 6 months will at least get a score of <code>0</code> and will therefore never fail the DKIM check. This is because DNS configuration (public key) may have changed since the email was sent, and it could have been valid at the time of sending.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.arc_pass","title":"protocols.imap_object.EMail.arc_pass","text":"<pre><code>arc_pass() -&gt; int\n</code></pre> <p>Check the authenticity of the ARC signature.</p> Note <p>The ARC signature is still experimental and not widely used. When an email is forwarded, by an user or through a mailing list, its DKIM signature will be invalidated and the email will appear forged/tampered. ARC authentifies the intermediate servers and aims at solving this issue.</p> RETURNS DESCRIPTION <code>score</code> <ul> <li><code>= 0</code>: there is no ARC signature,</li> <li><code>= 2</code>: the ARC signature is valid</li> <li><code>=-2</code>: the ARC signature is invalid. Typically, it means the signature has been forged.</li> </ul> <p> TYPE: <code>int</code> </p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.authenticity_score","title":"protocols.imap_object.EMail.authenticity_score","text":"<pre><code>authenticity_score() -&gt; int\n</code></pre> <p>Compute the score of authenticity of the email, summing the results of EMail.spf_pass, EMail.dkim_pass and EMail.arc_pass. The weighting is designed such that one valid check compensates one fail.</p> RETURNS DESCRIPTION <code>score</code> <ul> <li><code>== 0</code>: neutral, no explicit authentification is defined on DNS or no rule could be found</li> <li><code>&gt; 0</code>: explicitly authenticated by at least one method,</li> <li><code>== 6</code>: maximal authenticity (valid SPF, DKIM and ARC)</li> <li><code>&lt; 0</code>: spoofed, at least one of SPF or DKIM or ARC failed and</li> </ul> <p> TYPE: <code>int</code> </p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.is_authentic","title":"protocols.imap_object.EMail.is_authentic","text":"<pre><code>is_authentic() -&gt; bool\n</code></pre> <p>Helper function for EMail.authenticity_score, checking if at least one authentication method succeeded.</p> RETURNS DESCRIPTION <code>bool</code> <p>True if EMail.authenticity_score returns a score greater or equal to zero.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.age","title":"protocols.imap_object.EMail.age","text":"<pre><code>age() -&gt; timedelta\n</code></pre> <p>Compute the age of an email at the time of evaluation</p> RETURNS DESCRIPTION <code>timedelta</code> <p>time difference between current time and sending time of the email</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.now","title":"protocols.imap_object.EMail.now","text":"<pre><code>now() -&gt; str\n</code></pre> <p>Helper to get access to date/time from within the email object when writing filters</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.create_hash","title":"protocols.imap_object.EMail.create_hash","text":"<pre><code>create_hash()\n</code></pre> <p>Create a stable hash for the email from persistent headers and metadata.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.query_referenced_emails","title":"protocols.imap_object.EMail.query_referenced_emails","text":"<pre><code>query_referenced_emails() -&gt; list[EMail]\n</code></pre> <p>Fetch the list of all emails referenced in the present message, aka the whole email thread in wich the current email belongs.</p> <p>The list is sorted from newest to oldest. Queries emails having a <code>Message-ID</code> header matching the ones contained in the <code>References</code> header of the current email.</p> RETURNS DESCRIPTION <code>list[EMail]</code> <p>All emails referenced.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object.EMail.query_replied_email","title":"protocols.imap_object.EMail.query_replied_email","text":"<pre><code>query_replied_email() -&gt; EMail\n</code></pre> <p>Fetch the email being replied to by the current email.</p> RETURNS DESCRIPTION <code>EMail</code> <p>The email being replied to.</p>"},{"location":"api/protocols/imap_object.html#protocols.imap_object-functions","title":"Functions","text":""},{"location":"api/protocols/imap_object.html#protocols.imap_object.split_url","title":"protocols.imap_object.split_url","text":"<pre><code>split_url(url: str) -&gt; tuple[str, str, str, str, str] | None\n</code></pre> <p>Split a well-formed URL following RFC3986 into base elements.</p> RETURNS DESCRIPTION <code>tuple[str, str, str, str, str] | None</code> <p>a tuple of <code>(protocol, domain, page, parameters, anchor)</code>. </p> <code>tuple[str, str, str, str, str] | None</code> <p>Empty/missing fields are inited with empty strings so there is no need for individual <code>None</code> checks.</p> <code>tuple[str, str, str, str, str] | None</code> <p>If the <code>url</code> input doesn\u2019t match an URL format, return <code>None</code>.</p>"},{"location":"api/protocols/imap_server.html","title":"protocols.imap_server","text":""},{"location":"api/protocols/imap_server.html#protocols.imap_server","title":"protocols.imap_server","text":""},{"location":"api/protocols/imap_server.html#protocols.imap_server-classes","title":"Classes","text":""},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server","title":"protocols.imap_server.Server","text":"<pre><code>Server(logfile, secretary)\n</code></pre> <p>               Bases: <code>connectors.Server[imap_object.EMail]</code>, <code>imaplib.IMAP4_SSL</code></p> <p>IMAP server connector using mandatory SSL. Non-SSL connection is not implemented on purpose, because the Internet is a dangerous place and SSL only adds a little bit of safety, but it\u2019s better than going out naked.</p> <p>This class inherits from the Python standard class imaplib.IMAP4_SSL, so all the method are available, although most of them are re-wrapped here for direct and higher-level data handling.</p> <p>The connection credentials are passed from core.secretary.Secretary.load_connectors from the <code>settings.ini</code> file of the current config subfolder.</p> <p>Examples:</p> <p>Mandatory content of the <code>settings.ini</code> file to declare IMAP connection credentials:</p> <pre><code>[imap]\n    user = me@server.com\n    password = xyz\n    server = mail.server.com\n    entries = 20\n</code></pre>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server-attributes","title":"Attributes","text":""},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.mailbox","title":"protocols.imap_server.Server.mailbox  <code>instance-attribute</code>","text":"<pre><code>mailbox: str = ''\n</code></pre> <p>The currently-opened or last-opened mailbox (aka (sub)folder).</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.folders","title":"protocols.imap_server.Server.folders  <code>instance-attribute</code>","text":"<pre><code>folders: list[str]\n</code></pre> <p>The list of all IMAP mailboxes (folders and subfolders) found on the current server. This attribute is auto-set when initializing a connection to a server. It gets refreshed when new folders are added programmatically at runtime.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.inbox","title":"protocols.imap_server.Server.inbox  <code>instance-attribute</code>","text":"<pre><code>inbox: str\n</code></pre> <p>The case-sensitive name of the system top-level and default mailbox. Gmail and Dovecot comply with the standard and call it <code>INBOX</code>, but Outlook/Office365 gets creative and call it <code>Inbox</code>. This attribute is properly set for the current server and should be used for portability instead of hard-coding <code>\"INBOX\"</code> in filters.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.junk","title":"protocols.imap_server.Server.junk  <code>instance-attribute</code>","text":"<pre><code>junk: str\n</code></pre> <p>The case-sensitive name of the server spam mailbox, typically called <code>Junk</code> or <code>Spam</code>.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.trash","title":"protocols.imap_server.Server.trash  <code>instance-attribute</code>","text":"<pre><code>trash: str\n</code></pre> <p>The case-sensitive name of the server trashbin mailbox.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.sent","title":"protocols.imap_server.Server.sent  <code>instance-attribute</code>","text":"<pre><code>sent: str\n</code></pre> <p>The case-sensitive name of the server mailbox where copies of sent emails are kept. Note that some client store sent emails in the same folder as the email they reply to.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.archive","title":"protocols.imap_server.Server.archive  <code>instance-attribute</code>","text":"<pre><code>archive: str\n</code></pre> <p>The case-sensitive name of the server mailbox where old emails may be automatically archived. Not all servers use it.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.drafts","title":"protocols.imap_server.Server.drafts  <code>instance-attribute</code>","text":"<pre><code>drafts: str\n</code></pre> <p>The case-sensitive name of the server mailbox where emails written but not yet sent may be saved.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.flagged","title":"protocols.imap_server.Server.flagged  <code>instance-attribute</code>","text":"<pre><code>flagged: str\n</code></pre> <p>The case-sensitive name of the server mailbox where emails marked as important (having the standard flag <code>\\Flagged</code>) may be moved or duplicated. Not all servers use it.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.n_messages","title":"protocols.imap_server.Server.n_messages  <code>instance-attribute</code>","text":"<pre><code>n_messages: int\n</code></pre> <p>Default number of emails to retrieve (starting from the most recent). Set from the <code>entries</code> config parameter.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.server","title":"protocols.imap_server.Server.server  <code>instance-attribute</code>","text":"<pre><code>server: str\n</code></pre> <p>URL or IP of the mailserver. Set from the <code>server</code> config parameter.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.user","title":"protocols.imap_server.Server.user  <code>instance-attribute</code>","text":"<pre><code>user: str\n</code></pre> <p>Username of the mail account on the mailserver.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.password","title":"protocols.imap_server.Server.password  <code>instance-attribute</code>","text":"<pre><code>password: str\n</code></pre> <p>Password of the mail account on the mailserver.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.port","title":"protocols.imap_server.Server.port  <code>instance-attribute</code>","text":"<pre><code>port: int = 993\n</code></pre> <p>Connection port on the mailserver. Defaults to 993 (IMAP SSL).</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server-functions","title":"Functions","text":""},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.build_subfolder_name","title":"protocols.imap_server.Server.build_subfolder_name","text":"<pre><code>build_subfolder_name(path: list) -&gt; str\n</code></pre> <p>Assemble a complete subfolder name using the separator of the server.</p> <p>Path should be the complete list of parent folders, e.g. <code>path = [\"INBOX\", \"Money\", \"Taxes\"]</code> will be assembled as <code>INBOX.Money.Taxes</code> or <code>INBOX/Money/Taxes</code>, depending on server\u2019s defaults.</p> <p>Then, replace the <code>INBOX</code> marker with the actual case-sensitive inbox name. This is to deal with Outlook/Office365 discrepancies in folders name.</p> PARAMETER DESCRIPTION <code>path</code> <p>the tree of parents folders</p> <p> TYPE: <code>list</code> </p> RETURNS DESCRIPTION <code>path</code> <p>IMAP-encoded UTF-8 path</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.split_subfolder_path","title":"protocols.imap_server.Server.split_subfolder_path","text":"<pre><code>split_subfolder_path(folder: str) -&gt; list[str]\n</code></pre> <p>Find out what kind of separator is used on server for IMAP subfolder and split the parent folders into a list of folders.</p> <p>Most servers use dots, like <code>INBOX.Money.Taxes</code>, but Outlook/Office365 uses slashes, like <code>INBOX/Money/Taxes</code>.</p> PARAMETER DESCRIPTION <code>folder</code> <p>IMAP folder path</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>tree</code> <p>list of parent folders</p> <p> TYPE: <code>list</code> </p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.encode_imap_folder","title":"protocols.imap_server.Server.encode_imap_folder","text":"<pre><code>encode_imap_folder(folder: str) -&gt; bytes\n</code></pre> <p>Ensure the subfolders are properly separated using the actual server separator (<code>.</code> or <code>/</code>) and encode the names in IMAP-custom UTF-7, taking care of non-latin characters and enquoting strings containing whitespaces. The result is ready for direct use in IMAP server commands.</p> <p>This function takes fully-formed IMAP mailbox folder pathes, like <code>INBOX.Money.Taxes</code> or <code>INBOX/Money/Taxes</code> and will replace the subfolder separators with the server separator. The main <code>INBOX</code> will also be replaced by the proper, case-sensitive, inbox name for the current server.</p> PARAMETER DESCRIPTION <code>folder</code> <p>IMAP folder as Python string</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>folder</code> <p>IMAP folder as IMAP-custom UTF-7 bytes.</p> <p> TYPE: <code>bytes</code> </p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.get_imap_folders","title":"protocols.imap_server.Server.get_imap_folders","text":"<pre><code>get_imap_folders()\n</code></pre> <p>List all inbox subfolders as plain text, to be reused by filter definitions. Update the Server.folders list.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.get_email","title":"protocols.imap_server.Server.get_email","text":"<pre><code>get_email(uid: str, mailbox=None) -&gt; imap_object.EMail | None\n</code></pre> <p>Get an arbitrary email by its UID. If no mailbox is specified, use the one defined when we got objects in <code>self.set_objects()</code>.</p> <p>If a mailbox is specified, we select it temporarilly and we restore the original mailbox used to get objects. If no mailbox is selected, we use the previously-selected one, typically in Server.get_objects.</p> PARAMETER DESCRIPTION <code>uid</code> <p>the unique ID of the email in the mailbox. Be aware that this ID is unique only in the scope of one mailbox (aka IMAP (sub)folder) because it is defined as the positional order of reception of each email in the mailbox.</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>message</code> <p>the email object.</p> <p> TYPE: <code>imap_object.EMail</code> </p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.get_objects","title":"protocols.imap_server.Server.get_objects","text":"<pre><code>get_objects(mailbox: str, n_messages=-1)\n</code></pre> <p>Get the n last emails in a mailbox. Update the Server.objects list.</p> <p>Processed email get logged with the number</p> PARAMETER DESCRIPTION <code>mailbox</code> <p>the full path of the mailbox. It will be sanitized for folder/subfolder separator and actual <code>INBOX</code> name internally.</p> <p> TYPE: <code>str</code> </p> <code>n_messages</code> <p>number of messages to fetch, starting with the most recent. If <code>-1</code>, the preference set in <code>settings.ini</code> will be used. Any other value will set it temporarily.</p> <p> TYPE: <code>int</code> DEFAULT: <code>-1</code> </p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.run_filters","title":"protocols.imap_server.Server.run_filters","text":"<pre><code>run_filters(filter, action, runs=1)\n</code></pre> <p>Run the function <code>filter</code> and execute the function <code>action</code> if the filtering condition is met</p> PARAMETER DESCRIPTION <code>filter</code> <p>function performing checking arbitrary conditions on emails, returning <code>True</code> if the action should be performed. It will get an EMail object as argument.</p> <p> TYPE: <code>callable</code> </p> <code>action</code> <p>function performing the actual action. It will get an EMail object as argument.</p> <p> TYPE: <code>callable</code> </p> <code>runs</code> <p>how many times a filter should run at most on each email. <code>-1</code> means no limit.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.close_connection","title":"protocols.imap_server.Server.close_connection","text":"<pre><code>close_connection()\n</code></pre> <p>High-level method to logout from a server</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.create_folder","title":"protocols.imap_server.Server.create_folder","text":"<pre><code>create_folder(folder: str)\n</code></pre> <p>Create an IMAP (sub)folder recursively if needed (create the parent(s) if missing, then create the child).</p> <p>Calls Server.create at each recursivity level, so IMAP folder names are fully sanitized.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.create","title":"protocols.imap_server.Server.create","text":"<pre><code>create(mailbox: str)\n</code></pre> <p>Create a new mailbox. This is a wrapper over imaplib.IMAP4.create method, where <code>mailbox</code> is directly encoded to IMAP-custom UTF-7 format through Server.encode_imap_folder.</p> <p>The direct use of this function is discouraged, see Server.create_folder for a nicer method. Namely, this will error if trying to create a subfolder of a non-existing parent folder, and does not update Server.folders.</p>"},{"location":"api/protocols/imap_server.html#protocols.imap_server.Server.append","title":"protocols.imap_server.Server.append","text":"<pre><code>append(mailbox: str, flags: str, email: email.message.EmailMessage) -&gt; str\n</code></pre> <p>Add an arbitrary email to the specified mailbox.</p> PARAMETER DESCRIPTION <code>mailbox</code> <p>Name of the target mailbox.</p> <p> TYPE: <code>str</code> </p> <code>flags</code> <p>IMAP flags (RFC 3501 style), e.g.:</p> <ul> <li><code>(\\\\Seen)</code> \u2192 mark as read</li> <li><code>(\\\\Flagged)</code> \u2192 mark as important</li> </ul> <p>Custom flags are allowed as long as they do not start with <code>\\</code>.</p> <p> TYPE: <code>str</code> </p> <code>email</code> <p>Fully constructed email message ready for storage.</p> <p> TYPE: <code>email.message.EmailMessage</code> </p> RETURNS DESCRIPTION <code>str</code> <p>status</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/protocols/instagram_server.html","title":"protocols.instagram_server","text":""},{"location":"api/protocols/instagram_server.html#protocols.instagram_server","title":"protocols.instagram_server","text":""},{"location":"api/protocols/smtp_server.html","title":"protocols.smtp_server","text":""},{"location":"api/protocols/smtp_server.html#protocols.smtp_server","title":"protocols.smtp_server","text":""},{"location":"background/1-why.html","title":"Why Virtual Secretary ?","text":""},{"location":"background/1-why.html#context-intro","title":"Context &amp; Intro","text":"<p>Emails are underrated.</p> <p>Chats, forums and social medias all tried to replace them with something cooler : Facebook, Google chat/hangout, Slack, Matrix, Telegram, Discord, Discourse, Github\u2026 Your digital communications are now scattered over many platforms that promised you to make emails outdated. From there, you have 2 options :</p> <ol> <li>install all the desktop &amp; mobile apps of all the platforms you use on your mobile/desktop device, then get flooded with notifications at the worst possible time and have your batteries drained to refresh everything all the time, while still being unable to address the notifications content because typing on touch screens is a pain, and so is reading text on 5\u2019\u2019 screens where the visual keyboard takes a quarter of the space.</li> <li>centralize all notifications to your email box, then enjoy the paradox of all these emails created by services meant to replace them and the overwhelming of having notifications you can discard hiding important stuff requiring action.</li> </ol> <p>So, trying to replace emails just failed.</p> <p>Anyway, there are lots of hardly-replaceable uses for emails, like sending account credentials, password resets, unique passwords for double auth, security warnings, communicating with businesses, institutions, calendar events invites, etc. Not to mention, all those direct emails from individuals who still use them.</p> <p>People are convinced emails are bad because they hinder their productivity, but it\u2019s just that they don\u2019t use them properly.</p>"},{"location":"background/1-why.html#what-are-emails","title":"What are emails ?","text":"<p>Emails are pieces of text, possibly with media attachments, sent between two users, with a subject and a date.</p> <p>Realize that description applies to pretty much all electronic communications, from SMS to forum posts, blog comments, social media posts/comments/direct messages, etc. ?</p> <p>The huge difference is emails are decentralized while being centralized. Decentralized because you can send messages to users who don\u2019t have an account on the same server as you (seems like the web 3.0 idiots just dicovered that now, but felt the urge to coat it with bloaty CPU-burning blockchain). Centralized because you can gather messages from many servers to a single place, avoiding to open many websites and applications to check all your channels.</p>"},{"location":"background/1-why.html#why-emails-suck","title":"Why emails suck ?","text":"<p>Emails suck for 3 reasons:</p> <ol> <li>Desktop and mobile email clients suck, as well as mobile apps,</li> <li>85 to 90% of emails are spam or unwanted bulk/newsletters emailing, but spam filters suck,</li> <li>You can\u2019t decide who is allowed to send you emails.</li> </ol> <p>Note that none of these reasons actually affect emails as a communication medium: what sucks is how poorly they are managed by office applications and integrated into our modern ecosystem of office tools. Imagine if someone put as much effort into a mobile email app as Whatsapp (yes, that would give Blackberry Hub)\u2026</p> <p>Basically, emails are the communication service of the digital office aka the glue between people, servers and services. What sucks in email clients is they are self-centered and don\u2019t really connect to the rest of your services, like your customers SQL database, your agenda, your contact book, etc.</p> <p>The key to good email use is to filter and sort emails depending on their importance and whether they require an action from you. To achieve that, you need more than looking inside emails, you need to connect email content with other servers and sources to apply filters and workflows.</p>"},{"location":"background/1-why.html#email-filters","title":"Email filters","text":"<p>Every email client app has a way to define filters, which will allow you to write conditional actions that attribute tags, move emails to a folder, delete or reject them. It\u2019s basic but great.</p> <p>Problem is, filtering can\u2019t happen at the client-level anymore, because you typically have at least one desktop and one smartphone fetching your emails through IMAP at the same time, which means at least 2 clients. Whatever client gets the emails first will decide their fate, unless you duplicate all filters across all your clients. Nobody does that, especially since mobile email apps typically don\u2019t support filters.</p> <p>The only way to solve that is to have server-level filters, that run before clients get a chance to fetch emails. But there are lots of situations where you don\u2019t have control over your email server to do so.</p> <p>So you have to either resort to giving up your privacy to Gmail or to use some sort of bloated and mildly-unsecure webmail or groupware, operating on a server and running filters online. But the filtering based solely on email content is still archaic and incomplete.</p> <p>In real life, you need to cross-check email content with your contact book, your agenda, your project/tasks, possibly with some customer database, etc. You may also want to use regular expressions for advanced text parsing or even language processing AIs to identify the nature of the email. None of this is currently possible, in email clients as in webmails/groupwares.</p>"},{"location":"background/1-why.html#spam-filters","title":"Spam filters","text":"<p>All the emails clients I\u00a0have tested over the years (Thunderbird, Horde/Nextcloud email, EGroupware) have no simple way to permanently whitelist some sender\u2019s IP/email to prevent their spam filter to output false-positives. None of them have a way to permanently blacklist them either to prevent false-negatives. None of them can be explicitly trained by users.</p> <p>SpamAssassin and such look for IP and URLs reputation, and other technical matters (SPF, DKIM) but they don\u2019t process the content (keywords) well and don\u2019t take user input (other than tweaking directly the config files, perhaps). So they let through technically-valid newsletters from mailing-lists you never subscribed to.</p> <p>Then you have no easy way to unsubscribe to all that junk.</p>"},{"location":"background/1-why.html#connecting-to-other-services","title":"Connecting to other services","text":"<p>It took Thunderbird until 2018 to finally ship Lightning, the calendar extension (supporting CalDAV and Google Agenda), within its core app. As of 2022, it still doesn\u2019t provide a way to connect to CardDAV (contact books) servers CardDAV support was finally added in Thunderbird 102\u2026 released in June 2022, but you need to know where to look for it because nothing says \u201cadd an address book from a server\u201d. Its internal contact book still doesn\u2019t support CardDAV features like assigning categories/groups to contacts in order to send bulk emails, for example.</p> <p>Because emails are standardized, as well as CardDAV, CalDAV and WebDAV, and because email/contacts/agenda/file-sharing defines the base ground of all office work, it must connect and integrate flawlessly within each other. This is still not a reality in 2022 and is probably the reason why people endure emails.</p> <p>Thunderbird is of course not the only client available around, but KMail and Evolution are even worse. The only alternative is to resort to Horde, Zimbra, SoGO and such, which are all webmails to deploy on servers, but they are tedious to install, complicated to secure, and clearly overkill for individuals and small businesses.</p>"},{"location":"background/1-why.html#imagining-a-better-present","title":"Imagining a better present","text":"<p>We live crazy lives with too many inputs, and we get more emails than we can humanely process. In this mess, the only way to keep your sanity is to have your inputs served along with an instruction telling you what you should do with them :</p> <ul> <li>Do they require an action from you :</li> <li>yes and urgently ?</li> <li>yes but whenever you get the time ?</li> <li>optionaly, if you feel like it ?</li> <li>no, information for your archives only ?</li> <li>yes: unsubscribe from that bloody mailing-list ?</li> <li>Who is the correspondent :</li> <li>a friend or family ?</li> <li>a paying customer ?</li> <li>a prospect/future customer ?</li> <li>a colleague or supervisor ?</li> <li>a troll or a bully ?</li> <li>a bot or an automated mailing system ?</li> </ul> <p>In the past, people used to have secretaries to achieve this task and shield them from unwanted sollicitation, allowing them to focus and do their work efficiently while still allowing urgent matters to go through. They also sorted the mail by category and priority, tracked time, booked appointments and planned travels. The 21th century workers have to do all that by themselves, but as of 2022, the consensus is that multitasking doesn\u2019t work and only creates anxiety while destroying productivity.</p> <p>So we need software to sort the mail, remove the junk and give you hint about the urgency of the mail.</p>"},{"location":"background/1-why.html#why-is-virtual-secretary-better","title":"Why is Virtual Secretary better ?","text":"<ol> <li>It connects directly to your servers. Those are designed for concurrent access with mechanisms to prevent data corruption.</li> <li>It can run on server or on desktop. It\u2019s just a script to call, you can make it a cron job.</li> <li>It is independent from any client. You can keep using the clients you want, the filters are run standalone in background and don\u2019t rely on a particular client.</li> <li>Your configuration is a folder with meaningful text files in it. We already have a GUI\u00a0for that : your file browser.</li> <li>You can do a lot with few lines of code. Connections, decoding and parsing are done backstage, just write content filters.</li> <li>It\u2019s still Python inside. Train AIs, call Rest APIs, parse HTML, match regex, plot graphs, do stats, etc. with just a few imports and the 2nd most used language in the world.</li> <li>There is no GUI. Yes, it\u2019s actually better for an application designed for flexibility: replace a bloated window trying to account for every marginal sub-use by just a couple of lines of code that you can Ctrl+F and RTFM.</li> </ol>"},{"location":"background/1-why.html#performance","title":"Performance","text":"<p>CPU use and program runtimes are important if you are going to process large amounts of emails or need to use a server sparingly. Tested on Intel Xeon E3-1505M @ 3 GHz \u00d7 8 on battery with balanced performance P-State:</p> <ul> <li>Getting the last 50 emails takes between 32 and 72 s *,</li> <li>Parsing 50 emails takes in average 1.25 s,</li> <li>Filtering 50 emails takes between 0.1 and 1.1 s depending what you are doing.</li> </ul> <p>On the same laptop, plugged in AC, with balanced performance P-State:</p> <ul> <li>Getting the last 500 emails takes between 52 and 101 s,</li> <li>Parsing 500 emails takes between 6 and 8 s,</li> <li>Filtering 500 emails takes between 0.2 and 3.5 s.</li> </ul> <p>Runtimes are given in the standard output in terminal.</p> <p>Please note that:</p> <ol> <li>The time needed to fetch emails depends first and foremost on your network and server,</li> <li>The time needed to parse emails will increase if there are attachments,</li> <li>The time spent filtering takes into account only the filter check (aka deciding whether or not each email needs to be acted-on), not the actual action. Actions that need to reach a server through a network (move email to IMAP folder, auto-reply through SMTP, ping some server through HTTP) depend first and foremost on your network and server.</li> <li>Some data, like the email body, URLs and IPs found in email body, etc. are lazily parsed, which means that patterns are searched in body only if and when the properties are accessed. This will go in the filtering time. Only IMAP\u00a0headers are parsed all the time.</li> </ol>"},{"location":"background/1-why.html#backstory","title":"Backstory","text":"<p>2022 was by far my worse year, regarding emails and quantity of work. I\u00a0lost many important notifications from Github regarding darktable and got flooded under shit.</p> <p>Between January 1st 2022 and October 1st 2022, I\u00a0got 2164 notification emails linked to the darktable project, over 1200 newsletter emails (non-spam), 3190 spam emails and only 750 relevant emails trapped in-between. That\u2019s an average of more than 37.5 emails per day, of which 2.7 in average required my time and attention.</p> <p>Problem is, the kind of work I\u00a0do requires concentration and peace to manipulate abstract concepts and solve problems. So I don\u2019t open my mailboxes everyday. Good luck then finding those 2.7 relevant emails in the middle of the junk. That was a nightmare. Never again.</p> <p>I have used Thunderbird extensively since 2007. That was long before I actually needed a mailbox. As my daily flow of emails increased, I started setting filters and such. I lost my whole Thunderbird config twice, including filters and RSS feeds, due to updates triggering file corruptions. The second time, I didn\u2019t bother redoing it from scratch\u2026 Why invest in something unreliable ?</p> <p>Thunderbird has lots of features and yet manages to lack the ones I really need. I use the search engine, the \u201cwrite\u201d and \u201cforward\u201d buttons, and that\u2019s about it. No GPG thing since nobody uses that. Though my calendar is connected to Thunderbird/Lightning, my appointments are booked mostly by software or email invites, and I\u00a0find myself checking my day planner only on my phone. So Thunderbird is a big piece of bloat that still fails to provide good filtering features and agenda/contacts bindings.</p>"},{"location":"background/2-Proposed-workflow.html","title":"Proposed workflow","text":"<p>Virtual Secretary interconnects emails with other services, like calendars and address books, to trigger automatic responses (workflows) and sorting (filters). Since services are typically already able to send emails, the direction <code>services -&gt; emails</code> is not handled by Virtual Secretary, which will focus on the missing direction <code>emails -&gt; services</code>.</p>"},{"location":"background/2-Proposed-workflow.html#structure-of-email-addresses","title":"Structure of email addresses","text":"<p>It is better to try and keep different people and services on different email accounts. Virtual Secretary can process different email addresses in sequence, and you don\u2019t even have to duplicate the filters if you put them in the <code>common</code> subfolder of your <code>config</code> directory. Different email accounts can also share a single address book.</p> <p>Here are a couple of suggested emails to divide your email input:</p> <ul> <li>me@me.com:\u00a0your personal email, the one you give to friends and family, that you may sync with your smartphone,</li> <li>pro@me.com:\u00a0your professional email, the one you give to colleagues and collaborators, that you will not open on holidays and probably not sync on your personal devices, or at least not after  8 PM and on week-ends,</li> <li>banks@me.com:\u00a0your secure email, that you will give only to your trusted banks, insurances companies, revenue agencies etc.</li> <li>spam@me.com:\u00a0your unsecure email, that you will use on every semi-shady website or platform that may sell it and leak it in their next security breach to spammers,</li> <li>suppliers@me.com: the email you give to companies and people that sell you stuff,</li> <li>clients@me.com:\u00a0the email you give to companies and people you sell stuff to.</li> <li>contact@me.com:\u00a0the email people you don\u2019t know will use to contact you.</li> </ul> <p>The point of splitting matters between emails like this is mostly to spot phishing emails, trying to impersonate organizations and companies to steal your logins/passwords/money. If you get an invoice (or worse: a debt settling notice) on any other email than suppliers@me.com or a bank security alert on any other email than banks@me.com, you don\u2019t need to think twice:\u00a0these are phishing attempts.</p> <p>For each of these emails, you should subscribe to Firefox Monitor and Have I been pwnd ? alerts to get notified if they are found in a data breach and take appropriate measures.</p>"},{"location":"background/2-Proposed-workflow.html#general-principles","title":"General principles","text":"<p>There are mostly 3 types of emails:</p> <ol> <li>emails sent to you by persons,</li> <li>emails sent to you by automated systems (newsletters, notifications, invoices, etc.),</li> <li>emails sent to you by automated systems but originating from persons, like notifications of direct messages on social platforms or notifications of new replies on forums and chats.</li> </ol> <p>Those emails will trigger 4 different actions:</p> <ol> <li>urgent action,</li> <li>delayed action,</li> <li>acknowledge info,</li> <li>ignore.</li> </ol> <p>But those actions can all result from the 3 different types of emails, like automated emails notifying you of account password reset or new suspicious activity on your account typically require urgent action.</p> <p>Now, the overwhelming of modern life is we have limited time to process all these emails, which get received as a mixed pack, and we don\u2019t have secretaries anymore to handle them. So the whole idea is to use data contained within the emails but also from other sources to guess if each email require action, when, and deliver that to the user as a pre-digested queue.</p> <pre><code>%%{ init: { 'flowchart': { 'curve': 'stepAfter' } } }%%\ngraph TD;\n    message[/Email/] --&gt; is_spam{is spam ?};\n    is_spam -- yes --&gt; id5[Move to 'Junk']\n    is_spam -- no --&gt; email[/Ham/];\n\n    email --&gt; is_special{is special ?};\n    is_special -- yes --&gt; workflows[[Special processing]];\n    is_special -- no --&gt; normal[/Normal email/]\n\n    normal --&gt; is_mailinglist{is from a mailing-list ?};\n    is_mailinglist -- yes --&gt; newsletter[Archive in 'Newsletters'];\n    is_mailinglist -- no ---&gt; personal[/Personal email/];\n\n    personal --&gt; is_important{is important ?};\n    is_important -- yes --&gt; id6(Tag 'Important');\n\n    personal --&gt; is_human{is from a person ?};\n    is_human -- yes --&gt; is_direct{is direct email ?};\n    is_direct -- yes --&gt; id2[Archive in contacts folder];\n    is_direct -- no --&gt; social[/Social media DM notification/]\n    social --&gt; pecial2[[Special processing]]\n    is_human -- no --&gt; and((+))\n    id2 --&gt; needs_answer{needs answer ?};\n\n    needs_answer -- yes --&gt; id1(Tag 'Answer needed');\n\n    personal --&gt; needs_action{needs action ?};\n    needs_action -- no --&gt; and;\n    and --&gt; notification[Archive in 'Notifications'];\n\n    needs_action -- yes --&gt; id7(Tag 'Action requested');\n    id7 --&gt; is_urgent{is urgent ?};\n\n    is_urgent -- yes --&gt; id3(Tag 'Urgent');</code></pre> <p>Our goal will be to achieve all operations in square boxes automatically, and as many operations in rounded boxes as possible, using filters.</p> <p>Special processing will be needed at 2 places.</p> <p>At the beginning, special emails could be anything related to bank accounts and security, taxes and revenues agencies, social and health insurances, etc. Those emails are both very important and easy to filter, for example, in France, any email sent by an address ending in <code>impots.gouv.fr</code> or <code>finances.gouv.fr</code> has something to do with taxes. Taking care of these emails with simple filters at the beginning of the process will remove the probability of false negatives and prevent losses of information that we can\u2019t afford on such topics.</p> <p>At the end, for emails sent by people, special processing may aim at reconstructing an email thread from notifications of direct messages on social media. Social media DM are essentially emails but captured on a third-party platform that badly wants you to spend time on said platform to generate advertising revenues. Problem is they scatter your communication and people really want to keep using them because the UI/UX/ergonomy of most social media messenger apps are much better and more fun than your average mobile email client. But the emails notifications of DM are technically emails sent by bots but they represent a real human communication and are a grey area in-between.</p> <p>You may want to manage your social accounts from your mailbox as much as possible, for efficiency but also for archival. Some social media embed the content of the DM into the email notifications. In that case, you may want to move the notifications emails in contact-based archives, along with the other messages (emails or other social media messages) you exchanged with the same person. Ultimately, you may want to answer DM directly by replying to the notification email, which is possible on a few platforms like Discourse and Github.</p>"},{"location":"background/3-Authentication.html","title":"Email authentication","text":"<p>The Virtual Secretary supports email authentication through SPF, DKIM and ARC.</p>"},{"location":"background/3-Authentication.html#why","title":"Why ?","text":"<p>Sending an email by impersonating you is incredibly easy. To do it in Python, I\u00a0just need to run this snippet :</p> <pre><code>import turbomail\n\nmessage = turbomail.Message(\"you@gmail.com\", \"you@gmail.com\", \"I hacked your account\")\nmessage.plain = \"I have taken control of your mailbox and found interesting things, you dirty little minx. Pay me 0.5 bitcoin now or I\u00a0will send everything I\u00a0found to your family.\"\n\nturbomail.enqueue(message)\n</code></pre> <p>\u2026 And sent. That\u2019s it, no need for login/password. The <code>from</code> email address above is set the same as the <code>to</code> address, but it is purely declarative: the email above will be sent without needing the credentials of your actual Gmail account. This technique is used a lot by scammers who try to make you believe they hacked into your mailbox and try to ransom you.</p> <p>Because it is that easy to pretend to be anybody, and because spammers and scammers do it a lot, it is necessary to have ways of authenticating first the sender of an email (aka the <code>from</code> field is authentic) but also the content of the email (aka the content didn\u2019t got tempered during transport).</p>"},{"location":"background/3-Authentication.html#spf","title":"SPF","text":"<p>SPF allows to declare, on your mailserver, what IP addresses are allowed to actually send emails on \u201cyour\u201d behalf, \u201cyou\u201d being the <code>Return-Path</code> header of the email you send. Say you are <code>you@gmail.com</code>, the Virtual Secretary queries the <code>gmail.com</code> domain for the <code>MX</code> entries and checks if the email was sent by a server whose IP matches one of those and any additional one defined in the SPF DNS entry.</p> <p>SPF is fairly easy to set, as it requires a simple text entry, and has been around for quite some time. Discarding emails that fail SPF checks reliably removes 90 % of spams.</p>"},{"location":"background/3-Authentication.html#dkim","title":"DKIM","text":"<p>SPF allows to validate the origin server of the email, but does not validate neither the sender (the actual person) nor the content (integrity). DKIM solves that, with a public and private key. The public key is hosted in a DNS entry and can be retrieved from the server. The private key is typically managed by your SMTP server, the one you should login to through SSL. When an email is sent, the SMTP server will sign the headers of the email with the private key, which will produce a sort of hash. The recipient, fetching the public key on the server, can check that the hash is consistent with current headers and public key.</p> <p>DKIM is a bit more difficult to setup, as it needs to be handled at 2 places (DNS server entry and SMTP mailserver), but is supported by most email services. It is a safer than SPF alone.</p> <p>There are caveats though. DKIM keys are valid only for some period of time. It may happen that old emails will fail the DKIM test because the DKIM key has changed. It also happens a lot that the IT guys are sleeping on their desk and forget to renew the key after its expiration (it\u2019s currently the case with Proton mail).</p> <p>For example, I\u2019m writing this on March 28th. All emails from Github sent after the 23rd pass the DKIM check, but all emails sent prior to the 22nd fail with \u201cno DKIM found on the DNS\u201d, which means that Github updated its keys on the 23rd but without leaving the old ones as alternatives for some more time.</p>"},{"location":"background/3-Authentication.html#dmarc","title":"DMARC","text":"<p>DMARC is a policy that you can enforce on your domain to tell mailservers what they should do with emails allegedly coming from you but failing SPF and DKIM checks. When you are confident that your SPF and DKIM settings are working, you can setup DMARC to tell all mailservers around to refuse anything that appears to come from you which doesn\u2019t pass the checks, and that may help to preserve your online reputation if spammers try to impersonate you (avoiding you grey- and black-listing of your IP/domain).</p> <p>DMARC is used by the mailserver to decide whether emails are accepted or will bounce (and be sent to the <code>Return-Path</code> address) when they fail the checks. When you use the Virtual Secretary, you are only parsing emails already accepted by the server through IMAP, so DMARC\u00a0doesn\u2019t concern us. Also, I\u2019m not sure how compliant most mailservers are, so we may not rely on them. The Virtual Secretary allows you to define your own actions based on the succes of the SPF and DKIM checks.</p>"},{"location":"background/3-Authentication.html#arc","title":"ARC","text":"<p>DKIM still has one problem: some server may transfer emails by legitimately changing their headers, which will make the DKIM\u00a0check fail. This is legitimate for all cases where emails are forwarded, for example by marketing mass-mailing or mailing-lists. ARC allows to validate the forwarding step, and ARC validation can be used as a second-chance when DKIM validation has failed.</p>"},{"location":"background/3-Authentication.html#implementation-in-the-virtual-secretary","title":"Implementation in the Virtual Secretary","text":"<p>The Virtual Secretary implements a <code>email.authenticity_score()</code> method that computes a cumulative score of all ARC, DKIM and SPF checks and internally handles all the low-level parsing.</p> <p>It is worth mentionning that many spammers/scammers forge fake DKIM, SPF and ARC headers, so the emails look ok from aside (all the security bits are there), but when you actually run the checks of the cryptographic keys and hashes, you find out that nothing is valid. Forged signatures get penalized heavily, with an authenticity score of <code>-2</code>. Any email getting a score of <code>-6</code> has all DKIM, SPF, and ARC headers forged.</p> <p>Explicit success is awarded a score of <code>+2</code>. That is, when we have an header and it passes the check.</p> <p>When no header is found, or, for SPF, when the SPF rule doesn\u2019t explicitly disallow anything, the score is <code>0</code>.</p> <p>For real emails, I\u00a0got the following scores:</p> <ul> <li>Youtube, Gmail :\u00a0<code>+6</code> (valid ARC, explicitly valid SPF, valid DKIM),</li> <li>PayPal, Stripe, most banks since 2019, most email providers :\u00a0<code>+4</code> (no ARC, explicitly valid SPF, valid DKIM),</li> <li>Github, Proton Mail :\u00a0<code>+3</code> (no ARC, explicitely valid SPF, outdated DKIM signature),</li> <li>Semi-serious marketing emails from legit companies, banks before 2018, self-hosted emails with some IT skills : <code>+2</code> (no ARC, no DKIM, explicitly valid SPF),</li> <li>your uncle Bob who setup a mailserver in the basement, your nephew Jason who got a shared hosting without spending quality time on CPanel : <code>0</code> (no ARC, no DKIM, no explicit SPF rule),</li> <li>my grand-father who pushes all buttons without understanding what they do : <code>-1</code> (no ARC, no DKIM, bad/misformed SPF\u00a0rule),</li> <li>anybody trying to play you a nasty one : <code>-2</code> to <code>-6</code> (forged headers).</li> </ul> <p>Note that, in cases where emails have been forwarded, the DKIM header will fail with a score of <code>-2</code> but if the ARC check passes, the total score will be zero. Along with a valid SPF, that should still give a total greater than zero.</p> <p>You can use the <code>email.authenticity_score()</code> method directly from your filters, otherwise a convenience <code>email.is_authentic()</code> method is provided and returns <code>True</code> for scores above zero.</p> <p>Internally, the <code>email.authenticity_score()</code> method calls each check and adds their output score. You can also use them in your filter, they are <code>email.spf_pass()</code>, <code>email.dkim_pass()</code> and <code>email.arc_pass()</code>.</p>"},{"location":"background/3-Authentication.html#efficiency","title":"Efficiency","text":"<p>Discarding emails based on their authenticity score has proven to catch roughl\u00e0 98 % of spams, phising, spoofings and such, which is much better than any keyword-based content filtering. If you also check for emails which has the <code>Precendence: bulk</code> header (mass-mailing) without having a <code>List-Unsubscribe</code> (allowing for easy unsubscribe from mailing-lists), you get most of the nasty emails out of your box. Note that Gmail doesn\u2019t seem to do it.</p> <p>The drawback is authenticity needs to ping several DNS for each email (to get SPF rules, MX\u00a0entries, DKIM key, etc.), which needs an internet connection, needs some time, and will take forever if some DNS stutter.</p> <p>Running authenticity checks should be done before checking if the sender belongs to your contacts book to ensure the email actually comes from your contact.</p>"},{"location":"background/4-Risk-analysis.html","title":"Risk analysis","text":"<p>Emails, contacts and agendas usually contain personal and even sensitive information. Any application dealing with them should mention how these data are collected, stored and used such that users may assess if the security level of the application is satisfying for their use.</p> <p>Virtual Secretary connects to IMAP, SMTP, CardDAV servers through SSL only. By design, less-secure protocols are not implemented. This means that data are encrypted during transport through the network, but are stored plain (non-encrypted) on servers.</p> <p>Emails encrypted with PGP are not supported in read and write, meaning only headers will be read and no encrypted email will be written.</p> <p>The data fetched from IMAP, SMTP, CardDAV servers are stored in the computer RAM for the length of the session (during the <code>main.py</code> script execution). No local copy is made on disks and stored for later use, which means all data will be re-downloaded at each run. While this slows-down execution, it avoids storing unsecure data.</p> <p>The connections to IMAP, SMTP and CardDAV servers use simple passwords. This is unfortunately not very secure, though the use of SSL mitigates some risks, but many servers don\u2019t support certificates. The passwords are stored in plain text in <code>settings.ini</code> in the <code>config</code> directory. It is advised to use \u201capplication passwords\u201d that are particular to each application, such that the access of compromised applications can be quickly revoked.</p> <p>Aur\u00e9lien Pierre declines any responsibility regarding loss of data and security breaches contained in or made possible by Virtual Secretary. The scripts are provided for what they are worth and without any guaranty.</p>"},{"location":"starting/index.html","title":"Getting started","text":"<p>This section will show you how to build an application by implementing the Virtual Secretary features. This will involve writing Python scripts, for which the Virtual Secretary package provides helpers to achieve fairly common tasks with a minimum amount of (legible) code.</p> <p>At the end of this tutorial, you should be able to:</p> <ol> <li>run email filters on an IMAP server,</li> <li>connect your CardDAV address book to your email filters,</li> <li>write your own email filters and email auto-responders,</li> <li>crawl websites and local files to assemble a corpus of HTML and PDF documents,</li> <li>train a natural language processing artificial intelligence on the corpus, to be reused later,</li> <li>use the trained AI to create an email classifier,</li> <li>use the trained AI\u00a0to create a search engine of web pages and PDF documents,</li> <li>create a server-side Rest API for the search engine,</li> <li>connect an email auto-responder or a chat bot to the search engine results, to automatically answer customer inquiries.</li> </ol> <p>The purpose of the tutorial as to show you how quickly you can bootstrap an application performing typical tasks. As such, it focuses on basic, typical tasks. You can extend everything in the way you see fit by reusing the huge ecosystem of Python packages.</p>"},{"location":"starting/1-install.html","title":"Install","text":"<p>The Virtual Secretary has 4 parts:</p> <ol> <li>the Python 3.12 interpreter (aka the runtime),</li> <li>the Python packages upon which it depends (aka the libraries),</li> <li>the Virtual Secretary application source code (aka the application), located in the <code>src/</code> folder of the source code,</li> <li>the user filters (aka the implementation), typically installed in a <code>config/</code> folder.</li> </ol> <p>The Python interpreter is pretty standard in Linux distributions and easy to install on Windows and Mac. However, Linux distributions may have older versions than 3.12. If you plan on using the Virtual Secretary on a shared hosting server, you will not have access to a shell giving you rights to install or update Python. For those cases where you can\u2019t install or update the Python interpreter to version 3.12, download the Python 3.12 AppImage. See below how to deploy Python 3.12 to a server where you don\u2019t have <code>sudo</code> rights.</p>"},{"location":"starting/1-install.html#getting-the-source-code","title":"Getting the source code","text":"<p>The easiest way is to get and update the source code through Git (installation instructions).</p> <p>Once Git is installed, in a terminal, run:</p> <pre><code>git clone https://github.com/aurelienpierreeng/VirtualSecretary.git\ncd VirtualSecretary\n</code></pre> <p>To update in the future, run:</p> <pre><code>cd VirtualSecretary\ngit pull\n</code></pre>"},{"location":"starting/1-install.html#python-interpreter","title":"Python interpreter","text":"<p>If you have access to a package manager and you are able to simply install Python 3.12, do it. Otherwise, here is how to deploy the AppImage on a server:</p> <ol> <li>We assume here you are already in <code>VirtualSecretary</code> folder, after the <code>cd VirtualSecretary</code> command at the previous step.</li> <li> <p>Download the AppImage package: <pre><code>wget https://github.com/niess/python-appimage/releases/download/python3.12/python3.12.4-cp311-cp311-manylinux2014_x86_64.AppImage\n</code></pre></p> </li> <li> <p>Rename it <code>python3.12.AppImage</code> for convenience: <pre><code>mv python3.12.4-cp311-cp311-manylinux2014_x86_64.AppImage python3.12.AppImage\n</code></pre></p> </li> <li> <p>Give it execution permissions: <pre><code>chmod +x python3.12.AppImage\n</code></pre></p> </li> <li> <p>Run the test command: <pre><code>./python3.12.AppImage --version\n</code></pre> If it returns: <pre><code>dlopen(): error loading libfuse.so.2\nAppImages require FUSE to run.\n</code></pre> then you need to extract the AppImage because the system can\u2019t do it. Luckily, the AppImage package is self-extractable, so just run: <pre><code>./python3.12.AppImage --appimage-extract &amp;&amp; mv squashfs-root python3.12\n</code></pre></p> </li> </ol> <p>From there, to execute a Python script (<code>.py</code>), you have to use one of the following methods:</p> <ol> <li><code>python3.12 script.py</code> if you have multiple versions of Python installed,</li> <li><code>python script.py</code> if you have only Python 3.12 installed,</li> <li><code>./python3.12.AppImage script.py</code> from within the <code>VirtualSecretary</code> folder if you can use the non-extracted AppImage</li> <li><code>python3.12/AppRun script.py</code> from within the <code>VirtualSecretary</code> folder if you had to extract the AppImage</li> </ol> <p>In the rest of this document, we will write <code>PYTHON</code> as a generic way of calling the interpreter that will need to be replaced by one of the above commands depending on your case.</p>"},{"location":"starting/1-install.html#installing-the-dependencies","title":"Installing the dependencies","text":"<p>The project ships 3 manifests:  - <code>requirements.txt</code> for runtime dependencies, - <code>requirements-email.txt</code> for the email filtering/parsing stack, - <code>requirements-dev.txt</code> for the documentation toolchain.</p> <p>From the <code>VirtualSecretary/</code> folder, run: <pre><code>PYTHON -m pip install -r recipe/requirements.txt\nPYTHON -m pip install -r recipe/requirements-email.txt\n</code></pre></p> <p>If you also want to build and serve the documentation locally:</p> <pre><code>pip install -r requirements-dev.txt\nmkdocs serve\n</code></pre> <p>Notable runtime dependencies (already covered by <code>requirements.txt</code>):</p> Group Key packages Linear algebra / ML <code>numpy</code>, <code>scipy</code>, <code>numba</code>, <code>scikit-learn</code>, <code>gensim==4.4</code> NLP <code>nltk</code>, <code>fast-langdetect</code>, <code>blingfire</code>, <code>rank-bm25</code>, <code>levenshtein</code> Network / crawling <code>curl_cffi</code>, <code>httpx</code>, <code>requests</code>, <code>protego</code>, <code>charset_normalizer</code> HTML parsing <code>beautifulsoup4</code>, <code>html5lib</code> PDF / OCR <code>PyMuPDF</code>, <code>pdf2image</code>, <code>pytesseract</code>, <code>pillow==10.4.0</code>, <code>opencv-python-headless</code> Web app <code>flask</code>, <code>flask-caching</code>, <code>markdown</code> <p>Version pins matter</p> <p><code>numba==0.61.0</code> requires <code>numpy&gt;=2.0,&lt;=2.1</code>. <code>pillow==10.4.0</code> and <code>pypdf==3.8.1</code> are pinned to avoid breaking changes in newer releases. Install from <code>requirements.txt</code> as-is rather than resolving versions manually.</p> <p>Tesseract OCR binaries must be installed separately at the OS level for scanned PDF support:</p> <pre><code># Debian / Ubuntu\nsudo apt install tesseract-ocr tesseract-ocr-eng tesseract-ocr-fra\n</code></pre>"},{"location":"starting/1-install.html#notes-on-servers","title":"Notes on servers","text":"<p>Note</p> <p>This installation procedure was tested on Ubuntu Server 20.04 using an Amazon Elastic Cloud 2 instance, on Fedora Desktop 37 and 38, and on a CPanel-based shared hosting (Debian).</p> <p>If you use a private or shared hosting, you have access to a server where you may run applications, host websites and mailboxes. It is very desirable to install the Virtual Secretary on the same server hosting your mailbox, as it will be able to filter those email in-place. In that case, for security purposes, it is better to put the application code outside of the HTTP server directories (<code>var_html</code>, or <code>/var-www</code>, or any directory accessible through a web browser).</p> <p>CPanel-based shared hostings have limited resources, but my tests show they can still be used for a single user with a couple email addresses. Nowadays, they provide a web terminal allowing to launch command lines, even though they still don\u2019t come with <code>sudo</code> rights to install software.</p> <p>If your hosting does not have a web terminal interface, nor SSH\u00a0access, and is basically just a piece of harddrive connected to the internet with some way to define Cron jobs, you will have to prepare the <code>VirtualSecretary</code> folder with the source code and the extracted Python AppImage locally, on your computer, and then to dump it all on the server through FTP.</p>"},{"location":"starting/2-configure.html","title":"Configure","text":"<p>Virtual Secretary is designed to require as little programming as possible while still retaining the full potential of Python should you need it. The configuration is done by using declarative <code>settings.ini</code> files and templated Python filters.</p> <p>An example configuration is provided in the repository in the <code>examples</code> folder, you can copy it somewhere and start editing it.</p> <p>Note : only the IMAP emails connectors are implemented for now.</p>"},{"location":"starting/2-configure.html#configuration-structure","title":"Configuration structure","text":""},{"location":"starting/2-configure.html#folders-tree","title":"Folders tree","text":"<p>Let\u2019s say your configuration base folder is called <code>config</code> for simplicity. It should contain at least a subfolder <code>common</code>, for global filters, and an email subfolder that you can call whatever you want. You may add as many subfolders as you want and name them as you want, only the <code>common</code> on is mandatory and subfolders will be processed in alphabetical order.</p> <p>Each subfolder (except <code>common</code>) should contain a <code>settings.ini</code> file containing the credentials (server, login, password) for all your IMAP, SMTP, CardDAV, CalDAV and MySQL accounts. Each folder can be set for at most one account for each protocol, if you need to configure a \u201cone to many\u201d or a \u201cmany to one\u201d kind of situation, you have to manage each input -&gt; output combination in a separate subfolder. <code>settings.ini</code> files should look like that :</p> <pre><code>[imap]\nuser = me@server.com\npassword = XXXXXXXX\nserver = mail.server.com\nentries = 20\n</code></pre>"},{"location":"starting/2-configure.html#filter-naming-convention","title":"Filter naming convention","text":"<p>The subfolders contain the filters, which are Python files with <code>.py</code> extension. The names of the filter files contain processing instructions given to the main program, therefore they need to follow a specified format.</p> <p>There are 2 kinds of filters:</p> <ol> <li>Typical filters, performing read-write operations in real-time, which filenames are prefixed with 2 digits declaring their execution priority (<code>00</code> runs first, <code>99</code> runs last),</li> <li>Learning filters, performing read-only operation that can be deferred at convenient times, which filenames are prefixed with <code>LEARN</code>. They don\u2019t get priorities.</li> </ol> <p>The typical filters are run by calling <code>$ python src/main.py config process</code> and the learning filters are run <code>$ python src/main.py config learn</code>. This gives you the opportunity to run heavy data parsing operation at times where your mailbox is not busy, without destroying real-time responsivity. Learning filters have no explicit priorities, they will be processed by alphabetical order. Note that nothing prevents you from using learning filters as processing filters, and the other way around, they ultimately run the same \u2014 it\u2019s up to you to follow the scripting hygiene you find suitable and to apply some discipline. I suggest respecting the intended use.</p> <p>After the prefix comes, separated by a dash <code>-</code>, comes the name of the trigger protocol (<code>imap</code>, <code>carddav</code>, <code>caldav</code>). The trigger is the kind of data that will start the filtering process, for example the <code>imap</code> protocol will loop the filters over emails, <code>carddav</code> will loop the filters over vCard contacts, etc.</p> <p>Finally, the mnemonic name of the filter, separated again by a dash <code>-</code> from the previous. This is some meaningful name that will help you sort your filters for admin purposes. They accept letters (upper and lower case), digits, dashes and underscores.</p>"},{"location":"starting/2-configure.html#summary","title":"Summary","text":"<p>That should leave you with something like :</p> <ul> <li><code>./config/</code>:<ul> <li><code>/common/</code>:<ul> <li><code>00-imap-spam.py</code></li> <li><code>01-imap-urgent.py</code></li> <li><code>LEARN-imap-ban-ip.py</code></li> </ul> </li> <li><code>/me/</code>:<ul> <li><code>settings.ini</code></li> <li><code>03-imap-family.py</code></li> <li><code>04-imap-banks.px</code></li> </ul> </li> <li><code>/pro/</code>:<ul> <li><code>settings.ini</code></li> <li><code>03-imap-colleagues.py</code></li> <li><code>04-imap-clients.py</code></li> </ul> </li> </ul> </li> <li><code>./src/</code><ul> <li><code>core</code> and <code>protocols</code> modules (not covered in this section).</li> </ul> </li> </ul>"},{"location":"starting/2-configure.html#connectors","title":"Connectors","text":""},{"location":"starting/2-configure.html#emails","title":"Emails","text":""},{"location":"starting/2-configure.html#gmail","title":"Gmail","text":"<p>For Gmail, you need to enable the IMAP transfer and use an app-specific password.</p> <p>Gmail IMAP uses TLS on the default port (993) and Gmail SMTP uses TLS on the default port too (465), so you only need to configure user/password/server.</p>"},{"location":"starting/2-configure.html#outlook-and-office365","title":"Outlook and Office365","text":"<p>As of 2022, Outlook users are the free accounts and Office365 are the paying accounts. Both support IMAP\u00a0over TLS with the default port (993), so it\u2019s easy.</p> <p>However, Outlook (free) SMTP does not support TLS but only STARTTLS. This is not supported in Virtual Secretary because it\u2019s not secure enough. You can use a different SMTP provider though, like Gmail or anything self-hosted.</p> <p>Office365 SMTP supports TLS on port 587 (non-standard), so you need to add <code>port = 587</code> in the <code>[smtp]</code> section of your <code>settings.ini</code> configuration file.</p>"},{"location":"starting/2-configure.html#testing-and-debugging-filters","title":"Testing and debugging filters","text":"<p>When your filters and configuration files are written, you can start the main script with the path to the config file as an argument: <pre><code>$ python src/main.py /path/to/config process\n</code></pre></p> <p>This results in something like: <pre><code>$ python src/main.py config/ process\n\nExecuting filter 00-imap-spam.py :\n  - IMAP        took 6.689 s    to query        50 emails from INBOX\n  - Parsing     took 0.536 s    to parse        50 emails\n  - Filtering   took 0.041 s    to filter       50 emails\n\nExecuting filter 01-imap-newsletters.py :\n  - IMAP        took 5.826 s    to query        50 emails from INBOX\n  - Parsing     took 0.522 s    to parse        50 emails\n  - Filtering   took 0.042 s    to filter       50 emails\n\nExecuting filter 06-imap-calendar-rsvp.py :\n  - IMAP        took 5.972 s    to query        50 emails from INBOX\n  - Parsing     took 0.619 s    to parse        50 emails\n  - Filtering   took 0.044 s    to filter       50 emails\n\nGlobal execution took 27.59 s. Mind this if you use cron jobs.\n</code></pre></p> <p>In each subfolder, a <code>sync.log</code> plain-text file will keep track of every successful action made on emails. The output of the terminal, when you run the main script, will output all errors and exceptions met during the execution of the program, as well as a log of filters run and runtimes.</p> <p>The history database of all filters is stored as an hidden file along with their filter. Say you run a filter which name is <code>00-imap-spam.py</code>, its database is stored in the same subfolder and named <code>.00-imap-spam.py.log</code>. In case of a problem, you can safely remove this file but this will then re-run the filter for all emails so beware of the consequences if your filters are run more than once (or any specified number of times). Those logs are not plain-text but Python dictionaries saved as pickle format (binary dump of the memory). This makes them impossible to read outside Python but very efficient to read/write and reasonably small.</p>"},{"location":"starting/3-run.html","title":"Running the filters : the main script","text":""},{"location":"starting/3-run.html#basic-use","title":"Basic use","text":"<p>The <code>src/main.py</code> script is the entry point of the application. It will detect all filters declared in the config directory and run them in sequence, while keeping logfiles for user traceability as well as to avoid filtering the same objects more than once (in case a false-positive was manually reverted, avoid reprocessing it later).</p> <p>When you grab the source code from Github (see Install), you get a <code>VirtualSecretary</code> directory. You can add a <code>config</code> folder in it, to obtain the following tree:</p> <ul> <li><code>VirtualSecretary/</code>:<ul> <li><code>config/</code> (add it)</li> <li><code>data/</code> (default)</li> <li><code>models/</code> (default)</li> <li><code>src/</code> (default)<ul> <li><code>core/</code> (default)</li> <li><code>protocols/</code> (default)</li> <li><code>tests/</code> (default)</li> <li><code>user_scripts/</code> (default)</li> </ul> </li> </ul> </li> </ul> <p>The <code>config</code>, <code>models</code>, <code>data</code> and <code>src/user_scripts</code> folders are ignored by Git so you can write in them with no fear that the next <code>git pull</code> will override them. The configuration details and base filters are covered in a specific page.</p> <p>Once you have a <code>config</code> directory and some filters to process, you can execute:</p> <pre><code>cd VirtualSecretary\npython src/main.py config/ process\n</code></pre> <p>To run it from the non-extracted AppImage (on systems supporting FUSE):</p> <pre><code>./VirtualSecretary-x86_64.AppImage path/to/src/main.py path/to/config process\n</code></pre> <p>To run it from the extracted AppImage (as shown above \u2014 assuming you extracted it to <code>appimage/</code>), on systems not supporting FUSE:</p> <pre><code>./python3.12/AppRun virtualsecretary/src/main.py virtualsecretary/config/ process\n</code></pre>"},{"location":"starting/3-run.html#general-use","title":"General use","text":"<pre><code>python src/main.py CONFIG_DIR MODE\n</code></pre> <ul> <li><code>CONFIG_DIR</code>: the top directory where all configuration files are hosted</li> <li><code>MODE</code> is either:<ul> <li><code>process</code>:\u00a0to run the automation filters (which names starts by <code>01-imap-</code>, <code>02-caldav-</code>, etc),</li> <li><code>learn</code>:\u00a0to run the machine-learning filters (which names starts by <code>LEARN-</code>).</li> </ul> </li> </ul>"},{"location":"starting/3-run.html#options","title":"Options","text":"<p>Options need to be added after <code>python src/main.py config/ process</code>. The available options are :</p> <ul> <li><code>-h</code>:\u00a0display the help of the script in the terminal (showing this list of options and their meaning),</li> <li><code>-s &lt;SUBDIR&gt;</code>, <code>--single &lt;SUBDIR&gt;</code>: process only the specified <code>&lt;SUBDIR&gt;</code> sub-directory of the configuration directory (replace <code>&lt;SUBDIR&gt;</code> by the real sub-directory name). This is useful to debug filters on a single mail account.</li> <li><code>-n &lt;INT&gt;</code>, <code>--number &lt;INT&gt;</code>:\u00a0globally override the number of items to process defined in <code>settings.ini</code> with the <code>&lt;INT&gt;</code> value. This is useful when running the application for a first time, when you need to catch on.</li> <li><code>--server</code>:\u00a0enable the server mode, which adds a sleeping time between each element filtering, as to reduce system load and allow more breathing room to other running processes. This is useful if you keep the application running in background all the time, as it reduces the average CPU\u00a0usage below 2% on Intel Xeon and matters if you run the application on small servers or on server instances where CPU\u00a0load spikes are billed extra (like Amazon Elastic Cloud).</li> <li><code>-f</code>, <code>--force</code>: globally force reprocessing already-processed items. Normally, items are filtered only once (unless specified otherwise in filter files) and never filtered again on future runs. This is helpful if a filter didn\u2019t work as expected, and you modified it.</li> </ul>"},{"location":"starting/3-run.html#with-cron-job","title":"With Cron job","text":"<p>You can run your filters every <code>n</code> minutes, on Linux desktop and servers, using a Cron job. If you have several devices getting emails, for example, it may be a good idea to run them on your email servers every 10-30 minutes, so all your devices get a clean input.</p> <pre><code>contab -e\n*/10 * * * * python ~/VirtualSecretary/src/main.py ~/VirtualSecretary/config process --server\n</code></pre>"},{"location":"starting/4-example-filters.html","title":"Example filters","text":"<p>The <code>examples</code> folder contains fully-functional filters that can run as soon as you edit the <code>settings.ini</code> file with your credentials in your <code>config</code> directory. I\u00a0will present them here.</p>"},{"location":"starting/4-example-filters.html#anti-spam-filter","title":"Anti-spam filter","text":"<p>The elephant in the room, consists of 2 filters stored in <code>/common/</code> :</p> <ul> <li><code>00-imap-spam.py</code>,</li> <li><code>LEARN-imap-ban-ip.py</code>.</li> </ul> <p>It actually starts with <code>LEARN-imap-ban-ip.py</code>. It assumes both your spam and inbox folders have been manually cleaned, so they actually represent what you consider (un)desirable mail. This filter does not apply any action, instead it simply records the emails and IP address of spam mail and puts them in a blacklist file, then it does the same in your inbox and puts them in a whitelist file. Emails and IP\u00a0addresses that can be found in both spam and inbox are removed from both lists. If you manually move emails between spam and inbox folders, the lists are updated at the next run.</p> <p>The <code>00-imap-spam.py</code> filter then reuses those lists to immediately accept whitelisted senders and immediately mark as spam the blacklisted senders. For unknown senders (that are in neither lists), we then proceed to looking for SpamAssassins and check if it is a bulk email sent without a <code>List-Unsubscribe</code> (which is illegal in Europe and a reliable clue).</p> <p>The <code>00-imap-spam.py</code> can be extended by posting the sending IPs (<code>email.ip</code>) and URLs found in the body (<code>email.url</code>) to a reputation server, using GET or POST requests through HTTP with the Python modules <code>pyCurl</code> or <code>requests</code>.</p>"},{"location":"starting/4-example-filters.html#detect-calendar-event-invites","title":"Detect calendar event invites","text":"<p>The <code>examples/me@server.com/06-imap-rsvp.py</code> shows how to add an <code>RSVP</code> labels to emails containing invitations to calendar events. Those are generally attachments having <code>.ics</code> format. The filter flattens the list of attachments and checks if a filename ending in <code>.ics</code> is there. Though the use of regular expressions is slightly overkill in this context, it is a good example of how they can be used in filters.</p>"},{"location":"starting/4-example-filters.html#mass-unsubscribe-from-all-spam-mailing-lists","title":"Mass-unsubscribe from all spam mailing lists","text":"<p>The <code>examples/me@server.com/05-imap-mass-unsubscribe.py</code> tries to unsubscribe in bulk from all the newsletters in the spam box. This is done by following the link that should be in the <code>\"List-Unsubscribe\"</code>. Problem is, many spams don\u2019t provide one, and even for those which do, a lot of mailing lists use \u201cdouble opt-out\u201d mechanisms where you need to load the page AND click a button (which is ironic since they usually got your emails from leaks and didn\u2019t ask you to double opt-in). This will work for mailing-lists using \u201cOne-Click Unsubscribe\u201d.</p> <p>This can be extended by sending an email to the address in <code>\"List-Unsubscribe\"</code> once SMTP support is added.</p>"},{"location":"starting/4-example-filters.html#move-newsletters-away","title":"Move newsletters away","text":"<p>Newsletters (known as <code>bulk</code> emails) are typically content that doesn\u2019t require your immediate attention and doesn\u2019t need to stay in your main inbox. The <code>examples/me@server.com/01-imap-newsletters.py</code> filter uses the <code>\"Precedence\"</code> to detect such emails and moves them to a dedicated <code>Newsletters</code> folder. This needs to run after the anti-spam detection.</p>"},{"location":"starting/4-example-filters.html#move-invoices-away","title":"Move invoices away","text":"<p>Again, invoices sent by email are for your archives and future reference, but they don\u2019t require your immediate attention (unless someone else ordered). The <code>examples/me@server.com/02-imap-invoices.py</code> looks for the keyword \u201cinvoice\u201d into the email subject and body. This is a good example of simple keyword-based content filters</p>"},{"location":"starting/4-example-filters.html#detect-questions-in-emails","title":"Detect questions in emails","text":"<p>TODO. Emails containing questions may be inquiries from clients and need to be answered at some point.</p>"},{"location":"starting/4-example-filters.html#dependencies","title":"Dependencies","text":"<pre><code>pip install -U pip setuptools wheel\npip install -U scikit-learn nlkt\npip install -U 'spacy[cuda117,transformers,lookups]'\npython -m spacy download en_core_web_trf\npython -m spacy download fr_dep_news_trf\n</code></pre> <p>See https://spacy.io/usage to install your language support.</p>"},{"location":"starting/4-example-filters.html#sync-google-agenda-with-any-caldav-calendar","title":"Sync Google Agenda with any CalDAV calendar","text":"<p>TODO. Many services support Google Agenda API (REST) without supporting standard CalDAV servers. There are nasty ways of sharing read-only calendars between Google and CalDAV servers, using private .ics links, but these are read-only and not secure (everyone knowing the private link can access the agenda). Also, Google doesn\u2019t check your availability by looking into third-party calendars, so people or services that can only access Google Agenda may double-book you if you already have an appointment written on another calendar. To make things worse, Google Agenda doesn\u2019t provide a ready CalDAV API, this needs to be manually activated into the developers console, along with creating an OAuth token, and the CalDAV API is v2 only. Annoying and not even future-proof (Google may discontinue CalDAV support when they want).</p> <p>The most simple way to address this issue is by duplicating events and appointments between a reference CalDAV server and Google Agenda bidirectionally:</p> <ul> <li>Connect to Google Agenda through their native Rest API (with authentification),</li> <li>Connect to the CaldDAV server through CalDAV v3 protocol (with authentification),</li> <li>On first reading:<ul> <li>Google -&gt; CalDAV :\u00a0copy events integrally, including emails, locations, etc.<ul> <li>leave a token in the duplicated event, stating that it was originally a Google event,</li> <li>save the timestamp of the last edit on Google Agenda in the CalDAV duplicate,</li> </ul> </li> <li>CalDAV -&gt; Google : copy obfuscated events, discarding any personal data, just to lock the time slot for availability status,<ul> <li>leave a token in the duplicated event, stating that it was originally a CalDAV event,</li> <li>save the timestamp of the last edit on the CalDAV server in the Google Agenda duplicate,</li> </ul> </li> </ul> </li> <li>On later readings: on both origins, for each event, if the event is a duplicate, check if the last edit timestamp stored in the duplicate matches the timestamp of the original. If timestamps don\u2019t match, overwrite the duplicate or delete it if the original is not found.</li> </ul> <p>See https://google-calendar-simple-api.readthedocs.io/en/latest/getting_started.html</p>"},{"location":"starting/5-writing-filters.html","title":"Writing filters","text":""},{"location":"starting/5-writing-filters.html#email-filters","title":"Email filters","text":""},{"location":"starting/5-writing-filters.html#structure","title":"Structure","text":"<p>Filters named like <code>xx-imap-name.py</code> are triggered by incoming email fetched from IMAP. A basic email filter can be found in <code>examples/common/03-imap-invoices.py</code>. Here is the structure :</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\n\ndef filter(email) -&gt; bool:\n  query = [\"confirmation\", \"invoice\", \"billing\", \"facture\", \"achat\", \"facture\", \"commande\"]\n  return email.is_in(query, \"Subject\")\n\ndef action(email):\n  email.move(\"INBOX.Money.Invoices\")\n\nimap.get_objects(\"INBOX\")\nimap.run_filters(filter, None)\n</code></pre> <p>Let\u2019s analyse how it\u2019s written:</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\n</code></pre> <p>is mandatory to get the mail server from the main script <code>main.py</code>. The mail server contains a live SSL connection to the IMAP server which fetches the login credentials from the <code>settings.ini</code> files. You can copy-paste these as-is in all filters. Every server protocol implemented will be available from the <code>protocols</code> dictionary. Note that getting the servers trough <code>globals</code> is necessary only from outside the <code>action()</code> and <code>filter()</code> functions, to be able to call <code>imap.get_objects()</code> and <code>imap.run_filters()</code>. From within the functions, they are referenced from <code>email.server</code> and can be accessed locally.</p> <pre><code>def filter(email) -&gt; bool:\n  query = [\"confirmation\", \"invoice\", \"billing\", \"facture\", \"achat\", \"facture\", \"commande\"]\n  return email.is_in(query, \"Subject\")\n\ndef action(email):\n  email.move(\"INBOX.Money.Invoices\")\n</code></pre> <p>are the filtering template functions. As input argument, they take an <code>email</code> instance of the <code>EMail</code> instance as defined in <code>/src/protocols/imap_object.py</code>. The <code>filter</code> function performs the test over the email content and outputs a boolean, <code>True</code> if the conditions are met, <code>False</code> otherwise. If <code>True</code>, the <code>action</code> function is executed on the email, otherwise nothing happens. Here, the filter checks if the subject of the email contains any word in the <code>query</code> list and the action taken is to move the email to the <code>Money/Invoices</code> IMAP folder.</p> <p>Note : IMAP subfolders can be separated either with <code>/</code>, as in <code>INBOX/Money/Invoices</code> (format used by Gmail and Hotmail) or with <code>.</code>, as in <code>INBOX.Money.Invoices</code> (format used by most open-source servers). You may use one or the other when writing filters, the one you prefer, the program will detect automatically which one your mail server uses and will re-encode subfolders properly. Spaces and accentuated characters are also supported in IMAP folders.</p> <pre><code>imap.get_objects(\"INBOX\")\nimap.run_filters(filter, action)\n</code></pre> <p>finally opens a mailbox, here set to <code>INBOX</code> (the main inbox), and then passes on the <code>filter</code> and <code>action</code> methods to the mail loop. These will then be run sequentially on the <code>n</code> latests emails in <code>INBOX</code>. By default, the number of emails loaded from the mailbox is set to whatever is set as <code>entries =</code> in <code>settings.ini</code> under the <code>imap</code> section. It is possible to override the global setting by calling <code>imap.get_mailbox_emails(\"INBOX\", n_messages=20)</code>.</p> <p>The line <code>imap.run_filters(filter, action)</code> can be called as-is and will apply the filters only once by email, meaning if you execute the main script more than once, emails already processed will be ignored. This is done by keeping a database of already-processed emails in an hidden file, identifying emails by an unique MD5 hash and timestamp, and recording the number of runs for each filter. This is especially useful to manually take over auto spam filters : auto filters run just once, giving the user the opportunity to correct, but don\u2019t re-apply on top of user changes. To bypass this limit, you can call <code>imap.run_filters(filter, action, runs=-1)</code> or set <code>runs =</code> to the max number of runs you want.</p> <p>It is possible to call the filters on more than one mailbox in the same filter file, like:</p> <pre><code>imap.get_objects(\"INBOX\")\nimap.run_filters(filter, action, filtername)\n\nimap.get_objects(\"INBOX.spam\")\nimap.run_filters(filter, action, filtername)\n</code></pre> <p>or you can even loop over all the folders found on your IMAP server, excluding the spam folder:</p> <pre><code>for folder in imap.folders:\n  if folder != imap.junk:\n    imap.get_objects(folder)\n    imap.run_filters(filter, action)\n</code></pre>"},{"location":"starting/5-writing-filters.html#training-an-email-folder-classifier","title":"Training an email folder classifier","text":"<p>You can use your manually-sorted IMAP folders as labels for an AI classifier. The workflow is:</p> <ol> <li>fetch emails from each IMAP folder,</li> <li>convert each email into a <code>web_page</code> item, using the IMAP folder as <code>category</code>,</li> <li>train a core.nlp.Word2Vec language model on the email text,</li> <li>train a core.nlp.Classifier to predict the folder,</li> <li>load the classifier from an IMAP filter and move new inbox emails only when confidence is high enough.</li> </ol>"},{"location":"starting/5-writing-filters.html#step-1-collect-emails-as-training-data","title":"Step 1: collect emails as training data","text":"<p>Create a learning filter, for example <code>LEARN-imap-folder-dataset.py</code>, in your email account configuration folder:</p> <pre><code>from core import types, utils\n\nprotocols = globals()\nimap = protocols[\"imap\"]\n\nDATASET_NAME = \"email-folders\"\n\n# Keep only folders whose current contents are good examples of the label.\n# Remove any folder from this set if you want to train on it too.\nEXCLUDED_FOLDERS = {\n    imap.inbox,\n    imap.sent,\n    imap.drafts,\n    imap.trash,\n    imap.junk,\n}\n\n\ndef email_text(email):\n    body = email.get_body(\"plain\") or email.get_body() or \"\"\n    parts = [\n        email[\"Subject\"],\n        email[\"From\"],\n        body,\n    ]\n    return \"\\n\\n\".join(part for part in parts if part)\n\n\ndef email_to_web_page(email, folder):\n    content = email_text(email)\n\n    return types.sanitize_web_page(types.web_page(\n        title=email[\"Subject\"] or \"(no subject)\",\n        url=f\"imap://{email.server.user}/{folder}/{email.hash}\",\n        domain=email.server.server,\n        date=email.date.isoformat(),\n        datetime=email.date,\n        content=content,\n        excerpt=content[:800],\n        category=folder,\n        lang=None,\n    ))\n\n\npages = []\n\nfor folder in imap.folders:\n    if folder in EXCLUDED_FOLDERS or folder == \"[Gmail]\":\n        continue\n\n    # get_objects() fetches the n most recent emails. Use a large number\n    # to collect the whole folder, or a smaller cap while testing.\n    imap.get_objects(folder, n_messages=10**9)\n\n    for email in imap.objects:\n        pages.append(email_to_web_page(email, folder))\n\nutils.save_data(pages, DATASET_NAME)\nprint(f\"Saved {len(pages)} labelled emails to {DATASET_NAME}\")\n</code></pre> <p>Run learning filters with:</p> <pre><code>python src/main.py config learn\n</code></pre> <p>This creates <code>data/email-folders.pickle.tar.gz</code>.</p> <p>Tip</p> <p>The folder labels are only as good as your mailbox organization. Start with folders you already trust, and exclude catch-all folders, archives, spam, sent mail, and folders that mix unrelated topics.</p>"},{"location":"starting/5-writing-filters.html#step-2-train-the-language-model-and-classifier","title":"Step 2: train the language model and classifier","text":"<p>Create a user script, for example <code>src/user_scripts/train-email-folder-classifier.py</code>:</p> <pre><code>import os\nimport sys\n\nSCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))\nsys.path.append(os.path.dirname(SCRIPT_DIR))\n\nfrom core import nlp, utils\n\nDATASET_NAME = \"email-folders\"\nWORD2VEC_NAME = \"email-folder-word2vec\"\nCLASSIFIER_NAME = \"email-folder-classifier\"\n\npages = utils.open_data(DATASET_NAME, scheme=\"pickle\")\n\ntokenizer = nlp.Tokenizer()\n\ndocuments = [\n    tokenizer.tokenize_document_per_sentence(page[\"content\"])\n    for page in pages\n    if page[\"content\"] and page[\"category\"]\n]\n\nw2v = nlp.Word2Vec(\n    documents,\n    WORD2VEC_NAME,\n    tokenizer=tokenizer,\n    vector_size=300,\n    epochs=200,\n    window=8,\n    min_count=3,\n    sample=0.0005,\n)\n\ntraining_set = [\n    nlp.Data(page[\"content\"], page[\"category\"])\n    for page in pages\n    if page[\"content\"] and page[\"category\"]\n]\n\nclassifier = nlp.Classifier(\n    training_set,\n    CLASSIFIER_NAME,\n    w2v,\n    validate=True,\n    variant=\"svm\",\n)\n\nfor text in [\n    \"Invoice for your order\",\n    \"Meeting notes and next actions\",\n    \"Your newsletter subscription\",\n]:\n    print(text, classifier.prob_classify(text))\n</code></pre> <p>Run it from the project root:</p> <pre><code>python src/user_scripts/train-email-folder-classifier.py\n</code></pre> <p>When the validation accuracy looks good, run the same script with <code>validate=False</code> in the <code>nlp.Classifier(...)</code> call to train the final production model on all available emails.</p>"},{"location":"starting/5-writing-filters.html#step-3-sort-incoming-emails-from-classifier-predictions","title":"Step 3: sort incoming emails from classifier predictions","text":"<p>Create a normal processing filter, for example <code>10-imap-ai-folder-sort.py</code>:</p> <pre><code>from core import nlp\n\nprotocols = globals()\nimap = protocols[\"imap\"]\n\nCLASSIFIER_NAME = \"email-folder-classifier\"\nCONFIDENCE_THRESHOLD = 0.75\n\nclassifier = nlp.Classifier.load(CLASSIFIER_NAME)\n\n\ndef email_text(email):\n    body = email.get_body(\"plain\") or email.get_body() or \"\"\n    parts = [\n        email[\"Subject\"],\n        email[\"From\"],\n        body,\n    ]\n    return \"\\n\\n\".join(part for part in parts if part)\n\n\ndef filter(email):\n    folder, confidence = classifier.prob_classify(email_text(email))\n    email.predicted_folder = folder\n    email.prediction_confidence = confidence\n\n    return (\n        confidence &gt;= CONFIDENCE_THRESHOLD\n        and folder\n        and folder != email.server.mailbox\n    )\n\n\ndef action(email):\n    email.move(email.predicted_folder)\n\n\nimap.get_objects(imap.inbox)\n\n# runs=-1 lets the classifier reconsider low-confidence messages on future\n# runs after you retrain the model. Confident messages leave INBOX anyway.\nimap.run_filters(filter, action, runs=-1)\n</code></pre> <p>The confidence threshold is deliberately conservative. Start around <code>0.75</code> or <code>0.80</code>, inspect what was moved, then lower it only if the classifier is consistently right.</p>"},{"location":"starting/5-writing-filters.html#filtering-fields","title":"Filtering fields","text":"<p>The <code>/src/mailserver.py</code> script decodes and parses emails content, and provides direct access to the different fields through the <code>EMail</code> class. Here are the available fields that you can query to write your filters:</p> <ul> <li><code>Email</code> is an object that stores the content of an email and provides parsing methods running on-demand or in background when reading an email from the IMAP server:</li> <li><code>Email.msg</code> is a property that instanciate an <code>EmailMessage</code> object from the standard Python package <code>email</code>. Most of the methods in <code>Email</code> are helpers and proxies on top of this object. All the methods of <code>EmailMessage</code> can be accessed from there.</li> <li><code>Email.headers</code> is the list of header fields found in the message. It is a proxy for <code>EmailMessage.keys()</code> from the standard Python package <code>email</code>. The header fields can then be accessed as a Python dictionnary:<ul> <li>Basic fields:<ul> <li><code>EMail[\"From\"]</code>: sender of the email, either an single email like <code>j.doe@server.com</code> or a name/email pair like <code>\"John Doe\" &lt;j.doe@server.com&gt;</code>. For convenience, the email addresses and names are parsed and provided as lists in the method <code>names, addresses = EMail.get_sender()</code> as <code>['j.doe@server.com']</code> no matter how it is declared in <code>EMails[\"From\"]</code>,</li> <li><code>EMail[\"Date\"]</code>: date of sending of the email,</li> <li><code>EMail[\"Subject\"]</code>: subject of the email, may be set to empty string,</li> <li><code>EMail[\"To\"]</code>: recipient of the email, mandatory but mildly irrelevant here,</li> <li><code>EMail[\"Message-ID\"]</code>: an unique ID set by the SMTP server that sent the email. It should be defined but some spam emails don\u2019t have one.</li> </ul> </li> <li>Special fields:<ul> <li><code>EMail[\"Return-Path\"]</code>: usually used by newsletters and auto mailing, it sets the email address to notify when an email bounces (can\u2019t be delivered). Mailing-lists software can then run scripts that fetch bounces from the email capturing them and remove the bouncing address from their lists.</li> <li><code>EMail[\"Reply-To\"]</code>: the preferred email address to be used if you intend on replying to this email (typically the same as the one which sent it, but not necessarily),</li> <li><code>Email[\"In-Reply-To\"</code>: if the current email is a reply to another email, this header stores the <code>Message-ID</code> of that particular email.</li> <li><code>Email[\"References\"]</code>: if the current email is a reply to another email, this header stores the <code>Message-ID</code> of that particular email (aka duplicates the <code>In-Reply-To</code> field) but also all the <code>Message-ID</code> of the previous emails in the thread. This allows email clients to follow threads.</li> <li><code>EMail[\"Delivery-date\"]</code>: date and time of delivery on your IMAP server.</li> </ul> </li> <li>Mass-mailing (newsletters or spam) fields :<ul> <li><code>EMail[\"Precedence\"]</code>: set to <code>bulk</code> if the email was sent through a mass-mailing system, good hint to detect newsletters,</li> <li><code>EMail[\"List-Unsubscribe\"]</code> : for bulk emails, this must to be set to either an unique link to follow to unsubscribe from the mailing list, or an email address to which send some email to opt-out the mailing list. Many spam emails don\u2019t define it, which is a good clue.</li> <li><code>EMail[\"List-Unsubscribe-Post\"]</code> : defines the method to unsubscribe from the mailing list. Should be set to <code>List-Unsubscribe=One-Click</code>, which means you only need to visit the URL set in <code>EMail[\"List-Unsubscribe\"]</code> to get removed from the mailing list. Unfortunately, most spammers use a double opt-out technique where you need to visit the link AND click on a confirmation button, which means you can\u2019t script a mass-unsubscribe filter.</li> <li><code>EMail[\"List-ID\"]</code>: the mailing-list of that current email.</li> </ul> </li> <li>Custom fields: user agents are allowed to define their own custom fields, starting with <code>X</code>:<ul> <li><code>EMail[\"X-Mailer\"]</code>: user agent sending the email,</li> <li><code>EMail[\"X-Spam-Status\"]</code>, <code>EMail[\"X-Spam-Score\"]</code>, <code>EMail[\"X-Spam-Bar\"]</code>, <code>EMail[\"X-Ham-Report\"]</code>, <code>EMail[\"X-Spam-Flag\"]</code>: SpamAssassin headers giving clues on whether the message is spam or not.</li> </ul> </li> <li>Any header entry found in email will create a field here, accessible through the header name as a key in the dictionnary. The above fields are given as examples to get started, more can be found in the RFC822.</li> </ul> </li> <li><code>EMail.flags</code> is a flat string of characters containing all the IMAP flags, standard and user-defined, like <code>\\Seen</code> if the message has been read or <code>Junk</code> if Thunderbird detected it as spam, or any custom flag you set. User-defined flags are treated as labels or tags by most email clients.</li> <li><code>EMail.ips</code> is a list of all the IP of the servers through which your email transited before getting into your mailbox. It is parsed from the <code>Received</code> headers, which contains the whole server route taken by the email.</li> <li><code>EMail.urls</code> is a list of tuples containing all URLs in the email body, both from the plain text and HTML versions. The tuples split URLs as <code>(domain, page)</code> like :<ul> <li><code>https://google.com/index.php</code> is broken into <code>('google.com', '/index.php')</code></li> <li><code>https://google.com/</code> is broken into <code>('google.com', '/')</code></li> <li><code>https://google.com/login.php?id=xxx</code> is broken into <code>('google.com', '/login.php')</code></li> </ul> </li> <li><code>EMail.uid</code> is the unique ID set by the IMAP server. There is a caveat though : it is relative to a particular mailbox and doesn\u2019t follow emails through mailboxes, meaning if you move an email in another folder, this UID will change. Since this is very annoying, the <code>Email</code> class provides another unique ID :</li> <li><code>EMail.hash</code> is a truly unique ID for each email which addresses the problems of the UID above. It is generated from the email headers content and will therefore follow an email through folder moves.</li> <li><code>EMail.attachments</code> is a list of the file names of all the attachments. It does not contain the actual files, but allows to detect the files extension.</li> <li>Regular expressions:<ul> <li><code>EMail.url_pattern</code> is the regex used to extract URLs from the body,</li> <li><code>EMail.email_pattern</code> is the regex used to extract the email from the <code>From</code>.</li> <li>In your filters, to extract all emails from the body, you could then reuse them like <code>EMail.email_pattern.findall(EMail.email.get_body())</code>.</li> </ul> </li> <li>Tests and checks : those are helper functions performing usual tests over the properties of the email:<ul> <li><code>EMail.is_read() -&gt; bool</code>: check if the email has been read, by looking for the standard tag <code>\\Seen</code>,</li> <li><code>Email.is_unread() -&gt; bool</code>: same as above, but returns <code>True</code> if the email has not been read,</li> <li><code>EMail.is_recent() -&gt; bool</code>: check if the email has the standard tag <code>\\Recent</code> which means no other email client got it yet,</li> <li><code>EMail.is_draft() -&gt; bool</code>: check if the email has the standard tag <code>\\Draft</code> which means the email has been written but not sent yet,</li> <li><code>EMail.is_answered() -&gt; bool</code>: check if the email has the standard tag <code>\\Answered</code> which means you sent a reply to it,</li> <li><code>EMail.is_important() -&gt; bool</code>: check if the email has the standard tag <code>\\Flagged</code> which means it has been labelled as important.</li> <li><code>EMail.has_tag(tag: str) -&gt; bool</code>: check if <code>tag</code> is contained in the flags <code>EMail.flags</code>.</li> <li><code>EMail.is_in(query_list, field: str, case_sensitive: bool=False, mode: str=\"any\")</code>: search if the elements in query list can be found in <code>field</code>. Field can by any of keys of the email headers (see above), like <code>'From'</code> or the email body (using all HTML and plain text versions) using the key <code>'Body'</code>. The default <code>mode='any'</code> returns <code>True</code> if any of the elements in <code>query_list</code> is found in the field, otherwise <code>mode='all'</code> returns <code>True</code> if all elements in <code>query_list</code> are found in the field. If using the default <code>case_sensitive=False</code> mode, all elements in <code>query_list</code> should be in lower case. This method will perform the necessary checks on the validity of the field and will return <code>False</code> if the field is not present or empty in the current email.</li> </ul> </li> <li><code>Email.get_body(preferencelist=('related', 'html', 'plain'))</code>: parse and output the body. The optional argument <code>preferencelist</code> defines what part of the body is output ; if not specified, all parts are used. If no <code>html</code> body is found, the <code>plain</code> body is used instead. If no <code>plain</code>\u00a0body is found, one is automatically generated from the <code>html</code> body by removing all HTML markup.</li> <li><code>Email.age()</code>: returns the age of the email as a time delta compared to current time (using the <code>Email[\"Date\"]</code> header), as a <code>datetime.timedelta</code> type. This can be used to check if an email is older than a certain delta, with <code>Email.age() &gt; datetime.timedelta(days=7)</code> returns <code>True</code> if the email is more than 7 days old. The <code>age</code> is accurate down to the second and supports time zones.</li> </ul>"},{"location":"starting/5-writing-filters.html#email-actions","title":"Email actions","text":"<p>The <code>EMail</code> class references the <code>MailServer</code> instance containing the active IMAP connection in <code>EMail.server</code>. The <code>MailServer</code> class is inherited from the <code>imaplib.IMAP4_SSL</code> class that comes with the standard Python module <code>imaplib</code>. This can be used to call directly the underlying <code>imaplib</code> methods from the <code>Email</code> instance and therefore from your filters. But the <code>EMail</code> class provides proxy methods and implements various usual actions so you can reuse them efficiently in your filter code :</p> <ul> <li><code>print(EMail)</code> will print a condensed version of the email, including subject, date, sender, flags, UID, etc. Useful for debugging.</li> <li>Tagging :<ul> <li><code>EMail.tag(keyword:str)</code> : allows to add any tag (also named keyword or flag or label) to your emails. Note that Thunderbird has a quirk here : any tag you add programmatically here will need to be added also in Thunderbird GUI in order to appear in Thunderbird. NextCloud Mail (and apparently Horde, which provides the base libs for NextCloud Mail) detects them automatically and shows them without any fuss.</li> <li><code>EMail.untag(keyword:str)</code> : allows to remove any tag from your emails.</li> <li><code>EMail.mark_as_important(mode:str)</code> : add or remove the <code>\\Flagged</code> standard flag which is interpreted by most clients as an \u201cimportant\u201d label. The <code>mode</code> needs be set to <code>\"add\"</code> or <code>\"remove\"</code>. It is an alias of <code>EMail.tag(\"\\\\Flagged\")</code> and <code>EMail.untag(\"\\\\Flagged\")</code>.</li> <li><code>EMail.mark_as_read(mode:str)</code> : add or remove the <code>\\Seen</code> standard flag which is interpreted by clients as \u201cread\u201d. The <code>mode</code> is set as the previous. It is also an alias of <code>EMail.tag</code> and <code>EMail.untag</code>.</li> <li><code>EMail.mark_as_answered(mode:str)</code> : add or remove the <code>\\Answered</code> standard flag. The <code>mode</code> is set as the previous. It is again an alias of <code>EMail.tag</code> and <code>EMail.untag</code>.</li> </ul> </li> <li>Moving : <code>EMail.move(folder:str)</code> : move the email to the specified folder, like <code>INBOX</code>. If the folder does not exist, it will be automatically created. Folders can be hierarchical, for example <code>INBOX.Money.Taxes</code>, in which case the parent folders will be created too if needed. Note that folder names are case-sensitive. To see what folders are available in your mail account, look at the first lines of the <code>sync.log</code> in your email subfolder : they will be listed.</li> <li>Deleting : <code>EMail.delete()</code> : entirely removes an email from your mailbox. This is without recuperation and will not use the trash bin. If you want to use the trash been, you need to move the email to trash, for example with <code>EMail.move(\"Email.server.trash)</code>.</li> <li>Mark as spam : <code>EMail.spam(spam_folder=\"INBOX.spam\")</code> : add the <code>Junk</code> flag to the email (like Thunderbird) and move the email to the spam folder. Check that it\u2019s the right folder for your mail server.</li> </ul>"},{"location":"starting/5-writing-filters.html#email-threads","title":"Email threads","text":"<ul> <li><code>Email.query_replied_email()</code> finds the email to which the current email replies (referenced by <code>Message-ID</code> in the <code>In-Reply-To</code> header), if any, by looking for its <code>Message-ID</code> header in all folders in the IMAP server. It returns another <code>Email</code> object that can be manipulated the same way as the current one (read, parsed, moved, tagged, etc.), or <code>None</code> if nothing was found.</li> <li><code>Email.query_referenced_emails()</code> finds all the emails from the thread in which the current email belongs (referenced by <code>Message-ID</code> in the <code>References</code> header), by looking for their <code>Message-ID</code> header in all folders in the IMAP server. This can take some time since all queries go through the network. It returns a list of <code>Email</code> objects or an empty list if nothing was found.</li> </ul>"},{"location":"starting/5-writing-filters.html#sending-emails","title":"Sending emails","text":"<p>If you set an SMTP server in your <code>settings.ini</code> file, you can use it to conditionnaly send emails as a response to events triggered by incoming emails. Set your SMTP credentials like such:</p> <pre><code>[smtp]\nserver = server.com\nuser = me@server.com\npassword = xxxx\n</code></pre> <p>You will need to update the servers at the beginning of the filter:</p> <pre><code>protocols = globals()\nimap = protocols[\"imap\"]\nsmtp = protocols[\"smtp\"]\n</code></pre> <p>Then, from within the filter <code>action()</code> method, you can send an email if the filtering condition is met:</p> <pre><code>def action(email):\n  global smtp # needed because smtp is declared outside the scope of the function\n\n  # ensure we have a valid SMTP connection\n  if smtp and smtp.connection_inited:\n\n    # Init the outgoing email subject from the incoming email subject\n    subject = \"Re: \" + email[\"Subject\"]\n\n    # Dummy body content for a confirmation of receipt\n    body = \"[This is an automated answer from the mail server.]\\r\\n\\r\\n\"\n    body += \"Your message was delivered on %s\\r\\n\" % email[\"Delivery-date\"]\n    body += \"Thanks !\"\n\n    # Init the email object, fetching receipient into current email\n    smtp.write_message(subject, email[\"From\"], body, reply_to=email)\n\n    # Send email by SMTP and copy it to the IMAP \"Sent\" folder for archiving\n    smtp.send_message(copy_to_sent=True)\n</code></pre> <p>The <code>smtp</code> server class <code>Server</code> inherits from the class <code>smtplib.SMTP_SSL</code> from the built-in Python package <code>smtplib</code>. It has the following addition methods:</p> <ul> <li><code>Server.write_message(subject: str, to: str, content: str, reply_to=None)</code>: create an <code>message.EmailMessage</code> object from the built-in Python package <code>email</code> and store it into the <code>Server.msg</code> property. Subject, receipient and content are automatically set, and more headers can be added directly through the package <code>email.message</code> using the property <code>Server.msg</code>. If the written message replies to another message, the message replied to can be passed as a <code>message.EmailMessage</code> object to the optional argument <code>reply_to</code>. The relevant headers (<code>In-Reply-To</code> and <code>References</code>) will be automatically extracted from the message being replied to for correct support of email threading in clients.</li> <li><code>Server.send_message(copy_to_sent : bool = False)</code>: send the message through SMTP. If a valid IMAP connection is open and <code>copy_to_sent</code> is set <code>True</code>, the email will also be copied in the default <code>Sent</code> folder of the IMAP\u00a0server.</li> </ul> <p>You will find a basic \u201cleave of absence\u201d autoresponder in <code>examples/common/30-imap-autoresponder-absence.py</code>.</p>"},{"location":"starting/6-crawling-pages.html","title":"Crawling Pages","text":"<p>Whether you want to build a language model for an email classifier or a search engine for information retrieval, you will need to aggregate a corpus of text documents. Virtual Secretary has built-in methods to crawl HTML and PDF documents from websites, assemble them into a SQLite database, and remove duplicate documents to maintain a clean index.</p>"},{"location":"starting/6-crawling-pages.html#getting-page-content","title":"Getting Page Content","text":""},{"location":"starting/6-crawling-pages.html#websites-with-sitemaps","title":"Websites with Sitemaps","text":"<p>The easiest case is websites that publish a sitemap \u2014 an XML file listing all pages the webmaster wants search engines to index. Most CMS platforms include sitemap support out of the box. The usual location is <code>https://your-domain.com/sitemap.xml</code>. Sitemaps can be nested (a sitemap-of-sitemaps); both flat and nested structures are handled transparently by <code>Crawler.get_website_from_sitemap()</code>.</p> <p>Crawl output should always go into a temporary database first (no primary-key constraint on <code>url</code>), be cleaned up and deduplicated, then promoted to the permanent store. See Deduplication for the full pattern.</p> <p>In your <code>VirtualSecretary/src/user-scripts/</code> directory, create a new script called for example <code>scrape-ansel.py</code>, in which you can put:</p> <pre><code># Boilerplate stuff to call core package from user scripts\nimport os\nimport sys\n\nSCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))\nsys.path.append(os.path.dirname(SCRIPT_DIR))\n\nfrom core import crawler, database, deduplicator, nlp, batching\n\ntmp_db = database.create_temp_db()\n\nwith crawler.Crawler(delay=1.0) as cr:\n    pages = cr.get_website_from_sitemap(\n        website      = \"https://ansel.photos\",\n        default_lang = \"en\",\n        markup       = \"article\",\n        category     = \"reference\",\n    )\n\ndatabase.populate_db(tmp_db, pages)\n</code></pre> <p>The <code>default_lang</code> argument is used as a fallback when the HTML page does not declare a language. Language is later confirmed or overridden by machine-learning detection during text normalisation (<code>batch_parse_web_page</code>).</p>"},{"location":"starting/6-crawling-pages.html#the-markup-parameter","title":"The <code>markup</code> Parameter","text":"<p>Both crawling methods accept a <code>markup</code> argument that restricts content extraction to specific HTML elements, letting you capture article bodies while discarding sidebars, headers, and navigation menus:</p> <pre><code># Plain tag name\nmarkup = \"article\"\n\n# Tag + CSS attribute dict (BeautifulSoup find_all syntax)\nmarkup = (\"div\", {\"class\": \"post-content\"})\n\n# Tag + id\nmarkup = (\"div\", {\"id\": \"main-content\"})\n\n# Multiple selectors \u2014 content from all matches is concatenated in order\nmarkup = [(\"div\", {\"id\": \"content\"}), (\"article\", {\"class\": \"entry\"})]\n\n# \"body\" / None \u2014 grab the whole body (suitable for documentation and reference pages)\nmarkup = \"body\"\n</code></pre>"},{"location":"starting/6-crawling-pages.html#filtering-with-contains_str","title":"Filtering with <code>contains_str</code>","text":"<p>The <code>contains_str</code> argument restricts which URLs get added to the index. Pages not matching the filter are still visited to discover new links, but their content is discarded:</p> <pre><code># Only index thread pages, skip user profiles and category archives\npages = cr.get_website_from_sitemap(\n    website      = \"https://discuss.pixls.us\",\n    default_lang = \"en\",\n    markup       = (\"div\", {\"class\": \"topic-body\"}),\n    contains_str = \"/t/\",        # also accepts a list: [\"/t/\", \"/articles/\"]\n    category     = \"forum\",\n)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#extending-coverage-with-internal_links","title":"Extending Coverage with <code>internal_links</code>","text":"<p>By default the sitemap crawler only indexes pages listed in <code>sitemap.xml</code>. Set <code>internal_links=\"external\"</code> to also follow and index every <code>&lt;a&gt;</code> link found in those pages content \u2014 useful when to also index referenced pages and PDFs outside of the current website:</p> <pre><code>pages = cr.get_website_from_sitemap(\n    website        = \"https://aurelienpierre.com\",\n    default_lang   = \"fr\",\n    markup         = (\"div\", {\"class\": \"post-content\"}),\n    contains_str   = \"/photography/\",\n    internal_links = \"external\",\n    category       = \"blog\",\n)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#websites-without-sitemaps","title":"Websites without Sitemaps","text":"<p>Many forum platforms and institutional websites do not publish a sitemap. Use <code>get_website_from_crawling()</code> to recursively follow links from an entry point instead:</p> <pre><code>with crawler.Crawler(delay=1.5) as cr:\n    pages = cr.get_website_from_crawling(\n        website      = \"https://community.ansel.photos\",\n        default_lang = \"en\",\n        child        = \"/discussions-home/\",   # entry page within the domain\n        markup       = [(\"div\", {\"class\": \"bx-content-description\"}),\n                        (\"div\", {\"class\": \"cmt-body\"})],\n        contains_str = \"/view-discussion/\",\n        category     = \"forum\",\n    )\n</code></pre> <p>The <code>child</code> argument defines the starting page within the domain (defaults to <code>/</code>). Pages not matching <code>contains_str</code> are still visited to find new links, but their content is not indexed.</p> <p>To prevent accidentally crawling beyond a specific section \u2014 essential on large sites \u2014 use <code>restrict_section=True</code> combined with <code>child</code>. Link-following will then stay within <code>website + child/*</code>:</p> <pre><code>pages = cr.get_website_from_crawling(\n    website           = \"https://discuss.pixls.us\",\n    default_lang      = \"en\",\n    child             = \"/c/software/darktable\",\n    contains_str      = \"/t/\",\n    markup            = (\"div\", {\"class\": \"topic-body\"}),\n    max_recurse_level = -1,       # -1 = exhaustive, no depth limit\n    restrict_section  = True,     # stay within /c/software/darktable/*\n    category          = \"forum\",\n)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#combining-crawling-methods","title":"Combining Crawling Methods","text":"<p>A single <code>Crawler</code> instance tracks already-visited URLs across calls, so mixing sitemap and recursive crawling within one <code>with</code> block will never fetch the same URL twice, which makes it much more efficient for domains that use different CMS (for example, forum/community + blog) that often link against each other:</p> <pre><code>tmp_db = database.create_temp_db()\n\nwith crawler.Crawler(delay=1.0) as cr:\n    pages  = cr.get_website_from_sitemap(\"https://docs.darktable.org\", \"en\",\n                                          markup=\"body\", category=\"docs\")\n    pages += cr.get_website_from_crawling(\"https://darktable.fr\", \"fr\",\n                                           child=\"/blog/\", markup=\"article\",\n                                           category=\"blog\")\n\ndatabase.populate_db(tmp_db, pages)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#incremental-crawling","title":"Incremental crawling","text":"<p>All crawling methods \u2014 HTML, PDF, YouTube, and GitHub \u2014 share the same two-attribute incremental-update mechanism:</p> Attribute How to set Effect <code>cr.known_urls</code> <code>cr.load_known_urls(db)</code> Maps URL \u2192 last crawled <code>datetime</code> from an existing database <code>cr.since</code> <code>cr.since = datetime(\u2026)</code> Global cut-off: any URL in <code>known_urls</code> crawled on or after this datetime is skipped <p>For sitemap crawling the page\u2019s own <code>&lt;lastmod&gt;</code> field takes precedence over <code>since</code>, so a page that was modified after the last crawl is re-fetched even if it was crawled recently. <code>since</code> is used as a fallback for sitemap entries that carry no <code>&lt;lastmod&gt;</code>.</p> <pre><code>import datetime\nfrom core import crawler, database\n\n# Append new URLs to a temporary DB that supports\n# duplicated URLs. Deduplication will be handled after (see below)\ntmp_db = database.create_temp_db()\n\nwith crawler.Crawler(delay=1.0) as cr:\n    # Populate the map once \u2014 applies to all crawling calls below:\n    # Get all known URLs from a permament DB storage\n    # where URLs are primary keys and therefore unique\n    # or ensure to deduplicate a temporary database.\n    db = database.open_db(\"my-engine.db\")\n    cr.load_known_urls(db)\n    db.close()\n\n    cr.since = datetime.datetime(2025, 6, 1, tzinfo=datetime.timezone.utc)\n\n    # Skips sitemap entries whose &lt;lastmod&gt; &lt;= stored crawl date\n    pages  = cr.get_website_from_sitemap(\"https://ansel.photos\", \"en\", markup=\"article\")\n\n    # Skips any URL crawled after cr.since\n    pages += cr.get_website_from_crawling(\"https://community.ansel.photos\", \"en\",\n                                           child=\"/discussions-home/\", contains_str=\"/view-discussion/\")\n\n    # Skips videos crawled after cr.since (no API-side filter needed)\n    pages += cr.get_youtube_channels(\n        channel_ids  = [\"UCmsSn3fujI81EKEr4NLxrcg\"],\n        api_key      = \"AIza\u2026\",\n        since        = cr.since,\n    )\n\n    # Passes since to the GitHub API's ?since= param for issues/pulls/commits\n    pages += cr.get_github_repositories(\n        repositories = [(\"aurelienpierreeng\", \"ansel\")],\n        api_key      = \"ghp_\u2026\",\n        since        = cr.since,\n    )\n\ndatabase.populate_db(tmp_db, pages)\ntmp_db.close()\n</code></pre> <p>Recursively crawling websites that don\u2019t have a sitemap is expensive and can take ages. So you may want to re-crawl them only once every <code>n</code> months, which can be achieved automatically in your script with:</p> <pre><code>import utils\n\n# Recrawl everything older than 3 months\ncr.since = utils.get_past_n_months(3)\n</code></pre> <p>Then you don\u2019t need to worry about updating dates manually.</p>"},{"location":"starting/6-crawling-pages.html#mining-pdf-documents","title":"Mining PDF Documents","text":""},{"location":"starting/6-crawling-pages.html#embedded-in-a-crawl","title":"Embedded in a Crawl","text":"<p>Pass <code>mine_pdf=True</code> to either crawling method and every <code>.pdf</code> link found on the pages will be automatically downloaded and extracted \u2014 no separate handling needed:</p> <pre><code>pages = cr.get_website_from_sitemap(\n    website      = \"https://www.cie.co.at/publications\",\n    default_lang = \"en\",\n    markup       = \"body\",\n    mine_pdf     = True,\n)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#direct-get_pdf_content-call","title":"Direct <code>get_pdf_content()</code> Call","text":"<p>For PDFs that are not reachable through a crawl \u2014 large reference books, local files, or document archives \u2014 use <code>pdf.get_pdf_content()</code> directly. It handles both remote URLs and local file paths, extracts the table of contents to split long documents by chapter, and falls back to Tesseract OCR for scanned images:</p> <pre><code>from core.pdf import get_pdf_content\nfrom core.network import DelayedClass\n\nclass SimpleDelay(DelayedClass):\n    \"\"\"Minimal rate-limiter required by the get_pdf_content API.\"\"\"\n    def __init__(self):\n        self.delay = 0.5\n        self.last_requests = {}\n        self.domain_thresholds = {}\n        self.main_domain = None\n        self.robots_txt = None\n\ndelay = SimpleDelay()\n\n# Remote PDF \u2014 split by table of contents into individual chapters\nsections = get_pdf_content(\n    url             = \"https://colour-science.org/papers/luo_2001.pdf\",\n    lang            = \"en\",\n    delay           = delay,\n    process_outline = True,   # each ToC entry becomes its own web_page\n    category        = \"paper\",\n    ocr             = 1,      # use OCR only when no embedded text is found (default)\n)\n\n# Local scanned PDF \u2014 force full OCR\nsections += get_pdf_content(\n    url       = \"https://onlinelibrary.wiley.com/doi/book/10.1002/9781118653128\",\n    lang      = \"en\",\n    delay     = delay,\n    file_path = \"/home/user/fairchild_color_appearance_models.pdf\",\n    ocr       = 2,       # force OCR even when embedded text is present\n    max_size  = 50,      # skip files larger than 50 MiB\n    max_pages = 800,\n    repair    = 2,       # image pre-processing strength (0\u20133)\n    upscale   = 3,       # upscaling factor before OCR\n    contrast  = 1.4,\n)\n\ndatabase.populate_db(tmp_db, sections)\n</code></pre> <p>The URL passed as the first argument is used as the canonical address stored in the index. When <code>process_outline=True</code>, each section\u2019s URL receives a <code>#page=n</code> fragment so deep-linking from search results works directly in Chrome and Acrobat Reader.</p> <p>The <code>ocr</code> parameter controls when OCR is attempted:</p> Value Behaviour <code>0</code> Never \u2014 text extraction only <code>1</code> Only when no embedded text is found (default) <code>2</code> Always, even when embedded text is present"},{"location":"starting/6-crawling-pages.html#custom-pdf-handling","title":"Custom PDF Handling","text":"<p>If you need different extraction logic \u2014 tables, multi-column layouts, layered content \u2014 subclass <code>Crawler</code> and override <code>_parse_pdf_content()</code>:</p> <pre><code>from core import crawler, database\nfrom core.types import web_page, sanitize_web_page\nfrom io import BytesIO\nimport pytesseract, pdf2image, requests\nfrom pypdf import PdfReader\n\ndef my_pdf_handler(url: str, lang: str, category: str = \"\") -&gt; list[web_page]:\n    try:\n        if url.startswith(\"http\"):\n            resp = requests.get(url, timeout=30, allow_redirects=True)\n            if resp.status_code != 200:\n                return []\n            document = BytesIO(resp.content)\n        else:\n            document = open(url, \"rb\")\n\n        blob = document.read()\n        reader = PdfReader(BytesIO(blob))\n\n        # Page-by-page text extraction \u2014 replace with your own logic\n        content = \"\\n\".join(p.extract_text() or \"\" for p in reader.pages).strip()\n\n        if not content:\n            # No embedded text: fall back to OCR\n            for image in pdf2image.convert_from_bytes(blob):\n                content += pytesseract.image_to_string(image)\n\n        if content:\n            return [sanitize_web_page(web_page(\n                title    = url.split(\"/\")[-1],\n                url      = url,\n                content  = content,\n                excerpt  = content[:300],\n                lang     = lang,\n                category = category,\n                h1=[], h2=[], date=\"\",\n            ))]\n    except Exception as e:\n        print(e)\n    return []\n\n\nclass MyCrawler(crawler.Crawler):\n    def _parse_pdf_content(self, link, default_lang, category=\"\") -&gt; list[web_page]:\n        return my_pdf_handler(link, default_lang, category=category)\n\n\nwith MyCrawler(delay=1.0) as cr:\n    pages = cr.get_website_from_crawling(\"https://example.com\", \"en\")\n\ntmp_db = database.create_temp_db()\ndatabase.populate_db(tmp_db, pages)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#custom-data-sources-rest-apis","title":"Custom Data Sources (REST APIs)","text":"<p>Some sites serve content entirely through client-side rendering: their HTML is an empty canvas populated by JavaScript at runtime, so a standard HTML parser sees nothing useful. The workaround is to query the underlying REST API directly and construct <code>web_page</code> objects manually.</p>"},{"location":"starting/6-crawling-pages.html#youtube-example","title":"YouTube Example","text":"<pre><code>import requests, json\nfrom core import database\nfrom core.types import web_page, sanitize_web_page\n\nAPI_KEY    = \"...\"   # https://developers.google.com/youtube/v3/getting-started\nCHANNEL_ID = \"UCmsSn3fujI81EKEr4NLxrcg\"\n\nresponse = requests.get(\n    \"https://youtube.googleapis.com/youtube/v3/search\"\n    f\"?maxResults=1000&amp;part=snippet&amp;channelId={CHANNEL_ID}&amp;type=video&amp;key={API_KEY}\"\n)\nitems = json.loads(response.content)[\"items\"]\n\npages = []\nfor item in items:\n    vid_id  = item[\"id\"][\"videoId\"]\n    detail  = requests.get(\n        f\"https://youtube.googleapis.com/youtube/v3/videos?part=snippet&amp;id={vid_id}&amp;key={API_KEY}\"\n    )\n    snippet = json.loads(detail.content)[\"items\"][0][\"snippet\"]\n\n    pages.append(sanitize_web_page(web_page(\n        title    = snippet[\"title\"],\n        url      = f\"https://www.youtube.com/watch?v={vid_id}\",\n        excerpt  = item[\"snippet\"][\"description\"],\n        content  = snippet[\"description\"],\n        date     = snippet[\"publishedAt\"],\n        lang     = snippet.get(\"defaultLanguage\", \"en\"),\n        category = \"video\",\n        h1=[], h2=[],\n    )))\n\ntmp_db = database.create_temp_db()\ndatabase.populate_db(tmp_db, pages)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#github-issues-and-pull-requests","title":"GitHub Issues and Pull Requests","text":"<pre><code>import time, re, json, requests, markdown\nfrom core import database\nfrom core.types import web_page, sanitize_web_page\n\nAPI_KEY = \"...\"  # https://docs.github.com/en/rest/authentication\nHEADERS = {\n    \"Authorization\": f\"Bearer {API_KEY}\",\n    \"Accept\": \"application/vnd.github+json\",\n}\nPOSTS_PER_PAGE = 100   # GitHub maximum\n\n\ndef items_count(user: str, repo: str, feature: str) -&gt; int:\n    r = requests.get(\n        f\"https://api.github.com/repos/{user}/{repo}/{feature}?per_page=1\",\n        headers=HEADERS, timeout=30,\n    )\n    return int(re.search(r\"\\d+$\", r.links[\"last\"][\"url\"]).group())\n\n\ndef get_github_items(user: str, repo: str, feature: str, category: str) -&gt; list[web_page]:\n    total  = items_count(user, repo, feature)\n    pages_ = total // POSTS_PER_PAGE + 1\n    result = []\n    for page in range(pages_):\n        time.sleep(0.72)   # stay within GitHub's 5 000 req/hr limit\n        r = requests.get(\n            f\"https://api.github.com/repos/{user}/{repo}/{feature}\"\n            f\"?per_page={POSTS_PER_PAGE}&amp;page={page}\",\n            headers=HEADERS, timeout=30,\n        )\n        for item in json.loads(r.content):\n            url = item.get(\"html_url\", \"\")\n            if not url:\n                continue\n            body = markdown.markdown(item.get(\"body\") or \"\")\n            result.append(sanitize_web_page(web_page(\n                title    = f\"{feature.capitalize()}: {item.get('title', '')}\",\n                url      = url,\n                content  = body,\n                excerpt  = body[:300],\n                date     = item.get(\"created_at\", \"\"),\n                lang     = \"en\",\n                category = category,\n                h1=[], h2=[],\n            )))\n    return result\n\n\npages = []\nrepos = [(\"aurelienpierreeng\", \"ansel\"), (\"darktable-org\", \"rawspeed\")]\nfor user, repo in repos:\n    for feature in [\"issues\", \"pulls\"]:\n        pages += get_github_items(user, repo, feature, \"Github\")\n\ntmp_db = database.create_temp_db()\ndatabase.populate_db(tmp_db, pages)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#crawling-details","title":"Crawling Details","text":""},{"location":"starting/6-crawling-pages.html#robotstxt","title":"robots.txt","text":"<p>The crawler respects robots.txt automatically. For every new domain it visits it fetches <code>/robots.txt</code>, honours <code>Disallow</code> directives for its user-agent string, and reads <code>Crawl-delay</code> / <code>Request-rate</code> entries to set its per-domain inter-request throttle. If a <code>robots.txt</code> file cannot be fetched (domain unreachable, 404), crawling proceeds without restrictions.</p> <p>Pages listed in <code>sitemap.xml</code> are assumed pre-authorised and skip the per-URL <code>robots.txt</code> check, reducing request overhead on large sitemaps.</p> <p>Some servers implement Cloudflare-style bot blocking that goes beyond <code>robots.txt</code>. The crawler handles this by trying multiple user-agent / header combinations and falling back to web.archive.org in last resort for pages that return persistent 403 or 404 errors.</p>"},{"location":"starting/6-crawling-pages.html#cleaning-up-non-language-content","title":"Cleaning Up Non-Language Content","text":"<p>Websites come with navigation menus, breadcrumbs, sidebars, and metadata sections that are not natural-language content and are not unique to any particular page. The HTML parser removes the following elements before text extraction:</p> <p><code>&lt;style&gt;</code>, <code>&lt;script&gt;</code>, <code>&lt;svg&gt;</code>, <code>&lt;img&gt;</code>, <code>&lt;picture&gt;</code>, <code>&lt;audio&gt;</code>, <code>&lt;video&gt;</code>, <code>&lt;iframe&gt;</code>, <code>&lt;embed&gt;</code>, <code>&lt;aside&gt;</code>, <code>&lt;nav&gt;</code>, <code>&lt;input&gt;</code>, <code>&lt;header&gt;</code>, <code>&lt;button&gt;</code>, <code>&lt;form&gt;</code>, <code>&lt;fieldset&gt;</code>, <code>&lt;footer&gt;</code>, <code>&lt;summary&gt;</code>, <code>&lt;dialog&gt;</code>, <code>&lt;textarea&gt;</code>, <code>&lt;select&gt;</code>, <code>&lt;option&gt;</code></p> <p>Inline <code>style</code> and <code>data</code> attributes are also stripped from all remaining elements.</p> <p>If this is still not enough, use the <code>markup</code> argument to whitelist specific content containers \u2014 that is usually more reliable and faster than trying to remove noise after the fact.</p> <p><code>&lt;blockquote&gt;</code> and <code>&lt;code&gt;</code> are intentionally kept</p> <p>Unlike some older documentation may suggest, <code>&lt;blockquote&gt;</code>, <code>&lt;code&gt;</code>, and <code>&lt;pre&gt;</code> blocks are not stripped. Quoted replies in forum pages are a known source of near-duplicate content, but that is handled by the deduplicator rather than at parse time.</p>"},{"location":"starting/6-crawling-pages.html#the-no_follow-list","title":"The <code>no_follow</code> List","text":"<p>Both crawling methods share a default blocklist of URLs that are never fetched \u2014 social-media share links, login and signup pages, cart pages, and user-profile paths. You can extend it at instantiation time or by appending to the attribute:</p> <pre><code># Via the constructor (preferred \u2014 applies before any crawling starts)\nwith crawler.Crawler(\n    delay     = 1.0,\n    no_follow = [\n        \"google.com\",\n        \"amazon.com\",\n        \"/tag/\",           # tag archive pages anywhere\n        \"?replytocom=\",    # WordPress comment reply links\n        \".pdf\",            # skip PDFs during an HTML-only crawl\n    ],\n) as cr:\n    ...\n\n    # Appending after instantiation (same effect)\n    cr.no_follow += [\"/view-album/\", \"persons-profile-\"]\n</code></pre> <p><code>no_follow</code> entries are substring-matched against the full URL. Any match causes the URL to be silently discarded \u2014 it generates no network request at all. This is more aggressive than <code>contains_str</code>, which still visits non-matching URLs to discover new links.</p>"},{"location":"starting/6-crawling-pages.html#caveats","title":"Caveats","text":"<p>The crawler tries to ignore JSON, CSS, and JavaScript files based on MIME type and file extension. This does not work 100%, particularly on GitHub where declared MIME types can be inaccurate and code is sometimes embedded in HTML in unusual ways. Use the SQL filtering step in Deduplication to clean up any machine-parseable content that slips through.</p>"},{"location":"starting/6-crawling-pages.html#deduplication","title":"Deduplication","text":""},{"location":"starting/6-crawling-pages.html#the-two-database-pattern","title":"The Two-Database Pattern","text":"<p>Virtual Secretary uses two complementary database types throughout the pipeline:</p> Function Primary key on <code>url</code>? Use case Target folder <code>database.create_temp_db()</code> No Raw crawl buffer; tolerates duplicate URLs; lives in a temp directory <code>~/.virtual-secretary</code> <code>database.create_db(name)</code> Yes Permanent store; each URL is unique; used for all NLP and serving <code>VirtualSecretary/models</code> <p>Crawl output always goes into a temp database first. After content normalisation, filtering, and deduplication, the cleaned data is promoted to the permanent store with <code>database.import_pages()</code>. Writing directly from a multi-source crawl into <code>create_db</code> produces undefined behaviour when two sources contain the same URL, because the <code>ON CONFLICT</code> handler cannot determine which copy\u2019s content fields should win.</p> <p><code>database.create_db</code> is meant to prepare reusable models, for example to feed to <code>nlp.Word2Vec</code> training or <code>search.Indexer</code> web index. <code>database.create_temp_db</code> is meant to be saved to less storage-hungry datasets using <code>utils.save_data</code>.</p>"},{"location":"starting/6-crawling-pages.html#content-filtering","title":"Content Filtering","text":"<p>Before deduplication runs, remove pages that should never appear in the index. This is most efficiently done at the SQL level directly on the temp database, after <code>batch_parse_web_page</code> has run (which populates <code>lang</code>):</p> <pre><code>from core import batching, nlp\n\n# Detect languages and write the `parsed` column \u2014 required before any filtering on `lang`\nbatching.batch_parse_web_page(tmp_db, nlp.Tokenizer())\n\ncur = tmp_db.cursor()\n\n# Remove pages in languages your NLP pipeline doesn't support\ncur.execute(\"DELETE FROM pages WHERE lang NOT IN ('fr', 'en') OR lang IS NULL\")\n\n# Remove GitHub blob viewer pages (raw code, no natural-language content)\ncur.execute(\"DELETE FROM pages WHERE url LIKE ?\", (\"%/blob/%\",))\n\n# Remove Discourse boilerplate session-alert pages\ncur.execute(\"DELETE FROM pages WHERE content LIKE ?\",\n            (\"%you signed in with another tab or window%\",))\n\n# Remove a domain superseded without redirects\ncur.execute(\"DELETE FROM pages WHERE url LIKE ?\", (\"%old-domain.example.com%\",))\n\ntmp_db.commit()\n</code></pre> <p>Alternatively, filter at import time using <code>import_pages(where_clause=...)</code> to avoid even inserting unwanted rows:</p> <pre><code>database.import_pages(\n    source_db      = pixls_db,\n    destination_db = tmp_db,\n    where_clause   = \"title NOT LIKE ? AND content NOT LIKE ?\",\n    params         = (\"%playraw%\", \"%sigmoid%\"),\n)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#running-deduplication","title":"Running Deduplication","text":"<p><code>Deduplicator</code> compares the <code>parsed</code> (normalised) column of every pair of pages and retains the best copy \u2014 shortest URL, most recent date, or longest content when dates are equal. <code>batch_parse_web_page</code> must have run first, since that is what writes the <code>parsed</code> column.</p> <pre><code>from core import deduplicator\n\ndedup = deduplicator.Deduplicator(\n    threshold      = 1.0,    # 1.0 = exact duplicates only\n                             # &lt; 1.0 enables Levenshtein near-duplicate detection\n    distance       = 50,     # minimum tokens needed to compare two pages\n    discard_params = False,  # keep URL query parameters in the canonical URL\n    fix_urls       = False,\n)\n\n# Domains always discarded\ndedup.urls_to_ignore += [\n    \"translate.goog\",    # machine translations \u2014 keep canonical instead\n    \"flickr.com\",\n    \"facebook.com\",\n]\n\ndedup(tmp_db)\n</code></pre> <p>Near-duplicate detection (<code>threshold &lt; 1.0</code>) is more expensive. For a corpus of hundreds of thousands of pages, start with <code>threshold=1.0</code> and tighten it only if exact matching leaves obvious near-duplicates in the index.</p>"},{"location":"starting/6-crawling-pages.html#saving-the-dataset-for-later-reuse","title":"Saving the dataset for later reuse","text":"<p>Datasets are saved into the <code>VirtualSecretary/data</code> folder.</p> <pre><code>from core import utils, database\n\ntmp_db = database.create_temp_db()\n\n...\n\n# Run SQLite VACCUUM\ndatabase.compress_db(tmp_db)\n\n# Save as gzipped SQLite dump\nutils.save_data(tmp_db, \"my-dataset\")\n\n# Clean-up the temporary databases\n# WARNING: this removes all existing databases,\n# ensure another thread is not running that needs\n# one of those.\ndatabase.close_db(tmp_db)\ndatabase.cleanup_temp_db()\n</code></pre> <p>To re-open the database as it was saved, use <code>tmp_db = utils.open_data(\"my-dataset\")</code> and you will get the SQLite database opened in memory. To copy it into another opened database, you may use:</p> <pre><code>from core import utils, database\n\n# Open saved dataset\ntmp_db = utils.open_data(\"my-dataset\")\n\n# Create a new permanent database\nfinal_db = database.create_db(\"my-engine.db\")\n\n# Dump the memory-hosted dataset DB\ntmp_db.backup(final_db)\n\n# Close memory DB\ntmp_db.close()\n\n# Optimize the permament DB and close it\ndatabase.close_db(final_db)\n</code></pre>"},{"location":"starting/6-crawling-pages.html#full-pipeline-example","title":"Full Pipeline Example","text":"<p>Putting all steps together for a multi-source crawl:</p> <pre><code>from core import crawler, database, deduplicator, nlp, batching, utils\n\nfilename = \"photo-websites\"\n\ntmp_db = database.create_temp_db()\n\nsources = [\n    (\"https://ansel.photos\",     \"en\", \"article\",  \"docs\"),\n    (\"https://darktable.fr\",     \"fr\", \"article\",  \"blog\"),\n    (\"https://discuss.pixls.us\", \"en\", (\"div\", {\"class\": \"topic-body\"}), \"forum\"),\n]\n\n# 1. Crawl all sources into the temp database\nfor site, lang, markup, category in sources:\n    with crawler.Crawler(delay=1.0) as cr:\n        pages = cr.get_website_from_sitemap(site, lang, markup=markup, category=category)\n    database.populate_db(tmp_db, pages)\n\n# 2. Normalise text and detect language\nbatching.batch_parse_web_page(tmp_db, nlp.Tokenizer())\n\n# 3. Drop unsupported languages and known noise\ntmp_db.execute(\"DELETE FROM pages WHERE lang NOT IN ('fr', 'en') OR lang IS NULL\")\ntmp_db.execute(\"DELETE FROM pages WHERE url LIKE ?\", (\"%/blob/%\",))\ntmp_db.commit()\n\n# 4. Deduplicate while the temp DB has no primary-key constraint on url\ndedup = deduplicator.Deduplicator(threshold=1.0, discard_params=False, fix_urls=False)\ndedup.urls_to_ignore += [\"translate.goog\", \"facebook.com\", \"flickr.com\"]\ndedup(tmp_db)\n\n# 5. Compress and save. 2 options:\nsave_as = \"data\"\n\nif save_as == \"data\":\n\n    # Save to the data folder, \n    # as VirtualSecretary/data/photo-websites.sql.tar.gz\n    # This takes less storage space but loads slower\n    # on utils.open_data()\n    database.compress_db(tmp_db)\n    utils.save_data(tmp_db, filename)\n\nelif save_as == \"model\":\n\n    # Save to the models folder,\n    # as VirtualSecretary/models/my-engine.db\n    # This takes (a lot) more storage space,\n    # but is a directly-usable SQLite database (loads very fast)\n    final_db = database.create_db(\"my-engine.db\")\n    database.import_pages(source_db=tmp_db, destination_db=final_db)\n    database.close_db(final_db) \n    # this optimizes final_db before calling final_db.close()\n    # calling final_db.close() directly is ok too.\n\ntmp_db.close()\ndatabase.cleanup_temp_db()\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html","title":"Build Your Own Search Engine at Home","text":"<p>Virtual Secretary can be used for building domain-specific, semantics-aware search engines from scratch \u2014 without relying on any external API, proprietary cloud service, or pre-packaged solution. You harvest the data yourself, you train the language model yourself, and you run everything on your own hardware.</p> <p>This tutorial walks through the complete pipeline: from fetching web pages and PDFs to serving a search engine in your browser. Every step uses real code drawn from the framework and is designed to run on a standard Linux machine (a recent CPU with 8+ cores and 16 GB of RAM is comfortable; less is possible but slower).</p> <p>Live demo</p> <p>A fully-operational instance of the photography-domain search engine built with this pipeline can be explored at chantal.aurelienpierre.com. The implementation-specific scripts and app code for that deployment are not public, but this tutorial reproduces the same patterns generically.</p>"},{"location":"starting/7-build-your-own-search-engine.html#architecture-overview","title":"Architecture Overview","text":"<p>The pipeline is linear: each stage produces artefacts consumed by the next. Steps 1\u20132 of the acquisition phase are covered in the Crawling Pages guide; this tutorial picks up from Step 1 of the processing phase.</p> <pre><code>Web / PDF sources\n       \u2502\n       \u25bc\n  Crawling &amp; PDFs   \u2192  list[web_page]         (see Crawling Pages guide)\n       \u2502\n       \u25bc\n  1. Database        \u2192  pages SQLite table     (on disk)\n       \u2502\n       \u25bc\n     Deduplication   \u2192  cleaned pages table    (in-place, see Crawling Pages guide)\n       \u2502\n       \u25bc\n  2. Text processing \u2192  parsed / tokenized /   (DB columns)\n                        stemmed columns\n       \u2502\n       \u25bc\n  3. Language model  \u2192  Tokenizer + Word2Vec   (model files)\n       \u2502\n       \u25bc\n  4. Vectorization   \u2192  vectorized column      (DB column)\n       \u2502\n       \u25bc\n  5. Categories      \u2192  category column        (DB column)\n       \u2502\n       \u25bc\n  6. Search index    \u2192  Indexer joblib file    (model files)\n       \u2502\n       \u25bc\n  7. Query API       \u2192  rank() results         (in RAM)\n       \u2502\n       \u25bc\n  8. Flask app       \u2192  /api  +  /             (HTTP)\n</code></pre> <p>The central data structure throughout is <code>web_page</code>, a <code>TypedDict</code> whose keys map directly to SQLite columns:</p> Key Type Purpose <code>url</code> <code>str</code> Primary key \u2014 the canonical address of the document <code>title</code> <code>str</code> Page title <code>content</code> <code>str</code> Raw human-readable text extracted from the page <code>parsed</code> <code>str</code> Normalised version of the content (lowercase, ASCII) <code>tokenized</code> <code>list</code> Tokenized sentences, each a list of string tokens <code>stemmed</code> <code>list</code> Same but with stemming, stopword removal, and n-grams applied <code>vectorized</code> <code>np.ndarray</code> Semantic centroid vector for the page <code>lang</code> <code>str</code> 2-letter ISO language code <code>date</code> / <code>datetime</code> <code>str</code> / <code>datetime</code> Publication date <code>category</code> <code>str</code> Arbitrary user-defined label <code>excerpt</code> <code>str</code> Short description for result previews"},{"location":"starting/7-build-your-own-search-engine.html#data-acquisition-crawling-and-pdfs","title":"Data Acquisition \u2014 Crawling and PDFs","text":"<p>Before any NLP processing can begin, you need a corpus of text documents. Virtual Secretary provides crawlers for HTML websites (sitemap-based and recursive link-following), direct PDF mining with optional OCR, and a <code>web_page</code> data structure for wiring in custom REST API sources.</p> <p>The full crawling reference \u2014 parameters, markup selectors, <code>robots.txt</code> behaviour, deduplication patterns, and worked examples for web, PDF, YouTube and GitHub sources \u2014 is covered in the dedicated guide:</p> <p>\u279c Crawling Pages</p> <p>The key invariant to keep in mind when reading the rest of this tutorial: crawl output always goes into <code>database.create_temp_db()</code> first, gets deduplicated there, and is only then promoted to the permanent <code>database.create_db()</code> store where <code>url</code> is the primary key.</p>"},{"location":"starting/7-build-your-own-search-engine.html#step-1-restoring-data","title":"Step 1 \u2014 Restoring Data","text":""},{"location":"starting/7-build-your-own-search-engine.html#merging-from-individual-datasets","title":"Merging from individual datasets","text":"<p>In this scenario, each crawled source is stored in a seperate dataset, saved with <code>utils.save_data</code>. This is useful to split the crawling step (that can be deferred to weak hardware running 24/7) and language model training (that needs powerful hardware for just a couple of hours). So the datasets can be exchanged through FTP/SSH/Rsync etc. between both computers, and different sources can be re-crawled at different frequencies.</p> <p>In <code>VirtualSecretary/src/user_scripts</code>, add:</p> <pre><code>import os\nimport sys\n\nSCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))\nsys.path.append(os.path.dirname(SCRIPT_DIR))\n\nfiles = [\"site-a\", \"site-b\", \"site-c\"] \n# should match dataset names used with utils.save_data() earlier\n\n# Create a temporary DB and stream archives into it to avoid building a huge list in RAM\ntmp_db = database.create_temp_db()\n\nfor file in files:\n    # Merge all files, with possible duplicates, into the temporary DB\n    db = utils.open_data(file, scheme=\"sql\")\n    database.import_pages(db, tmp_db)\n    db.close()\n\n# Step 1: normalise text \u2014 required before deduplication can compare content\nbatching.batch_parse_web_page(tmp_db, nlp.Tokenizer())\n\n# Step 2: deduplicate in the temp database, while there is no primary key to violate\ndedup = deduplicator.Deduplicator(threshold=1.0, discard_params=False, fix_urls=False)\ndedup.urls_to_ignore += [\"translate.goog\", \"facebook.com\", \"flickr.com\"]\ndedup(tmp_db)\n\n# Step 3: only now import into the permanent database (URL primary key enforced)\nfinal_db = database.create_db(\"my-engine.db\")\ndatabase.import_pages(source_db=tmp_db, destination_db=final_db)\n\n# Close and cleanup the temp database\ndatabase.close_db(tmp_db)\ndatabase.cleanup_temp_db()\n\nfinal_db.close() # or maybe don't close yet if you are going to work more on it\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#fetching-a-full-model","title":"Fetching a full model","text":"<p>In this scenario, you already saved all crawled sources into a single database using <code>database.create_db(\"my-engine.db\")</code>.</p> <pre><code>from core import crawler, database, deduplicator, nlp, batching\n\nfinal_db = database.open_db(\"my-engine.db\")\n# You are technically ready to go, though you can still optionally run the following:\n\n# Step 1: normalise text \u2014 required before deduplication can compare content\nbatching.batch_parse_web_page(final_db, nlp.Tokenizer())\n\n# Step 2: deduplicate in the temp database, while there is no primary key to violate\ndedup = deduplicator.Deduplicator(threshold=1.0, discard_params=False, fix_urls=False)\ndedup.urls_to_ignore += [\"translate.goog\", \"facebook.com\", \"flickr.com\"]\ndedup(final_db)\n\nfinal_db.close() # or maybe don't close yet if you are going to work more on it\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#content-filtering-at-sql-level","title":"Content filtering at SQL level","text":"<p>Some pages should never make it into the index \u2014 authentication pages, boilerplate, machine-translated copies. Delete them from the temp database before going further, so they don\u2019t waste NLP processing time later and pollute storage:</p> <pre><code>cur = db.cursor() # runs on final or temporary DB just the same\n\n# Remove GitHub blob viewer pages (only raw code, useless for NLP)\ncur.execute(\"DELETE FROM pages WHERE url LIKE ?\", (\"%/blob/%\",))\n\n# Remove session-hijacking hints that end up in Discourse forum pages\ncur.execute(\"DELETE FROM pages WHERE content LIKE ?\",\n            (\"%you signed in with another tab or window%\",))\n\n# Remove an entire domain that has been superseded / moved\ncur.execute(\"DELETE FROM pages WHERE url LIKE ?\", (\"%old-domain.example.com%\",))\n\n# Remove pages in unsupported languages before spending any NLP budget on them.\n# Language is detected during batch_parse_web_page, so run this AFTER that step.\ncur.execute(\"DELETE FROM pages WHERE lang NOT IN ('fr', 'en') OR lang IS NULL\")\n\ndb.commit()\n</code></pre> <p>Filtering on import</p> <p><code>database.import_pages()</code> accepts a <code>where_clause</code> argument appended to the internal <code>SELECT</code>, so you can filter at import time rather than after:</p> <pre><code>database.import_pages(\n    source_db    = pixls_db,\n    destination_db = tmp_db,\n    where_clause = \"title NOT LIKE ? AND content NOT LIKE ?\",\n    params       = (\"%playraw%\", \"%presentation%\"),\n)\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#step-2-text-processing","title":"Step 2 \u2014 Text Processing","text":"<p>With a clean database, the NLP pipeline kicks in. All processing functions write directly to the database columns and are safely resumable \u2014 they skip rows that already have a value unless you pass <code>only_none=False</code>.</p>"},{"location":"starting/7-build-your-own-search-engine.html#parsing-and-normalisation","title":"Parsing and normalisation","text":"<p><code>batch_parse_web_page</code> normalises Unicode, strips multi-spaces, detects the language of each page, and writes the result to the <code>parsed</code> column. This is a prerequisite for deduplication and tokenisation.</p> <pre><code>from core import batching, nlp\n\ntokenizer = nlp.Tokenizer()  # base tokenizer, no vocabulary yet\nbatching.batch_parse_web_page(db, tokenizer)\n</code></pre> <p>This runs across all available CPU cores. Typical throughput on a modern machine is tens of thousands of short pages per minute.</p>"},{"location":"starting/7-build-your-own-search-engine.html#tokenisation","title":"Tokenisation","text":"<pre><code># Full tokeniser, optionally with custom vocabulary replacements\nfrom core import language\n\ntokenizer = nlp.Tokenizer(\n    replacements  = language.REPLACEMENTS,   # e.g. map \"colour\" \u2192 \"color\"\n    abbreviations = language.ABBREVIATIONS,  # e.g. expand \"ca.\" \u2192 \"circa\"\n)\n\n# Tokenise without stemming \u2014 preserves enough structure for n-gram discovery.\n# only_none=False forces re-tokenisation even for rows already processed.\nbatching.batch_tokenize(db, tokenizer, only_none=False)\n</code></pre> <p>The tokenised form stored in <code>tokenized</code> retains stopwords, punctuation meta-tokens, and original casing. This \u201clossless\u201d representation is later consumed by the n-gram trainer and can be used to reconstruct approximate original sentences.</p> <p>Language filtering before tokenisation</p> <p>Snowball stemming \u2014 used in the next stages \u2014 only supports a defined set of languages. Tokenising pages in unsupported languages wastes CPU and produces noisy tokens. If your database still contains mixed-language content at this point, remove unsupported languages before running <code>batch_tokenize</code>:</p> <pre><code># Keep only the languages your pipeline supports\ndb.execute(\"DELETE FROM pages WHERE lang NOT IN ('fr', 'en') OR lang IS NULL\")\ndb.commit()\n</code></pre> <p>This should ideally have been done earlier (see Step 1), but it is safe to run it again here as a safeguard \u2014 particularly when the permanent database is populated from multiple sources built at different times.</p>"},{"location":"starting/7-build-your-own-search-engine.html#training-n-grams","title":"Training n-grams","text":"<p>Multi-word expressions like \u201csignal-to-noise ratio\u201d, \u201ccolour science\u201d, or \u201cAur\u00e9lien Pierre\u201d should be treated as single vocabulary units. The tokeniser can learn them automatically from co-occurrence statistics:</p> <pre><code>from core import database\n\n# Train n-grams on French corpus\nfr_corpus = database.SQLitePageCorpus(\n    db,\n    \"SELECT tokenized FROM pages WHERE lang = 'fr'\",\n    max_depth=1,   # yields a flat list of token strings\n)\ntokenizer.train_ngrams(\n    fr_corpus,\n    # Common function words to exclude from multi-word expressions\n    \" le la l l' du de d d' sur sous en les des pour au aux \"\n)\n\n# Train on English corpus\nen_corpus = database.SQLitePageCorpus(\n    db,\n    \"SELECT tokenized FROM pages WHERE lang = 'en'\",\n    max_depth=1,\n)\ntokenizer.train_ngrams(\n    en_corpus,\n    \" a an the for of with at from to in on by and or \"\n)\n\ntokenizer.save(\"my-tokenizer\")\n</code></pre> <p>After saving, reload to verify integrity before committing to the slow Word2Vec training step:</p> <pre><code>tokenizer = nlp.Tokenizer.load(\"my-tokenizer\")\n\n# Quick sanity check\ntext = \"Aur\u00e9lien Pierre developed Ansel from the darktable codebase\"\ntokens = tokenizer.tokenize_document_flat(\n    tokenizer.normalize_text(text),\n    normalize=True, stem=True, remove_stopwords=True, n_grams=True\n)\nprint(tokens)\n# e.g. ['aurelien_pi', 'develop', 'ansel', 'darktabl', 'codebas']\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#stemming","title":"Stemming","text":"<p>With n-grams trained and saved, re-process every page to produce the stemmed form used by Word2Vec. Stemming is destructive and will remove suffixes, double consonnants etc. so the text representation symbolically encodes semantics (topics) but looses syntax. Stemmed text cannot be reconstructed into original sentences, doesn\u2019t read nicely for humans, and looses some nuance, at the benefit of being better generalized for information retrieval tasks.</p> <pre><code>batching.batch_stem(db, tokenizer, only_none=False)\n</code></pre> <p>If you ever need to re-stem only a specific subset \u2014 for instance after re-crawling one site \u2014 pass a URL list to avoid touching the rest of the database, because stemming is quite CPU-intensive so you may want to blindly run it on all data entries:</p> <pre><code>updated_urls = [\"https://ansel.photos/doc/page1\", \"https://ansel.photos/doc/page2\"]\nbatching.batch_stem(db, tokenizer, urls=updated_urls)\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#step-3-training-the-language-model","title":"Step 3 \u2014 Training the Language Model","text":"<p>Virtual Secretary\u2019s semantic search is powered by Word2Vec with dual embedding spaces, as described in Bojanowski et al. (2016). You train it directly on your own corpus, which means the resulting embedding space reflects the specific vocabulary and concepts of your domain \u2014 photography, medicine, law, or whatever you are building around.</p> <pre><code>from core import nlp, database\n\ndb = database.open_db(\"my-engine.db\", mode=\"ro\")\n\n# Only train on languages with enough data for reliable semantics\ncorpus = database.SQLitePageCorpus(\n    db,\n    \"SELECT stemmed FROM pages WHERE lang IN ('fr', 'en')\",\n    max_depth=0,   # yields a list-of-list-of-strings (one inner list per sentence)\n)\n\nw2v = nlp.Word2Vec(\n    corpus,\n    \"my-word2vec\",\n    vector_size  = 496,   # embedding dimensionality\n    epochs       = 40,\n    window       = 31,    # context window (wider = more thematic, narrower = more syntactic)\n    min_count    = 10,    # ignore words appearing fewer than 10 times\n    sample       = 1e-4,  # sub-sampling threshold for frequent words\n    ns_exponent  = -0.5,  # negative sampling exponent\n    negative     = 5,     # negative samples per positive example\n    tokenizer    = nlp.Tokenizer.load(\"my-tokenizer\"),\n)\n\ndb.close()\n</code></pre> <p>Validate the embeddings before moving on:</p> <pre><code>print(w2v.wv.most_similar(\"luminanc\"))      # stemmed form of \"luminance\"\n# [('brightn', 0.91), ('exposur', 0.88), ('lux', 0.86), ...]\n\nprint(w2v.wv.most_similar(\"raw_file\"))      # an n-gram treated as single token\n# [('dng', 0.89), ('raw_format', 0.87), ('nef', 0.84), ...]\n</code></pre> <p>If the top neighbours are semantically coherent, the model is ready.</p> <p>Training time</p> <p>Expect 5\u201330 minutes on 8 cores for a corpus of 100 000 pages. Training is parallelised internally by Gensim.</p>"},{"location":"starting/7-build-your-own-search-engine.html#step-4-vectorising-the-index","title":"Step 4 \u2014 Vectorising the Index","text":"<p>The next step embeds every page as a single centroid vector that will later power the semantic ranking:</p> <pre><code>from core import batching, nlp, database\n\ndb  = database.open_db(\"my-engine.db\")\nw2v = nlp.Word2Vec.load_model(\"my-word2vec\")\n\nbatching.batch_vectorize(db, w2v)\n\ndb.close()\n</code></pre> <p>Each vector is written to the <code>vectorized</code> column as a binary blob. This step is idempotent and parallelised across all CPU cores.</p>"},{"location":"starting/7-build-your-own-search-engine.html#step-5-setting-up-categories","title":"Step 5 \u2014 Setting Up Categories","text":"<p>Certain search filters (exclude forums, restrict to GitHub, etc.) rely on a <code>category</code> column that is set at crawl time or updated with a bulk SQL pass before building the index. Do this now if you haven\u2019t done so during crawling:</p> <pre><code>db = database.open_db(\"my-engine.db\")\n\ndb.execute(\"\"\"\n    UPDATE pages SET category = 'forum'\n    WHERE url LIKE '%forum%'\n       OR url LIKE '%discuss.%'\n       OR url LIKE '%/t/%'\n\"\"\")\n\ndb.execute(\"\"\"\n    UPDATE pages SET category = 'Github'\n    WHERE url LIKE '%github.com%'\n\"\"\")\n\ndb.commit()\ndb.close()\n</code></pre> <p>You can define as many categories as you want; the Indexer will make them available as filter facets at query time.</p>"},{"location":"starting/7-build-your-own-search-engine.html#step-6-building-the-search-index","title":"Step 6 \u2014 Building the Search Index","text":"<p>The <code>Indexer</code> class builds the in-memory data structures required for fast ranking:</p> <ul> <li>a BM25+ sparse matrix over the vocabulary for exact keyword matching,</li> <li>the full matrix of page vectors for semantic matching,</li> <li>a URL-to-position lookup table.</li> </ul> <pre><code>from core import search, database, nlp\n\ndb  = database.open_db(\"my-engine.db\")\nw2v = nlp.Word2Vec.load_model(\"my-word2vec\")\n\n# Build and immediately save the index\nmodel = search.Indexer(\n    db,\n    \"my-search-engine\",   # name used to save the .joblib file\n    w2v,\n    principal_components = 2,   # number of principal components to subtract from query vectors\n                                # to remove the \"all topics\" bias\n)\ndb.close()\n</code></pre> <p>Test a few queries while the index is still live in RAM, before you\u2019re sure it serialised correctly:</p> <pre><code>from core.utils import typography_undo\n\nqueries = [\n    \"install darktable on ubuntu\",\n    \"difference between lightness and brightness\",\n    \"meilleur appareil photo pour d\u00e9butant\",\n]\n\nfor q in queries:\n    tokens = model.tokenize_query(typography_undo(q))\n    results = model.rank(db, tokens, search.search_methods.AI)\n    print(f\"\\n[{q}]\")\n    for rank, url, score in results[:5]:\n        print(f\"  {score:.3f}  {url}\")\n</code></pre> <p>Then verify the on-disk version loads cleanly:</p> <pre><code>model = search.Indexer.load(\"my-search-engine\", db)\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#step-7-querying-the-index","title":"Step 7 \u2014 Querying the Index","text":"<p>The <code>Indexer.rank()</code> method supports three complementary search modes, which can be combined:</p>"},{"location":"starting/7-build-your-own-search-engine.html#semantic-ai-search","title":"Semantic (AI) search","text":"<p>Uses the dual Word2Vec embedding space \u2014 the most powerful mode for natural-language queries where the exact words may not appear in the document:</p> <pre><code>tokens  = model.tokenize_query(\"how to manage exposure in darktable\")\nresults = model.rank(db, tokens, search.search_methods.AI)\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#fuzzy-bm25-search","title":"Fuzzy (BM25+) search","text":"<p>Classic statistical keyword weighting. Useful for precise queries where exact word frequency matters:</p> <pre><code>tokens  = model.tokenize_query(\"filmic rgb tone mapping\")\nresults = model.rank(db, tokens, search.search_methods.FUZZY)\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#sql-filtered-search","title":"SQL-filtered search","text":"<p>Any <code>rank()</code> call accepts an optional SQL <code>WHERE</code> clause that intersects with the semantic ranking, enabling powerful faceted search:</p> <pre><code># Only French-language results from forum sources\ntokens  = model.tokenize_query(\"courbe des tons\")\nresults = model.rank(\n    db,\n    tokens,\n    search.search_methods.AI,\n    n_results  = 500,\n    sql_query  = \"WHERE lang = ? AND category = ?\",\n    sql_params = [\"fr\", \"forum\"],\n)\n\n# Full-text filter \u2014 pages whose parsed content contains a specific string\nresults = model.rank(\n    db, tokens, search.search_methods.AI,\n    sql_query  = \"WHERE instr(parsed, ?) &gt; 0\",\n    sql_params = [\"zone system\"],\n)\n\n# PCRE regex filter \u2014 supported transparently via the custom SQLite extension\nresults = model.rank(\n    db, tokens, search.search_methods.AI,\n    sql_query  = \"WHERE parsed REGEXP ?\",\n    sql_params = [r\"colour\\s+science\"],\n)\n</code></pre> <p>The result list is a list of <code>(index, url, similarity_score)</code> tuples, sorted best-first. Retrieve full page data with a follow-up database query:</p> <pre><code>urls = [url for _, url, _ in results[:20]]\nplaceholders = \",\".join([\"?\" for _ in urls])\ncursor = db.execute(\n    f\"SELECT title, url, excerpt, datetime FROM pages WHERE url IN ({placeholders})\",\n    urls\n)\nfor title, url, excerpt, date in cursor.fetchall():\n    print(f\"{title}\\n  {url}\\n  {excerpt[:120]}\\n\")\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#step-8-the-web-interface","title":"Step 8 \u2014 The Web Interface","text":"<p>The section below shows a minimal implementation of a web API exposing the <code>search.Indexer</code> object to search queries and returning responses as JSON object for asynchronous AJAX loading or as a basis to build Rest APIs. This implementation uses additional SQL filtering to narrow-down the search results.</p>"},{"location":"starting/7-build-your-own-search-engine.html#minimal-flask-app","title":"Minimal Flask app","text":"<pre><code># search_app.py\nimport os\nos.environ['OPENBLAS_NUM_THREADS'] = '2'  # avoid OpenBLAS threading conflicts\n\nfrom flask import Flask, request, jsonify, g\nfrom core import database, search\nfrom core.utils import typography_undo\nimport html\n\napp = Flask(__name__)\n\nDB_PATH    = \"my-engine.db\"\nINDEX_NAME = \"my-search-engine\"\n\n# \u2500\u2500 Database helpers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\ndef get_db():\n    \"\"\"Open one read-only DB connection per request.\"\"\"\n    if \"_db\" not in g:\n        g._db = database.open_db(DB_PATH, mode=\"ro\")\n    return g._db\n\n@app.teardown_appcontext\ndef close_db(exc):\n    db = g.pop(\"_db\", None)\n    if db:\n        db.close()\n\n# \u2500\u2500 Index \u2014 loaded once at startup \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n_engine = None\n\ndef get_engine():\n    global _engine\n    if _engine is None:\n        db = database.open_db(DB_PATH, mode=\"ro\")\n        _engine = search.Indexer.load(INDEX_NAME, db)\n        db.close()\n    return _engine\n\n# \u2500\u2500 Search endpoint \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n@app.route(\"/api\")\ndef api():\n    raw_query = request.args.get(\"s\", \"\").strip()\n    page      = int(request.args.get(\"page\", 0))\n    lang      = request.args.get(\"lang\", \"any\")\n    category  = request.args.get(\"category\", \"any\")\n\n    if not raw_query:\n        return jsonify({\"error\": \"empty query\"}), 400\n\n    # Sanitise and tokenise\n    query  = html.unescape(raw_query)\n    tokens = get_engine().tokenize_query(typography_undo(query))\n\n    if not tokens:\n        return jsonify({\"error\": \"no recognisable keywords in query\"}), 400\n\n    # Build optional SQL filter\n    clauses, params = [], []\n    if lang != \"any\":\n        clauses.append(\"lang = ?\")\n        params.append(lang)\n    if category != \"any\":\n        clauses.append(\"category = ?\")\n        params.append(category)\n\n    sql = (\"WHERE \" + \" AND \".join(clauses)) if clauses else \"\"\n\n    # Rank\n    results = get_engine().rank(\n        get_db(), tokens, search.search_methods.AI,\n        n_results  = 500,\n        sql_query  = sql,\n        sql_params = params,\n    )\n\n    # Paginate\n    per_page  = 20\n    n_pages   = max(1, -(-len(results) // per_page))   # ceiling division\n    page      = min(page, n_pages - 1)\n    slice_    = results[page * per_page : (page + 1) * per_page]\n\n    # Fetch display fields\n    urls = [url for _, url, _ in slice_]\n    if not urls:\n        return jsonify({\"error\": \"no results\"}), 200\n\n    ph   = \",\".join([\"?\"] * len(urls))\n    rows = get_db().execute(\n        f\"SELECT title, url, excerpt, datetime, category FROM pages WHERE url IN ({ph})\",\n        urls,\n    ).fetchall()\n\n    # Re-sort rows to match ranking order\n    row_map = {row[1]: row for row in rows}\n    items   = [\n        {\"title\": row_map[u][0], \"url\": u,\n         \"excerpt\": row_map[u][2], \"date\": str(row_map[u][3]),\n         \"category\": row_map[u][4]}\n        for u in urls if u in row_map\n    ]\n\n    return jsonify({\n        \"query\":    query,\n        \"tokens\":   tokens,\n        \"page\":     page,\n        \"n_pages\":  n_pages,\n        \"n_results\": len(results),\n        \"results\":  items,\n    })\n\nif __name__ == \"__main__\":\n    app.run(debug=False, host=\"0.0.0.0\", port=5000)\n</code></pre> <p>Run it with:</p> <pre><code>python search_app.py\n# or in production:\ngunicorn -w 2 -b 0.0.0.0:5000 search_app:app\n</code></pre> <p>Query it from the command line or a browser:</p> <pre><code>GET http://localhost:5000/api?s=exposure+compensation&amp;lang=en\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#minimal-html-front-end","title":"Minimal HTML front-end","text":"<p>Drop a single <code>index.html</code> next to <code>search_app.py</code> and add a <code>render_template</code> call to the <code>/</code> route if you want a browser UI:</p> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;\n  &lt;meta charset=\"UTF-8\"&gt;\n  &lt;title&gt;My Search Engine&lt;/title&gt;\n  &lt;style&gt;\n    body  { font-family: sans-serif; max-width: 800px; margin: 2rem auto; }\n    input { width: 70%; padding: .4em; font-size: 1rem; }\n    button { padding: .4em 1em; font-size: 1rem; }\n    .result { margin: 1.5rem 0; }\n    .result a { font-size: 1.1rem; font-weight: bold; }\n    .result .meta { color: #666; font-size: .85rem; }\n    .result p { margin: .3em 0; }\n  &lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;\ud83d\udd0d My Search Engine&lt;/h1&gt;\n  &lt;div&gt;\n    &lt;input id=\"q\" type=\"text\" placeholder=\"Type your query\u2026\" autofocus /&gt;\n    &lt;button onclick=\"search()\"&gt;Search&lt;/button&gt;\n  &lt;/div&gt;\n  &lt;p id=\"stats\"&gt;&lt;/p&gt;\n  &lt;div id=\"results\"&gt;&lt;/div&gt;\n\n  &lt;script&gt;\n    async function search(page = 0) {\n      const q = document.getElementById('q').value.trim();\n      if (!q) return;\n\n      const res  = await fetch(`/api?s=${encodeURIComponent(q)}&amp;page=${page}`);\n      const data = await res.json();\n\n      if (data.error) {\n        document.getElementById('results').innerHTML = `&lt;p style=\"color:red\"&gt;${data.error}&lt;/p&gt;`;\n        return;\n      }\n\n      document.getElementById('stats').textContent =\n        `${data.n_results} results \u2014 page ${data.page + 1} / ${data.n_pages}`;\n\n      document.getElementById('results').innerHTML = data.results.map(r =&gt; `\n        &lt;div class=\"result\"&gt;\n          &lt;a href=\"${r.url}\" target=\"_blank\"&gt;${r.title || r.url}&lt;/a&gt;\n          &lt;div class=\"meta\"&gt;${r.url} \u00b7 ${r.date || ''} \u00b7 ${r.category || ''}&lt;/div&gt;\n          &lt;p&gt;${r.excerpt || ''}&lt;/p&gt;\n        &lt;/div&gt;\n      `).join('');\n    }\n\n    document.getElementById('q').addEventListener('keydown', e =&gt; {\n      if (e.key === 'Enter') search();\n    });\n  &lt;/script&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#caching-with-flask-caching","title":"Caching with Flask-Caching","text":"<p>For production, wrap expensive calls with a cache. The reference app uses Memcached, but the file-system backend is zero-dependency:</p> <pre><code>from flask_caching import Cache\n\ncache = Cache(app, config={\n    \"CACHE_TYPE\":            \"FileSystemCache\",\n    \"CACHE_DIR\":             \"/tmp/my-engine-cache\",\n    \"CACHE_DEFAULT_TIMEOUT\": 3600 * 48,   # 48-hour TTL\n})\n\n@cache.memoize(3600 * 48)\ndef get_ranked_results(tokens, lang, category):\n    ...   # expensive ranking call here\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#advanced-topics","title":"Advanced Topics","text":""},{"location":"starting/7-build-your-own-search-engine.html#incremental-updates","title":"Incremental updates","text":"<p>Because <code>create_db</code> stores pages with <code>url</code> as the primary key, re-crawling a site and calling <code>database.populate_db</code> again will update existing rows in place and insert new ones \u2014 nothing is duplicated. The recommended update cycle is:</p> <ol> <li>Crawl changed sources into a fresh <code>create_temp_db()</code>.</li> <li>Run <code>batch_parse_web_page</code>, language-filter, and <code>Deduplicator</code> on the temp database.</li> <li>Import into the permanent database with <code>import_pages</code> (existing URLs are updated, new ones inserted).</li> <li>Re-run <code>batch_tokenize</code>, <code>batch_stem</code>, and <code>batch_vectorize</code> with <code>only_none=True</code> so only the new or changed rows are processed.</li> <li>Rebuild the <code>Indexer</code>.</li> </ol> <p>Steps 4\u20135 are fast when most of the corpus is unchanged; only the delta gets reprocessed.</p>"},{"location":"starting/7-build-your-own-search-engine.html#domain-specific-language-support","title":"Domain-specific language support","text":"<p>The <code>Tokenizer</code> delegates stemming to Snowball, which currently supports 26 languages. The <code>language</code> detection module uses <code>langdetect</code> under the hood. Adding a new language amounts to:</p> <ol> <li>adding its stopwords file to the <code>models/</code> directory,</li> <li>passing it in the <code>stopwords=</code> argument at <code>Tokenizer()</code> instantiation,</li> <li>including it in the SQL <code>WHERE lang IN (...)</code> corpus queries.</li> </ol>"},{"location":"starting/7-build-your-own-search-engine.html#result-ranking-transparency","title":"Result ranking transparency","text":"<p>The <code>search_methods.AI</code> mode blends two signals: 0.98 \u00d7 semantic vector similarity + 0.02 \u00d7 BM25+ keyword frequency. This blend can be tuned in <code>search.Indexer.rank()</code> to shift the balance between thematic relevance and exact-term matching, depending on your domain and typical query style.</p>"},{"location":"starting/7-build-your-own-search-engine.html#exposing-related-keywords","title":"Exposing \u201crelated keywords\u201d","text":"<p>The Indexer can suggest semantically related terms for query expansion or a \u201cdid you mean?\u201d feature:</p> <pre><code>related = model.get_related(tokens, n=20, k=5)\n# e.g. for tokens=['exposur', 'histogram']:\n# ['highlight', 'shadow', 'tone_curv', 'clipping', 'waveform', ...]\n</code></pre>"},{"location":"starting/7-build-your-own-search-engine.html#whats-next","title":"What\u2019s Next","text":"<p>With a working search engine at home you now have a foundation to explore further:</p> <ul> <li>RAG (Retrieval-Augmented Generation) \u2014 feed the top results from <code>rank()</code> as context to a local LLM to answer questions in natural language, grounded in your own curated corpus.</li> <li>Monitoring \u2014 the <code>stats</code> table built by <code>Indexer</code> tracks word counts, page counts, domain distribution, and the most recent crawl date. Surface those in your UI to show users the freshness of the index.</li> <li>Fine-tuned relevance \u2014 the <code>category</code> and <code>lang</code> columns are indexed in SQLite. Adding <code>WHERE category = 'documentation'</code> to a query is essentially free in terms of latency, making faceted filtering a first-class citizen.</li> <li>Private intranet search \u2014 the crawler handles any URL including <code>http://localhost:*</code>, so you can index internal wikis, documentation servers, or local PDF archives with exactly the same pipeline.</li> </ul>"},{"location":"starting/8-nlp.html","title":"Natural Language Processing","text":""},{"location":"starting/8-nlp.html#training-a-language-model","title":"Training a language model","text":"<p>The Word2Vec model is trained from pre-tokenized documents. Each document is a list of sentences, and each sentence is a list of tokens. The Tokenizer object is therefore part of the training pipeline and is saved inside the model so production code can tokenize new text the same way.</p> <p>Assuming you already saved a dataset of <code>web_page</code> objects with core.utils.save_data, create a user script:</p> <pre><code># Boilerplate stuff to access src/core from src/user_scripts\nimport os\nimport sys\n\nSCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))\nsys.path.append(os.path.dirname(SCRIPT_DIR))\n\n# Here starts the real code\nfrom core import nlp, utils\n\ntokenizer = nlp.Tokenizer()\npages = utils.open_data(\"ansel\", scheme=\"pickle\")\n\ndocuments = [\n    tokenizer.tokenize_document_per_sentence(page[\"content\"])\n    for page in pages\n    if page[\"content\"]\n]\n\nw2v = nlp.Word2Vec(\n    documents,\n    \"word2vec-ansel\",\n    tokenizer=tokenizer,\n    vector_size=300,\n    epochs=200,\n    window=15,\n    min_count=32,\n    sample=0.0005,\n)\n\nprint(w2v.wv.most_similar(w2v.tokenizer.normalize_token(\"free\", \"en\")))\n</code></pre> <p>This saves a <code>word2vec-ansel</code> model into <code>VirtualSecretary/models</code>. To retrieve it later:</p> <pre><code>from core import nlp\n\nw2v = nlp.Word2Vec.load_model(\"word2vec-ansel\")\n</code></pre>"},{"location":"starting/8-nlp.html#training-an-ai-classifier","title":"Training an AI classifier","text":"<p>Classifier learns labels from <code>Data(text, label)</code> samples, using an existing Word2Vec model to vectorize the text. The label can be any meaningful class: a topic, a document type, or an IMAP folder name.</p> <p>This example trains a classifier on the NPS Chat corpus:</p> <pre><code># Boilerplate stuff to access src/core from src/user_scripts\nimport os\nimport sys\n\nSCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))\nsys.path.append(os.path.dirname(SCRIPT_DIR))\n\n# Here starts the real code\nimport nltk\nfrom core import nlp\n\nnltk.download(\"nps_chat\")\n\ntokenizer = nlp.Tokenizer()\nposts = list(nltk.corpus.nps_chat.xml_posts())\n\ndocuments = [\n    tokenizer.tokenize_document_per_sentence(post.text)\n    for post in posts\n]\n\nw2v = nlp.Word2Vec(\n    documents,\n    \"word2vec-chat\",\n    tokenizer=tokenizer,\n    epochs=2000,\n    window=3,\n    min_count=2,\n)\n\ntraining_set = [\n    nlp.Data(post.text, post.get(\"class\"))\n    for post in posts\n]\n\nmodel = nlp.Classifier(\n    training_set,\n    \"chat-classifier\",\n    w2v,\n    validate=True,\n    variant=\"svm\",\n)\n\ntest_messages = [\n    \"Do you have time to meet at 5 pm ?\",\n    \"Come with me !\",\n    \"Nope\",\n    \"What do you think ?\",\n]\n\nfor item in test_messages:\n    label, confidence = model.prob_classify(item)\n    print(item, label, confidence)\n</code></pre> <p>Note</p> <p><code>validate=True</code> splits the corpus into training and test subsets and prints accuracy metrics. This helps tune the tokenizer, Word2Vec parameters, and classifier variant. Once the metrics are satisfactory, retrain with <code>validate=False</code> so the final model can use all available samples.</p> <p>The saved classifier can later be loaded in filters or user scripts:</p> <pre><code>from core import nlp\n\nmodel = nlp.Classifier.load(\"chat-classifier\")\nlabel, confidence = model.prob_classify(\"What do you think ?\")\n</code></pre>"},{"location":"starting/8-nlp.html#conclusion","title":"Conclusion","text":"<p>The NLP objects are designed to be saved and reused later in filters. The general workflow is:</p> <ol> <li>Gather labelled text, either from crawled pages, emails, contacts, comments, or another protocol.</li> <li>Tokenize the text with core.nlp.Tokenizer.</li> <li>Train a language model with core.nlp.Word2Vec.</li> <li>Train a classifier with core.nlp.Classifier from <code>Data(text, label)</code> samples.</li> <li>Load the classifier in a processing filter and act only when <code>prob_classify()</code> returns a confidence above a threshold.</li> </ol>"}]}