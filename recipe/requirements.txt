setuptools==58.2.0 # needed for pyspf to build wheel
wheel # just in case

# Common utils
regex
psutil
guppy3
lz4
python-dateutil
charset_normalizer

# Network
requests
requests-oauthlib
curl_cffi
protego
httpx

# High-perf linear algebra and machine learning
numba==0.61.0
numpy>=2.0,<=2.1 # needed for numba 0.61
cython # needed for Gensim fast version
scipy
gensim==4.4
pytz
pandas
scikit-learn
rank-bm25
levenshtein

# NLP stack
nltk
fast-langdetect
blingfire
langcodes
language_data
pycountry

# PDF I/O and OCR
opencv-python-headless
PyMuPDF
pycryptodome
pdf2image
pillow==10.4.0
pytesseract
# Pin pypdf to an older version because newer pypdf releases
# (>=6.x) change internal object implementations and can trigger
# recursive attribute access when unpickling legacy pickles created
# with older pypdf versions. See repository issue: legacy pickle
# compatibility. Downgrade to 3.8.1 to allow safe dataset loading.
pypdf==3.8.1

# Text parsing (HTML, XML, JSON)
beautifulsoup4
html5lib
jsonschema
markdown
